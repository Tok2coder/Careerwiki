# 🎨 템플릿 테스트 가이드

> 브라우저에서 직접 템플릿을 보면서 수정하고 테스트하는 방법

---

## 🚀 빠른 시작

### 1. 개발 서버 실행

```bash
# 방법 1: Vite dev server (핫 리로드 지원) - 추천!
npm run dev

# 방법 2: Wrangler pages dev (D1 포함)
npm run dev:sandbox

# 방법 3: PM2로 실행 (백그라운드)
pm2 start ecosystem.config.cjs
```

**추천**: `npm run dev` - 파일 수정 시 자동 리로드됨

---

## 📍 테스트 가능한 URL

### 직업 상세페이지 (Job Detail)

| URL | 설명 | 템플릿 파일 |
|-----|------|------------|
| http://localhost:3000/job/lawyer | 변호사 (샘플) | `src/templates/unifiedJobDetail.ts` |
| http://localhost:3000/job/software-developer | 소프트웨어 개발자 (샘플) | `src/templates/unifiedJobDetail.ts` |
| http://localhost:3000/job/lawyer?debug=true | 변호사 (디버그 모드) | `src/templates/dataDebugTemplate.ts` |

### 전공 상세페이지 (Major Detail)

| URL | 설명 | 템플릿 파일 |
|-----|------|------------|
| http://localhost:3000/major/digital-marketing-major | 디지털 마케팅 전공 (샘플) | `src/templates/unifiedMajorDetail.ts` |
| http://localhost:3000/major/computer-science-major | 컴퓨터 과학 전공 (샘플) | `src/templates/unifiedMajorDetail.ts` |
| http://localhost:3000/major/digital-marketing-major?debug=true | 디지털 마케팅 (디버그 모드) | `src/templates/dataDebugTemplate.ts` |

---

## 📂 템플릿 파일 위치

### 직업 상세페이지 템플릿
```
src/templates/unifiedJobDetail.ts
```
- **주요 함수**: `renderUnifiedJobDetail()`
- **라인 수**: 약 3,500줄
- **수정 시**: 브라우저 새로고침하면 반영됨 (Vite dev server 사용 시)

### 전공 상세페이지 템플릿
```
src/templates/unifiedMajorDetail.ts
```
- **주요 함수**: `renderUnifiedMajorDetail()`
- **수정 시**: 브라우저 새로고침하면 반영됨 (Vite dev server 사용 시)

### 디버그 템플릿
```
src/templates/dataDebugTemplate.ts
```
- **주요 함수**: `renderDataDebugPage()`
- **용도**: 원본 API 데이터 확인용

---

## 🔄 개발 워크플로우

### 1. 템플릿 수정
```bash
# VS Code나 에디터에서 파일 열기
code src/templates/unifiedJobDetail.ts
# 또는
code src/templates/unifiedMajorDetail.ts
```

### 2. 수정 후 확인
1. 파일 저장 (Ctrl+S / Cmd+S)
2. 브라우저 새로고침 (F5)
3. 변경사항 확인

**Vite dev server 사용 시**: 자동 리로드됨 (수동 새로고침 불필요)

### 3. 실시간 확인
- 브라우저 개발자 도구 열기 (F12)
- Elements 탭에서 HTML 구조 확인
- Console 탭에서 에러 확인

---

## 🎯 주요 수정 포인트

### 직업 상세페이지 (`unifiedJobDetail.ts`)

**섹션 위치 찾기:**
- 히어로 섹션: `pushDetailCard('직업 개요', ...)` 검색
- 업무 내용: `pushDetailCard('하는 일', ...)` 검색
- 필요 능력: `pushDetailCard('필요 능력', ...)` 검색
- 관련 학과: `pushDetailCard('관련 학과', ...)` 검색

**스타일 수정:**
- Tailwind CSS 클래스 사용
- `glass-card`, `wiki-primary` 등 커스텀 클래스 사용 가능

### 전공 상세페이지 (`unifiedMajorDetail.ts`)

**섹션 위치 찾기:**
- 히어로 섹션: `pushDetailCard('전공 개요', ...)` 검색
- 주요 과목: `pushDetailCard('주요 과목', ...)` 검색
- 관련 직업: `pushDetailCard('관련 직업', ...)` 검색
- 개설 대학: `pushDetailCard('개설 대학', ...)` 검색

---

## 🐛 디버깅 팁

### 1. 디버그 모드 사용
URL에 `?debug=true` 추가:
```
http://localhost:3000/job/lawyer?debug=true
http://localhost:3000/major/digital-marketing-major?debug=true
```

**디버그 모드에서 확인 가능:**
- 원본 API 데이터 구조
- 데이터 소스 정보
- 필드별 상세 정보

### 2. 콘솔 로그 확인
브라우저 개발자 도구 → Console 탭:
- `[ISR Cache HIT]` - 캐시 히트 로그
- `[ISR Cache MISS]` - 캐시 미스 로그
- 에러 메시지 확인

### 3. 네트워크 탭 확인
브라우저 개발자 도구 → Network 탭:
- 페이지 로딩 시간
- API 호출 여부
- 정적 파일 로딩 상태

---

## ⚡ 빠른 테스트 체크리스트

### 직업 상세페이지 테스트
- [ ] http://localhost:3000/job/lawyer 접속 확인
- [ ] 탭 전환 작동 확인 (개요/상세정보/업무특성)
- [ ] 모든 섹션 데이터 표시 확인
- [ ] 모바일 반응형 확인 (F12 → 디바이스 모드)

### 전공 상세페이지 테스트
- [ ] http://localhost:3000/major/digital-marketing-major 접속 확인
- [ ] 탭 전환 작동 확인
- [ ] Phase 1 필드 표시 확인 (관련 고교 교과목, 진로 탐색 활동 등)
- [ ] 모바일 반응형 확인

---

## 🔧 문제 해결

### 서버가 시작되지 않을 때
```bash
# 포트 3000 정리
npm run clean-port

# 다시 시작
npm run dev
```

### 변경사항이 반영되지 않을 때
1. 브라우저 강력 새로고침 (Ctrl+Shift+R / Cmd+Shift+R)
2. 캐시 비우기 (브라우저 개발자 도구 → Network 탭 → "Disable cache" 체크)
3. 서버 재시작

### ISR 캐시 문제
전공 페이지는 ISR 캐시를 사용하므로, 템플릿 수정 후:
1. 템플릿 버전 증가: `src/constants/template-versions.ts`
2. 또는 캐시 수동 삭제: D1 데이터베이스에서 `wiki_pages` 테이블 삭제

---

## 📝 다음 단계

템플릿 수정 후:
1. Git 커밋
2. 브라우저에서 최종 확인
3. 다른 샘플 페이지로도 테스트
4. 모바일 반응형 확인

---

**마지막 업데이트**: 2025-11-06
**작성자**: CareerWiki Development Team

