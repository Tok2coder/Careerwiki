<!-- b58698c1-b6ee-4315-a47b-1f1203556df8 6f7a2cc7-3c42-4638-94c0-2e6a1ec26093 -->
# Major Edit/Data Alignment Plan

1) Hero description alignment

- Verify major hero description render path (`src/templates/unifiedMajorDetail.ts` uses which fields) and make edit-data (`/api/major/:id/edit-data` in `src/index.tsx`) return the same prioritized value so edit UI matches published hero text.

2) Overview summary alignment

- Ensure `overview.summary` (전공 개요) in edit-data and edit UI (`public/static/edit-mode.js`, field config) maps to the same field the template renders. Populate from merged/user data so existing published content appears in the editor.

3) Curriculum section rename & structure

- In the major template, rename the detail tab section title to “교육과정” (no “주요 교과목”).
- Update edit UI fields/config to match published structure: `배우는 내용`, `기초과목`, `심화과목`, `대학 주요 교과목 상세`, `고교 추천 교과목`; remove/replace the old “주요 교과목/배우는 내용” mismatch.
- Ensure edit-data API provides/reads these fields from the same keys the template uses so published and editor stay in sync.

4) Regression check & cache bump

- Bump template version for majors to invalidate ISR cache.
- Sanity-check other tabs unaffected.

### To-dos

- [ ] Align major hero description source edit-data ↔ template
- [ ] Ensure overview.summary shows in editor & matches published
- [ ] Rename curriculum section and sync fields (배우는내용/기초/심화/대학상세/고교추천)
- [ ] Bump major template version after changes