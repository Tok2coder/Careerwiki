# AI 추천기 100% 완성 계획

> **목표**: Fit = Can + Like - Risk 공식을 완벽히 구현하여 유저에게 "맞는 직업"을 정확히 추천
> **작성일**: 2026-02-06
> **예상 기간**: 2-3주

---

## 📋 목차

1. [현재 상태 진단](#1-현재-상태-진단)
2. [목표 아키텍처](#2-목표-아키텍처)
3. [P0: Can 수집 강화](#3-p0-can-수집-강화)
4. [P1: Like/Risk 정교화](#4-p1-likerisk-정교화)
5. [P2: RAG/CAG/TAG 통합 개선](#5-p2-ragcagtag-통합-개선)
6. [P3: 심리분석-추천 연결](#6-p3-심리분석-추천-연결)
7. [파일별 수정 명세](#7-파일별-수정-명세)
8. [테스트 계획](#8-테스트-계획)
9. [롤백 계획](#9-롤백-계획)

---

## 1. 현재 상태 진단

### 1.1 Fit 점수 공식

**현재 구현** (`routes.ts:1441`):
```typescript
const fit = Math.round(0.5 * adjusted.like + 0.5 * adjusted.can - adjusted.risk_penalty)
```

**수집 현황**:
| 요소 | 수집률 | 반영률 | 문제점 |
|------|--------|--------|--------|
| Like | 90% | 85% | 경험 검증 부족 |
| Can | 50% | 40% | **치명적 부족** - 자기평가만 있음 |
| Risk | 85% | 90% | 강도 구분 미흡 |

### 1.2 핵심 문제

```
문제 1: Can 데이터 부족
- Q3 Strength: 6개 옵션 중 2개 선택 (자기평가)
- 실제 경험/역량 수준 검증 없음
- Can Boost 규칙이 Like 대비 적음 (30% 수준)

문제 2: RAG가 Fit보다 선행
- Vector Search가 먼저 800개 필터링
- Fit 점수는 이후에 계산됨
- "유사한 직업" ≠ "맞는 직업"

문제 3: CAG가 수집 진행도 미추적
- 질문 중복만 방지
- Can/Like/Risk 중 뭐가 부족한지 모름
```

---

## 2. 목표 아키텍처

### 2.1 개선된 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                    개선된 AI 추천 플로우                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [1단계: 데이터 수집]                                           │
│  ├─ 미니모듈 Q1~Q4 (기존)                                       │
│  ├─ 미니모듈 Q5~Q15 (기존)                                      │
│  └─ 🆕 Can 검증 질문 (NEW)                                      │
│       ├─ 경험 깊이 질문                                         │
│       ├─ 역량 수준 질문                                         │
│       └─ 배경 → Can 매핑                                        │
│                                                                 │
│  [2단계: CAG 상태 관리]                                         │
│  ├─ 질문 중복 방지 (기존)                                       │
│  └─ 🆕 수집 진행도 추적 (NEW)                                   │
│       ├─ like_progress: { collected, confidence }               │
│       ├─ can_progress: { collected, confidence }                │
│       └─ risk_progress: { collected, confidence }               │
│                                                                 │
│  [3단계: Fact → Score 변환]                                     │
│  ├─ Like Boost (기존 12개 규칙)                                 │
│  ├─ 🆕 Can Boost (NEW 8개 규칙 추가)                            │
│  └─ Risk Penalty (기존)                                         │
│                                                                 │
│  [4단계: RAG 후보 검색]                                         │
│  ├─ Vector Search (기존)                                        │
│  └─ 🆕 Can/Like 가중 쿼리 (NEW)                                 │
│                                                                 │
│  [5단계: TAG 필터링]                                            │
│  ├─ Hard Exclusion (기존 7개)                                   │
│  ├─ 🆕 Can 기반 필터 (NEW 3개)                                  │
│  └─ Risk Penalty (기존)                                         │
│                                                                 │
│  [6단계: Fit 계산 + 랭킹]                                       │
│  └─ Fit = 0.5*Like + 0.5*Can - Risk (기존)                     │
│                                                                 │
│  [7단계: LLM Judge + Report]                                    │
│  ├─ 후보 평가 (기존)                                            │
│  └─ 🆕 심리분석 → 추천 연결 (NEW)                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 목표 지표

| 지표 | 현재 | 목표 |
|------|------|------|
| Like 수집률 | 90% | 95% |
| Can 수집률 | 50% | 90% |
| Risk 수집률 | 85% | 95% |
| 전체 Fit 정확도 | ~70% | 90%+ |

---

## 2.5 ⚠️ 핵심 안전장치 (Critical Safeguards)

> **이 섹션은 전체 구현에서 반드시 적용해야 하는 안전장치입니다.**

### 2.5.1 Can-Like 밸런스 캡

**문제**: Can Boost 최대치(25)가 너무 강력해서 Like를 압도할 수 있음
- `analytical` 하나만 강해도 Like를 압도할 가능성
- "잘할 수는 있는데 하기 싫은 직업"이 상위에 올라감

**해결책**: Can이 단독으로 Fit 1위를 만들 수 없게 제한

```typescript
// routes.ts - Fit 계산 후 적용
function applyCanLikeBalanceCap(job: ScoredJob): ScoredJob {
  const { like, can, risk_penalty } = job.scores

  // Can이 Like보다 10점 이상 높으면 페널티
  if (can > like + 10) {
    const overCanPenalty = Math.min(10, Math.floor((can - like - 10) / 5) * 2)
    job.scores.fit -= overCanPenalty
    job.balanceWarning = `관심도 대비 역량 과잉 (-${overCanPenalty}점 조정)`
  }

  // Can이 Like보다 20점 이상 높으면 추가 경고
  if (can > like + 20) {
    job.riskWarnings.push('⚠️ 잘할 수는 있지만 흥미도가 낮을 수 있음')
  }

  return job
}

// 적용 위치: Fit 계산 직후
scoredJobs = scoredJobs.map(applyCanLikeBalanceCap)
```

**적용 파일**: `routes.ts`

---

### 2.5.2 Hard Exclusion 경력 단계 조건

**문제**: 현재 Hard Exclusion이 정적 규칙이라 가능성 있는 전환 직업까지 제외함
- "required"도 레벨이 있음 (초입/보조/주니어는 허용 가능)

**해결책**: 경력 단계 + Can 검증 결과에 따라 유연하게 적용

```typescript
// tag-filter.ts - Hard Exclusion 조건 완화

interface JobLevelInfo {
  level: 'entry' | 'junior' | 'mid' | 'senior' | 'expert'
  can_threshold_to_allow: number  // 이 Can Boost 이상이면 허용
}

const EXCLUSION_OVERRIDE_RULES: Record<string, JobLevelInfo[]> = {
  'no_analytical_experience': [
    { level: 'entry', can_threshold_to_allow: 8 },   // 프로젝트 경험 있으면 허용
    { level: 'junior', can_threshold_to_allow: 15 }, // 1-3년 경험 있으면 허용
  ],
  'no_creative_experience': [
    { level: 'entry', can_threshold_to_allow: 6 },
    { level: 'junior', can_threshold_to_allow: 12 },
  ],
  'no_communication_experience': [
    { level: 'entry', can_threshold_to_allow: 10 },
    { level: 'junior', can_threshold_to_allow: 15 },
  ],
}

function shouldOverrideExclusion(
  rule: HardExclusionRule,
  job: FilteredCandidate,
  canBoost: number
): boolean {
  const overrides = EXCLUSION_OVERRIDE_RULES[rule.userConstraint]
  if (!overrides) return false

  const jobLevel = job.attributes?.level || 'mid'
  const override = overrides.find(o => o.level === jobLevel)

  if (override && canBoost >= override.can_threshold_to_allow) {
    return true  // 제외하지 않음
  }

  return false
}

// Hard Exclusion 적용 시
if (rule.jobCondition(jobValue)) {
  // 경력 단계 조건 체크
  const canBoost = getCanBoostForAttribute(rule.userConstraint, canValidationResults)
  if (shouldOverrideExclusion(rule, candidate, canBoost)) {
    // 제외 대신 경고만 추가
    candidate.riskWarnings.push(`⚠️ ${rule.reason} (진입 가능성 있음)`)
    candidate.riskPenalty += 5  // 약한 페널티만
  } else {
    // 기존대로 Hard Exclusion
    isExcluded = true
  }
}
```

**적용 파일**: `tag-filter.ts`

---

### 2.5.3 세션당 질문 수 상한 (UX 피로도 제어)

**문제**: 질문 수가 늘어나면 유저 피로도 증가

**해결책**: 세션당 후속 질문 수 제한 + 명시적 이유 표시

```typescript
// constants.ts 또는 routes.ts 상단
const UX_LIMITS = {
  MAX_FOLLOWUP_QUESTIONS_PER_SESSION: 3,
  MAX_CAN_VALIDATION_QUESTIONS: 2,  // Can 검증은 최대 2개까지
  MAX_TOTAL_QUESTIONS_BEFORE_RESULT: 8,  // 결과 전 총 질문 수
}

// routes.ts - 후속 질문 생성 시
function limitFollowupQuestions(
  questions: FollowupQuestion[],
  cagState: CAGState
): { questions: FollowupQuestion[], hasMore: boolean, moreReason: string } {
  const askedCount = cagState.asked_questions_log.length
  const remaining = UX_LIMITS.MAX_FOLLOWUP_QUESTIONS_PER_SESSION -
    cagState.asked_questions_log.filter(q => q.round === cagState.currentRound).length

  if (remaining <= 0) {
    return {
      questions: [],
      hasMore: questions.length > 0,
      moreReason: '더 정확한 추천을 위해 다음 단계에서 추가 질문이 있습니다'
    }
  }

  const limited = questions.slice(0, remaining)
  const hasMore = questions.length > remaining

  return {
    questions: limited,
    hasMore,
    moreReason: hasMore
      ? `${questions.length - remaining}개의 질문이 더 있습니다. 응답하시면 추천 정확도가 높아집니다.`
      : ''
  }
}

// 응답에 포함
result.followup_questions = limitedQuestions.questions
result.has_more_questions = limitedQuestions.hasMore
result.more_questions_reason = limitedQuestions.moreReason
```

**적용 파일**: `routes.ts`, `types.ts`

---

### 2.5.4 제외 이유 표시 (Transparency)

**문제**: 유저가 왜 특정 직업이 추천 안 됐는지 모름

**해결책**: 제외된 직업에 대해 1줄 이유 항상 표시

```typescript
// types.ts - 응답 타입 확장
interface AnalysisResultJSON {
  // 기존 필드...

  // 🆕 제외된 직업 설명
  excluded_jobs_summary: {
    total_excluded: number
    by_reason: Array<{
      reason: string
      count: number
      example_jobs: string[]  // 최대 3개
    }>
    user_message: string  // "야근이 많은 직업 15개가 제외되었습니다" 등
  }
}

// routes.ts - 결과 생성 시
function buildExcludedJobsSummary(
  excluded: HardCutItem[]
): AnalysisResultJSON['excluded_jobs_summary'] {
  // 이유별 그룹핑
  const byReason = new Map<string, HardCutItem[]>()
  for (const item of excluded) {
    const existing = byReason.get(item.reason) || []
    existing.push(item)
    byReason.set(item.reason, existing)
  }

  // 요약 생성
  const summaries = Array.from(byReason.entries()).map(([reason, items]) => ({
    reason,
    count: items.length,
    example_jobs: items.slice(0, 3).map(i => i.job_name),
  }))

  // 유저 메시지
  const topReason = summaries.sort((a, b) => b.count - a.count)[0]
  const userMessage = topReason
    ? `${topReason.reason} (${topReason.count}개 직업 제외)`
    : '제외된 직업이 없습니다'

  return {
    total_excluded: excluded.length,
    by_reason: summaries,
    user_message: userMessage,
  }
}
```

**적용 파일**: `routes.ts`, `types.ts`

---

### 2.5.5 Top 3 추천 이유 1:1 연결

**문제**: 점수만 보여주면 "왜 이 직업인지" 이해 어려움

**해결책**: Top 3 직업에 점수 구성요소별 이유 표시

```typescript
// types.ts - Top 추천 타입 확장
interface TopRecommendationWithReason {
  job_id: string
  job_name: string
  scores: { fit: number, like: number, can: number, risk_penalty: number }

  // 🆕 점수별 이유 (1:1 대응)
  score_breakdown: {
    like_reason: string    // "데이터/숫자를 다루는 것에 관심이 있으시다고 하셔서"
    can_reason: string     // "분석 업무 3년+ 경험이 있으시다고 하셔서"
    risk_reason: string    // "야근이 적은 편이라 제약과 충돌 없음"
  }

  // 🆕 한 줄 요약
  one_liner: string  // "관심사(데이터 분석)와 경험(3년+)이 잘 맞고, 제약 충돌 없음"
}

// evidence-generator.ts 또는 새 파일
function generateScoreBreakdown(
  job: ScoredJob,
  facts: Fact[],
  miniModule: MiniModuleResult
): TopRecommendationWithReason['score_breakdown'] {
  // Like 이유 생성
  const likeFactors: string[] = []
  if (miniModule.interest_top?.includes('data_numbers')) {
    likeFactors.push('데이터/숫자를 다루는 것에 관심')
  }
  if (miniModule.value_top?.includes('autonomy')) {
    likeFactors.push('자율적으로 일하는 것을 중시')
  }

  // Can 이유 생성
  const canFactors: string[] = []
  const analyticalFact = facts.find(f => f.fact_key === 'can.analytical.experience_level')
  if (analyticalFact?.value?.canBoost >= 18) {
    canFactors.push('분석 업무 실무 경험 보유')
  }

  // Risk 이유 생성
  const riskFactors: string[] = []
  if (job.scores.risk_penalty === 0) {
    riskFactors.push('선택하신 제약과 충돌 없음')
  } else if (job.scores.risk_penalty < 10) {
    riskFactors.push('일부 주의 사항 있음')
  }

  return {
    like_reason: likeFactors.join(', ') || '관심 분야와 연관됨',
    can_reason: canFactors.join(', ') || '강점을 발휘할 수 있음',
    risk_reason: riskFactors.join(', ') || '제약 조건 확인 필요',
  }
}

function generateOneLiner(breakdown: TopRecommendationWithReason['score_breakdown']): string {
  return `${breakdown.like_reason} + ${breakdown.can_reason}. ${breakdown.risk_reason}`
}
```

**적용 파일**: `routes.ts`, `evidence-generator.ts`, `types.ts`

---

### 2.5.6 안전장치 적용 우선순위

```
구현 순서:
1. Can-Like 밸런스 캡 (P0와 함께)
2. 세션당 질문 수 상한 (P0와 함께)
3. Top 3 추천 이유 표시 (P0와 함께)
4. 제외 이유 표시 (P1과 함께)
5. Hard Exclusion 경력 조건 (P1과 함께)
```

---

## 3. P0: Can 수집 강화

> **우선순위**: 최상 (전체 시스템의 핵심 병목)
> **예상 기간**: 3-4일

### 3.1 Can 검증 질문 추가

#### 3.1.1 새 파일 생성: `can-validation-questions.ts`

```typescript
// src/services/ai-analyzer/can-validation-questions.ts

/**
 * Can 검증 질문 시스템
 *
 * 목적: Q3 Strength 자기평가를 실제 경험으로 검증
 * 트리거: strength_top에 특정 토큰이 있을 때 해당 질문 출력
 */

export interface CanValidationQuestion {
  id: string
  triggerToken: string  // strength_top에서 이 토큰 선택 시 트리거
  question: string
  options: CanValidationOption[]
  factKey: string
}

export interface CanValidationOption {
  value: string
  label: string
  canBoost: number
  description?: string
}

// ============================================
// Can 검증 질문 정의
// ============================================

export const CAN_VALIDATION_QUESTIONS: CanValidationQuestion[] = [
  // 1. 분석력 검증
  {
    id: 'can_val_analytical',
    triggerToken: 'analytical',
    question: '분석력이 강점이라고 하셨는데, 실제로 데이터/숫자를 다루는 업무 경험이 있으신가요?',
    options: [
      {
        value: 'professional_3plus',
        label: '업무로 3년 이상 했어요',
        canBoost: 25,
        description: 'SQL, Excel 고급, BI 도구 등 사용'
      },
      {
        value: 'professional_1to3',
        label: '업무로 1-3년 정도 했어요',
        canBoost: 18,
      },
      {
        value: 'project_experience',
        label: '프로젝트/학교에서 해봤어요',
        canBoost: 10,
      },
      {
        value: 'self_study',
        label: '독학/취미로 해봤어요',
        canBoost: 5,
      },
      {
        value: 'no_experience',
        label: '경험은 없지만 자신 있어요',
        canBoost: 2,
      },
    ],
    factKey: 'can.analytical.experience_level',
  },

  // 2. 창의력 검증
  {
    id: 'can_val_creative',
    triggerToken: 'creative',
    question: '창의력이 강점이라고 하셨는데, 실제로 새로운 것을 만들거나 기획한 경험이 있으신가요?',
    options: [
      {
        value: 'professional_lead',
        label: '기획/디자인을 리드한 경험 있어요',
        canBoost: 25,
      },
      {
        value: 'professional_participate',
        label: '기획/디자인 업무에 참여했어요',
        canBoost: 18,
      },
      {
        value: 'side_project',
        label: '사이드 프로젝트/개인 작업 경험 있어요',
        canBoost: 12,
      },
      {
        value: 'hobby',
        label: '취미로 만들기/창작을 해요',
        canBoost: 6,
      },
      {
        value: 'no_experience',
        label: '경험은 없지만 아이디어가 많아요',
        canBoost: 2,
      },
    ],
    factKey: 'can.creative.experience_level',
  },

  // 3. 소통/설명 능력 검증
  {
    id: 'can_val_communication',
    triggerToken: 'communication',
    question: '소통/설명이 강점이라고 하셨는데, 어떤 상황에서 발휘해보셨나요?',
    options: [
      {
        value: 'presentation_regular',
        label: '프레젠테이션/발표를 정기적으로 해요',
        canBoost: 22,
      },
      {
        value: 'client_facing',
        label: '고객/외부인 대응 업무 경험 있어요',
        canBoost: 20,
      },
      {
        value: 'team_communication',
        label: '팀 내 소통/조율 역할을 했어요',
        canBoost: 15,
      },
      {
        value: 'teaching',
        label: '가르치거나 멘토링한 경험 있어요',
        canBoost: 18,
      },
      {
        value: 'writing',
        label: '글쓰기/문서화를 잘한다는 평가를 받아요',
        canBoost: 12,
      },
    ],
    factKey: 'can.communication.experience_level',
  },

  // 4. 체계적 실행 검증
  {
    id: 'can_val_structured',
    triggerToken: 'structured_execution',
    question: '체계적 실행이 강점이라고 하셨는데, 실제로 프로젝트를 계획하고 완수한 경험이 있으신가요?',
    options: [
      {
        value: 'pm_lead',
        label: '프로젝트 관리/리드 경험 있어요',
        canBoost: 25,
      },
      {
        value: 'process_improvement',
        label: '업무 프로세스를 만들거나 개선했어요',
        canBoost: 20,
      },
      {
        value: 'deadline_management',
        label: '마감 준수율이 높은 편이에요',
        canBoost: 15,
      },
      {
        value: 'personal_system',
        label: '개인 일정/할일 관리를 체계적으로 해요',
        canBoost: 8,
      },
      {
        value: 'aspiration',
        label: '체계적으로 하고 싶은데 잘 안돼요',
        canBoost: 2,
      },
    ],
    factKey: 'can.structured.experience_level',
  },

  // 5. 끈기/지속력 검증
  {
    id: 'can_val_persistence',
    triggerToken: 'persistence',
    question: '끈기가 강점이라고 하셨는데, 실제로 오래 지속한 경험이 있으신가요?',
    options: [
      {
        value: 'long_project',
        label: '1년 이상 장기 프로젝트를 완수했어요',
        canBoost: 25,
      },
      {
        value: 'skill_mastery',
        label: '한 분야를 깊게 파서 전문성을 쌓았어요',
        canBoost: 22,
      },
      {
        value: 'difficult_task',
        label: '힘든 업무도 끝까지 해내는 편이에요',
        canBoost: 15,
      },
      {
        value: 'hobby_consistent',
        label: '취미/운동을 꾸준히 해요',
        canBoost: 8,
      },
      {
        value: 'starting_over',
        label: '시작은 잘하는데 유지가 어려워요',
        canBoost: 0,
      },
    ],
    factKey: 'can.persistence.experience_level',
  },

  // 6. 빠른 학습 검증
  {
    id: 'can_val_fast_learning',
    triggerToken: 'fast_learning',
    question: '빠른 학습이 강점이라고 하셨는데, 실제로 빠르게 습득한 경험이 있으신가요?',
    options: [
      {
        value: 'role_change',
        label: '새로운 역할/분야로 성공적으로 전환했어요',
        canBoost: 25,
      },
      {
        value: 'tech_adoption',
        label: '새 기술/도구를 빠르게 익혀 적용했어요',
        canBoost: 20,
      },
      {
        value: 'quick_onboarding',
        label: '새 업무 적응이 빠르다는 평가를 받아요',
        canBoost: 15,
      },
      {
        value: 'self_study',
        label: '독학으로 새로운 것을 배우는 걸 좋아해요',
        canBoost: 10,
      },
      {
        value: 'guided_learning',
        label: '누가 알려주면 빨리 배워요',
        canBoost: 5,
      },
    ],
    factKey: 'can.fast_learning.experience_level',
  },
]

// ============================================
// 트리거 로직
// ============================================

/**
 * strength_top 토큰 기반으로 물어볼 Can 검증 질문 결정
 */
export function getCanValidationQuestions(
  strengthTop: string[],
  alreadyAsked: string[]
): CanValidationQuestion[] {
  return CAN_VALIDATION_QUESTIONS
    .filter(q => strengthTop.includes(q.triggerToken))
    .filter(q => !alreadyAsked.includes(q.id))
}

/**
 * Can 검증 답변 → Fact 변환
 */
export function convertCanValidationToFact(
  questionId: string,
  selectedValue: string
): { factKey: string; factValue: { value: string; canBoost: number } } | null {
  const question = CAN_VALIDATION_QUESTIONS.find(q => q.id === questionId)
  if (!question) return null

  const option = question.options.find(o => o.value === selectedValue)
  if (!option) return null

  return {
    factKey: question.factKey,
    factValue: {
      value: selectedValue,
      canBoost: option.canBoost,
    }
  }
}
```

### 3.2 Can Boost 규칙 추가

#### 3.2.1 수정 파일: `fact-score-mapping.ts`

기존 `FACT_SCORE_RULES` 배열에 추가:

```typescript
// ============================================
// Can Boost 규칙 (NEW - P0)
// ============================================

// Can 검증 질문 기반 규칙
{
  fact_key_pattern: /^can\.analytical\.experience_level$/,
  priority: MAPPING_POLICY.PRIORITY_ORDER['discovery'],  // 40
  effects: (factValue) => {
    const boost = factValue.canBoost || 0
    return [
      { score_type: 'can', job_attribute: 'analytical', boost },
    ]
  }
},

{
  fact_key_pattern: /^can\.creative\.experience_level$/,
  priority: MAPPING_POLICY.PRIORITY_ORDER['discovery'],
  effects: (factValue) => {
    const boost = factValue.canBoost || 0
    return [
      { score_type: 'can', job_attribute: 'creative', boost },
    ]
  }
},

{
  fact_key_pattern: /^can\.communication\.experience_level$/,
  priority: MAPPING_POLICY.PRIORITY_ORDER['discovery'],
  effects: (factValue) => {
    const boost = factValue.canBoost || 0
    return [
      { score_type: 'can', job_attribute: 'people_facing', boost },
      { score_type: 'can', job_attribute: 'teamwork', boost: Math.round(boost * 0.5) },
    ]
  }
},

{
  fact_key_pattern: /^can\.structured\.experience_level$/,
  priority: MAPPING_POLICY.PRIORITY_ORDER['discovery'],
  effects: (factValue) => {
    const boost = factValue.canBoost || 0
    return [
      { score_type: 'can', job_attribute: 'execution', boost },
    ]
  }
},

{
  fact_key_pattern: /^can\.persistence\.experience_level$/,
  priority: MAPPING_POLICY.PRIORITY_ORDER['discovery'],
  effects: (factValue) => {
    const boost = factValue.canBoost || 0
    return [
      { score_type: 'can', job_attribute: 'execution', boost: Math.round(boost * 0.7) },
      { score_type: 'can', job_attribute: 'stability', boost: Math.round(boost * 0.3) },
    ]
  }
},

{
  fact_key_pattern: /^can\.fast_learning\.experience_level$/,
  priority: MAPPING_POLICY.PRIORITY_ORDER['discovery'],
  effects: (factValue) => {
    const boost = factValue.canBoost || 0
    return [
      { score_type: 'can', job_attribute: 'growth', boost },
    ]
  }
},

// ============================================
// 배경(Q6) → Can 매핑 규칙 (NEW)
// ============================================

{
  fact_key_pattern: 'profile.background.startup_experience',
  priority: MAPPING_POLICY.PRIORITY_ORDER['insight'],  // 30
  effects: (factValue) => {
    if (factValue === true || factValue === 'yes') {
      return [
        { score_type: 'can', job_attribute: 'execution', boost: 15 },
        { score_type: 'can', job_attribute: 'creative', boost: 10 },
        { score_type: 'can', job_attribute: 'growth', boost: 8 },
      ]
    }
    return []
  }
},

{
  fact_key_pattern: 'profile.background.research_academic',
  priority: MAPPING_POLICY.PRIORITY_ORDER['insight'],
  effects: (factValue) => {
    if (factValue === true || factValue === 'yes') {
      return [
        { score_type: 'can', job_attribute: 'analytical', boost: 18 },
        { score_type: 'can', job_attribute: 'solo_work', boost: 10 },
      ]
    }
    return []
  }
},

{
  fact_key_pattern: 'profile.background.overseas_living',
  priority: MAPPING_POLICY.PRIORITY_ORDER['insight'],
  effects: (factValue) => {
    if (factValue === true || factValue === 'yes') {
      return [
        { score_type: 'can', job_attribute: 'people_facing', boost: 8 },
        { score_type: 'can', job_attribute: 'growth', boost: 5 },
      ]
    }
    return []
  }
},

{
  fact_key_pattern: 'profile.background.license_cert',
  priority: MAPPING_POLICY.PRIORITY_ORDER['insight'],
  effects: (factValue) => {
    if (factValue === true || factValue === 'yes') {
      return [
        { score_type: 'can', job_attribute: 'execution', boost: 10 },
        { score_type: 'can', job_attribute: 'stability', boost: 8 },
      ]
    }
    return []
  }
},
```

### 3.3 상수 업데이트

```typescript
// fact-score-mapping.ts 상단 MAPPING_POLICY 수정

export const MAPPING_POLICY = {
  MAX_BOOST_PER_FACT: 25,  // 20 → 25 (Can 검증 질문 최대 25점)
  DUPLICATE_POLICY: 'highest_wins' as const,
  MAX_TOTAL_LIKE_BOOST: 40,
  MAX_TOTAL_CAN_BOOST: 50,  // 30 → 50 (Can Boost 상한 증가)
  MAX_TOTAL_RISK_REDUCTION: 20,

  PRIORITY_ORDER: {
    'confirmed_constraint': 100,
    'priority.dealbreaker': 80,
    'priority.top1': 70,
    'tradeoff': 60,
    'can_validation': 55,  // 🆕 Can 검증 우선순위 추가
    'motivation': 50,
    'discovery': 40,
    'insight': 30,
  } as Record<string, number>,
}
```

### 3.4 라우트 통합

#### 3.4.1 수정 파일: `routes.ts`

`/analyze` 엔드포인트에 Can 검증 질문 흐름 추가:

```typescript
// routes.ts - analyzerRoutes.post('/analyze', ...) 내부

// ... 기존 코드 ...

// 🆕 Can 검증 질문 처리
import {
  getCanValidationQuestions,
  convertCanValidationToFact,
  type CanValidationQuestion
} from './can-validation-questions'

// V3 분석 시 Can 검증 필요 여부 체크
if (isV3 && v3Payload.mini_module_result?.strength_top) {
  const strengthTop = v3Payload.mini_module_result.strength_top
  const cagState = await getOrCreateCAGState(db, v3Payload.session_id)

  // 이미 물어본 Can 검증 질문
  const askedCanQuestions = cagState.asked_questions_log
    .filter(q => q.questionId.startsWith('can_val_'))
    .map(q => q.questionId)

  // 물어볼 Can 검증 질문
  const canQuestionsToAsk = getCanValidationQuestions(strengthTop, askedCanQuestions)

  // Can 검증이 필요하면 followup_questions에 추가
  if (canQuestionsToAsk.length > 0) {
    // 첫 번째 Can 검증 질문만 추가 (한 번에 하나씩)
    const nextCanQuestion = canQuestionsToAsk[0]
    result.followup_questions.unshift({
      id: nextCanQuestion.id,
      type: 'can_validation',
      question: nextCanQuestion.question,
      options: nextCanQuestion.options.map(o => ({
        value: o.value,
        label: o.label,
      })),
      fact_key: nextCanQuestion.factKey,
      priority: 'high',  // Can 검증은 우선순위 높음
    })
  }
}

// Can 검증 답변 처리 (followup 응답 시)
if (payload.can_validation_answers) {
  for (const [questionId, answer] of Object.entries(payload.can_validation_answers)) {
    const fact = convertCanValidationToFact(questionId, answer as string)
    if (fact) {
      await saveUserFact(db, sessionId, userId, fact.factKey, fact.factValue)
    }
  }
}
```

---

## 4. P1: Like/Risk 정교화

> **우선순위**: 높음
> **예상 기간**: 2-3일

### 4.1 Like 검증 질문 추가

#### 4.1.1 수정 파일: `mini-module-questions.ts`

```typescript
// Like 검증 질문 추가

export const LIKE_VALIDATION_QUESTION: MiniModuleQuestion = {
  id: 'mm_like_val',
  text: '선택하신 관심사를 실제로 경험해보신 적 있나요?',
  token: 'like_validation',
  emoji: '✨'
}

export interface LikeValidationResult {
  interest_token: string
  validation_level: 'experienced_loved' | 'experienced_ok' | 'never_tried'
  like_adjustment: number  // +10, 0, +5
}

export const LIKE_VALIDATION_OPTIONS = [
  { value: 'experienced_loved', label: '해봤고 좋았어요', likeBoost: 10 },
  { value: 'experienced_ok', label: '해봤는데 그럭저럭', likeBoost: 0 },
  { value: 'never_tried', label: '안 해봤지만 끌려요', likeBoost: 5 },
]
```

### 4.2 Risk 강도 구분

#### 4.2.1 수정 파일: `tag-filter.ts`

```typescript
// Risk 강도 구분 시스템

export type ConstraintIntensity = 'absolute' | 'prefer_avoid' | 'acceptable'

export interface UserConstraintWithIntensity {
  constraint: keyof UserConstraints
  intensity: ConstraintIntensity
}

// Hard Exclusion은 absolute만 적용
export function shouldHardExclude(
  constraint: UserConstraintWithIntensity,
  rule: HardExclusionRule
): boolean {
  return constraint.intensity === 'absolute' && rule.userConstraint === constraint.constraint
}

// Risk Penalty는 강도에 따라 차등 적용
export function calculateIntensityPenalty(
  basepenalty: number,
  intensity: ConstraintIntensity
): number {
  switch (intensity) {
    case 'absolute': return basepenalty * 1.5  // 150%
    case 'prefer_avoid': return basepenalty     // 100%
    case 'acceptable': return basepenalty * 0.3 // 30%
  }
}
```

#### 4.2.2 강도 확인 질문

```typescript
// 새로 추가: constraint-intensity-questions.ts

export const CONSTRAINT_INTENSITY_QUESTIONS = [
  {
    id: 'intensity_time',
    trigger: 'time_constraint',
    question: '야근이 어려우시다고 하셨는데, 어느 정도인가요?',
    options: [
      { value: 'absolute', label: '절대 불가 (가족/건강 이유)', penalty_multiplier: 1.5 },
      { value: 'prefer_avoid', label: '가능하면 피하고 싶음', penalty_multiplier: 1.0 },
      { value: 'acceptable', label: '가끔은 괜찮음', penalty_multiplier: 0.3 },
    ],
    factKey: 'constraint.time.intensity',
  },
  {
    id: 'intensity_physical',
    trigger: 'physical_constraint',
    question: '육체적으로 힘든 일이 어렵다고 하셨는데, 어느 정도인가요?',
    options: [
      { value: 'absolute', label: '건강상 절대 불가', penalty_multiplier: 1.5 },
      { value: 'prefer_avoid', label: '가능하면 피하고 싶음', penalty_multiplier: 1.0 },
      { value: 'acceptable', label: '적당히는 괜찮음', penalty_multiplier: 0.3 },
    ],
    factKey: 'constraint.physical.intensity',
  },
]
```

---

## 5. P2: RAG/CAG/TAG 통합 개선

> **우선순위**: 중간
> **예상 기간**: 3-4일

### 5.1 RAG: Can/Like 가중 쿼리

#### 5.1.1 수정 파일: `vectorize-pipeline.ts`

```typescript
// 기존 buildSearchQueryFromMiniModule 함수 개선

export interface WeightedSearchQuery {
  primary_keywords: string[]      // 가중치 높은 키워드 (검증된 Can + Like)
  secondary_keywords: string[]    // 일반 키워드
  exclude_keywords: string[]      // 제외 키워드 (Risk)
  boost_weights: Map<string, number>
}

export function buildWeightedSearchQuery(
  miniModule: MiniModuleResult,
  canValidationResults?: Record<string, { canBoost: number }>
): WeightedSearchQuery {
  const primary: string[] = []
  const secondary: string[] = []
  const exclude: string[] = []
  const weights = new Map<string, number>()

  // 1. Like 키워드 (interest + value)
  for (const token of miniModule.interest_top || []) {
    const keyword = TOKEN_TO_KEYWORD[token]
    if (keyword) {
      secondary.push(keyword)
      weights.set(keyword, 1.0)
    }
  }

  // 2. Can 키워드 (검증된 강점 우선)
  for (const token of miniModule.strength_top || []) {
    const keyword = TOKEN_TO_KEYWORD[token]
    if (!keyword) continue

    // Can 검증 결과가 있으면 가중치 반영
    const validation = canValidationResults?.[`can.${token}.experience_level`]
    if (validation && validation.canBoost >= 15) {
      // 검증된 강점 → primary
      primary.push(keyword)
      weights.set(keyword, 1.5)  // 50% 가중치
    } else {
      secondary.push(keyword)
      weights.set(keyword, 0.8)
    }
  }

  // 3. Risk 제외 키워드
  for (const token of miniModule.constraint_flags || []) {
    const excludeKeyword = CONSTRAINT_TO_EXCLUDE_KEYWORD[token]
    if (excludeKeyword) {
      exclude.push(excludeKeyword)
    }
  }

  return { primary_keywords: primary, secondary_keywords: secondary, exclude_keywords: exclude, boost_weights: weights }
}

// Vector Search 시 가중치 적용
export async function searchWithWeights(
  vectorize: VectorizeIndex,
  query: WeightedSearchQuery,
  topK: number = 800
): Promise<VectorSearchResult[]> {
  // Primary 키워드로 먼저 검색 (더 많은 결과)
  const primaryQuery = query.primary_keywords.join(' ')
  const fullQuery = [...query.primary_keywords, ...query.secondary_keywords].join(' ')

  // 두 쿼리 결합 (primary 결과에 가중치)
  const results = await vectorize.query(fullQuery, { topK: topK * 1.2 })

  // 가중치 재계산
  return results.map(r => ({
    ...r,
    adjustedScore: r.score * calculateKeywordBoost(r, query.boost_weights)
  })).sort((a, b) => b.adjustedScore - a.adjustedScore).slice(0, topK)
}
```

### 5.2 CAG: 수집 진행도 추적

#### 5.2.1 수정 파일: `cag-manager.ts`

```typescript
// CAGState 확장

export interface CollectionProgress {
  collected: number      // 수집된 Fact 수
  needed: number         // 필요한 최소 Fact 수
  confidence: number     // 0-1 신뢰도
  lastUpdated: string
}

export interface CAGState {
  // 기존 필드...

  // 🆕 수집 진행도
  collection_progress: {
    like: CollectionProgress
    can: CollectionProgress
    risk: CollectionProgress
  }

  // 🆕 다음 질문 우선순위
  next_priority: 'can' | 'like' | 'risk' | 'validation' | 'complete'
}

// 진행도 계산 함수
export function calculateCollectionProgress(
  facts: Array<{ fact_key: string; value: any }>
): CAGState['collection_progress'] {
  const likeFacts = facts.filter(f =>
    f.fact_key.startsWith('interest.') ||
    f.fact_key.startsWith('value.') ||
    f.fact_key.includes('like')
  )

  const canFacts = facts.filter(f =>
    f.fact_key.startsWith('can.') ||
    f.fact_key.startsWith('strength.') ||
    f.fact_key.startsWith('profile.background')
  )

  const riskFacts = facts.filter(f =>
    f.fact_key.startsWith('constraint.') ||
    f.fact_key.startsWith('risk.') ||
    f.fact_key.includes('drain')
  )

  return {
    like: {
      collected: likeFacts.length,
      needed: 4,  // interest 2 + value 2
      confidence: Math.min(1, likeFacts.length / 4),
      lastUpdated: new Date().toISOString(),
    },
    can: {
      collected: canFacts.length,
      needed: 4,  // strength 2 + validation 2
      confidence: Math.min(1, canFacts.length / 4),
      lastUpdated: new Date().toISOString(),
    },
    risk: {
      collected: riskFacts.length,
      needed: 3,  // constraint + energy_drain
      confidence: Math.min(1, riskFacts.length / 3),
      lastUpdated: new Date().toISOString(),
    },
  }
}

// 다음 질문 우선순위 결정
export function decideNextPriority(
  progress: CAGState['collection_progress']
): CAGState['next_priority'] {
  // Can이 가장 부족하면 Can 우선
  if (progress.can.confidence < 0.5) return 'can'

  // Like 검증 필요
  if (progress.like.confidence < 0.7) return 'like'

  // Risk 보완 필요
  if (progress.risk.confidence < 0.6) return 'risk'

  // 추가 검증
  if (progress.can.confidence < 0.8 || progress.like.confidence < 0.9) return 'validation'

  return 'complete'
}
```

### 5.3 TAG: Can 기반 필터 추가

#### 5.3.1 수정 파일: `tag-filter.ts`

```typescript
// Can 기반 Hard Exclusion 규칙 추가

export const CAN_BASED_EXCLUSION_RULES: HardExclusionRule[] = [
  {
    id: 'no_analytical_exp_vs_required',
    userConstraint: 'no_analytical_experience',  // Can 검증에서 파생
    jobAttribute: 'analytical_required',
    jobCondition: (value) => value === 'high' || value === 'required',
    reason: '분석 역량 필수이나 관련 경험이 없음',
  },
  {
    id: 'no_creative_exp_vs_required',
    userConstraint: 'no_creative_experience',
    jobAttribute: 'creative_required',
    jobCondition: (value) => value === 'high' || value === 'required',
    reason: '창의/기획 역량 필수이나 관련 경험이 없음',
  },
  {
    id: 'no_communication_exp_vs_required',
    userConstraint: 'no_communication_experience',
    jobAttribute: 'communication_required',
    jobCondition: (value) => value === 'high' || value === 'required',
    reason: '소통/프레젠테이션 역량 필수이나 관련 경험이 없음',
  },
]

// Can 검증 결과에서 제약 파생
export function deriveCanConstraints(
  canValidationResults: Record<string, { value: string; canBoost: number }>
): Partial<UserConstraints> {
  const constraints: Partial<UserConstraints> = {}

  // canBoost가 5 이하면 "경험 없음"으로 판단
  for (const [key, result] of Object.entries(canValidationResults)) {
    if (result.canBoost <= 5) {
      if (key.includes('analytical')) constraints.no_analytical_experience = true
      if (key.includes('creative')) constraints.no_creative_experience = true
      if (key.includes('communication')) constraints.no_communication_experience = true
    }
  }

  return constraints
}
```

---

## 6. P3: 심리분석-추천 연결

> **우선순위**: 낮음 (P0~P2 완료 후)
> **예상 기간**: 2-3일

### 6.1 성장 곡선 → 직업 난이도 매칭

#### 6.1.1 수정 파일: `llm-reporter.ts`

```typescript
// 성장 곡선 유형 정의
export type GrowthCurveType =
  | 'gradual'        // 점진적 성장형
  | 'explosive'      // 폭발적 도약형
  | 'cyclical'       // 순환적 정체형
  | 'late_bloomer'   // 늦깎이 개화형

// 성장 곡선 → 추천 직업 난이도 매핑
export const GROWTH_CURVE_TO_JOB_DIFFICULTY: Record<GrowthCurveType, {
  preferred_difficulty: 'low' | 'medium' | 'high'
  risk_tolerance: number  // 0-1
  stability_preference: number  // 0-1
}> = {
  gradual: {
    preferred_difficulty: 'medium',
    risk_tolerance: 0.4,
    stability_preference: 0.7,
  },
  explosive: {
    preferred_difficulty: 'high',
    risk_tolerance: 0.8,
    stability_preference: 0.3,
  },
  cyclical: {
    preferred_difficulty: 'low',
    risk_tolerance: 0.3,
    stability_preference: 0.6,
  },
  late_bloomer: {
    preferred_difficulty: 'medium',
    risk_tolerance: 0.5,
    stability_preference: 0.5,
  },
}
```

### 6.2 내면 갈등 → Risk 조정

```typescript
// 내면 갈등 유형에 따른 Risk 조정
export function adjustRiskByConflict(
  baseRisk: number,
  conflictType: string
): number {
  // "실패 두려움" 갈등 → 불안정한 직업 Risk 증가
  if (conflictType.includes('실패') || conflictType.includes('안전')) {
    return baseRisk * 1.3  // 30% 증가
  }

  // "인정 vs 자유" 갈등 → 자율성 낮은 직업 Risk 증가
  if (conflictType.includes('인정') || conflictType.includes('통제')) {
    return baseRisk * 1.2
  }

  return baseRisk
}
```

### 6.3 통합: Fit 계산에 심리분석 반영

```typescript
// routes.ts - Fit 계산 시 심리분석 결과 반영

// 심리분석 결과가 있으면 Risk 조정
if (premiumReport?.psychology?.growthCurveType) {
  const curveConfig = GROWTH_CURVE_TO_JOB_DIFFICULTY[premiumReport.psychology.growthCurveType]

  for (const job of scoredJobs) {
    // 직업 난이도와 성장 곡선 매칭
    if (job.difficulty === 'high' && curveConfig.preferred_difficulty === 'low') {
      job.scores.risk_penalty += 10  // 난이도 불일치 페널티
    }

    // 내면 갈등 기반 Risk 조정
    if (premiumReport.psychology.innerConflict) {
      job.scores.risk_penalty = adjustRiskByConflict(
        job.scores.risk_penalty,
        premiumReport.psychology.innerConflict
      )
    }
  }
}
```

---

## 7. 파일별 수정 명세

### 7.1 신규 파일

| 파일 | 목적 | P단계 |
|------|------|-------|
| `can-validation-questions.ts` | Can 검증 질문 정의 | P0 |
| `constraint-intensity-questions.ts` | Risk 강도 질문 정의 | P1 |

### 7.2 수정 파일

| 파일 | 수정 내용 | P단계 |
|------|-----------|-------|
| `fact-score-mapping.ts` | Can Boost 규칙 8개 추가, 상수 업데이트 | P0 |
| `routes.ts` | Can 검증 질문 흐름 통합, **Can-Like 밸런스 캡**, **질문 수 제한**, **제외 이유 표시** | P0 |
| `mini-module-questions.ts` | Like 검증 질문 추가 | P1 |
| `tag-filter.ts` | Risk 강도 구분, Can 기반 필터, **경력 단계 조건** | P1 |
| `vectorize-pipeline.ts` | 가중 검색 쿼리 | P2 |
| `cag-manager.ts` | 수집 진행도 추적 | P2 |
| `llm-reporter.ts` | 성장곡선→직업 매핑 | P3 |
| `evidence-generator.ts` | **Top 3 추천 이유 1:1 연결** | P0 |
| `types.ts` | 새 타입 정의 추가, **제외 이유 타입**, **추천 이유 타입** | 전체 |

### 7.3 수정량 예측

```
P0 (Can 강화 + 핵심 안전장치)
├─ can-validation-questions.ts: +250 lines (신규)
├─ fact-score-mapping.ts: +120 lines (규칙 추가)
├─ routes.ts: +150 lines (통합 + 밸런스캡 + 질문제한)
├─ evidence-generator.ts: +80 lines (Top3 이유 생성)
├─ types.ts: +40 lines (새 타입)
└─ 총: ~640 lines

P1 (Like/Risk + 안전장치)
├─ mini-module-questions.ts: +40 lines
├─ tag-filter.ts: +150 lines (강도구분 + 경력조건)
├─ constraint-intensity-questions.ts: +80 lines (신규)
├─ routes.ts: +50 lines (제외이유 표시)
└─ 총: ~320 lines

P2 (RAG/CAG/TAG)
├─ vectorize-pipeline.ts: +150 lines
├─ cag-manager.ts: +100 lines
├─ tag-filter.ts: +60 lines
└─ 총: ~310 lines

P3 (심리분석 연결)
├─ llm-reporter.ts: +80 lines
├─ routes.ts: +50 lines
└─ 총: ~130 lines

전체 예상: ~1,400 lines 추가/수정
```

---

## 8. 테스트 계획

### 8.1 단위 테스트

```typescript
// tests/can-validation.test.ts

describe('Can Validation Questions', () => {
  test('트리거 토큰에 맞는 질문 반환', () => {
    const questions = getCanValidationQuestions(['analytical', 'creative'], [])
    expect(questions).toHaveLength(2)
    expect(questions[0].triggerToken).toBe('analytical')
  })

  test('이미 물어본 질문 제외', () => {
    const questions = getCanValidationQuestions(
      ['analytical', 'creative'],
      ['can_val_analytical']
    )
    expect(questions).toHaveLength(1)
    expect(questions[0].triggerToken).toBe('creative')
  })

  test('Can Boost 변환 정확성', () => {
    const fact = convertCanValidationToFact('can_val_analytical', 'professional_3plus')
    expect(fact?.factValue.canBoost).toBe(25)
  })
})
```

### 8.2 통합 테스트

```typescript
// tests/fit-calculation.test.ts

describe('Fit Score Calculation', () => {
  test('Can Boost가 Fit에 반영됨', async () => {
    const result = await analyzeWithMockData({
      strength_top: ['analytical'],
      can_validation_answers: {
        'can_val_analytical': 'professional_3plus'
      }
    })

    // Can Boost 25점 → Can 점수 증가 확인
    expect(result.scoredJobs[0].scores.can).toBeGreaterThan(50)
  })

  test('Like + Can + Risk 밸런스', async () => {
    const result = await analyzeWithMockData({
      interest_top: ['data_numbers'],      // Like +
      strength_top: ['analytical'],         // Can +
      can_validation_answers: { 'can_val_analytical': 'professional_3plus' },
      constraint_flags: ['time_constraint'] // Risk -
    })

    const topJob = result.scoredJobs[0]
    expect(topJob.scores.like).toBeGreaterThan(60)
    expect(topJob.scores.can).toBeGreaterThan(60)
    expect(topJob.scores.fit).toBeGreaterThan(50)
  })
})
```

### 8.3 E2E 테스트 시나리오

```markdown
## 시나리오 1: 분석가 지망생

입력:
- Interest: problem_solving, data_numbers
- Value: autonomy, growth
- Strength: analytical, fast_learning
- Can 검증: analytical → professional_3plus (25점)
- Constraint: time_constraint (absolute)

예상 결과:
- Top 3에 데이터 분석가, 데이터 엔지니어, 비즈니스 분석가 포함
- 야근 많은 직업 제외 (컨설턴트 등)
- Fit 점수 70+ 직업 최소 5개

## 시나리오 2: 창작자 지망생 (경험 없음)

입력:
- Interest: creating
- Strength: creative
- Can 검증: creative → no_experience (2점)

예상 결과:
- 창작 관련 직업 Like 높음
- 하지만 Can 낮아서 Fit 중간
- "경험을 쌓을 수 있는 진입 경로" 제안
```

---

## 9. 롤백 계획

### 9.1 버전 관리

```typescript
// 버전 플래그로 새 로직 on/off
const FEATURE_FLAGS = {
  CAN_VALIDATION_ENABLED: true,    // P0 완료 후 true
  WEIGHTED_RAG_ENABLED: false,     // P2 완료 후 true
  PSYCHOLOGY_FIT_ENABLED: false,   // P3 완료 후 true
}

// routes.ts에서 조건부 적용
if (FEATURE_FLAGS.CAN_VALIDATION_ENABLED) {
  // 새 Can 검증 로직
} else {
  // 기존 로직
}
```

### 9.2 롤백 트리거

- Fit 점수 분포가 급격히 변화 (평균 ±20% 이상)
- 에러율 5% 이상
- 응답 시간 3초 이상

### 9.3 롤백 절차

```bash
# 1. 피처 플래그 비활성화 (즉시 롤백)
# wrangler.jsonc에서 환경변수 수정
CAN_VALIDATION_ENABLED=false

# 2. 배포
npm run deploy

# 3. 모니터링
npm run check-metrics
```

---

## 📅 타임라인

```
Week 1 (Day 1-4): P0 Can 강화
├─ Day 1: can-validation-questions.ts 작성
├─ Day 2: fact-score-mapping.ts 규칙 추가
├─ Day 3: routes.ts 통합
└─ Day 4: 테스트 + 버그 수정

Week 2 (Day 5-7): P1 Like/Risk
├─ Day 5: Like 검증 질문, Risk 강도 구분
├─ Day 6: tag-filter.ts 업데이트
└─ Day 7: 테스트 + 버그 수정

Week 2-3 (Day 8-11): P2 RAG/CAG/TAG
├─ Day 8-9: vectorize-pipeline.ts 가중 쿼리
├─ Day 10: cag-manager.ts 진행도 추적
└─ Day 11: 테스트 + 통합

Week 3 (Day 12-14): P3 심리분석 연결 + 최종
├─ Day 12: llm-reporter.ts 심리분석 연결
├─ Day 13: E2E 테스트
└─ Day 14: 문서화 + 배포
```

---

## ✅ 완료 체크리스트

### P0 (Can 강화 + 핵심 안전장치)
- [ ] `can-validation-questions.ts` 생성
- [ ] Can 검증 질문 6개 정의
- [ ] Can Boost 규칙 8개 추가
- [ ] 배경(Q6) → Can 매핑 4개 추가
- [ ] routes.ts 통합
- [ ] **⚠️ Can-Like 밸런스 캡 구현** (Can > Like+10 시 페널티)
- [ ] **⚠️ 세션당 질문 수 상한 구현** (MAX 3개)
- [ ] **⚠️ Top 3 추천 이유 1:1 연결** (score_breakdown)
- [ ] 단위 테스트 통과

### P1 (Like/Risk + 안전장치)
- [ ] Like 검증 질문 추가
- [ ] Risk 강도 구분 시스템
- [ ] 강도별 페널티 차등 적용
- [ ] **⚠️ Hard Exclusion 경력 단계 조건** (entry/junior 완화)
- [ ] **⚠️ 제외 이유 표시** (excluded_jobs_summary)
- [ ] 테스트 통과

### P2 (RAG/CAG/TAG)
- [ ] 가중 검색 쿼리 구현
- [ ] CAG 수집 진행도 추적
- [ ] Can 기반 TAG 필터 추가
- [ ] 통합 테스트 통과

### P3 (심리분석)
- [ ] 성장곡선 → 직업 매핑
- [ ] 내면갈등 → Risk 조정
- [ ] E2E 테스트 통과

### 최종
- [ ] 전체 수집률 90%+ 달성
- [ ] Fit 점수 분포 정상
- [ ] **안전장치 5개 전부 동작 확인**
- [ ] 프로덕션 배포 완료

---

> **작성자**: Claude
> **최종 수정**: 2026-02-06
> **상태**: 계획 수립 완료, 구현 대기
