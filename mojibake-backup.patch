diff --git a/src/routes/upload.ts b/src/routes/upload.ts
index 0577041..da1b77b 100644
--- a/src/routes/upload.ts
+++ b/src/routes/upload.ts
@@ -1,6 +1,6 @@
 /**
- * 파일 업로드 + 이미지 생성 라우트
- * Phase 1-4: index.tsx에서 추출
+ * ???뵬 ??낆쨮??+ ???筌왖 ??밴쉐 ??깆뒭??
+ * Phase 1-4: index.tsx?癒?퐣 ?곕뗄??
  */
 import { Hono } from 'hono'
 import type { R2ObjectBody } from '@cloudflare/workers-types'
@@ -8,12 +8,14 @@ import type { AppEnv } from '../types/app'
 import { requireAuth } from '../middleware/auth'
 
 const uploadRoutes = new Hono<AppEnv>()
+const hasImageAdminRole = (role?: string) =>
+  role === 'admin' || role === 'super-admin' || role === 'operator'
 
 // =====================================================
-// 파일 업로드 API
+// ???뵬 ??낆쨮??API
 // =====================================================
 
-// 간단한 업로드 (FormData 방식)
+// 揶쏄쑬?????낆쨮??(FormData 獄쎻뫗??
 uploadRoutes.post('/api/upload', requireAuth, async (c) => {
   try {
     const user = c.get('user')!
@@ -21,36 +23,36 @@ uploadRoutes.post('/api/upload', requireAuth, async (c) => {
     const file = formData['file'] as File
 
     if (!file || !(file instanceof File)) {
-      return c.json({ success: false, error: '파일이 필요합니다' }, 400)
+      return c.json({ success: false, error: '파일이 없습니다.' }, 400)
     }
 
     const { validateContentType, validateFileSize, validateMagicNumber, uploadToR2, generateFileKey } = await import('../services/uploadService')
 
-    // 콘텐츠 타입 검증
+    // ?꾩꼹?쀯㎘?????野꺜筌?
     const typeResult = validateContentType(file.type)
     if (!typeResult.valid) {
       return c.json({ success: false, error: typeResult.error }, 400)
     }
 
-    // 파일 크기 검증
+    // ???뵬 ??由?野꺜筌?
     const sizeResult = validateFileSize(file.size)
     if (!sizeResult.valid) {
       return c.json({ success: false, error: sizeResult.error }, 400)
     }
 
-    // 파일 데이터 읽기
+    // ???뵬 ?怨쀬뵠????꾨┛
     const body = await file.arrayBuffer()
 
-    // 매직 넘버 검증
+    // 筌띲끉彛???롮쒔 野꺜筌?
     if (!validateMagicNumber(body, file.type)) {
-      return c.json({ success: false, error: '파일 형식이 올바르지 않습니다' }, 400)
+      return c.json({ success: false, error: '파일 형식이 올바르지 않습니다.' }, 400)
     }
 
-    // 파일 키 생성 (howto/YYYY/MM/DD/{파일명}-{shortId}.ext 형식)
+    // ???뵬 ????밴쉐 (howto/YYYY/MM/DD/{???뵬筌?-{shortId}.ext ?類ㅻ뻼)
     const ext = file.name.split('.').pop()?.toLowerCase() || 'bin'
     const fileKey = generateFileKey(ext, file.name)
 
-    // R2에 업로드
+    // R2????낆쨮??
     const uploadResult = await uploadToR2(c.env.UPLOADS, fileKey, body, file.type, {
       uploadedBy: user.id.toString(),
       originalName: file.name
@@ -60,17 +62,17 @@ uploadRoutes.post('/api/upload', requireAuth, async (c) => {
       return c.json({ success: false, error: uploadResult.error }, 500)
     }
 
-    // URL 반환
+    // URL 獄쏆꼹??
     const baseUrl = new URL(c.req.url).origin
     const publicUrl = `${baseUrl}/uploads/${fileKey}`
 
     return c.json({ success: true, url: publicUrl })
   } catch (error) {
-    return c.json({ success: false, error: '업로드 중 오류가 발생했습니다' }, 500)
+    return c.json({ success: false, error: '업로드 처리 중 오류가 발생했습니다.' }, 500)
   }
 })
 
-// 업로드 준비 (파일 키 발급)
+// ??낆쨮??餓Β??(???뵬 ??獄쏆뮄??
 uploadRoutes.post('/api/upload/prepare', requireAuth, async (c) => {
   try {
     const user = c.get('user')!
@@ -78,12 +80,12 @@ uploadRoutes.post('/api/upload/prepare', requireAuth, async (c) => {
     const { filename, contentType, contentLength } = body
 
     if (!filename || !contentType || !contentLength) {
-      return c.json({ success: false, error: '필수 정보가 누락되었습니다' }, 400)
+      return c.json({ success: false, error: '필수 입력값이 없습니다.' }, 400)
     }
 
     const { createUploadInfo, validateContentType, validateFileSize } = await import('../services/uploadService')
 
-    // 검증
+    // 野꺜筌?
     const typeResult = validateContentType(contentType)
     if (!typeResult.valid) {
       return c.json({ success: false, error: typeResult.error }, 400)
@@ -94,13 +96,13 @@ uploadRoutes.post('/api/upload/prepare', requireAuth, async (c) => {
       return c.json({ success: false, error: sizeResult.error }, 400)
     }
 
-    // 업로드 정보 생성
+    // ??낆쨮???類ｋ궖 ??밴쉐
     const uploadInfo = createUploadInfo({ filename, contentType, contentLength })
     if (!uploadInfo.success) {
       return c.json({ success: false, error: uploadInfo.error }, 400)
     }
 
-    // 기본 URL 결정
+    // 疫꿸퀡??URL 野껉퀣??
     const baseUrl = new URL(c.req.url).origin
 
     return c.json({
@@ -108,14 +110,14 @@ uploadRoutes.post('/api/upload/prepare', requireAuth, async (c) => {
       fileKey: uploadInfo.data!.fileKey,
       uploadUrl: `${baseUrl}/api/upload/file`,
       publicUrl: `${baseUrl}/uploads/${uploadInfo.data!.fileKey}`,
-      expiresIn: 300 // 5분
+      expiresIn: 300 // 5??
     })
   } catch (error) {
-    return c.json({ success: false, error: '업로드 준비 중 오류가 발생했습니다' }, 500)
+    return c.json({ success: false, error: '업로드 준비 중 오류가 발생했습니다.' }, 500)
   }
 })
 
-// 파일 업로드 (실제 업로드)
+// ???뵬 ??낆쨮??(??쇱젫 ??낆쨮??
 uploadRoutes.post('/api/upload/file', requireAuth, async (c) => {
   try {
     const user = c.get('user')!
@@ -123,26 +125,26 @@ uploadRoutes.post('/api/upload/file', requireAuth, async (c) => {
     const contentType = c.req.header('Content-Type') || ''
 
     if (!fileKey) {
-      return c.json({ success: false, error: '파일 키가 필요합니다' }, 400)
+      return c.json({ success: false, error: '파일 키가 없습니다.' }, 400)
     }
 
     const { validateContentType, validateMagicNumber, uploadToR2, getPublicUrl } = await import('../services/uploadService')
 
-    // 콘텐츠 타입 검증
+    // ?꾩꼹?쀯㎘?????野꺜筌?
     const typeResult = validateContentType(contentType)
     if (!typeResult.valid) {
       return c.json({ success: false, error: typeResult.error }, 400)
     }
 
-    // 파일 본문 읽기
+    // ???뵬 癰귣챶揆 ??꾨┛
     const body = await c.req.arrayBuffer()
 
-    // 매직 넘버 검증
+    // 筌띲끉彛???롮쒔 野꺜筌?
     if (!validateMagicNumber(body, contentType)) {
-      return c.json({ success: false, error: '파일 형식이 올바르지 않습니다 (파일 시그니처 불일치)' }, 400)
+      return c.json({ success: false, error: '파일 형식이 올바르지 않습니다.' }, 400)
     }
 
-    // R2에 업로드
+    // R2????낆쨮??
     const uploadResult = await uploadToR2(c.env.UPLOADS, fileKey, body, contentType, {
       uploadedBy: user.id.toString(),
       originalContentType: contentType
@@ -152,7 +154,7 @@ uploadRoutes.post('/api/upload/file', requireAuth, async (c) => {
       return c.json({ success: false, error: uploadResult.error }, 500)
     }
 
-    // DB에 메타데이터 저장
+    // DB??筌롫???怨쀬뵠??????
     await c.env.DB.prepare(`
       INSERT INTO uploaded_files (user_id, file_key, content_type, file_size)
       VALUES (?, ?, ?, ?)
@@ -167,11 +169,11 @@ uploadRoutes.post('/api/upload/file', requireAuth, async (c) => {
       size: body.byteLength
     })
   } catch (error) {
-    return c.json({ success: false, error: '파일 업로드 중 오류가 발생했습니다' }, 500)
+    return c.json({ success: false, error: '파일 업로드 중 오류가 발생했습니다.' }, 500)
   }
 })
 
-// 이미지 서빙 (R2 프록시)
+// ???筌왖 ??뺥뒅 (R2 ?袁⑥쨯??
 uploadRoutes.get('/uploads/*', async (c) => {
   try {
     const rawUrl = c.req.raw.url
@@ -186,27 +188,27 @@ uploadRoutes.get('/uploads/*', async (c) => {
       return c.json({ error: 'R2 bucket not bound' }, 500)
     }
 
-    // R2 키가 다양한 형태로 저장되어 있으므로 모든 변형을 시도
-    // 옛 이미지: 인코딩된 키 (jobs/job-%EB%B2%95%EC%9D%98%ED%95%99%EC%9E%90.webp)
-    // 새 이미지: 디코딩된 키 (jobs/job-법의학자.webp)
+    // R2 ??? ??쇰펶???類κ묶嚥????貫由????됱몵沃샕嚥?筌뤴뫀諭?癰궰?類ㅼ뱽 ??뺣즲
+    // ?????筌왖: ?紐꾪맜??몃쭆 ??(jobs/job-%EB%B2%95%EC%9D%98%ED%95%99%EC%9E%90.webp)
+    // ?????筌왖: ?遺욱맜??몃쭆 ??(jobs/job-甕곕벡???덉쁽.webp)
     const decodedPath = decodeURIComponent(pathAfterUploads)
 
-    // 파일명에서 슬래시→언더스코어 변환 (UI/UX 같은 이름 처리)
+    // ???뵬筌뤿굞肉???????뗫꼥?紐껊쐭??쇳맜??癰궰??(UI/UX 揶쏆늿? ??已?筌ｌ꼶??
     const firstSlash = decodedPath.indexOf('/')
     const dir = firstSlash !== -1 ? decodedPath.substring(0, firstSlash) : ''
     const filename = firstSlash !== -1 ? decodedPath.substring(firstSlash + 1) : decodedPath
     const safeFilename = filename.replace(/\//g, '_')
 
-    // 파일명 부분만 인코딩한 키 (디렉토리 구분자는 유지)
+    // ???뵬筌??봔?브쑬彛??紐꾪맜??븍립 ??(?遺얠젂?醫듼봺 ?닌됲뀋?癒?뮉 ?醫?)
     const reEncodedFilename = encodeURIComponent(safeFilename).replace(/%2F/g, '/')
     const reEncodedPath = dir ? `${dir}/${reEncodedFilename}` : reEncodedFilename
 
-    // 시도할 R2 키 목록 (중복 제거)
+    // ??뺣즲??R2 ??筌뤴뫖以?(餓λ쵎????볤탢)
     const keysToTry = [...new Set([
-      decodedPath,                                    // 1. 디코딩된 한글 (새 이미지)
-      pathAfterUploads,                               // 2. URL에서 추출한 원본 (인코딩된 상태)
-      reEncodedPath,                                  // 3. 파일명만 재인코딩
-      dir ? `${dir}/${safeFilename}` : safeFilename,  // 4. 슬래시→언더스코어 변환
+      decodedPath,                                    // 1. ?遺욱맜??몃쭆 ??? (?????筌왖)
+      pathAfterUploads,                               // 2. URL?癒?퐣 ?곕뗄????癒?궚 (?紐꾪맜??몃쭆 ?怨밴묶)
+      reEncodedPath,                                  // 3. ???뵬筌뤿굝彛????ㅸ굜遺얜뎃
+      dir ? `${dir}/${safeFilename}` : safeFilename,  // 4. ?????뗫꼥?紐껊쐭??쇳맜??癰궰??
     ])]
 
     let object: R2ObjectBody | null = null
@@ -227,7 +229,7 @@ uploadRoutes.get('/uploads/*', async (c) => {
       }, 404)
     }
 
-    // 확장자 기반 Content-Type 추론 (R2 메타데이터가 없을 경우 대비)
+    // ?類ㅼ삢??疫꿸퀡而?Content-Type ?곕뗀以?(R2 筌롫???怨쀬뵠?怨? ??곸뱽 野껋럩??????
     const getContentTypeByExtension = (filepath: string): string => {
       const ext = filepath.split('.').pop()?.toLowerCase()
       const mimeTypes: Record<string, string> = {
@@ -243,18 +245,18 @@ uploadRoutes.get('/uploads/*', async (c) => {
       return mimeTypes[ext || ''] || 'application/octet-stream'
     }
 
-    // 캐시 헤더 설정 (1년)
+    // 筌?Ŋ????삳쐭 ??쇱젟 (1??
     const headers = new Headers()
     const contentType = object.httpMetadata?.contentType || getContentTypeByExtension(usedPath)
     headers.set('Content-Type', contentType)
-    // ?v= 캐시 버스터가 있으면 장기 캐시, 없으면 revalidate
+    // ?v= 筌?Ŋ??甕곌쑴??怨? ??됱몵筌??觀由?筌?Ŋ?? ??곸몵筌?revalidate
     const hasVersionParam = new URL(c.req.raw.url).searchParams.has('v')
     headers.set('Cache-Control', hasVersionParam
       ? 'public, max-age=31536000, immutable'
       : 'public, max-age=3600, must-revalidate')
     headers.set('ETag', object.httpEtag)
 
-    // If-None-Match 헤더 확인 (304 응답)
+    // If-None-Match ??삳쐭 ?類ㅼ뵥 (304 ?臾먮뼗)
     const ifNoneMatch = c.req.header('If-None-Match')
     if (ifNoneMatch && ifNoneMatch === object.httpEtag) {
       return new Response(null, { status: 304, headers })
@@ -262,7 +264,7 @@ uploadRoutes.get('/uploads/*', async (c) => {
 
     return new Response(object.body as ReadableStream, { headers })
   } catch (error) {
-    // 에러 발생 시 상세 정보 반환
+    // ?癒?쑎 獄쏆뮇源????怨멸쉭 ?類ｋ궖 獄쏆꼹??
     return c.json({
       error: 'R2 proxy error',
       message: error instanceof Error ? error.message : String(error)
@@ -271,22 +273,22 @@ uploadRoutes.get('/uploads/*', async (c) => {
 })
 
 // ============================================
-// Z-Image Turbo 이미지 생성 API
+// Z-Image Turbo ???筌왖 ??밴쉐 API
 // ============================================
 
-// 이미지 생성 요청 (관리자 전용)
+// ???筌왖 ??밴쉐 ?遺욧퍕 (?온?귐딆쁽 ?袁⑹뒠)
 uploadRoutes.post('/api/image/generate', requireAuth, async (c) => {
   try {
     const user = c.get('user')!
 
-    // 관리자 권한 확인
+    // ?온?귐딆쁽 亦낅슦釉??類ㅼ뵥
     if (user.role !== 'admin') {
-      return c.json({ success: false, error: '관리자 권한이 필요합니다' }, 403)
+      return c.json({ success: false, error: '관리자 권한이 필요합니다.' }, 403)
     }
 
     const apiKey = c.env.EVOLINK_API_KEY
     if (!apiKey) {
-      return c.json({ success: false, error: 'EVOLINK_API_KEY가 설정되지 않았습니다' }, 500)
+      return c.json({ success: false, error: 'EVOLINK_API_KEY가 설정되지 않았습니다.' }, 500)
     }
 
     const body = await c.req.json()
@@ -297,10 +299,10 @@ uploadRoutes.post('/api/image/generate', requireAuth, async (c) => {
     }
 
     if (!type || !slug) {
-      return c.json({ success: false, error: 'type과 slug가 필요합니다' }, 400)
+      return c.json({ success: false, error: 'type과 slug가 필요합니다.' }, 400)
     }
 
-    // 프롬프트 결정: promptOverride → DB image_prompt → Gemini 자동 생성
+    // ?袁⑨세?袁る뱜 野껉퀣?? promptOverride ??DB image_prompt ??Gemini ?癒?짗 ??밴쉐
     let imagePrompt: string | undefined = promptOverride
     if (!imagePrompt) {
       const table = type === 'jobs' ? 'jobs' : 'majors'
@@ -312,7 +314,7 @@ uploadRoutes.post('/api/image/generate', requireAuth, async (c) => {
       if (record?.image_prompt) {
         imagePrompt = record.image_prompt
       } else if (record?.item_name && c.env.GEMINI_API_KEY) {
-        // Gemini로 자동 프롬프트 생성
+        // Gemini嚥??癒?짗 ?袁⑨세?袁る뱜 ??밴쉐
         const { generateJobImagePrompt, generateMajorImagePrompt } = await import('../services/geminiService')
         const geminiResult = type === 'jobs'
           ? await generateJobImagePrompt(c.env.GEMINI_API_KEY, record.item_name)
@@ -320,22 +322,22 @@ uploadRoutes.post('/api/image/generate', requireAuth, async (c) => {
 
         if (geminiResult.success && geminiResult.prompt) {
           imagePrompt = geminiResult.prompt
-          // 생성된 프롬프트를 DB에 저장 (다음에 재사용)
+          // ??밴쉐???袁⑨세?袁る뱜??DB??????(??쇱벉????沅??
           await c.env.DB.prepare(`UPDATE ${table} SET image_prompt = ? WHERE slug = ?`)
             .bind(imagePrompt, slug).run()
         } else {
-          return c.json({ success: false, error: '이미지 프롬프트 자동 생성 실패' }, 500)
+          return c.json({ success: false, error: '이미지 프롬프트 생성에 실패했습니다.' }, 500)
         }
       } else {
-        return c.json({ success: false, error: '해당 항목을 찾을 수 없습니다' }, 404)
+        return c.json({ success: false, error: '대상 항목을 찾을 수 없습니다.' }, 404)
       }
     }
 
-    // 콜백 URL 생성
+    // ?꾩뮆媛?URL ??밴쉐
     const baseUrl = new URL(c.req.url).origin
     const callbackUrl = `${baseUrl}/webhooks/image-completed`
 
-    // Z-Image Turbo API 호출 (매번 다른 이미지를 위해 랜덤 seed 사용)
+    // Z-Image Turbo API ?紐꾪뀱 (筌띲끇苡???삘뀲 ???筌왖???袁る퉸 ??뺣쑁 seed ????
     const { requestImageGeneration } = await import('../services/imageGenerationService')
     const randomSeed = Math.floor(Math.random() * 2147483647)
     console.log(`[ImageGen] Requesting: type=${type}, slug=${slug}, seed=${randomSeed}, prompt="${(imagePrompt || '').substring(0, 80)}..."`)
@@ -347,22 +349,22 @@ uploadRoutes.post('/api/image/generate', requireAuth, async (c) => {
     })
 
     if (!result.success || !result.data) {
-      return c.json({ success: false, error: result.error || '이미지 생성 요청 실패' }, 500)
+      return c.json({ success: false, error: result.error || '이미지 생성 요청에 실패했습니다.' }, 500)
     }
 
-    // 태스크 정보를 KV에 저장 (콜백 시 사용)
+    // ??뽯뮞???類ｋ궖??KV??????(?꾩뮆媛???????
     const taskMeta = {
       type,
       slug,
-      prompt: (imagePrompt || '').substring(0, 200), // 프롬프트 요약
+      prompt: (imagePrompt || '').substring(0, 200), // ?袁⑨세?袁る뱜 ?遺용튋
       createdAt: Date.now(),
       createdBy: user.id
     }
 
-    // KV가 바인딩되어 있으면 저장 (로컬 개발 환경에서는 없을 수 있음)
+    // KV揶쎛 獄쏅뗄???몃┷????됱몵筌?????(嚥≪뮇類?揶쏆뮆而???띻펾?癒?퐣????곸뱽 ????됱벉)
     if (c.env.KV) {
       await c.env.KV.put(`image-task:${result.data.id}`, JSON.stringify(taskMeta), {
-        expirationTtl: 86400 // 24시간
+        expirationTtl: 86400 // 24??볦퍢
       })
     } else {
     }
@@ -374,37 +376,37 @@ uploadRoutes.post('/api/image/generate', requireAuth, async (c) => {
       estimatedTime: result.data.task_info?.estimated_time || 10
     })
   } catch (error) {
-    return c.json({ success: false, error: '이미지 생성 요청 중 오류 발생' }, 500)
+    return c.json({ success: false, error: '이미지 생성 요청 중 오류가 발생했습니다.' }, 500)
   }
 })
 
-// 이미지 생성 상태 조회
+// ???筌왖 ??밴쉐 ?怨밴묶 鈺곌퀬??
 uploadRoutes.get('/api/image/status/:taskId', requireAuth, async (c) => {
   try {
     const taskId = c.req.param('taskId')
     const apiKey = c.env.EVOLINK_API_KEY
 
     if (!apiKey) {
-      return c.json({ success: false, error: 'EVOLINK_API_KEY가 설정되지 않았습니다' }, 500)
+      return c.json({ success: false, error: 'EVOLINK_API_KEY가 설정되지 않았습니다.' }, 500)
     }
 
     const { queryTaskStatus } = await import('../services/imageGenerationService')
     const result = await queryTaskStatus(apiKey, taskId)
 
-    // 🔍 디버그: API 응답 로깅
+    // ?逾??遺얠쒔域? API ?臾먮뼗 嚥≪뮄??
     console.log(`[ImageStatus] taskId=${taskId}, raw response:`, JSON.stringify(result.data).substring(0, 500))
 
     if (!result.success || !result.data) {
-      return c.json({ success: false, error: result.error || '상태 조회 실패' }, 500)
+      return c.json({ success: false, error: result.error || '상태 조회에 실패했습니다.' }, 500)
     }
 
-    // 상태 정규화 (다양한 API 응답 형식 처리)
+    // ?怨밴묶 ?類?뇣??(??쇰펶??API ?臾먮뼗 ?類ㅻ뻼 筌ｌ꼶??
     const rawStatus = (result.data as any).status || (result.data as any).state || ''
     const normalizedStatus = rawStatus.toLowerCase()
     const isCompleted = normalizedStatus === 'completed' || normalizedStatus === 'success' || normalizedStatus === 'done'
     const isFailed = normalizedStatus === 'failed' || normalizedStatus === 'error'
 
-    // results 배열 우선, data.url/urls는 fallback
+    // results 獄쏄퀣肉??怨쀪퐨, data.url/urls??fallback
     const imageUrl = (result.data as any).results?.[0] || (result.data as any).data?.url || (result.data as any).data?.urls?.[0] || (result.data as any).output?.url
 
     console.log(`[ImageStatus] taskId=${taskId}, status=${rawStatus}, isCompleted=${isCompleted}, imageUrl=${imageUrl ? imageUrl.substring(0, 120) : 'null'}`)
@@ -417,18 +419,18 @@ uploadRoutes.get('/api/image/status/:taskId', requireAuth, async (c) => {
       imageUrl
     })
   } catch (error) {
-    return c.json({ success: false, error: '상태 조회 중 오류 발생' }, 500)
+    return c.json({ success: false, error: '상태 조회 중 오류가 발생했습니다.' }, 500)
   }
 })
 
-// 이미지 저장 API (폴링 방식용 - 로컬 환경에서 콜백이 작동하지 않을 때 사용)
+// ???筌왖 ????API (??彛?獄쎻뫗???- 嚥≪뮇類???띻펾?癒?퐣 ?꾩뮆媛???臾먮짗??? ??놁뱽 ??????
 uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
   try {
     const user = c.get('user')!
 
-    // 관리자 권한 확인
+    // ?온?귐딆쁽 亦낅슦釉??類ㅼ뵥
     if (user.role !== 'admin') {
-      return c.json({ success: false, error: '관리자 권한이 필요합니다' }, 403)
+      return c.json({ success: false, error: '관리자 권한이 필요합니다.' }, 403)
     }
 
     const body = await c.req.json()
@@ -440,40 +442,40 @@ uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
     }
 
     if (!taskId || !type || !slug || !imageUrl) {
-      return c.json({ success: false, error: 'taskId, type, slug, imageUrl가 필요합니다' }, 400)
+      return c.json({ success: false, error: 'taskId, type, slug, imageUrl이 필요합니다.' }, 400)
     }
 
     const { downloadImage, generateImageFileKey, getImagePublicUrl } =
       await import('../services/imageGenerationService')
     const { uploadToR2 } = await import('../services/uploadService')
 
-    // 이미지 다운로드
+    // ???筌왖 ??쇱뒲嚥≪뮆諭?
     console.log(`[ImageSave] Downloading from: ${imageUrl}`)
     const downloadResult = await downloadImage(imageUrl)
     if (!downloadResult.success || !downloadResult.data) {
       console.log(`[ImageSave] Download FAILED: ${downloadResult.error}`)
-      return c.json({ success: false, error: downloadResult.error || '이미지 다운로드 실패', debug: { imageUrl, downloadError: downloadResult.error } }, 500)
+      return c.json({ success: false, error: downloadResult.error || '이미지 다운로드에 실패했습니다.', debug: { imageUrl, downloadError: downloadResult.error } }, 500)
     }
 
     const downloadSize = downloadResult.data.byteLength
     console.log(`[ImageSave] Downloaded: ${downloadSize} bytes, type=${downloadResult.contentType}, url=${imageUrl}`)
 
-    // R2에 업로드 (기존 이미지 덮어쓰기)
+    // R2????낆쨮??(疫꿸퀣?????筌왖 ??堉?怨뚮┛)
     const actualContentType = downloadResult.contentType || 'image/png'
     const fileKey = generateImageFileKey(type, slug, actualContentType)
 
-    // 기존 이미지가 다른 확장자 또는 다른 인코딩으로 저장되어 있을 수 있으므로
-    // 모든 변형(인코딩/디코딩 + 확장자)을 삭제
+    // 疫꿸퀣?????筌왖揶쎛 ??삘뀲 ?類ㅼ삢???癒?뮉 ??삘뀲 ?紐꾪맜??뱀몵嚥????貫由????됱뱽 ????됱몵沃샕嚥?
+    // 筌뤴뫀諭?癰궰???紐꾪맜???遺욱맜??+ ?類ㅼ삢????????
     const extensions = ['webp', 'png', 'jpg']
     const prefix = type === 'jobs' ? 'job' : 'major'
     const safeSlug = slug.replace(/\//g, '_')
     const encodedSlug = encodeURIComponent(safeSlug)
-    const decodedSlug = safeSlug  // 한글 그대로
+    const decodedSlug = safeSlug  // ??? 域밸챶?嚥?
 
     for (const ext of extensions) {
-      // 인코딩된 키 (generateImageFileKey가 생성하는 형식)
+      // ?紐꾪맜??몃쭆 ??(generateImageFileKey揶쎛 ??밴쉐??롫뮉 ?類ㅻ뻼)
       const encodedKey = `${type}/${prefix}-${encodedSlug}.${ext}`
-      // 디코딩된 키 (기존 autoImageService 등이 저장한 형식)
+      // ?遺욱맜??몃쭆 ??(疫꿸퀣??autoImageService ?源놁뵠 ???館釉??類ㅻ뻼)
       const decodedKey = `${type}/${prefix}-${decodedSlug}.${ext}`
 
       for (const oldKey of [encodedKey, decodedKey]) {
@@ -498,18 +500,18 @@ uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
 
     if (!uploadResult.success) {
       console.log(`[ImageSave] R2 upload FAILED: ${uploadResult.error}`)
-      return c.json({ success: false, error: uploadResult.error || 'R2 업로드 실패' }, 500)
+      return c.json({ success: false, error: uploadResult.error || 'R2 업로드에 실패했습니다.' }, 500)
     }
     console.log(`[ImageSave] R2 upload OK: key=${fileKey}`)
 
-    // DB 업데이트 (image_url 컬럼 + merged_profile_json 둘 다 업데이트)
+    // DB ??낅쑓??꾨뱜 (image_url ?뚎됱쓥 + merged_profile_json ??????낅쑓??꾨뱜)
     const baseUrl = new URL(c.req.url).origin
     const publicUrl = getImagePublicUrl(fileKey, baseUrl)
-    // 캐시 버스터 추가 (브라우저 캐시 우회)
+    // 筌?Ŋ??甕곌쑴????곕떽? (?됰슢??怨? 筌?Ŋ???怨좎돳)
     const publicUrlWithCache = `${publicUrl}?v=${Date.now()}`
     const table = type === 'jobs' ? 'jobs' : 'majors'
 
-    // slug로 레코드 찾기 - image_url 컬럼과 merged_profile_json 둘 다 업데이트
+    // slug嚥???됲맜??筌≪뼐由?- image_url ?뚎됱쓥??merged_profile_json ??????낅쑓??꾨뱜
     let updateResult = await c.env.DB.prepare(`
       UPDATE ${table}
       SET image_url = ?,
@@ -517,7 +519,7 @@ uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
       WHERE slug = ?
     `).bind(publicUrlWithCache, publicUrlWithCache, slug).run()
 
-    // slug로 못 찾으면 name으로 시도
+    // slug嚥?筌?筌≪뼚?앾쭖?name??곗쨮 ??뺣즲
     if (updateResult.meta.changes === 0) {
       updateResult = await c.env.DB.prepare(`
         UPDATE ${table}
@@ -528,14 +530,14 @@ uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
     }
 
 
-    // CDN 캐시 퍼지 (페이지 + 이미지 URL 모두)
+    // CDN 筌?Ŋ????? (??륁뵠筌왖 + ???筌왖 URL 筌뤴뫀紐?
     try {
       const baseUrl = new URL(c.req.url).origin
       const pageType = type === 'jobs' ? 'job' : 'major'
       const pageUrl = `${baseUrl}/${pageType}/${encodeURIComponent(slug)}`
-      const imageUrlFull = `${baseUrl}${publicUrl}`  // /uploads/... 절대 URL
+      const imageUrlFull = `${baseUrl}${publicUrl}`  // /uploads/... ??? URL
 
-      // 1) Workers Cache API — 페이지와 이미지 모두 퍼지
+      // 1) Workers Cache API ????륁뵠筌왖?? ???筌왖 筌뤴뫀紐????
       const cache = (caches as any).default
       const urlsToPurge = [
         pageUrl,
@@ -548,7 +550,7 @@ uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
       }
       console.log(`[ImageSave] Cache purged: ${urlsToPurge.length} URLs`)
 
-      // 1.5) KV 목록 캐시 무효화 (썸네일 갱신)
+      // 1.5) KV 筌뤴뫖以?筌?Ŋ???얜똾???(?紐껉퐬??揶쏄퉮??
       if (c.env.KV) {
         const { invalidateListCache } = await import('../services/cacheService')
         const listType = pageType as 'job' | 'major'
@@ -556,11 +558,11 @@ uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
         console.log(`[ImageSave] List KV cache purged: ${deleted} keys for ${listType}`)
       }
 
-      // 2) Cloudflare Zone Purge API (CF_ZONE_ID + CF_API_TOKEN 필요)
+      // 2) Cloudflare Zone Purge API (CF_ZONE_ID + CF_API_TOKEN ?袁⑹뒄)
       const zoneId = c.env.CF_ZONE_ID
       const apiToken = c.env.CLOUDFLARE_API_TOKEN
       if (zoneId && apiToken) {
-        // prefix_purge로 이미지 URL 변형 모두 퍼지
+        // prefix_purge嚥????筌왖 URL 癰궰??筌뤴뫀紐????
         await fetch(`https://api.cloudflare.com/client/v4/zones/${zoneId}/purge_cache`, {
           method: 'POST',
           headers: { 'Authorization': `Bearer ${apiToken}`, 'Content-Type': 'application/json' },
@@ -580,15 +582,15 @@ uploadRoutes.post('/api/image/save', requireAuth, async (c) => {
     return c.json({
       success: true,
       imageUrl: publicUrlWithCache,
-      message: '이미지가 성공적으로 저장되었습니다. 페이지를 새로고침해주세요.',
+      message: '???筌왖揶쎛 ?源껊궗?怨몄몵嚥????貫由??됰뮸??덈뼄. ??륁뵠筌왖????덉쨮?⑥쥙臾??곻폒?紐꾩뒄.',
       debug: { fileKey, downloadSize, actualContentType, sourceUrl: imageUrl, dbChanges: updateResult.meta.changes }
     })
   } catch (error) {
-    return c.json({ success: false, error: '이미지 저장 중 오류 발생' }, 500)
+    return c.json({ success: false, error: '이미지 저장 중 오류가 발생했습니다.' }, 500)
   }
 })
 
-// Z-Image Turbo 콜백 핸들러 (이미지 생성 완료 시 호출됨)
+// Z-Image Turbo ?꾩뮆媛??紐껊굶??(???筌왖 ??밴쉐 ?袁⑥┷ ???紐꾪뀱??
 uploadRoutes.post('/webhooks/image-completed', async (c) => {
   try {
     const body = await c.req.json()
@@ -597,7 +599,7 @@ uploadRoutes.post('/webhooks/image-completed', async (c) => {
       await import('../services/imageGenerationService')
     const { uploadToR2 } = await import('../services/uploadService')
 
-    // 콜백 데이터 파싱
+    // ?꾩뮆媛??怨쀬뵠?????뼓
     const taskData = parseCallbackData(body)
     if (!taskData) {
       return c.json({ success: false, error: 'Invalid callback data' }, 400)
@@ -606,7 +608,7 @@ uploadRoutes.post('/webhooks/image-completed', async (c) => {
     const taskId = taskData.id
     const status = taskData.status
 
-    // 태스크 메타데이터 조회
+    // ??뽯뮞??筌롫???怨쀬뵠??鈺곌퀬??
     const taskMetaStr = await c.env.KV.get(`image-task:${taskId}`)
     if (!taskMetaStr) {
       return c.json({ success: false, error: 'Task metadata not found' }, 404)
@@ -620,35 +622,35 @@ uploadRoutes.post('/webhooks/image-completed', async (c) => {
       createdBy: number
     }
 
-    // 실패 처리
+    // ??쎈솭 筌ｌ꼶??
     if (status === 'failed') {
-      // 실패 로그 저장 (선택사항)
+      // ??쎈솭 嚥≪뮄??????(?醫뤾문??鍮?
       await c.env.KV.put(`image-task-failed:${taskId}`, JSON.stringify({
         ...taskMeta,
         error: taskData.error,
         failedAt: Date.now()
-      }), { expirationTtl: 604800 }) // 7일
+      }), { expirationTtl: 604800 }) // 7??
       return c.json({ success: true, status: 'failed' })
     }
 
-    // 완료가 아니면 무시
+    // ?袁⑥┷揶쎛 ?袁⑤빍筌??얜똻??
     if (status !== 'completed') {
       return c.json({ success: true, status })
     }
 
-    // 이미지 URL 추출 (results 배열 우선)
+    // ???筌왖 URL ?곕뗄??(results 獄쏄퀣肉??怨쀪퐨)
     const imageUrl = taskData.results?.[0] || taskData.data?.url || taskData.data?.urls?.[0]
     if (!imageUrl) {
       return c.json({ success: false, error: 'No image URL' }, 400)
     }
 
-    // 이미지 다운로드
+    // ???筌왖 ??쇱뒲嚥≪뮆諭?
     const downloadResult = await downloadImage(imageUrl)
     if (!downloadResult.success || !downloadResult.data) {
       return c.json({ success: false, error: downloadResult.error }, 500)
     }
 
-    // R2에 업로드
+    // R2????낆쨮??
     const fileKey = generateImageFileKey(taskMeta.type, taskMeta.slug)
     const uploadResult = await uploadToR2(
       c.env.UPLOADS,
@@ -666,7 +668,7 @@ uploadRoutes.post('/webhooks/image-completed', async (c) => {
       return c.json({ success: false, error: uploadResult.error }, 500)
     }
 
-    // DB 업데이트
+    // DB ??낅쑓??꾨뱜
     const baseUrl = new URL(c.req.url).origin
     const publicUrl = getImagePublicUrl(fileKey, baseUrl)
     const table = taskMeta.type === 'jobs' ? 'jobs' : 'majors'
@@ -678,22 +680,22 @@ uploadRoutes.post('/webhooks/image-completed', async (c) => {
     `).bind(publicUrl, taskMeta.slug).run()
 
 
-    // 목록 KV 캐시 무효화 (썸네일 갱신)
+    // 筌뤴뫖以?KV 筌?Ŋ???얜똾???(?紐껉퐬??揶쏄퉮??
     if (c.env.KV) {
       const { invalidateListCache } = await import('../services/cacheService')
       const listType = (taskMeta.type === 'jobs' ? 'job' : 'major') as 'job' | 'major'
       await invalidateListCache(c.env.KV, listType)
     }
 
-    // 태스크 메타데이터 삭제
+    // ??뽯뮞??筌롫???怨쀬뵠??????
     await c.env.KV.delete(`image-task:${taskId}`)
 
-    // 성공 로그 저장 (선택사항)
+    // ?源껊궗 嚥≪뮄??????(?醫뤾문??鍮?
     await c.env.KV.put(`image-task-completed:${taskId}`, JSON.stringify({
       ...taskMeta,
       imageUrl: publicUrl,
       completedAt: Date.now()
-    }), { expirationTtl: 604800 }) // 7일
+    }), { expirationTtl: 604800 }) // 7??
 
     return c.json({ success: true, status: 'completed', imageUrl: publicUrl })
   } catch (error) {
@@ -701,4 +703,111 @@ uploadRoutes.post('/webhooks/image-completed', async (c) => {
   }
 })
 
+uploadRoutes.post('/api/admin/image/regenerate', requireAuth, async (c) => {
+  try {
+    const user = c.get('user')!
+    if (!hasImageAdminRole(user.role)) {
+      return c.json({ success: false, error: '관리자 권한이 필요합니다.' }, 403)
+    }
+
+    const body = await c.req.json()
+    const { type, slug } = body as { type: 'jobs' | 'majors'; slug: string }
+
+    if (!type || !slug || !['jobs', 'majors'].includes(type)) {
+      return c.json({ success: false, error: 'type과 slug가 필요합니다.' }, 400)
+    }
+
+    const table = type === 'jobs' ? 'jobs' : 'majors'
+    const record = await c.env.DB.prepare(
+      `SELECT name FROM ${table} WHERE slug = ? LIMIT 1`
+    ).bind(slug).first<{ name: string }>()
+
+    if (!record?.name) {
+      return c.json({ success: false, error: '대상 항목을 찾을 수 없습니다.' }, 404)
+    }
+
+    const baseUrl = new URL(c.req.url).origin
+    const source = { prompt: 'gemini', image: 'evolink', mode: 'remote-fallback' }
+    const autoImageService = await import('../services/autoImageService')
+    const imageResult = type === 'jobs'
+      ? await autoImageService.generateJobImage(
+          {
+            GEMINI_API_KEY: c.env.GEMINI_API_KEY || '',
+            EVOLINK_API_KEY: c.env.EVOLINK_API_KEY || '',
+            UPLOADS: c.env.UPLOADS
+          },
+          record.name,
+          slug,
+          baseUrl
+        )
+      : await autoImageService.generateMajorImage(
+          {
+            GEMINI_API_KEY: c.env.GEMINI_API_KEY || '',
+            EVOLINK_API_KEY: c.env.EVOLINK_API_KEY || '',
+            UPLOADS: c.env.UPLOADS
+          },
+          record.name,
+          slug,
+          baseUrl
+        )
+
+    if (!imageResult.success || !imageResult.imageUrl) {
+      return c.json({
+        success: false,
+        error: imageResult.error || '?대?吏 ?앹꽦???ㅽ뙣?덉뒿?덈떎.',
+        imagePrompt: imageResult.imagePrompt || null,
+        source
+      }, 500)
+    }
+
+    const generationMeta = {
+      generatedAt: new Date().toISOString(),
+      generatedBy: 'remote-admin-regenerate',
+      promptSource: source.prompt,
+      imageSource: source.image,
+      loraApplied: false,
+    }
+    const cacheBustedUrl = `${imageResult.imageUrl}${imageResult.imageUrl.includes('?') ? '&' : '?'}v=${Date.now()}`
+
+    await c.env.DB.prepare(`
+      UPDATE ${table}
+      SET image_url = ?,
+          image_prompt = ?,
+          merged_profile_json = json_set(
+            COALESCE(merged_profile_json, '{}'),
+            '$.image_url', ?,
+            '$.image_generation_meta', json(?)
+          )
+      WHERE slug = ?
+    `).bind(
+      cacheBustedUrl,
+      imageResult.imagePrompt || null,
+      cacheBustedUrl,
+      JSON.stringify(generationMeta),
+      slug
+    ).run()
+
+    if (c.env.KV) {
+      const { invalidateListCache } = await import('../services/cacheService')
+      await invalidateListCache(c.env.KV, type === 'jobs' ? 'job' : 'major')
+    }
+
+    return c.json({
+      success: true,
+      imageUrl: cacheBustedUrl,
+      imagePrompt: imageResult.imagePrompt || null,
+      source,
+      generationMeta,
+      details: imageResult.details || null,
+    })
+  } catch (error) {
+    return c.json({
+      success: false,
+      error: error instanceof Error ? error.message : '?대?吏 ?ъ깮??以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.'
+    }, 500)
+  }
+})
+
 export { uploadRoutes }
+
+
diff --git a/src/templates/detailTemplateUtils.ts b/src/templates/detailTemplateUtils.ts
index b0f1262..9df1c8c 100644
--- a/src/templates/detailTemplateUtils.ts
+++ b/src/templates/detailTemplateUtils.ts
@@ -2,17 +2,17 @@ import type { DataSource, SourceIdentifiers } from '../types/unifiedProfiles'
 import type { SourceStatusRecord } from '../services/profileDataService'
 
 export const DEFAULT_SOURCE_LABELS: Partial<Record<DataSource, string>> = {
-  CAREERNET: '커리어넷 직업백과',
-  GOYONG24: '고용24 직업정보',
-  WORK24_DJOB: '고용24 직업사전'
+  CAREERNET: '而ㅻ━?대꽬 吏곸뾽諛깃낵',
+  GOYONG24: '怨좎슜24 吏곸뾽?뺣낫',
+  WORK24_DJOB: '怨좎슜24 吏곸뾽?ъ쟾'
 }
 
 export const escapeHtml = (value?: string | null | unknown): string => {
   if (!value) return ''
-  // 문자열이 아닌 경우 문자열로 변환 시도
+  // 臾몄옄?댁씠 ?꾨땶 寃쎌슦 臾몄옄?대줈 蹂???쒕룄
   const strValue = typeof value === 'string' ? value : String(value)
   return strValue
-    .replace(/<br\s*\/?>/gi, ' ')  // <br> 태그를 공백으로 치환
+    .replace(/<br\s*\/?>/gi, ' ')  // <br> ?쒓렇瑜?怨듬갚?쇰줈 移섑솚
     .replace(/&/g, '&amp;')
     .replace(/</g, '&lt;')
     .replace(/>/g, '&gt;')
@@ -20,10 +20,10 @@ export const escapeHtml = (value?: string | null | unknown): string => {
     .replace(/'/g, '&#39;')
 }
 
-// HTML 태그 제거 함수 (고용24 데이터의 HTML 태그 처리용)
+// HTML ?쒓렇 ?쒓굅 ?⑥닔 (怨좎슜24 ?곗씠?곗쓽 HTML ?쒓렇 泥섎━??
 const stripHtmlTags = (html: string): string => {
   return html
-    .replace(/<[^>]*>/g, '') // 모든 HTML 태그 제거
+    .replace(/<[^>]*>/g, '') // 紐⑤뱺 HTML ?쒓렇 ?쒓굅
     .replace(/&nbsp;/g, ' ')
     .replace(/&amp;/g, '&')
     .replace(/&lt;/g, '<')
@@ -34,10 +34,10 @@ const stripHtmlTags = (html: string): string => {
 }
 
 /**
- * footnoteMap: 필드별 로컬번호 → 전역번호 매핑
- * 예: { 'way': { '1': 5, '2': 6 }, 'overviewProspect.main': { '1': 9, '2': 10 } }
- * sourceTextMap: 전역번호 → 출처 설명 텍스트 (각주 hover 시 표시)
- * 예: { 5: '대한간호협회 간호사 되기 안내', 6: '한국보건의료인국가시험원 2024 통계' }
+ * footnoteMap: ?꾨뱶蹂?濡쒖뺄踰덊샇 ???꾩뿭踰덊샇 留ㅽ븨
+ * ?? { 'way': { '1': 5, '2': 6 }, 'overviewProspect.main': { '1': 9, '2': 10 } }
+ * sourceTextMap: ?꾩뿭踰덊샇 ??異쒖쿂 ?ㅻ챸 ?띿뒪??(媛곸＜ hover ???쒖떆)
+ * ?? { 5: '??쒓컙?명삊??媛꾪샇???섍린 ?덈궡', 6: '?쒓뎅蹂닿굔?섎즺?멸뎅媛?쒗뿕??2024 ?듦퀎' }
  */
 export type FootnoteMap = Record<string, Record<string, number>>
 
@@ -47,36 +47,36 @@ export const formatRichText = (value?: string | null | string[], fieldKey?: stri
     value = value.join('\n')
   }
   if (!value || (typeof value === 'string' && !value.trim())) {
-    return '<p class="content-text text-wiki-muted">정보가 제공되지 않았습니다.</p>'
+    return '<p class="content-text text-wiki-muted">?뺣낫媛 ?쒓났?섏? ?딆븯?듬땲??</p>'
   }
   if (typeof value !== 'string') {
     value = String(value)
   }
 
-  // HTML 태그가 포함되어 있으면 제거
+  // HTML ?쒓렇媛 ?ы븿?섏뼱 ?덉쑝硫??쒓굅
   let cleanedValue = value.includes('<') || value.includes('>')
     ? stripHtmlTags(value)
     : value
 
-  // 리터럴 \n을 실제 줄바꿈으로 변환
+  // 由ы꽣??\n???ㅼ젣 以꾨컮轅덉쑝濡?蹂??
   cleanedValue = cleanedValue.replace(/\\n/g, '\n')
 
-  // 깨진 문자 및 특수 기호 제거 (□, ■, ▢, �, 제어문자 등)
+  // 源⑥쭊 臾몄옄 諛??뱀닔 湲고샇 ?쒓굅 (?? ?? ?? 占? ?쒖뼱臾몄옄 ??
   cleanedValue = cleanedValue
-    .replace(/[\u25A0-\u25FF]/g, '')  // 기하학적 도형 (□, ■, ▢ 등)
-    .replace(/\uFFFD/g, '')           // Replacement character (�)
-    .replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F]/g, '')  // 제어문자 (탭/개행/CR 제외)
-    .replace(/\u00A0/g, ' ')          // Non-breaking space → 일반 공백
-    .replace(/[^\S\n]+/g, ' ')        // 연속 공백 정리 (줄바꿈 보존)
+    .replace(/[\u25A0-\u25FF]/g, '')  // 湲고븯?숈쟻 ?꾪삎 (?? ?? ????
+    .replace(/\uFFFD/g, '')           // Replacement character (占?
+    .replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F]/g, '')  // ?쒖뼱臾몄옄 (??媛쒗뻾/CR ?쒖쇅)
+    .replace(/\u00A0/g, ' ')          // Non-breaking space ???쇰컲 怨듬갚
+    .replace(/[^\S\n]+/g, ' ')        // ?곗냽 怨듬갚 ?뺣━ (以꾨컮轅?蹂댁〈)
 
-  // ── 각주 정규화: 중복 제거 + 전역 번호 적용 ──
-  // 같은 [N]이 여러 번 등장하면 마지막 것만 남기고,
-  // footnoteMap으로 로컬번호 → 전역번호(페이지 통합 1~N)로 변환.
-  // 나무위키처럼 페이지 전체에서 순서대로 이어지는 번호 체계.
+  // ?? 媛곸＜ ?뺢퇋?? 以묐났 ?쒓굅 + ?꾩뿭 踰덊샇 ?곸슜 ??
+  // 媛숈? [N]???щ윭 踰??깆옣?섎㈃ 留덉?留?寃껊쭔 ?④린怨?
+  // footnoteMap?쇰줈 濡쒖뺄踰덊샇 ???꾩뿭踰덊샇(?섏씠吏 ?듯빀 1~N)濡?蹂??
+  // ?섎Т?꾪궎泥섎읆 ?섏씠吏 ?꾩껜?먯꽌 ?쒖꽌?濡??댁뼱吏??踰덊샇 泥닿퀎.
 
   const fieldMap = (footnoteMap && fieldKey) ? footnoteMap[fieldKey] : null
 
-  // Phase 1: 중복 제거 (같은 [N]이 여러 번 → 마지막 것만 유지)
+  // Phase 1: 以묐났 ?쒓굅 (媛숈? [N]???щ윭 踰???留덉?留?寃껊쭔 ?좎?)
   const fnScan = /\[(\d+)\](?!\()/g
   const fnOccurrences = new Map<string, number[]>()
   let scanMatch: RegExpExecArray | null
@@ -86,7 +86,7 @@ export const formatRichText = (value?: string | null | string[], fieldKey?: stri
     fnOccurrences.get(num)!.push(scanMatch.index)
   }
 
-  // 첫 번째 것을 남기고 나머지 제거 (순서 보존을 위해 — 마지막 대신 첫 것 유지)
+  // 泥?踰덉㎏ 寃껋쓣 ?④린怨??섎㉧吏 ?쒓굅 (?쒖꽌 蹂댁〈???꾪빐 ??留덉?留????泥?寃??좎?)
   const positionsToRemove: Array<{ pos: number; len: number }> = []
   for (const [num, positions] of fnOccurrences) {
     if (positions.length > 1) {
@@ -100,15 +100,15 @@ export const formatRichText = (value?: string | null | string[], fieldKey?: stri
     cleanedValue = cleanedValue.substring(0, pos) + cleanedValue.substring(pos + len)
   }
 
-  // Phase 2: 로컬번호 → 전역번호 치환 (페이지 전체 통합 번호)
-  // footnoteMap이 있으면 [1] → [7], [2] → [8] 등으로 변환
-  // 없으면 원본 번호 유지
+  // Phase 2: 濡쒖뺄踰덊샇 ???꾩뿭踰덊샇 移섑솚 (?섏씠吏 ?꾩껜 ?듯빀 踰덊샇)
+  // footnoteMap???덉쑝硫?[1] ??[7], [2] ??[8] ?깆쑝濡?蹂??
+  // ?놁쑝硫??먮낯 踰덊샇 ?좎?
   if (fieldMap) {
     cleanedValue = cleanedValue.replace(
       /\[(\d+)\](?!\()/g,
       (_match, localNum) => {
         const globalNum = fieldMap[localNum] ?? parseInt(localNum)
-        return `[__GN${globalNum}__]` // 임시 마커 (재치환 충돌 방지)
+        return `[__GN${globalNum}__]` // ?꾩떆 留덉빱 (?ъ튂??異⑸룎 諛⑹?)
       }
     )
     cleanedValue = cleanedValue.replace(/\[__GN(\d+)__\]/g, '[$1]')
@@ -119,12 +119,12 @@ export const formatRichText = (value?: string | null | string[], fieldKey?: stri
     .split(/\n{2,}/)
     .map((paragraph) => {
       let safe = escapeHtml(paragraph.trim()).replace(/\n/g, '<br>')
-      // 마크다운 볼드 **텍스트** → <strong>텍스트</strong>
+      // 留덊겕?ㅼ슫 蹂쇰뱶 **?띿뒪??* ??<strong>?띿뒪??/strong>
       safe = safe.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
-      // 각주 위치 정규화: "했다[N]." → "했다.[N]" (마침표 뒤에 각주)
-      safe = safe.replace(/(\[(\d+)\])([.。])/g, '$3$1')
-      // 마크다운 링크 [텍스트](url) → 클릭 가능한 <a> 태그로 변환
-      // ⚠️ 각주 변환보다 먼저 처리해야 [N](url) 패턴이 충돌하지 않음
+      // 媛곸＜ ?꾩튂 ?뺢퇋?? "?덈떎[N]." ??"?덈떎.[N]" (留덉묠???ㅼ뿉 媛곸＜)
+      safe = safe.replace(/(\[(\d+)\])([.??)/g, '$3$1')
+      // 留덊겕?ㅼ슫 留곹겕 [?띿뒪??(url) ???대┃ 媛?ν븳 <a> ?쒓렇濡?蹂??
+      // ?좑툘 媛곸＜ 蹂?섎낫??癒쇱? 泥섎━?댁빞 [N](url) ?⑦꽩??異⑸룎?섏? ?딆쓬
       safe = safe.replace(
         /\[([^\]]+?)\]\((https?:\/\/[^)]+)\)/g,
         (_match, text, url) => {
@@ -134,14 +134,14 @@ export const formatRichText = (value?: string | null | string[], fieldKey?: stri
           return `<a href="${escapeHtml(url)}" target="_blank" rel="noopener noreferrer" class="text-wiki-primary hover:underline">${text}</a>${domainHtml}`
         }
       )
-      // 인라인 각주 [N] → 클릭 가능한 superscript 링크로 변환
-      // 이미 전역번호로 변환된 상태이므로 그대로 표시
+      // ?몃씪??媛곸＜ [N] ???대┃ 媛?ν븳 superscript 留곹겕濡?蹂??
+      // ?대? ?꾩뿭踰덊샇濡?蹂?섎맂 ?곹깭?대?濡?洹몃?濡??쒖떆
       safe = safe.replace(
         /\[(\d+)\]/g,
         (_match, numStr) => {
           const globalNum = parseInt(numStr)
           const sourceDesc = sourceTextMap?.[globalNum] || ''
-          const titleText = sourceDesc ? sourceDesc.replace(/"/g, '&quot;') : `출처 [${globalNum}]`
+          const titleText = sourceDesc ? sourceDesc.replace(/"/g, '&quot;') : `異쒖쿂 [${globalNum}]`
           return `<sup class="user-footnote-ref cursor-pointer transition" style="font-size:11px;font-weight:600;color:var(--wiki-primary,#8b5cf6);margin-left:1px;vertical-align:super;line-height:1;" data-source-id="${globalNum}" id="user-fnref-${globalNum}" title="${titleText}">[${globalNum}]</sup>`
         }
       )
@@ -151,9 +151,9 @@ export const formatRichText = (value?: string | null | string[], fieldKey?: stri
 }
 
 /**
- * 배열 항목 텍스트에 인라인 각주 [N] → <sup> 변환을 적용한다.
- * formatRichText와 동일한 각주 처리 로직이지만 <p> 래핑 없이 반환한다.
- * detailReady 배열 항목(curriculum, recruit, training) 전용.
+ * 諛곗뿴 ??ぉ ?띿뒪?몄뿉 ?몃씪??媛곸＜ [N] ??<sup> 蹂?섏쓣 ?곸슜?쒕떎.
+ * formatRichText? ?숈씪??媛곸＜ 泥섎━ 濡쒖쭅?댁?留?<p> ?섑븨 ?놁씠 諛섑솚?쒕떎.
+ * detailReady 諛곗뿴 ??ぉ(curriculum, recruit, training) ?꾩슜.
  */
 export const applyInlineFootnotes = (
   text: string,
@@ -165,7 +165,7 @@ export const applyInlineFootnotes = (
   let working = text
   const fieldMap = (footnoteMap && fieldKey) ? footnoteMap[fieldKey] : null
 
-  // Phase 1: 중복 [N] 제거 (첫 번째 유지, 나머지 제거)
+  // Phase 1: 以묐났 [N] ?쒓굅 (泥?踰덉㎏ ?좎?, ?섎㉧吏 ?쒓굅)
   const fnScan = /\[(\d+)\](?!\()/g
   const fnOccurrences = new Map<string, number[]>()
   let scanMatch: RegExpExecArray | null
@@ -185,7 +185,7 @@ export const applyInlineFootnotes = (
     working = working.substring(0, pos) + working.substring(pos + len)
   }
 
-  // Phase 2: 로컬번호 → 전역번호 치환
+  // Phase 2: 濡쒖뺄踰덊샇 ???꾩뿭踰덊샇 移섑솚
   if (fieldMap) {
     working = working.replace(/\[(\d+)\](?!\()/g, (_match, localNum) => {
       const globalNum = fieldMap[localNum] ?? parseInt(localNum)
@@ -194,19 +194,19 @@ export const applyInlineFootnotes = (
     working = working.replace(/\[__GN(\d+)__\]/g, '[$1]')
   }
 
-  // HTML escape + 각주 위치 정규화 ([N]. → .[N]) + <sup> 변환
+  // HTML escape + 媛곸＜ ?꾩튂 ?뺢퇋??([N]. ??.[N]) + <sup> 蹂??
   let safe = escapeHtml(working)
-  safe = safe.replace(/(\[(\d+)\])([.。])/g, '$3$1')
+  safe = safe.replace(/(\[(\d+)\])([.??)/g, '$3$1')
   safe = safe.replace(/\[(\d+)\]/g, (_match, numStr) => {
     const globalNum = parseInt(numStr)
     const sourceDesc = sourceTextMap?.[globalNum] || ''
-    const titleText = sourceDesc ? sourceDesc.replace(/"/g, '&quot;') : `출처 [${globalNum}]`
+    const titleText = sourceDesc ? sourceDesc.replace(/"/g, '&quot;') : `異쒖쿂 [${globalNum}]`
     return `<sup class="user-footnote-ref cursor-pointer transition" style="font-size:11px;font-weight:600;color:var(--wiki-primary,#8b5cf6);margin-left:1px;vertical-align:super;line-height:1;" data-source-id="${globalNum}" id="user-fnref-${globalNum}" title="${titleText}">[${globalNum}]</sup>`
   })
   return safe
 }
 
-export const renderChips = (items?: string[] | null, emptyText = '정보 없음'): string => {
+export const renderChips = (items?: string[] | null, emptyText = '?뺣낫 ?놁쓬'): string => {
   if (!items || items.length === 0) {
     return `<p class="content-text text-wiki-muted">${escapeHtml(emptyText)}</p>`
   }
@@ -234,8 +234,8 @@ export interface BuildCardOptions {
   anchorId?: string
   telemetryScope?: string
   telemetryComponent?: string
-  editButton?: EditButtonOptions  // 편집 버튼 옵션
-  dataSources?: string[]  // 데이터 출처 목록 (CAREERNET, GOYONG24, WORK24_DJOB 등)
+  editButton?: EditButtonOptions  // ?몄쭛 踰꾪듉 ?듭뀡
+  dataSources?: string[]  // ?곗씠??異쒖쿂 紐⑸줉 (CAREERNET, GOYONG24, WORK24_DJOB ??
 }
 
 interface HeroImageRule {
@@ -245,23 +245,23 @@ interface HeroImageRule {
 
 const HERO_IMAGE_RULES: HeroImageRule[] = [
   {
-    keywords: ['ai', '인공지능', '데이터', 'data'],
+    keywords: ['ai', '?멸났吏??, '?곗씠??, 'data'],
     url: 'https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=1600&q=80'
   },
   {
-    keywords: ['marketing', 'growth', '마케팅'],
+    keywords: ['marketing', 'growth', '留덉???],
     url: 'https://images.unsplash.com/photo-1521737604893-d14cc237f11d?auto=format&fit=crop&w=1600&q=80'
   },
   {
-    keywords: ['design', 'ux', 'ui', '디자인'],
+    keywords: ['design', 'ux', 'ui', '?붿옄??],
     url: 'https://images.unsplash.com/photo-1522202176988-66273c2fd55f?auto=format&fit=crop&w=1600&q=80'
   },
   {
-    keywords: ['finance', 'investment', '금융', '재무'],
+    keywords: ['finance', 'investment', '湲덉쑖', '?щТ'],
     url: 'https://images.unsplash.com/photo-1520607162513-77705c0f0d4a?auto=format&fit=crop&w=1600&q=80'
   },
   {
-    keywords: ['health', '의료', 'bio', 'medical'],
+    keywords: ['health', '?섎즺', 'bio', 'medical'],
     url: 'https://images.unsplash.com/photo-1580281657521-93a6c3be1f4c?auto=format&fit=crop&w=1600&q=80'
   }
 ]
@@ -281,13 +281,30 @@ export const resolveHeroImageUrl = (title?: string): string => {
 
 export const renderHeroImage = (
   title?: string,
-  options: { imageUrl?: string; imageAlt?: string; dataAttribute?: string; context?: string; slug?: string } = {}
+  options: {
+    imageUrl?: string
+    imageAlt?: string
+    dataAttribute?: string
+    context?: string
+    slug?: string
+    generationMeta?: {
+      promptSource?: string
+      imageSource?: string
+      loraApplied?: boolean
+      generatedAt?: string
+    } | null
+  } = {}
 ): string => {
   const imageUrl = options.imageUrl ?? resolveHeroImageUrl(title)
-  const imageAlt = options.imageAlt ?? `${escapeHtml(title ?? '')}를 상징하는 대표 이미지`
+  const imageAlt = options.imageAlt ?? `${escapeHtml(title ?? '')}瑜??곸쭠?섎뒗 ????대?吏`
   const dataAttribute = options.dataAttribute ?? 'data-hero-image'
   const context = options.context ?? 'default'
   const slug = options.slug ?? ''
+  const generationMeta = options.generationMeta ?? null
+  const promptSource = generationMeta?.promptSource ?? ''
+  const imageSource = generationMeta?.imageSource ?? ''
+  const loraApplied = generationMeta?.loraApplied ? 'true' : 'false'
+  const generatedAt = generationMeta?.generatedAt ?? ''
   const dataAttributeSnippet = `${dataAttribute}="true"`
 
   return `
@@ -308,28 +325,36 @@ export const renderHeroImage = (
           class="w-full h-full object-cover object-center"
           loading="lazy"
           decoding="async"
-          onerror="this.onerror=null; this.src='${DEFAULT_HERO_IMAGE}'; this.alt='${escapeHtml(title ?? '')} 대표 이미지 준비 중';"
+          onerror="this.onerror=null; this.src='${DEFAULT_HERO_IMAGE}'; this.alt='${escapeHtml(title ?? '')} ????대?吏 以鍮?以?;"
         >
         <div class="absolute inset-0 bg-gradient-to-b from-black/35 via-wiki-bg/20 to-wiki-bg/80 mix-blend-multiply"></div>
         <div class="absolute inset-0 bg-gradient-to-tr from-wiki-primary/15 via-transparent to-wiki-secondary/20 mix-blend-screen"></div>
         <div class="absolute inset-6 rounded-2xl border border-white/10 pointer-events-none"></div>
       </div>
-      <!-- 관리자용 이미지 재생성 버튼 (클라이언트에서 동적으로 표시) -->
+      <!-- 愿由ъ옄???대?吏 ?ъ깮??踰꾪듉 (?대씪?댁뼵?몄뿉???숈쟻?쇰줈 ?쒖떆) -->
       <div 
         class="absolute top-4 right-4 hidden" 
         data-admin-image-controls
         data-image-type="${context === 'job' ? 'jobs' : 'majors'}"
         data-image-slug="${escapeHtml(slug)}"
         data-image-title="${escapeHtml(title ?? '')}"
+        data-current-prompt-source="${escapeHtml(promptSource)}"
+        data-current-image-source="${escapeHtml(imageSource)}"
+        data-current-lora-applied="${escapeHtml(loraApplied)}"
+        data-current-generated-at="${escapeHtml(generatedAt)}"
       >
+        <div
+          class="mb-2 hidden items-center gap-2 px-3 py-2 bg-wiki-bg/90 backdrop-blur-sm rounded-lg border border-wiki-border text-xs text-wiki-muted"
+          data-image-generation-status
+        ></div>
         <button 
           type="button"
           class="flex items-center gap-2 px-3 py-2 bg-wiki-bg/90 backdrop-blur-sm rounded-lg border border-wiki-border hover:border-wiki-primary transition-colors text-sm text-wiki-text hover:text-wiki-primary"
           data-action="regenerate-image"
-          title="이미지 재생성"
+          title="?대?吏 ?ъ깮??
         >
           <i class="fas fa-sync-alt"></i>
-          <span>이미지 변경</span>
+          <span>?대?吏 蹂寃?/span>
         </button>
       </div>
       <figcaption class="sr-only">${escapeHtml(imageAlt)}</figcaption>
@@ -360,7 +385,7 @@ export const buildCard = (title: string, icon: string, body: string, options: Bu
     attributeParts.push(`data-cw-telemetry-component="${escapeHtml(telemetryComponent)}"`)
   }
 
-  // 데이터 출처 속성 추가 (하이라이트 기능용)
+  // ?곗씠??異쒖쿂 ?띿꽦 異붽? (?섏씠?쇱씠??湲곕뒫??
   if (dataSources && dataSources.length > 0) {
     attributeParts.push(`data-source="${dataSources.map(s => escapeHtml(s)).join(',')}"`)
   }
@@ -386,18 +411,18 @@ export const buildCard = (title: string, icon: string, body: string, options: Bu
 }
 
 /**
- * 편집 버튼 렌더링
+ * ?몄쭛 踰꾪듉 ?뚮뜑留?
  * 
- * @param options 편집 버튼 옵션
- * @returns 편집 버튼 HTML
+ * @param options ?몄쭛 踰꾪듉 ?듭뀡
+ * @returns ?몄쭛 踰꾪듉 HTML
  */
 export interface EditButtonOptions {
   entityType: 'job' | 'major' | 'howto'
   entityId: string
-  field: string  // 'summary', 'duties', 'salary' 등
-  label?: string  // 버튼 라벨 (기본: '편집')
-  enabled?: boolean  // 편집 가능 여부 (기본: true)
-  dataSource?: 'api' | 'user' | 'admin'  // 데이터 출처
+  field: string  // 'summary', 'duties', 'salary' ??
+  label?: string  // 踰꾪듉 ?쇰꺼 (湲곕낯: '?몄쭛')
+  enabled?: boolean  // ?몄쭛 媛???щ? (湲곕낯: true)
+  dataSource?: 'api' | 'user' | 'admin'  // ?곗씠??異쒖쿂
 }
 
 export const renderEditButton = (options: EditButtonOptions): string => {
@@ -405,7 +430,7 @@ export const renderEditButton = (options: EditButtonOptions): string => {
     entityType,
     entityId,
     field,
-    label = '편집',
+    label = '?몄쭛',
     enabled = true,
     dataSource
   } = options
@@ -426,9 +451,9 @@ export const renderEditButton = (options: EditButtonOptions): string => {
   const badgeLabel = dataSource === 'api'
     ? 'API'
     : dataSource === 'user'
-    ? '사용자'
+    ? '?ъ슜??
     : dataSource === 'admin'
-    ? '관리자'
+    ? '愿由ъ옄'
     : ''
 
   return `
@@ -441,7 +466,7 @@ export const renderEditButton = (options: EditButtonOptions): string => {
       data-field="${escapeHtml(field)}"
       ${enabled ? '' : 'disabled'}
       aria-label="${escapeHtml(label)} (${escapeHtml(field)})"
-      title="${enabled ? `${escapeHtml(label)} (${escapeHtml(field)})` : '편집 불가'}"
+      title="${enabled ? `${escapeHtml(label)} (${escapeHtml(field)})` : '?몄쭛 遺덇?'}"
     >
       <i class="fas fa-edit text-xs" aria-hidden="true"></i>
       <span>${escapeHtml(label)}</span>
@@ -455,7 +480,7 @@ export type TabEntry = {
   label: string
   icon: string
   content: string
-  /** 아이콘만 표시 (탭 오른쪽 끝으로 밀림, 호버 시 라벨 표시) */
+  /** ?꾩씠肄섎쭔 ?쒖떆 (???ㅻⅨ履??앹쑝濡?諛由? ?몃쾭 ???쇰꺼 ?쒖떆) */
   iconOnly?: boolean
 }
 export type DetailComponentEntityType = 'job' | 'major' | 'guide'
@@ -474,20 +499,20 @@ export interface TabsetOptions {
 export const renderTabset = ({ entityType, entityId, entries, fallback }: TabsetOptions): string => {
   if (!entries.length) {
     return buildCard(
-      fallback?.title ?? '데이터 준비 중',
+      fallback?.title ?? '?곗씠??以鍮?以?,
       fallback?.icon ?? 'fa-circle-info',
       `<p class="content-text text-wiki-muted">${escapeHtml(
-        fallback?.message ?? '상세 정보가 수집되는 대로 탭이 활성화됩니다.'
+        fallback?.message ?? '?곸꽭 ?뺣낫媛 ?섏쭛?섎뒗 ?濡???씠 ?쒖꽦?붾맗?덈떎.'
       )}</p>`
     )
   }
 
   const tabsetLabel =
     entityType === 'job'
-      ? '직업 상세 정보 탭'
+      ? '吏곸뾽 ?곸꽭 ?뺣낫 ??
       : entityType === 'major'
-        ? '전공 상세 정보 탭'
-        : 'HowTo 가이드 탭'
+        ? '?꾧났 ?곸꽭 ?뺣낫 ??
+        : 'HowTo 媛?대뱶 ??
   const normalizedBase = `${entityType}-${entityId}`
     .toLowerCase()
     .replace(/[^a-z0-9]+/g, '-')
@@ -498,7 +523,7 @@ export const renderTabset = ({ entityType, entityId, entries, fallback }: Tabset
   const instructionsId = `${tabsetId}-instructions`
   const totalTabs = entries.length
 
-  // 일반 탭과 아이콘 전용 탭 분리
+  // ?쇰컲 ??낵 ?꾩씠肄??꾩슜 ??遺꾨━
   const regularEntries = entries.filter(e => !e.iconOnly)
   const iconOnlyEntries = entries.filter(e => e.iconOnly)
 
@@ -511,7 +536,7 @@ export const renderTabset = ({ entityType, entityId, entries, fallback }: Tabset
       ? 'text-white border-wiki-primary bg-transparent md:bg-wiki-border/30'
       : 'text-wiki-muted border-transparent hover:text-white hover:border-wiki-border'
     
-    // 아이콘 전용 탭: 아이콘만 표시, title로 호버 시 라벨 표시
+    // ?꾩씠肄??꾩슜 ?? ?꾩씠肄섎쭔 ?쒖떆, title濡??몃쾭 ???쇰꺼 ?쒖떆
     const buttonContent = isIconOnly
       ? `<i class="fas ${entry.icon} text-base" aria-hidden="true"></i>
          <span class="sr-only">${escapeHtml(entry.label)}</span>`
@@ -553,7 +578,7 @@ export const renderTabset = ({ entityType, entityId, entries, fallback }: Tabset
     `
   }
 
-  // 일반 탭 버튼
+  // ?쇰컲 ??踰꾪듉
   const regularTabButtons = regularEntries
     .map((entry, idx) => {
       const originalIndex = entries.indexOf(entry)
@@ -561,7 +586,7 @@ export const renderTabset = ({ entityType, entityId, entries, fallback }: Tabset
     })
     .join('')
   
-  // 아이콘 전용 탭 버튼 (오른쪽 끝에 배치)
+  // ?꾩씠肄??꾩슜 ??踰꾪듉 (?ㅻⅨ履??앹뿉 諛곗튂)
   const iconOnlyTabButtons = iconOnlyEntries.length > 0
     ? `<li class="flex-grow"></li>` + iconOnlyEntries
         .map((entry) => {
@@ -611,13 +636,13 @@ export const renderTabset = ({ entityType, entityId, entries, fallback }: Tabset
       data-cw-telemetry-component="tabset"
       data-cw-telemetry-action="tabset-render"
       role="region"
-      aria-roledescription="콘텐츠 탭 영역"
+      aria-roledescription="肄섑뀗痢????곸뿭"
       aria-labelledby="${tabsetId}-label"
       aria-describedby="${statusId} ${instructionsId}"
     >
       <header class="sr-only">
         <h2 id="${tabsetId}-label">${escapeHtml(tabsetLabel)}</h2>
-        <p id="${instructionsId}">방향키로 탭을 이동하고 Enter 키로 선택할 수 있습니다. Home/End 키로 처음과 마지막 탭으로 이동합니다.</p>
+        <p id="${instructionsId}">諛⑺뼢?ㅻ줈 ??쓣 ?대룞?섍퀬 Enter ?ㅻ줈 ?좏깮?????덉뒿?덈떎. Home/End ?ㅻ줈 泥섏쓬怨?留덉?留???쑝濡??대룞?⑸땲??</p>
       </header>
       <div
         class="flex flex-wrap gap-2 px-2 md:px-4 pt-3 md:pt-4 bg-wiki-bg/95 md:bg-wiki-bg/70 border-b border-wiki-border/50 md:border-wiki-border sticky top-0 z-30 backdrop-blur-sm"
@@ -681,10 +706,10 @@ export const renderCtaGroup = ({ entityType, entityId, links }: CtaGroupOptions)
 
   const navLabel =
     entityType === 'job'
-      ? '직업 상세 주요 행동'
+      ? '吏곸뾽 ?곸꽭 二쇱슂 ?됰룞'
       : entityType === 'major'
-        ? '전공 상세 주요 행동'
-        : 'HowTo 가이드 주요 행동'
+        ? '?꾧났 ?곸꽭 二쇱슂 ?됰룞'
+        : 'HowTo 媛?대뱶 二쇱슂 ?됰룞'
   const navId = normalizedBase ? `cw-${normalizedBase}-cta` : `cw-${entityType}-cta`
   const noteId = `${navId}-note`
   const instructionsId = `${navId}-instructions`
@@ -726,13 +751,13 @@ export const renderCtaGroup = ({ entityType, entityId, links }: CtaGroupOptions)
       data-entity-id="${escapeHtml(entityId)}"
       aria-labelledby="${navId}-label"
       aria-describedby="${noteId} ${instructionsId}"
-      aria-roledescription="주요 행동 내비게이션"
+      aria-roledescription="二쇱슂 ?됰룞 ?대퉬寃뚯씠??
       role="navigation"
     >
       <div class="sr-only">
         <h2 id="${navId}-label">${escapeHtml(navLabel)}</h2>
-        <p id="${noteId}">Careerwiki 주요 전환 행동 모음입니다.</p>
-        <p id="${instructionsId}">각 버튼은 Enter 또는 Space 키로 실행할 수 있으며, 링크는 새 탭에서 열릴 수 있습니다.</p>
+        <p id="${noteId}">Careerwiki 二쇱슂 ?꾪솚 ?됰룞 紐⑥쓬?낅땲??</p>
+        <p id="${instructionsId}">媛?踰꾪듉? Enter ?먮뒗 Space ?ㅻ줈 ?ㅽ뻾?????덉쑝硫? 留곹겕??????뿉???대┫ ???덉뒿?덈떎.</p>
       </div>
       <ul class="flex flex-wrap gap-3" role="list" data-cw-telemetry-component="cta-list">
         ${buttons}
@@ -802,31 +827,31 @@ export const resolveCommentPolicy = (policy?: CommentPolicyAttributes): Required
 
 export const buildCommentGovernanceItems = (policy: Required<CommentPolicyAttributes>): string[] => {
   const governanceItems: string[] = []
-  governanceItems.push('로그인 없이도 댓글을 작성할 수 있습니다. 익명 작성 시 4자리 숫자 비밀번호가 필요합니다.')
-  governanceItems.push('익명 사용자는 하루 최대 5개의 댓글을 작성할 수 있습니다.')
-  governanceItems.push('댓글은 최대 500자까지 작성 가능하며, 최대 3단계까지 답글을 달 수 있습니다.')
-  governanceItems.push(`좋아요 ${policy.bestLikeThreshold}개 이상 댓글은 BEST로 강조됩니다.`)
-  governanceItems.push('BEST 댓글은 목록 상단에 고정됩니다.')
-  governanceItems.push(`신고 ${policy.reportBlindThreshold}회 이상 시 자동으로 블라인드 처리됩니다.`)
-  governanceItems.push('여러 댓글에 공감/비공감을 표시하는 것은 제한이 없습니다.')
-  governanceItems.push('단, 한 댓글에는 공감 또는 비공감 중 하나만 선택할 수 있습니다.')
+  governanceItems.push('濡쒓렇???놁씠???볤????묒꽦?????덉뒿?덈떎. ?듬챸 ?묒꽦 ??4?먮━ ?レ옄 鍮꾨?踰덊샇媛 ?꾩슂?⑸땲??')
+  governanceItems.push('?듬챸 ?ъ슜?먮뒗 ?섎（ 理쒕? 5媛쒖쓽 ?볤????묒꽦?????덉뒿?덈떎.')
+  governanceItems.push('?볤?? 理쒕? 500?먭퉴吏 ?묒꽦 媛?ν븯硫? 理쒕? 3?④퀎源뚯? ?듦????????덉뒿?덈떎.')
+  governanceItems.push(`醫뗭븘??${policy.bestLikeThreshold}媛??댁긽 ?볤?? BEST濡?媛뺤“?⑸땲??`)
+  governanceItems.push('BEST ?볤?? 紐⑸줉 ?곷떒??怨좎젙?⑸땲??')
+  governanceItems.push(`?좉퀬 ${policy.reportBlindThreshold}???댁긽 ???먮룞?쇰줈 釉붾씪?몃뱶 泥섎━?⑸땲??`)
+  governanceItems.push('?щ윭 ?볤???怨듦컧/鍮꾧났媛먯쓣 ?쒖떆?섎뒗 寃껋? ?쒗븳???놁뒿?덈떎.')
+  governanceItems.push('?? ???볤??먮뒗 怨듦컧 ?먮뒗 鍮꾧났媛?以??섎굹留??좏깮?????덉뒿?덈떎.')
   if (policy.ipDisplayMode === 'masked') {
-    governanceItems.push('IP는 마스킹된 형태로만 노출됩니다.')
+    governanceItems.push('IP??留덉뒪?밸맂 ?뺥깭濡쒕쭔 ?몄텧?⑸땲??')
   } else if (policy.ipDisplayMode === 'hash') {
-    governanceItems.push('IP는 해시값으로 저장되며 원본은 노출되지 않습니다.')
+    governanceItems.push('IP???댁떆媛믪쑝濡???λ릺硫??먮낯? ?몄텧?섏? ?딆뒿?덈떎.')
   } else if (policy.ipDisplayMode === 'hidden') {
-    governanceItems.push('IP는 사용자에게 노출되지 않습니다.')
+    governanceItems.push('IP???ъ슜?먯뿉寃??몄텧?섏? ?딆뒿?덈떎.')
   }
   if (policy.moderatorIpBlockEnabled) {
-    governanceItems.push('모더레이터는 신고 누적 IP를 차단할 수 있습니다.')
+    governanceItems.push('紐⑤뜑?덉씠?곕뒗 ?좉퀬 ?꾩쟻 IP瑜?李⑤떒?????덉뒿?덈떎.')
   }
   if (policy.moderatorRoles.length) {
-    governanceItems.push(`모더레이터 역할 계층: ${policy.moderatorRoles.join(' > ')}`)
+    governanceItems.push(`紐⑤뜑?덉씠????븷 怨꾩링: ${policy.moderatorRoles.join(' > ')}`)
   }
   return governanceItems
 }
 
-// ────────── YouTube Section Helpers ──────────
+// ?????????? YouTube Section Helpers ??????????
 
 export const extractYouTubeVideoId = (url: string): string | null => {
   const match = url.match(
@@ -860,7 +885,7 @@ export const renderYouTubeSection = (links: YouTubeLink[]): string => {
          class="block rounded-xl border border-wiki-border/40 overflow-hidden
                 hover:border-red-500/30 hover:shadow-lg hover:shadow-red-500/5 transition-all duration-300 group"
          style="background: linear-gradient(135deg, rgba(15,23,42,0.8), rgba(30,41,59,0.6));">
-        <!-- 모바일: 가로 배치 -->
+        <!-- 紐⑤컮?? 媛濡?諛곗튂 -->
         <div class="flex gap-3 p-3 md:hidden">
           <div class="flex-shrink-0 w-[130px] aspect-video rounded-lg overflow-hidden bg-wiki-card relative">
             <img src="${thumbUrl}" alt="" class="w-full h-full object-cover" loading="lazy">
@@ -877,7 +902,7 @@ export const renderYouTubeSection = (links: YouTubeLink[]): string => {
             </p>
           </div>
         </div>
-        <!-- 데스크톱: 세로 카드 (그리드 1행) -->
+        <!-- ?곗뒪?ы넲: ?몃줈 移대뱶 (洹몃━??1?? -->
         <div class="hidden md:block">
           <div class="aspect-video relative rounded-t-xl overflow-hidden">
             <img src="${thumbUrl}" alt="" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300" loading="lazy">
@@ -899,15 +924,15 @@ export const renderYouTubeSection = (links: YouTubeLink[]): string => {
 
   const allHtml = validLinks.map(renderCard).join('')
 
-  // 모바일: 세로 스택, 데스크톱: 가로 스크롤 (3개 초과 시) 또는 고정 그리드
+  // 紐⑤컮?? ?몃줈 ?ㅽ깮, ?곗뒪?ы넲: 媛濡??ㅽ겕濡?(3媛?珥덇낵 ?? ?먮뒗 怨좎젙 洹몃━??
   const cols = Math.min(validLinks.length, 3)
-  // 3개 이하 → 고정 그리드 (최소 2열), 4개 이상 → 가로 스크롤
+  // 3媛??댄븯 ??怨좎젙 洹몃━??(理쒖냼 2??, 4媛??댁긽 ??媛濡??ㅽ겕濡?
   if (validLinks.length <= 3) {
     const gridCols = Math.max(cols, 2)
     return `<div class="space-y-3 md:space-y-0 md:grid md:grid-cols-${gridCols} md:gap-3">${allHtml}</div>`
   }
 
-  // 4개 이상: 모바일은 세로, 데스크톱은 가로 스크롤
+  // 4媛??댁긽: 紐⑤컮?쇱? ?몃줈, ?곗뒪?ы넲? 媛濡??ㅽ겕濡?
   return `
     <div class="space-y-3 md:hidden">${allHtml}</div>
     <div class="hidden md:flex md:gap-3 md:overflow-x-auto md:pb-2" style="scroll-snap-type:x mandatory;-webkit-overflow-scrolling:touch;scrollbar-width:thin;">
@@ -915,7 +940,7 @@ export const renderYouTubeSection = (links: YouTubeLink[]): string => {
     </div>`
 }
 
-// ────────── Comments Section ──────────
+// ?????????? Comments Section ??????????
 
 export interface CommentsPlaceholderOptions {
   entityType: DetailComponentEntityType
@@ -984,7 +1009,7 @@ export const renderCommentsPlaceholder = ({
   const policyJsonAttr = escapeHtml(JSON.stringify(policySnapshot))
   const srTitle = title?.trim().length ? escapeHtml(title.trim()) : ''
   const helpCenterHref = '/help/community-guidelines'
-  const visibleHeading = '커뮤니티 댓글'
+  const visibleHeading = '而ㅻ??덊떚 ?볤?'
   const sanitizedDescription = description && description.trim().length ? escapeHtml(description.trim()) : ''
   const sampleJson = sampleComments && sampleComments.length ? escapeHtml(JSON.stringify(sampleComments)) : ''
   const sampleAttributes = sampleJson ? ` data-comment-sample="${sampleJson}" data-comment-demo="true"` : ''
@@ -996,12 +1021,12 @@ export const renderCommentsPlaceholder = ({
   const resolvedReportThreshold = Number.isFinite(policySnapshot.reportBlindThreshold)
     ? policySnapshot.reportBlindThreshold
     : COMMENT_POLICY_DEFAULTS.reportBlindThreshold
-  const bestScoreboardMessage = escapeHtml(`좋아요 ${resolvedBestThreshold}개 이상 댓글은 BEST로 승격됩니다.`)
-  const moderationScoreboardMessage = escapeHtml(`신고 ${resolvedReportThreshold}회 이상 시 자동으로 블라인드 처리됩니다.`)
-  const bestEmptyMessage = escapeHtml('아직 BEST 댓글이 없습니다.')
-  const allEmptyMessage = escapeHtml(emptyLabel ?? '아직 등록된 댓글이 없습니다. 가장 먼저 의견을 남겨주세요.')
+  const bestScoreboardMessage = escapeHtml(`醫뗭븘??${resolvedBestThreshold}媛??댁긽 ?볤?? BEST濡??밴꺽?⑸땲??`)
+  const moderationScoreboardMessage = escapeHtml(`?좉퀬 ${resolvedReportThreshold}???댁긽 ???먮룞?쇰줈 釉붾씪?몃뱶 泥섎━?⑸땲??`)
+  const bestEmptyMessage = escapeHtml('?꾩쭅 BEST ?볤????놁뒿?덈떎.')
+  const allEmptyMessage = escapeHtml(emptyLabel ?? '?꾩쭅 ?깅줉???볤????놁뒿?덈떎. 媛??癒쇱? ?섍껄???④꺼二쇱꽭??')
 
-  // 로그인 CTA 제거 - 익명 사용자도 댓글 작성 가능
+  // 濡쒓렇??CTA ?쒓굅 - ?듬챸 ?ъ슜?먮룄 ?볤? ?묒꽦 媛??
   const loginCtaBlock = ''
 
   const passwordId = `${normalizedBase}-password`
@@ -1013,12 +1038,12 @@ export const renderCommentsPlaceholder = ({
         <div class="flex-1 space-y-3">
             <div class="flex flex-col sm:flex-row gap-3 items-start sm:items-center justify-start">
             <div class="flex-1" data-cw-comment-nickname-wrapper>
-              <label class="sr-only" for="${escapeHtml(nicknameId)}">닉네임</label>
+              <label class="sr-only" for="${escapeHtml(nicknameId)}">?됰꽕??/label>
                 <div class="flex items-center gap-2 pl-2">
                 <div class="w-6 h-6 rounded-full bg-wiki-card flex items-center justify-center flex-shrink-0 hidden" data-cw-comment-author-avatar>
                   <i class="fas fa-user-circle text-sm text-wiki-muted"></i>
                 </div>
-                <span class="hidden text-xs px-2 py-1 rounded-full bg-red-500/15 text-red-200 font-semibold" data-cw-comment-admin-badge aria-label="관리자">관리자</span>
+                <span class="hidden text-xs px-2 py-1 rounded-full bg-red-500/15 text-red-200 font-semibold" data-cw-comment-admin-badge aria-label="愿由ъ옄">愿由ъ옄</span>
                 <span class="hidden text-sm text-wiki-text" data-cw-comment-nickname-display></span>
                 <span class="hidden text-xs text-wiki-muted" data-cw-comment-ip-display></span>
                 <input
@@ -1026,7 +1051,7 @@ export const renderCommentsPlaceholder = ({
                   name="nickname"
                   type="text"
                   maxlength="40"
-                  placeholder="닉네임"
+                  placeholder="?됰꽕??
                   autocomplete="nickname"
                   class="w-full px-4 py-2 bg-wiki-bg border border-wiki-border rounded-lg focus:border-wiki-primary focus:outline-none text-sm"
                   data-cw-comment-nickname
@@ -1037,16 +1062,16 @@ export const renderCommentsPlaceholder = ({
               <div class="w-6 h-6 rounded-full bg-wiki-card flex items-center justify-center flex-shrink-0" data-cw-comment-anon-avatar>
                 <i class="fas fa-user-circle text-sm text-wiki-muted"></i>
               </div>
-              <span class="text-sm text-wiki-text whitespace-nowrap" data-cw-comment-anonymous-number>익명</span>
+              <span class="text-sm text-wiki-text whitespace-nowrap" data-cw-comment-anonymous-number>?듬챸</span>
               <span class="text-xs text-wiki-muted" data-cw-comment-current-ip></span>
-              <label class="sr-only" for="${escapeHtml(passwordId)}">비밀번호 (익명 필수)</label>
+              <label class="sr-only" for="${escapeHtml(passwordId)}">鍮꾨?踰덊샇 (?듬챸 ?꾩닔)</label>
               <input
                 id="${escapeHtml(passwordId)}"
                 name="password"
                 type="password"
                 maxlength="4"
                 pattern="[0-9]{4}"
-                placeholder="비밀번호 (4자리 숫자)"
+                placeholder="鍮꾨?踰덊샇 (4?먮━ ?レ옄)"
                 autocomplete="new-password"
                 style="width: 175px;"
                 class="px-4 py-2 bg-wiki-bg border border-wiki-border rounded-lg focus:border-wiki-primary focus:outline-none text-sm"
@@ -1062,33 +1087,33 @@ export const renderCommentsPlaceholder = ({
             <button type="button" data-comment-prompt="question"
               class="px-3 py-1.5 text-xs rounded-full border border-wiki-border bg-wiki-bg
               hover:border-wiki-primary/50 hover:text-wiki-primary text-wiki-muted transition">
-              <i class="fas fa-question-circle mr-1"></i>궁금한 점
+              <i class="fas fa-question-circle mr-1"></i>沅곴툑????
             </button>
             <button type="button" data-comment-prompt="info"
               class="px-3 py-1.5 text-xs rounded-full border border-wiki-border bg-wiki-bg
               hover:border-wiki-primary/50 hover:text-wiki-primary text-wiki-muted transition">
-              <i class="fas fa-lightbulb mr-1"></i>정보 공유
+              <i class="fas fa-lightbulb mr-1"></i>?뺣낫 怨듭쑀
             </button>
             <button type="button" data-comment-prompt="experience"
               class="px-3 py-1.5 text-xs rounded-full border border-wiki-border bg-wiki-bg
               hover:border-wiki-primary/50 hover:text-wiki-primary text-wiki-muted transition">
-              <i class="fas fa-user-check mr-1"></i>경험담
+              <i class="fas fa-user-check mr-1"></i>寃쏀뿕??
             </button>
             <button type="button" data-comment-prompt="cheer"
               class="px-3 py-1.5 text-xs rounded-full border border-wiki-border bg-wiki-bg
               hover:border-wiki-primary/50 hover:text-wiki-primary text-wiki-muted transition">
-              <i class="fas fa-heart mr-1"></i>응원 한마디
+              <i class="fas fa-heart mr-1"></i>?묒썝 ?쒕쭏??
             </button>
           </div>
           <div>
-            <label class="sr-only" for="${escapeHtml(contentId)}">댓글 작성</label>
+            <label class="sr-only" for="${escapeHtml(contentId)}">?볤? ?묒꽦</label>
             <textarea
               id="${escapeHtml(contentId)}"
               name="content"
               rows="4"
               maxlength="500"
               required
-              placeholder="${entityType === 'major' ? '이 전공에 대해 자유롭게 이야기해주세요' : entityType === 'guide' ? '이 가이드에 대해 자유롭게 이야기해주세요' : '이 직업에 대해 자유롭게 이야기해주세요'}"
+              placeholder="${entityType === 'major' ? '???꾧났??????먯쑀濡?쾶 ?댁빞湲고빐二쇱꽭?? : entityType === 'guide' ? '??媛?대뱶??????먯쑀濡?쾶 ?댁빞湲고빐二쇱꽭?? : '??吏곸뾽??????먯쑀濡?쾶 ?댁빞湲고빐二쇱꽭??}"
               class="w-full px-4 py-3 bg-wiki-bg border border-wiki-border rounded-lg focus:border-wiki-primary focus:outline-none resize-y text-sm"
               aria-required="true"
               aria-describedby="${statusMessageId}"
@@ -1099,7 +1124,7 @@ export const renderCommentsPlaceholder = ({
       </div>
       <div class="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between" data-cw-comments-toolbar data-cw-telemetry-component="comments-toolbar">
         <div class="flex items-start gap-3 text-xs text-wiki-muted pl-4 leading-none">
-          <div class="leading-none" data-cw-comment-char-count>0 / 500자</div>
+          <div class="leading-none" data-cw-comment-char-count>0 / 500??/div>
           <div id="${statusMessageId}" data-cw-comment-status role="status" aria-live="polite" aria-atomic="true"></div>
         </div>
         <button
@@ -1110,7 +1135,7 @@ export const renderCommentsPlaceholder = ({
           data-cw-telemetry-component="comment-submit"
         >
           <i class="fas fa-paper-plane" aria-hidden="true"></i>
-          <span>등록</span>
+          <span>?깅줉</span>
         </button>
       </div>
     </form>
@@ -1139,7 +1164,7 @@ export const renderCommentsPlaceholder = ({
     data-cw-comment-governance-version="1"
     aria-labelledby="${normalizedBase}-title"
     aria-describedby="${authMessageId} ${statusMessageId} ${instructionsId} ${policyLinkId}"
-    aria-roledescription="댓글 상호작용 영역"
+    aria-roledescription="?볤? ?곹샇?묒슜 ?곸뿭"
     role="complementary"${sampleAttributes}
   >
         <header class="flex items-center justify-between gap-3" data-cw-comments-header data-cw-telemetry-scope="comments-header" data-cw-telemetry-component="comments-header">
@@ -1152,25 +1177,25 @@ export const renderCommentsPlaceholder = ({
             </div>
           </div>
           <div class="text-xs text-wiki-muted flex items-center" id="${policyLinkId}">
-            <a href="${helpCenterHref}" class="text-wiki-primary hover:text-wiki-secondary transition whitespace-nowrap" data-cw-comment-policy-link>커뮤니티 이용 정책</a>
+            <a href="${helpCenterHref}" class="text-wiki-primary hover:text-wiki-secondary transition whitespace-nowrap" data-cw-comment-policy-link>而ㅻ??덊떚 ?댁슜 ?뺤콉</a>
           </div>
         </header>
-    <p class="sr-only" id="${instructionsId}">댓글 목록을 볼 수 있으며, 로그인 여부와 관계없이 댓글 작성이 가능합니다.</p>
+    <p class="sr-only" id="${instructionsId}">?볤? 紐⑸줉??蹂????덉쑝硫? 濡쒓렇???щ?? 愿怨꾩뾾???볤? ?묒꽦??媛?ν빀?덈떎.</p>
     <div class="text-xs text-wiki-muted" id="${authMessageId}" data-cw-comment-auth aria-live="polite" aria-atomic="true"></div>
     ${loginCtaBlock}
     ${composerBlock}
     <div class="space-y-6" data-cw-comments-body data-cw-telemetry-component="comments-body" role="region" aria-live="polite" aria-atomic="false" aria-busy="false">
       <div class="flex items-center gap-3 text-sm text-wiki-muted" data-cw-comments-loading hidden>
         <i class="fas fa-spinner fa-spin" aria-hidden="true"></i>
-        <span>댓글을 불러오는 중...</span>
+        <span>?볤???遺덈윭?ㅻ뒗 以?..</span>
       </div>
       <div class="space-y-4" data-comment-toolbar>
         <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
-          <!-- 탭: 전체 댓글 / BEST -->
+          <!-- ?? ?꾩껜 ?볤? / BEST -->
           <div
             class="flex items-center gap-1 rounded-full border border-wiki-border/80 bg-wiki-bg/80 p-1 w-full sm:w-auto"
             role="tablist"
-            aria-label="댓글 보기 유형"
+            aria-label="?볤? 蹂닿린 ?좏삎"
             data-comment-tablist
           >
             <button
@@ -1181,7 +1206,7 @@ export const renderCommentsPlaceholder = ({
               aria-selected="true"
               id="${allTabId}"
               aria-controls="${allPanelId}"
-            >전체 댓글</button>
+            >?꾩껜 ?볤?</button>
             <button
               type="button"
               class="comment-tab-btn flex-1 sm:flex-none px-4 py-2.5 min-h-[44px] rounded-full text-sm font-semibold transition border border-transparent text-gray-300 hover:text-white hover:bg-wiki-primary/60"
@@ -1192,9 +1217,9 @@ export const renderCommentsPlaceholder = ({
               aria-controls="${bestPanelId}"
             >BEST</button>
           </div>
-          <!-- 정렬: 최신순 / 공감순 -->
+          <!-- ?뺣젹: 理쒖떊??/ 怨듦컧??-->
           <div class="flex items-center justify-between sm:justify-end gap-3 text-xs text-wiki-muted" data-comment-sort-toolbar>
-            <span class="uppercase tracking-wide text-[11px]">정렬</span>
+            <span class="uppercase tracking-wide text-[11px]">?뺣젹</span>
             <div class="flex flex-1 sm:flex-none rounded-lg border border-wiki-border/70 bg-wiki-bg/70 p-1" role="group" data-comment-sort-group>
               <button
                 type="button"
@@ -1202,21 +1227,21 @@ export const renderCommentsPlaceholder = ({
                 data-comment-sort="latest"
                 aria-pressed="true"
                 id="${sortLatestId}"
-              >최신순</button>
+              >理쒖떊??/button>
               <button
                 type="button"
                 class="comment-sort-btn flex-1 sm:flex-none px-3 sm:px-4 py-2 min-h-[40px] rounded-md text-xs font-semibold transition border border-transparent text-gray-300 hover:text-white hover:bg-wiki-border/50"
                 data-comment-sort="likes"
                 aria-pressed="false"
                 id="${sortLikesId}"
-              >공감순</button>
+              >怨듦컧??/button>
             </div>
           </div>
         </div>
         <p class="text-xs text-wiki-muted" data-cw-comment-guidance hidden></p>
       </div>
       <section id="${bestPanelId}" class="space-y-3" role="tabpanel" aria-labelledby="${bestTabId}" data-comment-panel="best" hidden>
-        <h3 class="sr-only">BEST 댓글</h3>
+        <h3 class="sr-only">BEST ?볤?</h3>
         <ul class="space-y-4" data-comment-list="best" role="list"></ul>
         <div class="flex flex-col items-center justify-center gap-3 rounded-xl border border-dashed border-wiki-border/60 bg-wiki-bg/40 px-6 py-10 text-center text-sm text-wiki-muted" data-comment-empty="best">
           <i class="fas fa-sparkles text-lg text-wiki-secondary" aria-hidden="true"></i>
@@ -1224,7 +1249,7 @@ export const renderCommentsPlaceholder = ({
         </div>
       </section>
       <section id="${allPanelId}" class="space-y-3" role="tabpanel" aria-labelledby="${allTabId}" data-comment-panel="all">
-        <h3 class="sr-only">전체 댓글</h3>
+        <h3 class="sr-only">?꾩껜 ?볤?</h3>
         <ul class="space-y-4" data-comment-list="all" role="list"></ul>
         <div class="flex flex-col items-center justify-center gap-3 rounded-xl border border-dashed border-wiki-border/60 bg-wiki-bg/40 px-6 py-10 text-center text-sm text-wiki-muted" data-comment-empty="all">
           <i class="fas fa-comment-dots text-lg text-wiki-secondary" aria-hidden="true"></i>
@@ -1236,30 +1261,30 @@ export const renderCommentsPlaceholder = ({
         <ul data-cw-comments-list></ul>
       </div>
     </div>
-    <!-- 댓글 신고 모달 -->
+    <!-- ?볤? ?좉퀬 紐⑤떖 -->
     <div class="fixed inset-0 z-[2000] hidden" data-cw-comment-delete-modal role="dialog" aria-modal="true" aria-labelledby="${normalizedBase}-comment-delete-title">
       <div class="fixed inset-0 bg-black/60 backdrop-blur-sm" data-cw-comment-delete-backdrop></div>
       <div class="fixed inset-0 flex items-center justify-center px-4">
         <div class="w-full max-w-sm rounded-2xl bg-wiki-bg border border-wiki-border shadow-2xl" data-cw-comment-delete-dialog>
           <div class="flex items-center justify-between px-6 py-4 border-b border-wiki-border">
-            <h3 id="${normalizedBase}-comment-delete-title" class="text-lg font-semibold text-white">댓글 삭제</h3>
-            <button type="button" class="text-wiki-muted hover:text-white transition" data-cw-comment-delete-close aria-label="닫기">
+            <h3 id="${normalizedBase}-comment-delete-title" class="text-lg font-semibold text-white">?볤? ??젣</h3>
+            <button type="button" class="text-wiki-muted hover:text-white transition" data-cw-comment-delete-close aria-label="?リ린">
               <i class="fas fa-times"></i>
             </button>
           </div>
           <div class="p-6 space-y-4">
-            <p class="text-sm text-wiki-muted">이 댓글을 삭제하시겠습니까? 삭제된 댓글은 복구할 수 없습니다.</p>
+            <p class="text-sm text-wiki-muted">???볤?????젣?섏떆寃좎뒿?덇퉴? ??젣???볤?? 蹂듦뎄?????놁뒿?덈떎.</p>
             <div class="hidden" data-cw-comment-delete-pw-wrap>
-              <label class="text-sm font-semibold text-white">비밀번호 (숫자 4자리)</label>
-              <input type="password" inputmode="numeric" maxlength="4" pattern="\\d{4}" class="mt-1 w-full rounded-lg border border-wiki-border bg-wiki-bg px-3 py-2 text-white focus:border-wiki-primary focus:outline-none" style="font-size: 16px;" placeholder="비밀번호 입력" data-cw-comment-delete-password>
+              <label class="text-sm font-semibold text-white">鍮꾨?踰덊샇 (?レ옄 4?먮━)</label>
+              <input type="password" inputmode="numeric" maxlength="4" pattern="\\d{4}" class="mt-1 w-full rounded-lg border border-wiki-border bg-wiki-bg px-3 py-2 text-white focus:border-wiki-primary focus:outline-none" style="font-size: 16px;" placeholder="鍮꾨?踰덊샇 ?낅젰" data-cw-comment-delete-password>
             </div>
             <div class="text-sm text-red-400 hidden" data-cw-comment-delete-error role="alert" aria-live="polite"></div>
             <div class="flex items-center justify-between gap-3 pt-2">
               <button type="button" class="flex-1 px-4 py-2 rounded-lg border border-wiki-border text-sm text-wiki-muted hover:text-white hover:border-wiki-primary transition" data-cw-comment-delete-close>
-                취소
+                痍⑥냼
               </button>
               <button type="button" class="flex-1 px-4 py-2 rounded-lg bg-rose-600 hover:bg-rose-500 text-sm font-semibold text-white transition" data-cw-comment-delete-confirm>
-                삭제하기
+                ??젣?섍린
               </button>
             </div>
           </div>
@@ -1272,34 +1297,34 @@ export const renderCommentsPlaceholder = ({
       <div class="fixed inset-0 flex items-center justify-center px-4">
         <div class="w-full max-w-md rounded-2xl bg-wiki-bg border border-wiki-border shadow-2xl" data-cw-comment-report-dialog>
           <div class="flex items-center justify-between px-6 py-4 border-b border-wiki-border">
-            <h3 id="${normalizedBase}-comment-report-title" class="text-lg font-semibold text-white">댓글 신고</h3>
-            <button type="button" class="text-wiki-muted hover:text-white transition" data-cw-comment-report-close aria-label="닫기">
+            <h3 id="${normalizedBase}-comment-report-title" class="text-lg font-semibold text-white">?볤? ?좉퀬</h3>
+            <button type="button" class="text-wiki-muted hover:text-white transition" data-cw-comment-report-close aria-label="?リ린">
               <i class="fas fa-times"></i>
             </button>
           </div>
           <form class="p-6 space-y-4" data-cw-comment-report-form>
             <div class="space-y-2">
-              <label class="text-sm font-semibold text-white">신고 사유 *</label>
+              <label class="text-sm font-semibold text-white">?좉퀬 ?ъ쑀 *</label>
               <select class="w-full rounded-lg border border-wiki-border bg-wiki-bg px-3 py-2 text-sm text-white focus:border-wiki-primary focus:outline-none" data-cw-comment-report-reason required>
-                <option value="">선택해주세요</option>
-                <option value="defamation">명예훼손</option>
-                <option value="obscene">음란물</option>
-                <option value="spam">스팸/광고</option>
-                <option value="copyright">저작권 침해</option>
-                <option value="false_info">허위정보</option>
-                <option value="other">기타</option>
+                <option value="">?좏깮?댁＜?몄슂</option>
+                <option value="defamation">紐낆삁?쇱넀</option>
+                <option value="obscene">?뚮?臾?/option>
+                <option value="spam">?ㅽ뙵/愿묎퀬</option>
+                <option value="copyright">??묎텒 移⑦빐</option>
+                <option value="false_info">?덉쐞?뺣낫</option>
+                <option value="other">湲고?</option>
               </select>
             </div>
             <div class="space-y-2">
-              <label class="text-sm font-semibold text-white">상세 사유 (선택)</label>
-              <textarea class="w-full rounded-lg border border-wiki-border bg-wiki-bg px-3 py-2 text-white min-h-[96px] resize-none focus:border-wiki-primary focus:outline-none" style="font-size: 16px;" maxlength="300" placeholder="추가 설명이 있다면 작성해주세요 (최대 300자)" data-cw-comment-report-detail></textarea>
+              <label class="text-sm font-semibold text-white">?곸꽭 ?ъ쑀 (?좏깮)</label>
+              <textarea class="w-full rounded-lg border border-wiki-border bg-wiki-bg px-3 py-2 text-white min-h-[96px] resize-none focus:border-wiki-primary focus:outline-none" style="font-size: 16px;" maxlength="300" placeholder="異붽? ?ㅻ챸???덈떎硫??묒꽦?댁＜?몄슂 (理쒕? 300??" data-cw-comment-report-detail></textarea>
             </div>
             <div class="flex items-center justify-between gap-3 pt-2">
               <button type="button" class="flex-1 px-4 py-2 rounded-lg border border-wiki-border text-sm text-wiki-muted hover:text-white hover:border-wiki-primary transition" data-cw-comment-report-close>
-                취소
+                痍⑥냼
               </button>
               <button type="submit" class="flex-1 px-4 py-2 rounded-lg bg-rose-600 hover:bg-rose-500 text-sm font-semibold text-white transition" data-cw-comment-report-submit>
-                신고하기
+                ?좉퀬?섍린
               </button>
             </div>
             <div class="text-sm" data-cw-comment-report-message role="alert" aria-live="polite"></div>
@@ -1464,9 +1489,9 @@ export const buildDetailScaffold = ({
 }
 
 /**
- * 광고 슬롯 플레이스홀더
- * 출처 섹션과 댓글 섹션 사이에 배치되는 광고 영역
- * 실제 광고가 로드되지 않으면 빈 상태로 collapse됨
+ * 愿묎퀬 ?щ’ ?뚮젅?댁뒪???
+ * 異쒖쿂 ?뱀뀡怨??볤? ?뱀뀡 ?ъ씠??諛곗튂?섎뒗 愿묎퀬 ?곸뿭
+ * ?ㅼ젣 愿묎퀬媛 濡쒕뱶?섏? ?딆쑝硫?鍮??곹깭濡?collapse??
  */
 export const renderAdSlot = (options: {
   entityType: 'job' | 'major' | 'howto'
@@ -1486,7 +1511,7 @@ export const renderAdSlot = (options: {
             <div class="ad-slot-content rounded-xl border border-wiki-border/20 bg-wiki-card/30 overflow-hidden"
                  data-ad-content
                  style="min-height: 90px;">
-              <!-- 광고 콘텐츠가 여기에 삽입됩니다 -->
+              <!-- 愿묎퀬 肄섑뀗痢좉? ?ш린???쎌엯?⑸땲??-->
             </div>
           </div>
         </div>
@@ -1541,14 +1566,14 @@ export const renderSourcesPanel = ({
       return hasData || hasError
     })
 
-  // API 출처를 한 줄에 나란히 표시 (그리드) + 설명 포함
+  // API 異쒖쿂瑜???以꾩뿉 ?섎????쒖떆 (洹몃━?? + ?ㅻ챸 ?ы븿
   const sourceCount = validSources.length
   const gridCols = sourceCount >= 3 ? 'grid-cols-3' : sourceCount === 2 ? 'grid-cols-2' : 'grid-cols-1'
   const entries = validSources.length > 0
     ? `<div class="grid ${gridCols} gap-2">${validSources.map((source) => {
         const hasData = Boolean(partials?.[source])
         const status = sources?.[source]
-        const note = hasData ? '통합 데이터에 포함됨' : status?.error ?? ''
+        const note = hasData ? '?듯빀 ?곗씠?곗뿉 ?ы븿?? : status?.error ?? ''
         const icon = hasData ? 'fa-circle-check text-green-400' : 'fa-circle-exclamation text-amber-300'
         const clickableClass = hasData ? 'cursor-pointer hover:border-wiki-primary/60 hover:bg-wiki-primary/5' : ''
         const descLine = descriptions?.[source] ? `<p class="text-[11px] text-wiki-muted mt-1">${escapeHtml(descriptions[source] ?? '')}</p>` : ''
@@ -1641,7 +1666,7 @@ export const sanitizeJson = (value: unknown): unknown => {
   return value ?? undefined
 }
 
-// ─── Unified Chart Data ───
+// ??? Unified Chart Data ???
 
 export interface UnifiedChartData {
   chartType: 'bar' | 'doughnut' | 'horizontalBar'
@@ -1696,7 +1721,7 @@ export const renderUnifiedChart = (data: UnifiedChartData, idPrefix: string): st
     ? `<h4 class="text-base font-bold text-wiki-secondary mb-4">${escapeHtml(data.title)}</h4>`
     : ''
   const noteHtml = data.note
-    ? `<p class="text-xs text-wiki-muted mt-3">※ ${escapeHtml(data.note)}</p>`
+    ? `<p class="text-xs text-wiki-muted mt-3">??${escapeHtml(data.note)}</p>`
     : ''
 
   if (data.chartType === 'doughnut') {
@@ -1783,14 +1808,14 @@ export const renderUnifiedChart = (data: UnifiedChartData, idPrefix: string): st
     </div>`
 }
 
-// ─── 커리어트리 렌더링 ─────────────────────────────────────
+// ??? 而ㅻ━?댄듃由??뚮뜑留??????????????????????????????????????
 
 import type { CareerTreeForJob, CareerTreeStage } from '../types/careerTree'
 
 /**
- * 커리어트리 섹션 전체 렌더링
- * - 기본 2명 표시 + "더보기" 토글
- * - 각 인물: 인물 카드 + 세로 타임라인
+ * 而ㅻ━?댄듃由??뱀뀡 ?꾩껜 ?뚮뜑留?
+ * - 湲곕낯 2紐??쒖떆 + "?붾낫湲? ?좉?
+ * - 媛??몃Ъ: ?몃Ъ 移대뱶 + ?몃줈 ??꾨씪??
  */
 export const renderCareerTreeSection = (
   trees: CareerTreeForJob[],
@@ -1804,16 +1829,16 @@ export const renderCareerTreeSection = (
     return `<div class="ct-panel${i === 0 ? '' : ' hidden'}" data-ct-idx="${i}">${card}</div>`
   }).join('')
 
-  // 1명이면 네비게이션 불필요
+  // 1紐낆씠硫??ㅻ퉬寃뚯씠??遺덊븘??
   const nav = total > 1 ? `
     <div class="flex items-center justify-between mb-4">
-      <p class="text-sm text-wiki-muted">이 직업과 관련된 인물의 실제 커리어 경로입니다.</p>
+      <p class="text-sm text-wiki-muted">??吏곸뾽怨?愿?⑤맂 ?몃Ъ???ㅼ젣 而ㅻ━??寃쎈줈?낅땲??</p>
       <div class="flex items-center gap-2 flex-shrink-0">
-        <button onclick="ctNav(-1)" class="ct-prev p-1.5 text-wiki-muted hover:text-wiki-text rounded-md hover:bg-wiki-primary/5 transition-colors disabled:opacity-30 disabled:cursor-not-allowed" disabled aria-label="이전 인물">
+        <button onclick="ctNav(-1)" class="ct-prev p-1.5 text-wiki-muted hover:text-wiki-text rounded-md hover:bg-wiki-primary/5 transition-colors disabled:opacity-30 disabled:cursor-not-allowed" disabled aria-label="?댁쟾 ?몃Ъ">
           <i class="fas fa-chevron-left text-xs"></i>
         </button>
         <span class="ct-counter text-xs text-wiki-muted tabular-nums min-w-[3ch] text-center">${total > 1 ? `1 / ${total}` : ''}</span>
-        <button onclick="ctNav(1)" class="ct-next p-1.5 text-wiki-muted hover:text-wiki-text rounded-md hover:bg-wiki-primary/5 transition-colors disabled:opacity-30 disabled:cursor-not-allowed" aria-label="다음 인물">
+        <button onclick="ctNav(1)" class="ct-next p-1.5 text-wiki-muted hover:text-wiki-text rounded-md hover:bg-wiki-primary/5 transition-colors disabled:opacity-30 disabled:cursor-not-allowed" aria-label="?ㅼ쓬 ?몃Ъ">
           <i class="fas fa-chevron-right text-xs"></i>
         </button>
       </div>
@@ -1834,7 +1859,7 @@ export const renderCareerTreeSection = (
     })();
     </script>` : ''
 
-  const desc = total <= 1 ? '<p class="text-sm text-wiki-muted mb-4">이 직업과 관련된 인물의 실제 커리어 경로입니다.</p>' : ''
+  const desc = total <= 1 ? '<p class="text-sm text-wiki-muted mb-4">??吏곸뾽怨?愿?⑤맂 ?몃Ъ???ㅼ젣 而ㅻ━??寃쎈줈?낅땲??</p>' : ''
 
   return `
     <div>
@@ -1845,12 +1870,12 @@ export const renderCareerTreeSection = (
     ${script}`
 }
 
-/** 개별 인물의 커리어트리 (인물 카드 + 타임라인) */
+/** 媛쒕퀎 ?몃Ъ??而ㅻ━?댄듃由?(?몃Ъ 移대뱶 + ??꾨씪?? */
 function renderPersonCareerTree(tree: CareerTreeForJob): string {
   const nameEn = tree.personNameEn ? ` <span class="text-wiki-muted font-normal">(${escapeHtml(tree.personNameEn)})</span>` : ''
   const title = tree.personTitle ? `<p class="text-sm text-wiki-muted mt-0.5">${escapeHtml(tree.personTitle)}</p>` : ''
 
-  // 인물 이미지 또는 폴백 아이콘
+  // ?몃Ъ ?대?吏 ?먮뒗 ?대갚 ?꾩씠肄?
   const imageHtml = tree.personImageUrl
     ? `<img src="${escapeHtml(tree.personImageUrl)}" alt="${escapeHtml(tree.personName)}" class="w-12 h-12 rounded-full object-cover border-2 border-wiki-border/40" loading="lazy">`
     : `<div class="w-12 h-12 rounded-full bg-wiki-primary/10 border-2 border-wiki-border/40 flex items-center justify-center">
@@ -1861,7 +1886,7 @@ function renderPersonCareerTree(tree: CareerTreeForJob): string {
 
   return `
     <div class="rounded-xl border border-wiki-border/40 bg-wiki-bg/30 overflow-hidden">
-      <!-- 인물 헤더 -->
+      <!-- ?몃Ъ ?ㅻ뜑 -->
       <div class="flex items-center gap-3 px-4 py-3 border-b border-wiki-border/30">
         ${imageHtml}
         <div class="min-w-0">
@@ -1869,48 +1894,48 @@ function renderPersonCareerTree(tree: CareerTreeForJob): string {
           ${title}
         </div>
       </div>
-      <!-- 타임라인 -->
+      <!-- ??꾨씪??-->
       <div class="px-4 py-4">
         ${timeline}
       </div>
     </div>`
 }
 
-/** 세로 타임라인 렌더링 */
+/** ?몃줈 ??꾨씪???뚮뜑留?*/
 function renderCareerTimeline(
   stages: CareerTreeStage[],
   highlightIndex: number | null
 ): string {
-  if (!stages || stages.length === 0) return '<p class="text-sm text-wiki-muted">경로 정보가 없습니다.</p>'
+  if (!stages || stages.length === 0) return '<p class="text-sm text-wiki-muted">寃쎈줈 ?뺣낫媛 ?놁뒿?덈떎.</p>'
 
-  // stages를 order 순으로 정렬
+  // stages瑜?order ?쒖쑝濡??뺣젹
   const sorted = [...stages].sort((a, b) => a.order - b.order)
 
   const nodes = sorted.map((stage, i) => {
     const isHighlight = highlightIndex !== null && i === highlightIndex
     const isLast = i === sorted.length - 1
 
-    // 노드 점 스타일
+    // ?몃뱶 ???ㅽ???
     const dotClass = isHighlight
       ? 'w-3.5 h-3.5 rounded-full bg-[rgb(var(--wp))] border-2 border-[rgb(var(--wp))] shadow-[0_0_8px_rgba(var(--wp),0.4)]'
       : 'w-3.5 h-3.5 rounded-full bg-wiki-bg border-2 border-wiki-border'
 
-    // 카드 배경
+    // 移대뱶 諛곌꼍
     const cardBg = isHighlight
       ? 'bg-[rgb(var(--wp))]/5 border border-[rgb(var(--wp))]/20 rounded-lg px-3 py-2'
       : 'px-3 py-2'
 
-    // 타이틀 색상
+    // ??댄? ?됱긽
     const titleClass = isHighlight
       ? 'text-sm font-bold text-[rgb(var(--wp))]'
       : 'text-sm font-semibold text-wiki-text'
 
-    // 연결선 (마지막 노드 제외)
+    // ?곌껐??(留덉?留??몃뱶 ?쒖쇅)
     const connectorLine = !isLast
       ? '<div class="absolute left-[6.5px] top-[18px] bottom-[-8px] w-0.5 bg-wiki-border/50"></div>'
       : ''
 
-    // 조직 + 기간 (전공 링크 지원)
+    // 議곗쭅 + 湲곌컙 (?꾧났 留곹겕 吏??
     const meta: string[] = []
     if (stage.organization) {
       const orgText = escapeHtml(stage.organization)
@@ -1922,15 +1947,15 @@ function renderCareerTimeline(
     }
     if (stage.years) meta.push(escapeHtml(stage.years))
     const metaHtml = meta.length > 0
-      ? `<p class="text-xs text-wiki-muted mt-0.5">${meta.join(' · ')}</p>`
+      ? `<p class="text-xs text-wiki-muted mt-0.5">${meta.join(' 쨌 ')}</p>`
       : ''
 
-    // 설명
+    // ?ㅻ챸
     const descHtml = stage.description
       ? `<p class="text-xs text-wiki-muted/80 mt-1">${escapeHtml(stage.description)}</p>`
       : ''
 
-    // job_slug 링크 (하이라이트가 아닌 경우 다른 직업 페이지로 이동 가능)
+    // job_slug 留곹겕 (?섏씠?쇱씠?멸? ?꾨땶 寃쎌슦 ?ㅻⅨ 吏곸뾽 ?섏씠吏濡??대룞 媛??
     let titleContent = escapeHtml(stage.title)
     if (!isHighlight && stage.job_slug) {
       titleContent = `<a href="/job/${encodeURIComponent(stage.job_slug)}" class="hover:underline decoration-wiki-secondary/40">${escapeHtml(stage.title)}</a>`
@@ -1938,12 +1963,12 @@ function renderCareerTimeline(
 
     return `
       <div class="relative flex gap-3 ${!isLast ? 'pb-4' : ''}">
-        <!-- 연결선 + 점 -->
+        <!-- ?곌껐??+ ??-->
         <div class="relative flex-shrink-0 w-3.5 mt-2.5">
           ${connectorLine}
           <div class="${dotClass}"></div>
         </div>
-        <!-- 내용 -->
+        <!-- ?댁슜 -->
         <div class="flex-1 ${cardBg}">
           <p class="${titleClass}">${titleContent}</p>
           ${metaHtml}
@@ -1955,3 +1980,5 @@ function renderCareerTimeline(
   return `<div class="space-y-0">${nodes.join('')}</div>`
 }
 
+
+
diff --git a/src/templates/unifiedJobDetail.ts b/src/templates/unifiedJobDetail.ts
index 8a276eb..7b5b182 100644
--- a/src/templates/unifiedJobDetail.ts
+++ b/src/templates/unifiedJobDetail.ts
@@ -34,46 +34,46 @@ export interface UnifiedJobDetailTemplateParams {
   profile: UnifiedJobDetail
   partials?: Partial<Record<DataSource, UnifiedJobDetail | null>>
   sources?: SourceStatusRecord
-  existingJobSlugs?: Map<string, string>  // DB에 존재하는 직업명 → slug 매핑
-  relatedHowtos?: Array<{ slug: string; title: string; summary: string }>  // 이 직업을 참조하는 HowTo
-  classificationData?: { large_category?: string; medium_category?: string }  // job_categories 테이블에서
-  careerTrees?: import('../types/careerTree').CareerTreeForJob[]  // 커리어트리 데이터
+  existingJobSlugs?: Map<string, string>  // DB??議댁옱?섎뒗 吏곸뾽紐???slug 留ㅽ븨
+  relatedHowtos?: Array<{ slug: string; title: string; summary: string }>  // ??吏곸뾽??李몄“?섎뒗 HowTo
+  classificationData?: { large_category?: string; medium_category?: string }  // job_categories ?뚯씠釉붿뿉??
+  careerTrees?: import('../types/careerTree').CareerTreeForJob[]  // 而ㅻ━?댄듃由??곗씠??
 }
 
 const SOURCE_DESCRIPTIONS: Record<DataSource, string> = {
-  CAREERNET: '교육부 산하 진로·진학 정보 플랫폼',
-  GOYONG24: '고용노동부 고용24 직업 정보',
-  WORK24_JOB: '고용노동부 고용24 직업 정보',
-  WORK24_DJOB: '고용노동부 고용24 직업사전',
-  WORK24_MAJOR: '고용노동부 고용24 전공 정보',
-  AI: 'AI 보조 생성 데이터',
-  USER_CONTRIBUTED: '사용자 기여 데이터',
-  ADMIN_OVERRIDE: '관리자 검증 데이터'
+  CAREERNET: '援먯쑁遺 ?고븯 吏꾨줈쨌吏꾪븰 ?뺣낫 ?뚮옯??,
+  GOYONG24: '怨좎슜?몃룞遺 怨좎슜24 吏곸뾽 ?뺣낫',
+  WORK24_JOB: '怨좎슜?몃룞遺 怨좎슜24 吏곸뾽 ?뺣낫',
+  WORK24_DJOB: '怨좎슜?몃룞遺 怨좎슜24 吏곸뾽?ъ쟾',
+  WORK24_MAJOR: '怨좎슜?몃룞遺 怨좎슜24 ?꾧났 ?뺣낫',
+  AI: 'AI 蹂댁“ ?앹꽦 ?곗씠??,
+  USER_CONTRIBUTED: '?ъ슜??湲곗뿬 ?곗씠??,
+  ADMIN_OVERRIDE: '愿由ъ옄 寃利??곗씠??
 }
 
 const SOURCE_NOTE_LABELS: Record<DataSource, string> = {
-  CAREERNET: '커리어넷 데이터',
-  GOYONG24: '고용24 데이터',
-  WORK24_JOB: '고용24 데이터',
-  WORK24_DJOB: '고용24 사전 데이터',
-  WORK24_MAJOR: '고용24 학과 데이터',
-  AI: 'AI 생성 데이터',
-  USER_CONTRIBUTED: '사용자 기여',
-  ADMIN_OVERRIDE: '관리자 보정'
+  CAREERNET: '而ㅻ━?대꽬 ?곗씠??,
+  GOYONG24: '怨좎슜24 ?곗씠??,
+  WORK24_JOB: '怨좎슜24 ?곗씠??,
+  WORK24_DJOB: '怨좎슜24 ?ъ쟾 ?곗씠??,
+  WORK24_MAJOR: '怨좎슜24 ?숆낵 ?곗씠??,
+  AI: 'AI ?앹꽦 ?곗씠??,
+  USER_CONTRIBUTED: '?ъ슜??湲곗뿬',
+  ADMIN_OVERRIDE: '愿由ъ옄 蹂댁젙'
 }
 
 const SKIPPED_REASON_MESSAGES: Record<string, string> = {
-  excluded: '제외됨',
-  'keyword-required': '키워드 필요',
-  'missing-id': '식별자 필요',
-  'missing-params': '추가 정보 필요',
-  'synthetic-fallback': '샘플 데이터',
-  default: '상태 확인 중'
+  excluded: '?쒖쇅??,
+  'keyword-required': '?ㅼ썙???꾩슂',
+  'missing-id': '?앸퀎???꾩슂',
+  'missing-params': '異붽? ?뺣낫 ?꾩슂',
+  'synthetic-fallback': '?섑뵆 ?곗씠??,
+  default: '?곹깭 ?뺤씤 以?
 }
 
 const LAWYER_IDENTIFIER_TOKENS = [
   'lawyer',
-  '변호사',
+  '蹂?몄궗',
   'job:c_375',
   'job-c-375',
   'job:g_k000007482',
@@ -86,18 +86,18 @@ const LAWYER_IDENTIFIER_TOKENS = [
 const LAWYER_EXACT_IDS = ['375', 'k000007482']
 
 /**
- * 안전한 trim 함수 - undefined/null 체크
+ * ?덉쟾??trim ?⑥닔 - undefined/null 泥댄겕
  */
 const safeTrim = (value: any): string => {
   if (value === null || value === undefined) return ''
   if (typeof value !== 'string') return String(value).trim()
-  return value.trim()  // ✅ Fixed: value가 string일 때는 직접 trim() 호출
+  return value.trim()  // ??Fixed: value媛 string???뚮뒗 吏곸젒 trim() ?몄텧
 }
 
 /**
- * 배열 항목에서 표시 텍스트 안전 추출
- * 객체인 경우 주요 키를 우선순위대로 시도하여 문자열 반환
- * DB에 저장된 "[object Object]" 문자열도 필터링
+ * 諛곗뿴 ??ぉ?먯꽌 ?쒖떆 ?띿뒪???덉쟾 異붿텧
+ * 媛앹껜??寃쎌슦 二쇱슂 ?ㅻ? ?곗꽑?쒖쐞?濡??쒕룄?섏뿬 臾몄옄??諛섑솚
+ * DB????λ맂 "[object Object]" 臾몄옄?대룄 ?꾪꽣留?
  */
 const extractDisplayText = (item: any): string => {
   if (item === null || item === undefined) return ''
@@ -159,7 +159,7 @@ const renderDutyListItems = (items: string[]): string => {
     .map(
       (sentence) => `
         <li class="flex items-start gap-2 text-base leading-relaxed text-wiki-text" data-job-duty-item>
-          <span class="text-wiki-primary mt-0.5 text-xs font-semibold">▶</span>
+          <span class="text-wiki-primary mt-0.5 text-xs font-semibold">??/span>
           <span>${escapeHtml(sentence)}</span>
         </li>
       `
@@ -210,7 +210,7 @@ const resolveAbilityLabel = (item: any): string => {
     safeTrim(item.description)
   const score =
     typeof item.score === 'number' && Number.isFinite(item.score) && item.score > 0
-      ? ` (${item.score}점)`
+      ? ` (${item.score}??`
       : ''
   if (base) {
     return `${base}${score}`
@@ -229,38 +229,38 @@ const hasStructuredForecastData = (item?: ForecastItem | null): boolean => {
 }
 
 /**
- * 안전한 문자열 체크 - 비어있지 않은 문자열인지 확인
+ * ?덉쟾??臾몄옄??泥댄겕 - 鍮꾩뼱?덉? ?딆? 臾몄옄?댁씤吏 ?뺤씤
  */
 const isNonEmptyString = (value: any): boolean => {
   return value !== null && value !== undefined && typeof value === 'string' && safeTrim(value).length > 0
 }
 
 /**
- * 문장을 마침표(.)로 분리하여 불릿 리스트로 변환
- * 쉼표는 문장 내 구분자로 유지됨
+ * 臾몄옣??留덉묠??.)濡?遺꾨━?섏뿬 遺덈┸ 由ъ뒪?몃줈 蹂??
+ * ?쇳몴??臾몄옣 ??援щ텇?먮줈 ?좎???
  */
 const formatAsBulletList = (text: string): string => {
   if (!text || !safeTrim(text)) {
-    return '<p class="content-text text-wiki-muted">정보가 제공되지 않았습니다.</p>'
+    return '<p class="content-text text-wiki-muted">?뺣낫媛 ?쒓났?섏? ?딆븯?듬땲??</p>'
   }
   
-  // 마침표로만 문장 분리 (쉼표는 문장 내 구분자로 유지)
+  // 留덉묠?쒕줈留?臾몄옣 遺꾨━ (?쇳몴??臾몄옣 ??援щ텇?먮줈 ?좎?)
   const sentences = text
     .split(/\.\s*/)
     .map(s => safeTrim(s))
-    .map(s => s.replace(/^[,，]\s*/, ''))  // 문장 시작의 쉼표 제거
+    .map(s => s.replace(/^[,竊?\s*/, ''))  // 臾몄옣 ?쒖옉???쇳몴 ?쒓굅
     .filter(s => s.length > 0)
   
   if (sentences.length === 0) {
-    return '<p class="content-text text-wiki-muted">정보가 제공되지 않았습니다.</p>'
+    return '<p class="content-text text-wiki-muted">?뺣낫媛 ?쒓났?섏? ?딆븯?듬땲??</p>'
   }
   
   if (sentences.length === 1) {
-    // 문장이 하나만 있으면 불릿 없이 그냥 표시
+    // 臾몄옣???섎굹留??덉쑝硫?遺덈┸ ?놁씠 洹몃깷 ?쒖떆
     return `<p class="content-text leading-relaxed text-wiki-text">${escapeHtml(sentences[0])}</p>`
   }
   
-  // 여러 문장이 있으면 불릿 리스트로 변환
+  // ?щ윭 臾몄옣???덉쑝硫?遺덈┸ 由ъ뒪?몃줈 蹂??
   const listItems = sentences
     .map(sentence => `<li>${escapeHtml(sentence)}</li>`)
     .join('')
@@ -269,67 +269,67 @@ const formatAsBulletList = (text: string): string => {
 }
 
 /**
- * 업무 상세를 계층적 구조로 렌더링 (대분류 제목 + bullet point 항목)
+ * ?낅Т ?곸꽭瑜?怨꾩링??援ъ“濡??뚮뜑留?(?遺꾨쪟 ?쒕ぉ + bullet point ??ぉ)
  */
 const formatWorkDetailAsNumberedCards = (text: string): string => {
   if (!text || !safeTrim(text)) {
-    return '<p class="content-text text-wiki-muted">정보가 제공되지 않았습니다.</p>'
+    return '<p class="content-text text-wiki-muted">?뺣낫媛 ?쒓났?섏? ?딆븯?듬땲??</p>'
   }
   
-  // 줄 단위로 분리
+  // 以??⑥쐞濡?遺꾨━
   const lines = text
     .split(/\n/)
     .map(s => safeTrim(s))
     .filter(s => s.length > 0)
   
   if (lines.length === 0) {
-    return '<p class="content-text text-wiki-muted">정보가 제공되지 않았습니다.</p>'
+    return '<p class="content-text text-wiki-muted">?뺣낫媛 ?쒓났?섏? ?딆븯?듬땲??</p>'
   }
   
-  // 단일 항목인 경우
+  // ?⑥씪 ??ぉ??寃쎌슦
   if (lines.length === 1) {
     const line = lines[0]
-    const isCategory = /^[\[\【]/.test(line)
+    const isCategory = /^[\[\??/.test(line)
     if (isCategory) {
-      // 대분류만 있는 경우
-      const categoryName = line.replace(/[\[\】\]]/g, '').trim()
+      // ?遺꾨쪟留??덈뒗 寃쎌슦
+      const categoryName = line.replace(/[\[\??]]/g, '').trim()
       return `<h4 class="text-base font-semibold text-white mb-2">${escapeHtml(categoryName)}</h4>`
     } else {
-      // 일반 텍스트
-      const cleanText = line.replace(/^[-–—•]\s*/, '').trim()
+      // ?쇰컲 ?띿뒪??
+      const cleanText = line.replace(/^[-?볛붴?\s*/, '').trim()
       return `<p class="content-text leading-relaxed text-wiki-text">${escapeHtml(cleanText)}</p>`
     }
   }
   
-  // 계층 구조 파싱: 대분류([...])와 항목(- ...) 구분
+  // 怨꾩링 援ъ“ ?뚯떛: ?遺꾨쪟([...])? ??ぉ(- ...) 援щ텇
   const sections: Array<{ category?: string; items: string[] }> = []
   let currentCategory: string | null = null
   let currentItems: string[] = []
   
   for (const line of lines) {
-    // 대분류 감지: [철도신호원], 【철도수송원】 등
-    const categoryMatch = line.match(/^[\[\【]([^\]】]+)[\]】]/)
+    // ?遺꾨쪟 媛먯?: [泥좊룄?좏샇??, ?먯쿋?꾩닔?≪썝????
+    const categoryMatch = line.match(/^[\[\??([^\]??+)[\]??/)
     if (categoryMatch) {
-      // 이전 섹션 저장
+      // ?댁쟾 ?뱀뀡 ???
       if (currentCategory || currentItems.length > 0) {
         sections.push({
           category: currentCategory || undefined,
           items: currentItems
         })
       }
-      // 새 섹션 시작
+      // ???뱀뀡 ?쒖옉
       currentCategory = categoryMatch[1].trim()
       currentItems = []
     } else {
-      // 항목 추가 (하이픈 제거)
-      const cleanItem = line.replace(/^[-–—•]\s*/, '').trim()
+      // ??ぉ 異붽? (?섏씠???쒓굅)
+      const cleanItem = line.replace(/^[-?볛붴?\s*/, '').trim()
       if (cleanItem) {
         currentItems.push(cleanItem)
       }
     }
   }
   
-  // 마지막 섹션 저장
+  // 留덉?留??뱀뀡 ???
   if (currentCategory || currentItems.length > 0) {
     sections.push({
       category: currentCategory || undefined,
@@ -337,18 +337,18 @@ const formatWorkDetailAsNumberedCards = (text: string): string => {
     })
   }
   
-  // 대분류가 없는 경우: 단순 리스트로 렌더링
+  // ?遺꾨쪟媛 ?녿뒗 寃쎌슦: ?⑥닚 由ъ뒪?몃줈 ?뚮뜑留?
   if (sections.length === 1 && !sections[0].category) {
     const items = sections[0].items
     return `<ul class="space-y-2" data-job-duty-list>${renderDutyListItems(items)}</ul>`
   }
   
-  // 계층 구조 렌더링: 대분류 제목 + 항목 리스트
+  // 怨꾩링 援ъ“ ?뚮뜑留? ?遺꾨쪟 ?쒕ぉ + ??ぉ 由ъ뒪??
   const sectionsHtml = sections
     .map((section, index) => {
       if (!section.items.length) return ''
       
-      // 대분류 제목
+      // ?遺꾨쪟 ?쒕ぉ
       const categoryMargin = index === 0 ? 'mt-0' : 'mt-6'
       const categoryHtml = section.category
         ? `<h4 class="text-base font-semibold text-white mb-3 ${categoryMargin}">${escapeHtml(section.category)}</h4>`
@@ -401,21 +401,21 @@ const isLawyerProfile = (profile?: UnifiedJobDetail | null): boolean => {
 
 const describeSourceStatus = (source: DataSource, status?: SourceStatus): string => {
   if (!status) {
-    return '상태 확인 중'
+    return '?곹깭 ?뺤씤 以?
   }
   if (status.error) {
-    return '오류 발생'
+    return '?ㅻ쪟 諛쒖깮'
   }
   if (typeof status.count === 'number' && status.count > 0) {
-    return '실시간 데이터'
+    return '?ㅼ떆媛??곗씠??
   }
   if (status.skippedReason) {
     return SKIPPED_REASON_MESSAGES[status.skippedReason] ?? SKIPPED_REASON_MESSAGES.default
   }
   if (status.attempted) {
-    return '응답 없음'
+    return '?묐떟 ?놁쓬'
   }
-  return '대기 중'
+  return '?湲?以?
 }
 
 const resolveJobTelemetryVariant = (profile: UnifiedJobDetail): string | undefined => {
@@ -455,7 +455,7 @@ const hasMatrixValue = (value: unknown): boolean => {
 
 const renderMatrixValue = (value: unknown, labelMap?: Record<string, string>): string => {
   if (!hasMatrixValue(value)) {
-    return `<span class="text-[11px] text-wiki-muted">제공 안됨</span>`
+    return `<span class="text-[11px] text-wiki-muted">?쒓났 ?덈맖</span>`
   }
   if (typeof value === 'string') {
     return value
@@ -479,7 +479,7 @@ const renderMatrixValue = (value: unknown, labelMap?: Record<string, string>): s
       })
       .filter((item) => item.length > 0)
     if (!items.length) {
-      return `<span class="text-[11px] text-wiki-muted">제공 안됨</span>`
+      return `<span class="text-[11px] text-wiki-muted">?쒓났 ?덈맖</span>`
     }
     return `<ul class="space-y-1">${items
       .map((item) => `<li class="text-[11px] text-wiki-text">${escapeHtml(item)}</li>`)
@@ -500,7 +500,7 @@ const renderMatrixValue = (value: unknown, labelMap?: Record<string, string>): s
       })
       .filter((entry): entry is { label: string; value: string } => Boolean(entry))
     if (!entries.length) {
-      return `<span class="text-[11px] text-wiki-muted">제공 안됨</span>`
+      return `<span class="text-[11px] text-wiki-muted">?쒓났 ?덈맖</span>`
     }
     return `<ul class="space-y-1">${entries
       .map(
@@ -515,8 +515,8 @@ const renderMatrixValue = (value: unknown, labelMap?: Record<string, string>): s
 }
 
 /**
- * 직업 내/직업 간 비교 데이터를 표 형식으로 렌더링하는 헬퍼 함수
- * Goyong24 API의 비교 데이터를 표로 시각화합니다.
+ * 吏곸뾽 ??吏곸뾽 媛?鍮꾧탳 ?곗씠?곕? ???뺤떇?쇰줈 ?뚮뜑留곹븯???ы띁 ?⑥닔
+ * Goyong24 API??鍮꾧탳 ?곗씠?곕? ?쒕줈 ?쒓컖?뷀빀?덈떎.
  */
 const renderComparisonTable = (
   withinJob: any,
@@ -532,11 +532,11 @@ const renderComparisonTable = (
   const blocks: string[] = []
   const suffix = uniqueSuffix ? `-${uniqueSuffix}` : ''
 
-  // 직업 내 비교 (Within Job) - 표 형식
+  // 吏곸뾽 ??鍮꾧탳 (Within Job) - ???뺤떇
   if (withinJob && Array.isArray(withinJob) && withinJob.length > 0) {
     const tableId = `within-job-${title.replace(/\s+/g, '-')}${suffix}`
     
-    // 먼저 유효한 데이터만 필터링
+    // 癒쇱? ?좏슚???곗씠?곕쭔 ?꾪꽣留?
     const validItems = withinJob.map((item: any) => {
       const label = item.chrNmCmpr || item.intrstNmCmpr || item.valsNmCmpr || 
                     item.jobActvImprtncNmCmpr || item.jobActvLvlNmCmpr ||
@@ -562,13 +562,13 @@ const renderComparisonTable = (
       return { label, value: numericValue, description }
     }).filter(Boolean)
     
-    // 유효한 항목이 없으면 다음 블록(Between Jobs)으로
+    // ?좏슚????ぉ???놁쑝硫??ㅼ쓬 釉붾줉(Between Jobs)?쇰줈
     if (validItems.length === 0) {
-      // return ''하지 않고 계속 진행
+      // return ''?섏? ?딄퀬 怨꾩냽 吏꾪뻾
     } else {
     
-    // 필터링된 유효한 항목들로 행 생성
-    // 설명을 항목 아래에 표시 (모바일/데스크톱 동일)
+    // ?꾪꽣留곷맂 ?좏슚????ぉ?ㅻ줈 ???앹꽦
+    // ?ㅻ챸????ぉ ?꾨옒???쒖떆 (紐⑤컮???곗뒪?ы넲 ?숈씪)
     const rows = validItems.map((item: any, index: number) => {
       const isHidden = index >= 5
       const hideClass = isHidden ? 'hidden' : ''
@@ -593,7 +593,7 @@ const renderComparisonTable = (
           id="${tableId}-toggle"
           class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-wiki-primary hover:text-wiki-secondary border border-wiki-primary/50 hover:border-wiki-secondary/50 rounded-lg transition-all cursor-pointer"
         >
-          <span id="${tableId}-text">전체보기 (${validItems.length - 5}개 항목)</span>
+          <span id="${tableId}-text">?꾩껜蹂닿린 (${validItems.length - 5}媛???ぉ)</span>
           <i id="${tableId}-icon" class="fas fa-chevron-down transition-transform"></i>
         </div>
       </div>
@@ -603,18 +603,18 @@ const renderComparisonTable = (
         <div class="bg-white/5 rounded-2xl px-3 py-4 md:px-6 md:py-6 border border-wiki-border/50">
           <div class="mb-4">
             <h3 class="text-base font-bold text-wiki-secondary mb-2">
-              <i class="fas fa-clover mr-2"></i>직업 내 비교
+              <i class="fas fa-clover mr-2"></i>吏곸뾽 ??鍮꾧탳
             </h3>
             <p class="text-xs text-wiki-muted leading-relaxed">
-              500여 개 직업 종사자들의 자신의 직업에 대해 평가한 ${title} 관련 항목별 중요도를 직업 내에 비교하여 본 직업에서 중요성(평가점수)이 높게 나타난 항목을 순서대로 제시함
+              500??媛?吏곸뾽 醫낆궗?먮뱾???먯떊??吏곸뾽??????됯???${title} 愿????ぉ蹂?以묒슂?꾨? 吏곸뾽 ?댁뿉 鍮꾧탳?섏뿬 蹂?吏곸뾽?먯꽌 以묒슂???됯??먯닔)???믨쾶 ?섑?????ぉ???쒖꽌?濡??쒖떆??
             </p>
           </div>
           <div class="overflow-x-auto -mx-1 md:mx-0">
             <table class="w-full" id="${tableId}">
               <thead class="bg-wiki-card/50 border-b-2 border-wiki-primary/30">
                 <tr>
-                  <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text text-sm md:text-base" style="width: 70px; min-width: 70px;">평균</th>
-                  <th class="px-2 py-2 md:px-4 md:py-3 text-left font-semibold text-wiki-text text-sm md:text-base">항목</th>
+                  <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text text-sm md:text-base" style="width: 70px; min-width: 70px;">?됯퇏</th>
+                  <th class="px-2 py-2 md:px-4 md:py-3 text-left font-semibold text-wiki-text text-sm md:text-base">??ぉ</th>
                 </tr>
               </thead>
               <tbody>
@@ -628,11 +628,11 @@ const renderComparisonTable = (
     }
   }
 
-  // 직업 간 비교 (Between Jobs) - 표 형식
+  // 吏곸뾽 媛?鍮꾧탳 (Between Jobs) - ???뺤떇
   if (betweenJobs && Array.isArray(betweenJobs) && betweenJobs.length > 0) {
     const tableId = `between-jobs-${title.replace(/\s+/g, '-')}${suffix}`
     
-    // 먼저 유효한 데이터만 필터링
+    // 癒쇱? ?좏슚???곗씠?곕쭔 ?꾪꽣留?
     const validItems = betweenJobs.map((item: any) => {
       const label = item.chrNm || item.intrstNm || item.valsNm ||
                     item.jobActvImprtncNm || item.jobActvLvlNm ||
@@ -658,13 +658,13 @@ const renderComparisonTable = (
       return { label, value: numericValue, description }
     }).filter(Boolean)
     
-    // 유효한 항목이 없으면 건너뜀
+    // ?좏슚????ぉ???놁쑝硫?嫄대꼫?
     if (validItems.length === 0) {
-      // 계속 진행
+      // 怨꾩냽 吏꾪뻾
     } else {
     
-    // 필터링된 유효한 항목들로 행 생성
-    // 설명을 항목 아래에 표시 (모바일/데스크톱 동일)
+    // ?꾪꽣留곷맂 ?좏슚????ぉ?ㅻ줈 ???앹꽦
+    // ?ㅻ챸????ぉ ?꾨옒???쒖떆 (紐⑤컮???곗뒪?ы넲 ?숈씪)
     const rows = validItems.map((item: any, index: number) => {
       const isHidden = index >= 5
       const hideClass = isHidden ? 'hidden' : ''
@@ -689,7 +689,7 @@ const renderComparisonTable = (
           id="${tableId}-toggle"
           class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-wiki-primary hover:text-wiki-secondary border border-wiki-primary/50 hover:border-wiki-secondary/50 rounded-lg transition-all cursor-pointer"
         >
-          <span id="${tableId}-text">전체보기 (${validItems.length - 5}개 항목)</span>
+          <span id="${tableId}-text">?꾩껜蹂닿린 (${validItems.length - 5}媛???ぉ)</span>
           <i id="${tableId}-icon" class="fas fa-chevron-down transition-transform"></i>
         </div>
       </div>
@@ -699,18 +699,18 @@ const renderComparisonTable = (
         <div class="bg-white/5 rounded-2xl px-3 py-4 md:px-6 md:py-6 border border-wiki-border/50 ${withinJob ? 'mt-8' : ''}">
           <div class="mb-4">
             <h3 class="text-base font-bold text-wiki-secondary mb-2">
-              <i class="fas fa-exchange-alt mr-2"></i>직업 간 비교
+              <i class="fas fa-exchange-alt mr-2"></i>吏곸뾽 媛?鍮꾧탳
             </h3>
             <p class="text-xs text-wiki-muted leading-relaxed">
-              500여 개 직업 종사자들의 자신의 직업에 대해 평가한 ${title} 관련 항목별 중요도를 직업 간에 비교하여 본 직업에서 중요성(백분위)이 높게 나타난 항목을 순서대로 제시함
+              500??媛?吏곸뾽 醫낆궗?먮뱾???먯떊??吏곸뾽??????됯???${title} 愿????ぉ蹂?以묒슂?꾨? 吏곸뾽 媛꾩뿉 鍮꾧탳?섏뿬 蹂?吏곸뾽?먯꽌 以묒슂??諛깅텇?????믨쾶 ?섑?????ぉ???쒖꽌?濡??쒖떆??
             </p>
           </div>
           <div class="overflow-x-auto -mx-1 md:mx-0">
             <table class="w-full" id="${tableId}">
               <thead class="bg-wiki-card/50 border-b-2 border-wiki-primary/30">
                 <tr>
-                  <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text text-sm md:text-base" style="width: 70px; min-width: 70px;">백분위</th>
-                  <th class="px-2 py-2 md:px-4 md:py-3 text-left font-semibold text-wiki-text text-sm md:text-base">항목</th>
+                  <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text text-sm md:text-base" style="width: 70px; min-width: 70px;">諛깅텇??/th>
+                  <th class="px-2 py-2 md:px-4 md:py-3 text-left font-semibold text-wiki-text text-sm md:text-base">??ぉ</th>
                 </tr>
               </thead>
               <tbody>
@@ -728,8 +728,8 @@ const renderComparisonTable = (
 }
 
 /**
- * 직업 내/직업 간 비교 데이터를 렌더링하는 헬퍼 함수 (레거시 - 진행바 형식)
- * Goyong24 API의 비교 데이터를 시각화합니다.
+ * 吏곸뾽 ??吏곸뾽 媛?鍮꾧탳 ?곗씠?곕? ?뚮뜑留곹븯???ы띁 ?⑥닔 (?덇굅??- 吏꾪뻾諛??뺤떇)
+ * Goyong24 API??鍮꾧탳 ?곗씠?곕? ?쒓컖?뷀빀?덈떎.
  */
 const renderComparisonData = (
   withinJob: any,
@@ -743,19 +743,19 @@ const renderComparisonData = (
 
   const blocks: string[] = []
 
-  // 직업 내 비교 (Within Job)
+  // 吏곸뾽 ??鍮꾧탳 (Within Job)
   if (withinJob && Array.isArray(withinJob) && withinJob.length > 0) {
     const withinJobHtml = withinJob
-      .slice(0, 10) // 최대 10개 항목
+      .slice(0, 10) // 理쒕? 10媛???ぉ
       .map((item: any) => {
-        // Goyong24 API 필드명 매핑
-        // 성격: chrNmCmpr, chrStatusCmpr
-        // 흥미: intrstNmCmpr, intrstStatusCmpr
-        // 가치관: valsNmCmpr, valsStatusCmpr
-        // 활동: jobActvImprtncNmCmpr, jobActvImprtncStatusCmpr, jobActvLvlNmCmpr, jobActvLvlStatusCmpr
-        // 지식: KnwldgNmCmpr, KnwldgStatusCmpr, KnwldgLvlNmCmpr, KnwldgLvlStatusCmpr (대문자 K!)
-        // 능력: jobAblNmCmpr, jobAblStatusCmpr, jobAblLvlNmCmpr, jobAblLvlStatusCmpr
-        // 업무환경: jobEnvNmCmpr, jobEnvStatusCmpr
+        // Goyong24 API ?꾨뱶紐?留ㅽ븨
+        // ?깃꺽: chrNmCmpr, chrStatusCmpr
+        // ?λ?: intrstNmCmpr, intrstStatusCmpr
+        // 媛移섍?: valsNmCmpr, valsStatusCmpr
+        // ?쒕룞: jobActvImprtncNmCmpr, jobActvImprtncStatusCmpr, jobActvLvlNmCmpr, jobActvLvlStatusCmpr
+        // 吏?? KnwldgNmCmpr, KnwldgStatusCmpr, KnwldgLvlNmCmpr, KnwldgLvlStatusCmpr (?臾몄옄 K!)
+        // ?λ젰: jobAblNmCmpr, jobAblStatusCmpr, jobAblLvlNmCmpr, jobAblLvlStatusCmpr
+        // ?낅Т?섍꼍: jobEnvNmCmpr, jobEnvStatusCmpr
         const label = item.chrNmCmpr || item.intrstNmCmpr || item.valsNmCmpr || 
                       item.jobActvImprtncNmCmpr || item.jobActvLvlNmCmpr ||
                       item.KnwldgNmCmpr || item.KnwldgLvlNmCmpr ||
@@ -771,13 +771,13 @@ const renderComparisonData = (
         
         if (!safeTrim(label)) return ''
         
-        // value가 있으면 진행 바 표시, 없으면 텍스트만
+        // value媛 ?덉쑝硫?吏꾪뻾 諛??쒖떆, ?놁쑝硫??띿뒪?몃쭔
         if (value) {
           const numericValue = typeof value === 'number' ? value : Number.parseFloat(String(value)) || 0
-          // 값이 1-5 범위면 100으로 스케일링 (직업 내 비교)
+          // 媛믪씠 1-5 踰붿쐞硫?100?쇰줈 ?ㅼ??쇰쭅 (吏곸뾽 ??鍮꾧탳)
           const percentage = numericValue <= 5 ? (numericValue / 5) * 100 : Math.min(numericValue, 100)
           
-          // 진행 바 색상 결정 (값에 따라)
+          // 吏꾪뻾 諛??됱긽 寃곗젙 (媛믪뿉 ?곕씪)
           const barColor = percentage >= 80 ? 'bg-green-500' : percentage >= 60 ? 'bg-blue-500' : percentage >= 40 ? 'bg-yellow-500' : 'bg-gray-400'
           
           return `
@@ -801,7 +801,7 @@ const renderComparisonData = (
       blocks.push(`
         <div>
           <h3 class="content-heading">
-            <i class="${icon} mr-2"></i>직업 내 비교
+            <i class="${icon} mr-2"></i>吏곸뾽 ??鍮꾧탳
           </h3>
           <div>${withinJobHtml}</div>
         </div>
@@ -809,19 +809,19 @@ const renderComparisonData = (
     }
   }
 
-  // 직업 간 비교 (Between Jobs)
+  // 吏곸뾽 媛?鍮꾧탳 (Between Jobs)
   if (betweenJobs && Array.isArray(betweenJobs) && betweenJobs.length > 0) {
     const betweenJobsHtml = betweenJobs
-      .slice(0, 10) // 최대 10개 항목
+      .slice(0, 10) // 理쒕? 10媛???ぉ
       .map((item: any) => {
-        // Goyong24 API 필드명 매핑 (직업 간 비교는 Cmpr 없음)
-        // 성격: chrNm, chrStatus
-        // 흥미: intrstNm, intrstStatus
-        // 가치관: valsNm, valsStatus
-        // 활동: jobActvImprtncNm, jobActvImprtncStatus, jobActvLvlNm, jobActvLvlStatus
-        // 지식: knwldgNm, knwldgStatus, knwldgLvlNm, knwldgLvlStatus (소문자!)
-        // 능력: jobAblNm, jobAblStatus, jobAblLvlNm, jobAblLvlStatus
-        // 업무환경: jobEnvNm, jobEnvStatus
+        // Goyong24 API ?꾨뱶紐?留ㅽ븨 (吏곸뾽 媛?鍮꾧탳??Cmpr ?놁쓬)
+        // ?깃꺽: chrNm, chrStatus
+        // ?λ?: intrstNm, intrstStatus
+        // 媛移섍?: valsNm, valsStatus
+        // ?쒕룞: jobActvImprtncNm, jobActvImprtncStatus, jobActvLvlNm, jobActvLvlStatus
+        // 吏?? knwldgNm, knwldgStatus, knwldgLvlNm, knwldgLvlStatus (?뚮Ц??)
+        // ?λ젰: jobAblNm, jobAblStatus, jobAblLvlNm, jobAblLvlStatus
+        // ?낅Т?섍꼍: jobEnvNm, jobEnvStatus
         const label = item.chrNm || item.intrstNm || item.valsNm ||
                       item.jobActvImprtncNm || item.jobActvLvlNm ||
                       item.knwldgNm || item.knwldgLvlNm ||
@@ -839,10 +839,10 @@ const renderComparisonData = (
         
         if (value) {
           const numericValue = typeof value === 'number' ? value : Number.parseFloat(String(value)) || 0
-          // 직업 간 비교는 0-100 범위 (백분위 점수)
+          // 吏곸뾽 媛?鍮꾧탳??0-100 踰붿쐞 (諛깅텇???먯닔)
           const percentage = Math.max(0, Math.min(numericValue, 100))
           
-          // 진행 바 색상 결정 (백분위 기준)
+          // 吏꾪뻾 諛??됱긽 寃곗젙 (諛깅텇??湲곗?)
           const barColor = percentage >= 75 ? 'bg-green-500' : percentage >= 50 ? 'bg-blue-500' : percentage >= 25 ? 'bg-yellow-500' : 'bg-orange-500'
           
           return `
@@ -866,7 +866,7 @@ const renderComparisonData = (
       blocks.push(`
         <div class="mt-6">
           <h3 class="content-heading">
-            <i class="${icon} mr-2"></i>직업 간 비교
+            <i class="${icon} mr-2"></i>吏곸뾽 媛?鍮꾧탳
           </h3>
           <div>${betweenJobsHtml}</div>
         </div>
@@ -877,24 +877,24 @@ const renderComparisonData = (
   return blocks.join('')
 }
 
-// 진로탐색활동 렌더링
+// 吏꾨줈?먯깋?쒕룞 ?뚮뜑留?
 const renderResearchList = (researchList: any[]): string => {
   if (!researchList || !Array.isArray(researchList) || researchList.length === 0) {
     return ''
   }
 
-  // 각 활동 유형에 맞는 아이콘 매핑
+  // 媛??쒕룞 ?좏삎??留욌뒗 ?꾩씠肄?留ㅽ븨
   const getActivityIcon = (text: string): string => {
-    if (text.includes('법 체험') || text.includes('진로체험') || text.includes('프로그램')) {
-      return 'fa-user-graduate'  // 체험 프로그램
+    if (text.includes('踰?泥댄뿕') || text.includes('吏꾨줈泥댄뿕') || text.includes('?꾨줈洹몃옩')) {
+      return 'fa-user-graduate'  // 泥댄뿕 ?꾨줈洹몃옩
     }
-    if (text.includes('원격영상') || text.includes('멘토링') || text.includes('사이트')) {
-      return 'fa-video'  // 원격 멘토링
+    if (text.includes('?먭꺽?곸긽') || text.includes('硫섑넗留?) || text.includes('?ъ씠??)) {
+      return 'fa-video'  // ?먭꺽 硫섑넗留?
     }
-    if (text.includes('모의법정') || text.includes('동아리')) {
-      return 'fa-gavel'  // 모의법정 (법정 망치)
+    if (text.includes('紐⑥쓽踰뺤젙') || text.includes('?숈븘由?)) {
+      return 'fa-gavel'  // 紐⑥쓽踰뺤젙 (踰뺤젙 留앹튂)
     }
-    return 'fa-compass'  // 기본 아이콘 (진로 탐색)
+    return 'fa-compass'  // 湲곕낯 ?꾩씠肄?(吏꾨줈 ?먯깋)
   }
 
   const items = researchList
@@ -944,7 +944,7 @@ const renderResearchBullets = (researchList: any[]): string => {
   const list = normalizedItems
     .map(
       (text) =>
-        `<li class="flex items-start gap-2 text-base leading-relaxed text-wiki-text"><span class="text-wiki-secondary">•</span><span>${escapeHtml(
+        `<li class="flex items-start gap-2 text-base leading-relaxed text-wiki-text"><span class="text-wiki-secondary">??/span><span>${escapeHtml(
           text
         )}</span></li>`
     )
@@ -952,13 +952,13 @@ const renderResearchBullets = (researchList: any[]): string => {
   return `<ul class="space-y-2">${list}</ul>`
 }
 
-// 태그 렌더링
+// ?쒓렇 ?뚮뜑留?
 const renderTags = (tagList: any[]): string => {
   if (!tagList || !Array.isArray(tagList) || tagList.length === 0) {
     return ''
   }
 
-  // tagList는 단순 string[] 배열
+  // tagList???⑥닚 string[] 諛곗뿴
   const tags = tagList
     .filter((tag) => typeof tag === 'string' && safeTrim(tag))
     .map((tag) => {
@@ -983,8 +983,8 @@ const renderTags = (tagList: any[]): string => {
           <i class="fas fa-tags text-lg"></i>
         </span>
         <div>
-          <h2 class="text-lg sm:text-xl font-bold text-white leading-tight">관련 태그</h2>
-          <p class="mt-1 content-text text-wiki-muted">직업과 관련된 키워드를 확인하세요</p>
+          <h2 class="text-lg sm:text-xl font-bold text-white leading-tight">愿???쒓렇</h2>
+          <p class="mt-1 content-text text-wiki-muted">吏곸뾽怨?愿?⑤맂 ?ㅼ썙?쒕? ?뺤씤?섏꽭??/p>
         </div>
       </header>
       <div class="flex flex-wrap gap-2">
@@ -994,37 +994,37 @@ const renderTags = (tagList: any[]): string => {
   `
 }
 
-// 한국의 직업지표 바 차트 렌더링
+// ?쒓뎅??吏곸뾽吏??諛?李⑦듃 ?뚮뜑留?
 const renderIndicatorChart = (indicatorData: any[]): string => {
   if (!indicatorData || !Array.isArray(indicatorData) || indicatorData.length === 0) {
     return ''
   }
 
-  // indicatorChart 데이터 구조:
-  // Type 1 (타입 정의): [{ indicator: "name", indicator_data: "value", source: "..." }]
-  // Type 2 (실제 API): [{ category: "융합성,대인관계,...", value: 0, description: "1.7,0.2,..." }]
+  // indicatorChart ?곗씠??援ъ“:
+  // Type 1 (????뺤쓽): [{ indicator: "name", indicator_data: "value", source: "..." }]
+  // Type 2 (?ㅼ젣 API): [{ category: "?듯빀????멸?怨?...", value: 0, description: "1.7,0.2,..." }]
   
   let categories: string[] = []
   let values: number[] = []
   
-  // Type 2 형식 확인 (category와 description 필드)
+  // Type 2 ?뺤떇 ?뺤씤 (category? description ?꾨뱶)
   const firstItem = indicatorData[0]
   if (firstItem && firstItem.category && firstItem.description) {
     categories = firstItem.category.split(',').map((c: string) => safeTrim(c))
     values = firstItem.description.split(',').map((v: string) => Number.parseFloat(safeTrim(v)) || 0)
   }
-  // Type 1 형식 확인 (indicator와 indicator_data 필드)
+  // Type 1 ?뺤떇 ?뺤씤 (indicator? indicator_data ?꾨뱶)
   else if (indicatorData.every(item => item && (item.indicator || item.indicator_data))) {
-    // indicator와 indicator_data가 콤마로 구분된 문자열인 경우 처리
+    // indicator? indicator_data媛 肄ㅻ쭏濡?援щ텇??臾몄옄?댁씤 寃쎌슦 泥섎━
     const firstIndicator = indicatorData[0]
     if (firstIndicator && firstIndicator.indicator && typeof firstIndicator.indicator === 'string' && firstIndicator.indicator.includes(',')) {
-      // 콤마로 구분된 경우
+      // 肄ㅻ쭏濡?援щ텇??寃쎌슦
       categories = firstIndicator.indicator.split(',').map((c: string) => safeTrim(c))
       const dataStr = String(firstIndicator.indicator_data || '')
       values = dataStr.split(',').map((v: string) => Number.parseFloat(safeTrim(v)) || 0)
     } else {
-      // 각 항목이 개별 객체인 경우
-      categories = indicatorData.map(item => item.indicator || '지표').filter(Boolean)
+      // 媛???ぉ??媛쒕퀎 媛앹껜??寃쎌슦
+      categories = indicatorData.map(item => item.indicator || '吏??).filter(Boolean)
       values = indicatorData.map(item => {
         const val = item.indicator_data
         return typeof val === 'number' ? val : Number.parseFloat(String(val)) || 0
@@ -1036,7 +1036,7 @@ const renderIndicatorChart = (indicatorData: any[]): string => {
     return ''
   }
 
-  // 각 지표별 색상 (스크린샷 참고)
+  // 媛?吏?쒕퀎 ?됱긽 (?ㅽ겕由곗꺑 李멸퀬)
   const barColors = [
     'rgba(168, 85, 247, 0.8)',   // purple
     'rgba(59, 130, 246, 0.8)',   // blue
@@ -1048,35 +1048,35 @@ const renderIndicatorChart = (indicatorData: any[]): string => {
     'rgba(16, 185, 129, 0.8)'    // green
   ]
 
-  // 카테고리와 값을 함께 묶어서 정렬 (높은 순으로)
+  // 移댄뀒怨좊━? 媛믪쓣 ?④퍡 臾띠뼱???뺣젹 (?믪? ?쒖쑝濡?
   const combined = categories.map((cat, idx) => ({
     category: cat,
     value: values[idx],
     originalIndex: idx
   }))
   
-  // 값이 높은 순으로 정렬
+  // 媛믪씠 ?믪? ?쒖쑝濡??뺣젹
   combined.sort((a, b) => b.value - a.value)
   
-  // 정렬된 데이터로 분리
+  // ?뺣젹???곗씠?곕줈 遺꾨━
   const sortedCategories = combined.map(item => item.category)
   const sortedValues = combined.map(item => item.value)
   const sortedColors = combined.map(item => barColors[item.originalIndex % barColors.length])
 
-  // 최대값 찾기 (스케일링용)
-  const maxValue = Math.max(...sortedValues, 5) // 최소 5로 설정
+  // 理쒕?媛?李얘린 (?ㅼ??쇰쭅??
+  const maxValue = Math.max(...sortedValues, 5) // 理쒖냼 5濡??ㅼ젙
 
-  // Chart.js용 고유 ID
+  // Chart.js??怨좎쑀 ID
   const chartId = `indicator-chart-${Date.now()}`
 
   return `
     <div class="space-y-6">
-      <!-- 차트 컨테이너 -->
+      <!-- 李⑦듃 而⑦뀒?대꼫 -->
       <div class="w-full bg-white/5 rounded-2xl px-2 py-4 sm:px-6 sm:py-6 border border-wiki-border/50">
         <canvas id="${chartId}" style="max-height: 400px;"></canvas>
       </div>
       
-      <!-- 범례 -->
+      <!-- 踰붾? -->
       <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
         ${sortedCategories.map((label, idx) => `
           <div class="flex items-center gap-2">
@@ -1086,9 +1086,9 @@ const renderIndicatorChart = (indicatorData: any[]): string => {
         `).join('')}
       </div>
       
-      <!-- 출처 표시 -->
+      <!-- 異쒖쿂 ?쒖떆 -->
       <div class="text-center text-xs text-wiki-muted mt-4">
-        [자료 : 한국직업정보연구원, 맞춤형취업지원을 위한 직업지표 연구(2020~22)]
+        [?먮즺 : ?쒓뎅吏곸뾽?뺣낫?곌뎄?? 留욎땄?뺤랬?낆??먯쓣 ?꾪븳 吏곸뾽吏???곌뎄(2020~22)]
       </div>
     </div>
     
@@ -1200,15 +1200,15 @@ const renderLawyerFieldMatrix = (
     extract: (profile?: UnifiedJobDetail | null) => unknown
     labelMap?: Record<string, string>
   }> = [
-    // 기본 정보
-    { key: 'summary', label: '직업 소개 (하는 일)', extract: (entry) => entry?.summary },
-    { key: 'duties', label: '수행 직무 (주요 업무)', extract: (entry) => entry?.duties },
-    { key: 'way', label: '되는 방법 (진입 경로)', extract: (entry) => entry?.way },
+    // 湲곕낯 ?뺣낫
+    { key: 'summary', label: '吏곸뾽 ?뚭컻 (?섎뒗 ??', extract: (entry) => entry?.summary },
+    { key: 'duties', label: '?섑뻾 吏곷Т (二쇱슂 ?낅Т)', extract: (entry) => entry?.duties },
+    { key: 'way', label: '?섎뒗 諛⑸쾿 (吏꾩엯 寃쎈줈)', extract: (entry) => entry?.way },
     
-    // 분류 정보
+    // 遺꾨쪟 ?뺣낫
     {
       key: 'classifications',
-      label: '직업 분류 체계',
+      label: '吏곸뾽 遺꾨쪟 泥닿퀎',
       extract: (entry) => entry?.classifications ? [
         entry.classifications.large,
         entry.classifications.medium,
@@ -1217,186 +1217,186 @@ const renderLawyerFieldMatrix = (
     },
     {
       key: 'kecoCodes',
-      label: '한국표준직업분류 (KECO)',
+      label: '?쒓뎅?쒖?吏곸뾽遺꾨쪟 (KECO)',
       extract: (entry) => entry?.kecoCodes?.map((item) => `${item.code} - ${item.name}`).filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     
-    // 임금 및 전망
-    { key: 'salary', label: '평균 연봉 (임금 수준)', extract: (entry) => entry?.salary },
-    { key: 'satisfaction', label: '직업 만족도', extract: (entry) => entry?.satisfaction },
-    { key: 'prospect', label: '직업 전망', extract: (entry) => entry?.prospect },
-    { key: 'status', label: '고용 형태', extract: (entry) => entry?.status },
+    // ?꾧툑 諛??꾨쭩
+    { key: 'salary', label: '?됯퇏 ?곕큺 (?꾧툑 ?섏?)', extract: (entry) => entry?.salary },
+    { key: 'satisfaction', label: '吏곸뾽 留뚯”??, extract: (entry) => entry?.satisfaction },
+    { key: 'prospect', label: '吏곸뾽 ?꾨쭩', extract: (entry) => entry?.prospect },
+    { key: 'status', label: '怨좎슜 ?뺥깭', extract: (entry) => entry?.status },
     
-    // 역량 및 특성
-    { key: 'abilities', label: '업무수행능력 (능력)', extract: (entry) => entry?.abilities },
-    { key: 'knowledge', label: '지식 중요도 (지식)', extract: (entry) => entry?.knowledge },
-    { key: 'technKnow', label: '필요 기술 및 지식', extract: (entry) => entry?.technKnow },
+    // ??웾 諛??뱀꽦
+    { key: 'abilities', label: '?낅Т?섑뻾?λ젰 (?λ젰)', extract: (entry) => entry?.abilities },
+    { key: 'knowledge', label: '吏??以묒슂??(吏??', extract: (entry) => entry?.knowledge },
+    { key: 'technKnow', label: '?꾩슂 湲곗닠 諛?吏??, extract: (entry) => entry?.technKnow },
     
-    // 성향 및 환경
-    { key: 'personality', label: '성격 중요도', extract: (entry) => entry?.personality },
-    { key: 'interests', label: '흥미 중요도', extract: (entry) => entry?.interests },
-    { key: 'values', label: '가치관 중요도', extract: (entry) => entry?.values },
-    { key: 'environment', label: '업무환경 중요도', extract: (entry) => entry?.environment },
+    // ?깊뼢 諛??섍꼍
+    { key: 'personality', label: '?깃꺽 以묒슂??, extract: (entry) => entry?.personality },
+    { key: 'interests', label: '?λ? 以묒슂??, extract: (entry) => entry?.interests },
+    { key: 'values', label: '媛移섍? 以묒슂??, extract: (entry) => entry?.values },
+    { key: 'environment', label: '?낅Т?섍꼍 以묒슂??, extract: (entry) => entry?.environment },
     
-    // 업무활동
-    { key: 'activitiesImportance', label: '업무활동 중요도', extract: (entry) => entry?.activitiesImportance },
-    { key: 'activitiesLevels', label: '업무활동 수준', extract: (entry) => entry?.activitiesLevels },
+    // ?낅Т?쒕룞
+    { key: 'activitiesImportance', label: '?낅Т?쒕룞 以묒슂??, extract: (entry) => entry?.activitiesImportance },
+    { key: 'activitiesLevels', label: '?낅Т?쒕룞 ?섏?', extract: (entry) => entry?.activitiesLevels },
     
-    // 교육 및 전공
+    // 援먯쑁 諛??꾧났
     {
       key: 'educationDistribution',
-      label: '학력 분포',
+      label: '?숇젰 遺꾪룷',
       extract: (entry) => entry?.educationDistribution,
       labelMap: {
-        middleSchoolOrLess: '중학교 이하',
-        highSchool: '고등학교',
-        college: '전문대',
-        university: '대학',
-        graduate: '석사 이상',
-        doctor: '박사'
+        middleSchoolOrLess: '以묓븰援??댄븯',
+        highSchool: '怨좊벑?숆탳',
+        college: '?꾨Ц?',
+        university: '???,
+        graduate: '?앹궗 ?댁긽',
+        doctor: '諛뺤궗'
       }
     },
     {
       key: 'majorDistribution',
-      label: '전공 계열 분포 (전공학과분포)',
+      label: '?꾧났 怨꾩뿴 遺꾪룷 (?꾧났?숆낵遺꾪룷)',
       extract: (entry) => entry?.majorDistribution,
       labelMap: {
-        humanities: '인문계열',
-        social: '사회계열',
-        education: '교육계열',
-        engineering: '공학계열',
-        natural: '자연계열',
-        medical: '의약계열',
-        artsSports: '예체능'
+        humanities: '?몃Ц怨꾩뿴',
+        social: '?ы쉶怨꾩뿴',
+        education: '援먯쑁怨꾩뿴',
+        engineering: '怨듯븰怨꾩뿴',
+        natural: '?먯뿰怨꾩뿴',
+        medical: '?섏빟怨꾩뿴',
+        artsSports: '?덉껜??
       }
     },
     {
       key: 'relatedMajors',
-      label: '관련 학과 (관련 전공)',
+      label: '愿???숆낵 (愿???꾧났)',
       extract: (entry) => entry?.relatedMajors?.map((item) => item.name).filter((name): name is string => Boolean(name && safeTrim(name)))
     },
-    { key: 'relatedCertificates', label: '관련 자격증 (추천 자격)', extract: (entry) => entry?.relatedCertificates },
+    { key: 'relatedCertificates', label: '愿???먭꺽利?(異붿쿇 ?먭꺽)', extract: (entry) => entry?.relatedCertificates },
     
-    // 관련 정보
+    // 愿???뺣낫
     {
       key: 'relatedJobs',
-      label: '관련 직업 (유사 직업)',
+      label: '愿??吏곸뾽 (?좎궗 吏곸뾽)',
       extract: (entry) => entry?.relatedJobs?.map((item) => item.name).filter((name): name is string => Boolean(name && safeTrim(name)))
     },
     {
       key: 'relatedOrganizations',
-      label: '관련 기관 (관련 단체)',
+      label: '愿??湲곌? (愿???⑥껜)',
       extract: (entry) => entry?.relatedOrganizations?.map((item) => item.name).filter((name): name is string => Boolean(name && safeTrim(name)))
     },
     
-    // CareerNet 확장 필드들 (배열 데이터)
+    // CareerNet ?뺤옣 ?꾨뱶??(諛곗뿴 ?곗씠??
     {
       key: 'workList',
-      label: '세부 업무 목록',
+      label: '?몃? ?낅Т 紐⑸줉',
       extract: (entry) => entry?.workList?.map((item) => `${item.workName}: ${item.workDesc}`).filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'performList.environment',
-      label: '업무 환경 (중요도별)',
+      label: '?낅Т ?섍꼍 (以묒슂?꾨퀎)',
       extract: (entry) => entry?.performList?.environment
         ?.sort((a, b) => (b.importance || 0) - (a.importance || 0))
         .slice(0, 10)
-        .map((item) => `${item.name} (중요도: ${item.importance})`)
+        .map((item) => `${item.name} (以묒슂?? ${item.importance})`)
         .filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'performList.perform',
-      label: '업무 수행 능력 (중요도별)',
+      label: '?낅Т ?섑뻾 ?λ젰 (以묒슂?꾨퀎)',
       extract: (entry) => entry?.performList?.perform
         ?.sort((a, b) => (b.importance || 0) - (a.importance || 0))
         .slice(0, 10)
-        .map((item) => `${item.name} (중요도: ${item.importance})`)
+        .map((item) => `${item.name} (以묒슂?? ${item.importance})`)
         .filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'performList.knowledge',
-      label: '필요 지식 (중요도별)',
+      label: '?꾩슂 吏??(以묒슂?꾨퀎)',
       extract: (entry) => entry?.performList?.knowledge
         ?.sort((a, b) => (b.importance || 0) - (a.importance || 0))
         .slice(0, 10)
-        .map((item) => `${item.name} (중요도: ${item.importance})`)
+        .map((item) => `${item.name} (以묒슂?? ${item.importance})`)
         .filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'abilityList',
-      label: '필요 능력 (점수별)',
+      label: '?꾩슂 ?λ젰 (?먯닔蹂?',
       extract: (entry) => entry?.abilityList
         ?.sort((a, b) => (b.score || 0) - (a.score || 0))
         .slice(0, 10)
-        .map((item) => `${item.name} (${item.score}점)`)
+        .map((item) => `${item.name} (${item.score}??`)
         .filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'aptitudeList',
-      label: '필요 적성 (점수별)',
+      label: '?꾩슂 ?곸꽦 (?먯닔蹂?',
       extract: (entry) => entry?.aptitudeList
         ?.sort((a, b) => (b.score || 0) - (a.score || 0))
         .slice(0, 10)
-        .map((item) => `${item.name} (${item.score}점)`)
+        .map((item) => `${item.name} (${item.score}??`)
         .filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'interestList',
-      label: '흥미 유형 (점수별)',
+      label: '?λ? ?좏삎 (?먯닔蹂?',
       extract: (entry) => entry?.interestList
         ?.sort((a, b) => (b.score || 0) - (a.score || 0))
         .slice(0, 10)
-        .map((item) => `${item.name} (${item.score}점)`)
+        .map((item) => `${item.name} (${item.score}??`)
         .filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'jobReadyList.recruit',
-      label: '채용 정보',
+      label: '梨꾩슜 ?뺣낫',
       extract: (entry) => entry?.jobReadyList?.recruit
     },
     {
       key: 'jobReadyList.certificate',
-      label: '추천 자격증 목록',
+      label: '異붿쿇 ?먭꺽利?紐⑸줉',
       extract: (entry) => entry?.jobReadyList?.certificate
     },
     {
       key: 'jobReadyList.training',
-      label: '추천 교육 과정',
+      label: '異붿쿇 援먯쑁 怨쇱젙',
       extract: (entry) => entry?.jobReadyList?.training
     },
     {
       key: 'jobReadyList.curriculum',
-      label: '관련 교육 과정',
+      label: '愿??援먯쑁 怨쇱젙',
       extract: (entry) => entry?.jobReadyList?.curriculum
     },
     {
       key: 'forecastList',
-      label: '미래 전망 상세',
+      label: '誘몃옒 ?꾨쭩 ?곸꽭',
       extract: (entry) => entry?.forecastList?.map((item) => `[${item.period}] ${item.outlook}: ${item.description}`).filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'indicatorChart',
-      label: '직업 지표',
+      label: '吏곸뾽 吏??,
       extract: (entry) => entry?.indicatorChart?.map((item) => `${item.category}: ${item.value} (${item.description})`).filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'relVideoList',
-      label: '관련 영상 자료',
+      label: '愿???곸긽 ?먮즺',
       extract: (entry) => entry?.relVideoList?.map((item) => `${item.title} [${item.duration}]`).filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'relSolList',
-      label: '관련 자료',
+      label: '愿???먮즺',
       extract: (entry) => entry?.relSolList?.map((item) => item.title).filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'relJinsolList',
-      label: '진로 상담 자료',
+      label: '吏꾨줈 ?곷떞 ?먮즺',
       extract: (entry) => entry?.relJinsolList?.map((item) => item.title).filter((str): str is string => Boolean(str && safeTrim(str)))
     },
     {
       key: 'researchList',
-      label: '연구 자료',
+      label: '?곌뎄 ?먮즺',
       extract: (entry) => entry?.researchList?.map((item) => `${item.title} (${item.author}, ${item.date})`).filter((str): str is string => Boolean(str && safeTrim(str)))
     }
   ]
@@ -1474,13 +1474,13 @@ const renderLawyerFieldMatrix = (
   return `
     <div class="space-y-3" data-job-source-matrix data-cw-telemetry-scope="job-source-matrix"${variantAttr}>
       <p class="text-xs text-wiki-muted leading-relaxed">
-        CareerNet과 고용24가 제공한 법조 직업 데이터를 필드별로 비교한 매트릭스입니다.
+        CareerNet怨?怨좎슜24媛 ?쒓났??踰뺤“ 吏곸뾽 ?곗씠?곕? ?꾨뱶蹂꾨줈 鍮꾧탳??留ㅽ듃由?뒪?낅땲??
       </p>
       <div class="overflow-x-auto rounded-xl border border-wiki-border/60" data-cw-telemetry-component="job-source-matrix">
         <table class="min-w-full divide-y divide-wiki-border/60 text-left">
           <thead class="bg-wiki-bg/70 text-[11px] uppercase tracking-wide text-wiki-muted">
             <tr>
-              <th scope="col" class="px-4 py-3 font-semibold text-wiki-text">필드</th>
+              <th scope="col" class="px-4 py-3 font-semibold text-wiki-text">?꾨뱶</th>
               ${headerMarkup}
             </tr>
           </thead>
@@ -1493,7 +1493,7 @@ const renderLawyerFieldMatrix = (
   `
 }
 
-// ETL 데이터 구조에서 이름 추출 (majorNm, depart_name, name 등 다양한 필드 지원)
+// ETL ?곗씠??援ъ“?먯꽌 ?대쫫 異붿텧 (majorNm, depart_name, name ???ㅼ뼇???꾨뱶 吏??
 const extractEntityName = (entity: any): string => {
   if (!entity) return ''
   if (typeof entity === 'string') return entity
@@ -1511,7 +1511,7 @@ const buildEntityUrl = (entity: JobRelatedEntity | any, type: 'job' | 'major'):
 const renderEntityList = (
   entities: JobRelatedEntity[] | any[] | null | undefined,
   type: 'job' | 'major' = 'job',
-  emptyText = '정보 없음'
+  emptyText = '?뺣낫 ?놁쓬'
 ): string => {
   if (!entities || entities.length === 0) {
     return `<p class="content-text text-wiki-muted">${escapeHtml(emptyText)}</p>`
@@ -1572,7 +1572,7 @@ const renderDistributionList = (distribution?: Record<string, string | undefined
   `
 }
 
-const formatSalaryValue = (value: number): string => `${value.toLocaleString('ko-KR')}만원`
+const formatSalaryValue = (value: number): string => `${value.toLocaleString('ko-KR')}留뚯썝`
 
 const renderSalaryCard = (salary?: string | null, options?: BuildCardOptions, footnoteMap?: Record<string, Record<string, number>>, sourceTextMap?: Record<number, string>, wageNumber?: string | null): string => {
   if (!salary || !safeTrim(salary)) {
@@ -1581,12 +1581,12 @@ const renderSalaryCard = (salary?: string | null, options?: BuildCardOptions, fo
 
   const raw = safeTrim(salary)
 
-  // 고용24 공식 형식 파싱: "하위(25%) 8450만원, 평균(50%) 10000만원, 상위(25%) 14200만원"
-  const goyong24Pattern = /하위\(25%\)\s*([\d,]+)만원.*?평균\(50%\)\s*([\d,]+)만원.*?상위\(25%\)\s*([\d,]+)만원/
+  // 怨좎슜24 怨듭떇 ?뺤떇 ?뚯떛: "?섏쐞(25%) 8450留뚯썝, ?됯퇏(50%) 10000留뚯썝, ?곸쐞(25%) 14200留뚯썝"
+  const goyong24Pattern = /?섏쐞\(25%\)\s*([\d,]+)留뚯썝.*??됯퇏\(50%\)\s*([\d,]+)留뚯썝.*??곸쐞\(25%\)\s*([\d,]+)留뚯썝/
   const goyong24Match = raw.match(goyong24Pattern)
 
-  // 커리어넷 공식 형식: "조사년도:YYYY년, 임금 하위(25%) N만원, 평균(50%) N만원, 상위(25%) N만원"
-  const careernetPattern = /임금\s*하위\(25%\)\s*([\d,]+)만원.*?평균\(50%\)\s*([\d,]+)만원.*?상위\(25%\)\s*([\d,]+)만원/
+  // 而ㅻ━?대꽬 怨듭떇 ?뺤떇: "議곗궗?꾨룄:YYYY?? ?꾧툑 ?섏쐞(25%) N留뚯썝, ?됯퇏(50%) N留뚯썝, ?곸쐞(25%) N留뚯썝"
+  const careernetPattern = /?꾧툑\s*?섏쐞\(25%\)\s*([\d,]+)留뚯썝.*??됯퇏\(50%\)\s*([\d,]+)留뚯썝.*??곸쐞\(25%\)\s*([\d,]+)留뚯썝/
   const careernetMatch = raw.match(careernetPattern)
 
   let dataset: { label: string; value: number }[] = []
@@ -1598,65 +1598,65 @@ const renderSalaryCard = (salary?: string | null, options?: BuildCardOptions, fo
       const median = Number.parseInt(m[2].replace(/,/g, ''), 10)
       const upper = Number.parseInt(m[3].replace(/,/g, ''), 10)
       dataset = [
-        { label: '하위 25%', value: lower },
-        { label: '평균 50%', value: median },
-        { label: '상위 25%', value: upper }
+        { label: '?섏쐞 25%', value: lower },
+        { label: '?됯퇏 50%', value: median },
+        { label: '?곸쐞 25%', value: upper }
       ]
     }
   } else if (raw.includes('~') && raw.match(/(\d[\d,]*)/g)?.length === 2) {
-    // 범위 형식: "3,000~5,000만원"
+    // 踰붿쐞 ?뺤떇: "3,000~5,000留뚯썝"
     const matches = raw.match(/(\d[\d,]*)/g)!
     const values = matches.map(v => Number.parseInt(v.replace(/,/g, ''), 10)).filter(v => Number.isFinite(v) && v > 0)
     if (values.length === 2) {
       dataset = [
-        { label: '하한', value: values[0] },
-        { label: '상한', value: values[1] }
+        { label: '?섑븳', value: values[0] },
+        { label: '?곹븳', value: values[1] }
       ]
     }
   }
 
-  // 단순 숫자만 있는 경우 (예: "5076") → 평균 단일 바로 표시
+  // ?⑥닚 ?レ옄留??덈뒗 寃쎌슦 (?? "5076") ???됯퇏 ?⑥씪 諛붾줈 ?쒖떆
   if (dataset.length === 0) {
-    const numOnly = raw.replace(/[,\s만원]/g, '')
+    const numOnly = raw.replace(/[,\s留뚯썝]/g, '')
     const parsed = Number.parseInt(numOnly, 10)
     if (/^\d+$/.test(numOnly) && Number.isFinite(parsed) && parsed > 0) {
       const formatted = parsed.toLocaleString('ko-KR')
-      dataset = [{ label: '평균', value: parsed }]
+      dataset = [{ label: '?됯퇏', value: parsed }]
     } else {
-      // 공식 형식이 아닌 사용자 기여 텍스트 → 각주 지원하는 텍스트로 렌더링
+      // 怨듭떇 ?뺤떇???꾨땶 ?ъ슜??湲곗뿬 ?띿뒪????媛곸＜ 吏?먰븯???띿뒪?몃줈 ?뚮뜑留?
       const textHtml = formatRichText(raw, 'overviewSalary.sal', footnoteMap, sourceTextMap)
-      // wage 숫자가 별도로 있으면 bar 차트 + 텍스트 함께 표시
+      // wage ?レ옄媛 蹂꾨룄濡??덉쑝硫?bar 李⑦듃 + ?띿뒪???④퍡 ?쒖떆
       if (wageNumber) {
-        const wageNum = Number.parseInt(safeTrim(wageNumber).replace(/[,\s만원]/g, ''), 10)
+        const wageNum = Number.parseInt(safeTrim(wageNumber).replace(/[,\s留뚯썝]/g, ''), 10)
         if (Number.isFinite(wageNum) && wageNum > 0) {
           const wageBar = `
             <div class="space-y-1" data-cw-telemetry-component="job-salary-bar">
               <div class="flex justify-between text-base text-white font-bold">
-                <span>평균</span>
-                <span>${wageNum.toLocaleString('ko-KR')}만원</span>
+                <span>?됯퇏</span>
+                <span>${wageNum.toLocaleString('ko-KR')}留뚯썝</span>
               </div>
               <div class="h-3 rounded-full bg-wiki-border/40 overflow-hidden">
                 <div class="h-full bg-gradient-to-r from-wiki-primary to-wiki-secondary" style="width:100%"></div>
               </div>
             </div>`
-          return buildCard('임금 정보', 'fa-coins', `<div class="space-y-4" data-cw-telemetry-component="job-salary-card"><div class="space-y-2">${wageBar}</div>${textHtml}</div>`, { ...(options ?? {}) })
+          return buildCard('?꾧툑 ?뺣낫', 'fa-coins', `<div class="space-y-4" data-cw-telemetry-component="job-salary-card"><div class="space-y-2">${wageBar}</div>${textHtml}</div>`, { ...(options ?? {}) })
         }
       }
-      return buildCard('임금 정보', 'fa-coins', `<div data-cw-telemetry-component="job-salary-card">${textHtml}</div>`, { ...(options ?? {}) })
+      return buildCard('?꾧툑 ?뺣낫', 'fa-coins', `<div data-cw-telemetry-component="job-salary-card">${textHtml}</div>`, { ...(options ?? {}) })
     }
   }
 
   const maxValue = Math.max(...dataset.map((item) => item.value))
-  // 평균값만 있는 경우 (dataset이 1개)
-  const isSingleAverage = dataset.length === 1 && dataset[0].label === '평균'
+  // ?됯퇏媛믩쭔 ?덈뒗 寃쎌슦 (dataset??1媛?
+  const isSingleAverage = dataset.length === 1 && dataset[0].label === '?됯퇏'
   
   const barMarkup = dataset
     .map(({ label, value }) => {
       const percent = maxValue === 0 ? 0 : Math.round((value / maxValue) * 100)
       const width = Math.min(100, Math.max(12, percent))
-      const isMedian = label === '평균 50%'
-      const isAverage = label === '평균'
-      // 평균 50% 또는 단일 평균값일 때 강조
+      const isMedian = label === '?됯퇏 50%'
+      const isAverage = label === '?됯퇏'
+      // ?됯퇏 50% ?먮뒗 ?⑥씪 ?됯퇏媛믪씪 ??媛뺤“
       const shouldEmphasize = isMedian || (isSingleAverage && isAverage)
       const textSize = shouldEmphasize ? 'text-base' : 'text-[15px]'
       const textColor = shouldEmphasize ? 'text-white font-bold' : 'text-wiki-muted font-semibold'
@@ -1675,18 +1675,18 @@ const renderSalaryCard = (salary?: string | null, options?: BuildCardOptions, fo
     })
     .join('')
 
-  // 조사년도 추출
-  const yearMatch = raw.match(/조사년도\s*[:：]?\s*(\d{4})\s*년?/)
-  const yearInfo = yearMatch ? `※ 조사년도: ${yearMatch[1]}년` : ''
+  // 議곗궗?꾨룄 異붿텧
+  const yearMatch = raw.match(/議곗궗?꾨룄\s*[:竊??\s*(\d{4})\s*??/)
+  const yearInfo = yearMatch ? `??議곗궗?꾨룄: ${yearMatch[1]}?? : ''
 
-  // 공식 설문 포맷(Goyong24/CareerNet)일 때만 설문조사 방법론 면책조항 표시
+  // 怨듭떇 ?ㅻЦ ?щ㎎(Goyong24/CareerNet)???뚮쭔 ?ㅻЦ議곗궗 諛⑸쾿濡?硫댁콉議고빆 ?쒖떆
   const isOfficialSurveyFormat = !!(goyong24Match || careernetMatch)
   const disclaimer = isOfficialSurveyFormat
-    ? '※ 위의 임금정보는 직업당 평균 30명의 재직자를 대상으로 실시한 설문조사 결과로, 재직자의 자기보고에 근거한 통계치입니다. 재직자의 경력, 근무업체의 규모 등에 따라 실제 임금과 차이가 있을 수 있으니, 직업간 비교를 위한 참고 자료로만 활용하여 주시길 바랍니다.'
+    ? '???꾩쓽 ?꾧툑?뺣낫??吏곸뾽???됯퇏 30紐낆쓽 ?ъ쭅?먮? ??곸쑝濡??ㅼ떆???ㅻЦ議곗궗 寃곌낵濡? ?ъ쭅?먯쓽 ?먭린蹂닿퀬??洹쇨굅???듦퀎移섏엯?덈떎. ?ъ쭅?먯쓽 寃쎈젰, 洹쇰Т?낆껜??洹쒕え ?깆뿉 ?곕씪 ?ㅼ젣 ?꾧툑怨?李⑥씠媛 ?덉쓣 ???덉쑝?? 吏곸뾽媛?鍮꾧탳瑜??꾪븳 李멸퀬 ?먮즺濡쒕쭔 ?쒖슜?섏뿬 二쇱떆湲?諛붾엻?덈떎.'
     : ''
 
   return buildCard(
-    '임금 정보',
+    '?꾧툑 ?뺣낫',
     'fa-coins',
     `
       <div class="space-y-4" data-cw-telemetry-component="job-salary-card">
@@ -1702,8 +1702,8 @@ const renderSalaryCard = (salary?: string | null, options?: BuildCardOptions, fo
 }
 
 /**
- * 파이 차트로 분포 데이터를 렌더링하는 함수
- * Chart.js를 사용하여 시각화
+ * ?뚯씠 李⑦듃濡?遺꾪룷 ?곗씠?곕? ?뚮뜑留곹븯???⑥닔
+ * Chart.js瑜??ъ슜?섏뿬 ?쒓컖??
  */
 const renderDistributionPieChart = (
   title: string,
@@ -1743,10 +1743,10 @@ const renderDistributionPieChart = (
     return listMarkup ? buildCard(title, icon, listMarkup, {}) : ''
   }
 
-  // 고유 ID 생성
+  // 怨좎쑀 ID ?앹꽦
   const chartId = `chart-${normalizeAnchorValue(title)}-${Date.now()}`
   
-  // 차트 데이터 준비
+  // 李⑦듃 ?곗씠??以鍮?
   const chartLabels = numericEntries.map(e => e.label)
   const chartData = numericEntries.map(e => e.numeric)
   const chartColors = [
@@ -1821,13 +1821,13 @@ const renderDistributionPieChart = (
   return buildCard(title, icon, chartHtml, { ...(options ?? {}) })
 }
 
-// 학력·전공 분포 통합 렌더링 함수
+// ?숇젰쨌?꾧났 遺꾪룷 ?듯빀 ?뚮뜑留??⑥닔
 const renderCombinedDistributionCharts = (
   educationDistribution?: Record<string, string | undefined> | null,
   majorDistribution?: Record<string, string | undefined> | null,
   options?: BuildCardOptions
 ): string => {
-  // 데이터 파싱 헬퍼 함수
+  // ?곗씠???뚯떛 ?ы띁 ?⑥닔
   const parseDistributionData = (
     distribution: Record<string, string | undefined> | null | undefined,
     labels: Record<string, string>
@@ -1856,28 +1856,28 @@ const renderCombinedDistributionCharts = (
     return numericEntries.length > 0 ? numericEntries : null
   }
 
-  // 학력 데이터 파싱
+  // ?숇젰 ?곗씠???뚯떛
   const educationData = parseDistributionData(educationDistribution, {
-    middleSchoolOrLess: '중학교 이하',
-    highSchool: '고등학교',
-    college: '전문대',
-    university: '대학',
-    graduate: '석사 이상',
-    doctor: '박사'
+    middleSchoolOrLess: '以묓븰援??댄븯',
+    highSchool: '怨좊벑?숆탳',
+    college: '?꾨Ц?',
+    university: '???,
+    graduate: '?앹궗 ?댁긽',
+    doctor: '諛뺤궗'
   })
 
-  // 전공 데이터 파싱
+  // ?꾧났 ?곗씠???뚯떛
   const majorData = parseDistributionData(majorDistribution, {
-    humanities: '인문계열',
-    social: '사회계열',
-    education: '교육계열',
-    engineering: '공학계열',
-    natural: '자연계열',
-    medical: '의약계열',
-    artsSports: '예체능'
+    humanities: '?몃Ц怨꾩뿴',
+    social: '?ы쉶怨꾩뿴',
+    education: '援먯쑁怨꾩뿴',
+    engineering: '怨듯븰怨꾩뿴',
+    natural: '?먯뿰怨꾩뿴',
+    medical: '?섏빟怨꾩뿴',
+    artsSports: '?덉껜??
   })
 
-  // 둘 다 데이터가 없으면 빈 문자열 반환
+  // ?????곗씠?곌? ?놁쑝硫?鍮?臾몄옄??諛섑솚
   if (!educationData && !majorData) {
     return ''
   }
@@ -1893,14 +1893,14 @@ const renderCombinedDistributionCharts = (
     'rgba(239, 68, 68, 0.8)'    // red
   ]
 
-  // 단일 차트 렌더링 함수
+  // ?⑥씪 李⑦듃 ?뚮뜑留??⑥닔
   const renderSingleChart = (
     data: Array<{ label: string; raw: string; numeric: number | null }>,
     title: string,
     chartId: string,
     sortByValue: boolean = false
   ) => {
-    // 전공 분포의 경우 높은 순으로 정렬
+    // ?꾧났 遺꾪룷??寃쎌슦 ?믪? ?쒖쑝濡??뺣젹
     const sortedData = sortByValue 
       ? [...data].sort((a, b) => (b.numeric || 0) - (a.numeric || 0))
       : data
@@ -1916,7 +1916,7 @@ const renderCombinedDistributionCharts = (
         </div>
         <div class="w-full space-y-1.5 sm:space-y-2.5">
           ${sortedData.map((entry, idx) => {
-            // 원본 데이터에서 인덱스 찾기 (색상 매칭용)
+            // ?먮낯 ?곗씠?곗뿉???몃뜳??李얘린 (?됱긽 留ㅼ묶??
             const originalIdx = data.findIndex(d => d.label === entry.label)
             return `
             <div class="flex items-center justify-between py-0.5 sm:py-1" style="font-size:14px;">
@@ -1974,28 +1974,28 @@ const renderCombinedDistributionCharts = (
   const timestamp = Date.now()
   let chartsHtml = ''
 
-  // 두 차트 모두 있을 경우 가로로 배치
+  // ??李⑦듃 紐⑤몢 ?덉쓣 寃쎌슦 媛濡쒕줈 諛곗튂
   if (educationData && majorData) {
     const educationChartId = `chart-education-${timestamp}`
     const majorChartId = `chart-major-${timestamp}`
     
     chartsHtml = `
       <div class="grid grid-cols-2 gap-4 sm:gap-8">
-        ${renderSingleChart(educationData, '학력 분포', educationChartId, false)}
-        ${renderSingleChart(majorData, '전공 분포', majorChartId, true)}
+        ${renderSingleChart(educationData, '?숇젰 遺꾪룷', educationChartId, false)}
+        ${renderSingleChart(majorData, '?꾧났 遺꾪룷', majorChartId, true)}
       </div>
     `
   } else if (educationData) {
-    // 학력만 있을 경우
+    // ?숇젰留??덉쓣 寃쎌슦
     const educationChartId = `chart-education-${timestamp}`
-    chartsHtml = renderSingleChart(educationData, '학력 분포', educationChartId, false)
+    chartsHtml = renderSingleChart(educationData, '?숇젰 遺꾪룷', educationChartId, false)
   } else if (majorData) {
-    // 전공만 있을 경우
+    // ?꾧났留??덉쓣 寃쎌슦
     const majorChartId = `chart-major-${timestamp}`
-    chartsHtml = renderSingleChart(majorData, '전공 분포', majorChartId, true)
+    chartsHtml = renderSingleChart(majorData, '?꾧났 遺꾪룷', majorChartId, true)
   }
 
-  return buildCard('학력·전공 분포', 'fa-graduation-cap', chartsHtml, { ...(options ?? {}) })
+  return buildCard('?숇젰쨌?꾧났 遺꾪룷', 'fa-graduation-cap', chartsHtml, { ...(options ?? {}) })
 }
 
 const renderDistributionBars = (
@@ -2070,7 +2070,7 @@ const normalizeAnchorValue = (value: string): string => {
   }
   return value
     .normalize('NFKC')
-    .replace(/[^0-9A-Za-z가-힣\s-]/g, '')
+    .replace(/[^0-9A-Za-z媛-??s-]/g, '')
     .trim()
     .replace(/\s+/g, '-')
     .replace(/-+/g, '-')
@@ -2104,7 +2104,7 @@ type SectionCardDescriptor = { id: string; label: string; icon: string; markup:
 type TocItem = { id: string; label: string; icon: string }
 
 const renderSectionToc = (sectionKey: 'overview' | 'details' | 'characteristics', heading: string, items: TocItem[]): string => {
-  // 항목이 1개 이하면 목차 숨김 (목차가 의미 없음)
+  // ??ぉ??1媛??댄븯硫?紐⑹감 ?④? (紐⑹감媛 ?섎? ?놁쓬)
   if (items.length <= 1) {
     return ''
   }
@@ -2160,14 +2160,14 @@ const resolveRecommendedHowtos = (jobId: string): Array<{ label: string; href: s
   const normalized = jobId.toLowerCase()
   if (normalized === 'growth-marketing-director') {
     return [
-      { label: '법무법인 SEO 성장 플레이북', href: '/howto/seo-growth-playbook' },
-      { label: 'AI 프로덕트 로드맵 스프린트', href: '/howto/ai-product-roadmap' }
+      { label: '踰뺣Т踰뺤씤 SEO ?깆옣 ?뚮젅?대턿', href: '/howto/seo-growth-playbook' },
+      { label: 'AI ?꾨줈?뺥듃 濡쒕뱶留??ㅽ봽由고듃', href: '/howto/ai-product-roadmap' }
     ]
   }
   if (normalized === 'ai-product-strategist') {
     return [
-      { label: 'AI 프로덕트 로드맵 스프린트', href: '/howto/ai-product-roadmap' },
-      { label: '법무법인 SEO 성장 플레이북', href: '/howto/seo-growth-playbook' }
+      { label: 'AI ?꾨줈?뺥듃 濡쒕뱶留??ㅽ봽由고듃', href: '/howto/ai-product-roadmap' },
+      { label: '踰뺣Т踰뺤씤 SEO ?깆옣 ?뚮젅?대턿', href: '/howto/seo-growth-playbook' }
     ]
   }
   if (
@@ -2177,8 +2177,8 @@ const resolveRecommendedHowtos = (jobId: string): Array<{ label: string; href: s
     normalized.includes('lawyer')
   ) {
     return [
-      { label: '로펌 비즈니스 개발 전략 캔버스', href: '/howto/law-firm-business-development' },
-      { label: 'AI 리걸 리서치 워크플로우 설계', href: '/howto/legal-ai-research' }
+      { label: '濡쒗럩 鍮꾩쫰?덉뒪 媛쒕컻 ?꾨왂 罹붾쾭??, href: '/howto/law-firm-business-development' },
+      { label: 'AI 由ш구 由ъ꽌移??뚰겕?뚮줈???ㅺ퀎', href: '/howto/legal-ai-research' }
     ]
   }
   return []
@@ -2205,22 +2205,22 @@ const renderSidebarSection = (title: string, icon: string, body: string): string
 const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<string, string>, relatedHowtos?: Array<{ slug: string; title: string; summary: string }>): string => {
   const sections: string[] = []
 
-  // 연관 직업 (ETL 구조화 필드 sidebarJobs 사용)
+  // ?곌? 吏곸뾽 (ETL 援ъ“???꾨뱶 sidebarJobs ?ъ슜)
   const sidebarJobs = profile.sidebarJobs
   if (sidebarJobs?.length) {
     const limit = 5
-    // 유효한 이름이 있는 직업만 필터링 + 자기 자신 제외
+    // ?좏슚???대쫫???덈뒗 吏곸뾽留??꾪꽣留?+ ?먭린 ?먯떊 ?쒖쇅
     const currentJobName = profile.name?.trim().toLowerCase()
     const validJobs = sidebarJobs.filter((entity) => {
       const entityName = extractEntityName(entity)
       if (!entityName) return false
-      // 자기 자신 제외 (이름이 같으면 제외)
+      // ?먭린 ?먯떊 ?쒖쇅 (?대쫫??媛숈쑝硫??쒖쇅)
       if (currentJobName && entityName.trim().toLowerCase() === currentJobName) return false
       return true
     })
     
     if (validJobs.length > 0) {
-      // DB에 존재하는 직업을 위로, 존재하지 않는 직업을 아래로 정렬
+      // DB??議댁옱?섎뒗 吏곸뾽???꾨줈, 議댁옱?섏? ?딅뒗 吏곸뾽???꾨옒濡??뺣젹
       const sortedJobs = [...validJobs].sort((a, b) => {
         const aName = extractEntityName(a)?.toLowerCase() || ''
         const bName = extractEntityName(b)?.toLowerCase() || ''
@@ -2240,13 +2240,13 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
         const name = escapeHtml(rawName)
         const nameLower = rawName?.toLowerCase() || ''
         
-        // DB에 존재하는 직업은 상세페이지로, 존재하지 않는 직업은 검색페이지로
+        // DB??議댁옱?섎뒗 吏곸뾽? ?곸꽭?섏씠吏濡? 議댁옱?섏? ?딅뒗 吏곸뾽? 寃?됲럹?댁?濡?
         const existsInDb = existingJobSlugs?.has(nameLower)
         const url = existsInDb 
           ? buildEntityUrl(entity, 'job')
           : `/search?q=${encodeURIComponent(rawName)}`
         
-        // 존재하지 않는 직업은 아이콘과 스타일을 약간 다르게
+        // 議댁옱?섏? ?딅뒗 吏곸뾽? ?꾩씠肄섍낵 ?ㅽ??쇱쓣 ?쎄컙 ?ㅻⅤ寃?
         const icon = existsInDb ? 'fa-briefcase' : 'fa-search'
         const opacity = existsInDb ? '' : 'opacity-75'
         
@@ -2270,7 +2270,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
       
       const toggleButton = hasMore ? `
         <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-          <span class="toggle-text">더보기</span>
+          <span class="toggle-text">?붾낫湲?/span>
           <span class="toggle-count text-xs opacity-75">(+${hiddenJobs.length})</span>
           <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
         </button>
@@ -2278,7 +2278,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
       
       sections.push(
         renderSidebarSection(
-          '관련 직업',
+          '愿??吏곸뾽',
           'fa-user-tie',
           `
             <div class="expandable-list">
@@ -2291,11 +2291,11 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
     }
   }
 
-  // 관련 전공 (ETL 구조화 필드 sidebarMajors 사용)
+  // 愿???꾧났 (ETL 援ъ“???꾨뱶 sidebarMajors ?ъ슜)
   const sidebarMajors = profile.sidebarMajors
   if (sidebarMajors?.length) {
     const limit = 5
-    // 유효한 이름이 있는 전공만 필터링
+    // ?좏슚???대쫫???덈뒗 ?꾧났留??꾪꽣留?
     const validMajors = sidebarMajors.filter((m: any) => !!extractEntityName(m))
     
     if (validMajors.length > 0) {
@@ -2326,7 +2326,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
       
       const toggleButton = hasMore ? `
         <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-          <span class="toggle-text">더보기</span>
+          <span class="toggle-text">?붾낫湲?/span>
           <span class="toggle-count text-xs opacity-75">(+${hiddenMajors.length})</span>
           <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
         </button>
@@ -2334,7 +2334,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
       
       sections.push(
         renderSidebarSection(
-          '관련 전공',
+          '愿???꾧났',
           'fa-graduation-cap',
           `
             <div class="expandable-list">
@@ -2347,7 +2347,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
     }
   }
 
-  // 관련 가이드 (이 직업을 참조하는 HowTo)
+  // 愿??媛?대뱶 (??吏곸뾽??李몄“?섎뒗 HowTo)
   if (relatedHowtos?.length) {
     const guideLimit = 5
     const visibleHowtos = relatedHowtos.slice(0, guideLimit)
@@ -2375,7 +2375,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
 
     const toggleButton = hasMoreGuides ? `
       <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-        <span class="toggle-text">더보기</span>
+        <span class="toggle-text">?붾낫湲?/span>
         <span class="toggle-count text-xs opacity-75">(+${hiddenHowtos.length})</span>
         <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
       </button>
@@ -2383,7 +2383,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
 
     sections.push(
       renderSidebarSection(
-        '관련 가이드',
+        '愿??媛?대뱶',
         'fa-book-open',
         `
           <div class="expandable-list">
@@ -2397,14 +2397,14 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
 
   const organizations = renderOrganizationsList(profile)
   if (organizations) {
-    sections.push(renderSidebarSection('관련 기관', 'fa-building', organizations))
+    sections.push(renderSidebarSection('愿??湲곌?', 'fa-building', organizations))
   }
 
-  // 관련 자격증 (ETL 구조화 필드 sidebarCerts 사용)
+  // 愿???먭꺽利?(ETL 援ъ“???꾨뱶 sidebarCerts ?ъ슜)
   const sidebarCerts = profile.sidebarCerts
   if (sidebarCerts?.length) {
     const limit = 5
-    // sidebarCerts는 { name, url } 객체 배열
+    // sidebarCerts??{ name, url } 媛앹껜 諛곗뿴
     const certificates = sidebarCerts
       .map((item: any) => {
         if (typeof item === 'string') return safeTrim(item)
@@ -2436,7 +2436,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
       const toggleButton = hasMore
         ? `
       <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-        <span class="toggle-text">더보기</span>
+        <span class="toggle-text">?붾낫湲?/span>
         <span class="toggle-count text-xs opacity-75">(+${hiddenCertificates.length})</span>
         <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
       </button>
@@ -2445,7 +2445,7 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
 
       sections.push(
         renderSidebarSection(
-          '추천 자격증',
+          '異붿쿇 ?먭꺽利?,
           'fa-certificate',
           `
             <div class="expandable-list">
@@ -2458,16 +2458,16 @@ const renderJobSidebar = (profile: UnifiedJobDetail, existingJobSlugs?: Map<stri
     }
   }
   
-  // 직업 준비 정보 (jobReadyList) - 사이드바에서 제거 (상세정보 탭으로 이동)
+  // 吏곸뾽 以鍮??뺣낫 (jobReadyList) - ?ъ씠?쒕컮?먯꽌 ?쒓굅 (?곸꽭?뺣낫 ??쑝濡??대룞)
 
   return sections.filter((section) => section && safeTrim(section).length > 0).join('')
 }
 
-// 사용자 추가 출처를 평탄화하여 공통으로 활용 (페이지 표시 순서대로 정렬, 1부터 번호 부여)
+// ?ъ슜??異붽? 異쒖쿂瑜??됲깂?뷀븯??怨듯넻?쇰줈 ?쒖슜 (?섏씠吏 ?쒖떆 ?쒖꽌?濡??뺣젹, 1遺??踰덊샇 遺??
 const normalizeUserSources = (src: any): Array<{ id: number; fieldKey: string; text: string; url?: string }> => {
   if (!src) return []
 
-  // 단순 문자열 배열 포맷: ["출처1", "출처2", ...]
+  // ?⑥닚 臾몄옄??諛곗뿴 ?щ㎎: ["異쒖쿂1", "異쒖쿂2", ...]
   if (Array.isArray(src) && src.every((item: any) => typeof item === 'string')) {
     return src
       .filter((s: string) => s && s.trim())
@@ -2481,31 +2481,31 @@ const normalizeUserSources = (src: any): Array<{ id: number; fieldKey: string; t
 
   if (typeof src !== 'object') return []
 
-  // 필드 표시 순서 (소개 탭 → 과정 탭)
+  // ?꾨뱶 ?쒖떆 ?쒖꽌 (?뚭컻 ????怨쇱젙 ??
   const fieldOrder: string[] = [
-    // 히어로 섹션
+    // ?덉뼱濡??뱀뀡
     'summary', 'heroCategory', 'heroTags',
-    // 소개 탭 (UI 렌더링 순서와 동일하게 — 각주 번호가 페이지 스크롤 순서대로 매겨짐)
+    // ?뚭컻 ??(UI ?뚮뜑留??쒖꽌? ?숈씪?섍쾶 ??媛곸＜ 踰덊샇媛 ?섏씠吏 ?ㅽ겕濡??쒖꽌?濡?留ㅺ꺼吏?
     'overviewWork.main', 'overviewWork.physicalAct', 'overviewWork.mentalAct',
     'overviewProspect.main',
     'detailWlb', 'detailWlb.wlb', 'detailWlb.social', 'detailWlb.wlbDetail', 'detailWlb.socialDetail',
     'overviewSalary.sal',
     'trivia',
-    // 상세정보 탭 (핵심 능력/자격)
+    // ?곸꽭?뺣낫 ??(?듭떖 ?λ젰/?먭꺽)
     'overviewAbilities.abilityList', 'overviewAbilities.aptitudeList', 'overviewAbilities.interestList',
     'overviewAbilities.technKnow',
-    // 과정 탭 (되는 방법)
+    // 怨쇱젙 ??(?섎뒗 諛⑸쾿)
     'way',
-    // 상세정보 탭 (직업 준비하기)
+    // ?곸꽭?뺣낫 ??(吏곸뾽 以鍮꾪븯湲?
     'detailReady.curriculum', 'detailReady.recruit', 'detailReady.certificate', 'detailReady.training', 'detailReady.researchList',
-    // 사이드바
+    // ?ъ씠?쒕컮
     'sidebarMajors', 'sidebarCerts'
   ]
 
   const flat: Array<{ id: number; fieldKey: string; text: string; url?: string; displayOrder: number; originalId: number }> = []
 
   for (const [rawFieldKey, val] of Object.entries(src)) {
-    // "way[1]", "overviewSalary.sal[2]" 같은 잘못된 키 → "way", "overviewSalary.sal"로 정규화
+    // "way[1]", "overviewSalary.sal[2]" 媛숈? ?섎せ??????"way", "overviewSalary.sal"濡??뺢퇋??
     const fieldKey = rawFieldKey.replace(/\[\d+\]$/, '')
     const arr = Array.isArray(val) ? val : [val]
     arr.forEach((item: any) => {
@@ -2515,7 +2515,7 @@ const normalizeUserSources = (src: any): Array<{ id: number; fieldKey: string; t
       const originalId = item?.id || 0
       const displayOrder = fieldOrder.indexOf(fieldKey)
       flat.push({
-        id: 0, // 나중에 순서대로 재부여
+        id: 0, // ?섏쨷???쒖꽌?濡??щ???
         fieldKey,
         text: text || url,
         url: url || undefined,
@@ -2525,14 +2525,14 @@ const normalizeUserSources = (src: any): Array<{ id: number; fieldKey: string; t
     })
   }
 
-  // 표시 순서대로 정렬 후 1부터 번호 재부여
+  // ?쒖떆 ?쒖꽌?濡??뺣젹 ??1遺??踰덊샇 ?щ???
   flat.sort((a, b) => {
     if (a.displayOrder !== b.displayOrder) return a.displayOrder - b.displayOrder
-    return a.originalId - b.originalId // 같은 필드 내에서는 입력 순서
+    return a.originalId - b.originalId // 媛숈? ?꾨뱶 ?댁뿉?쒕뒗 ?낅젰 ?쒖꽌
   })
 
   return flat.map((item, idx) => ({
-    id: idx + 1, // 1부터 순차 번호
+    id: idx + 1, // 1遺???쒖감 踰덊샇
     fieldKey: item.fieldKey,
     text: item.text,
     url: item.url
@@ -2568,7 +2568,7 @@ const renderSourcesCollapsible = (
     normalizedPartials.WORK24_DJOB = profile
   }
 
-  // 사용자가 추가한 출처 (_sources) - 배열 지원
+  // ?ъ슜?먭? 異붽???異쒖쿂 (_sources) - 諛곗뿴 吏??
   const userSources = (profile as any)._sources as Record<string, { id?: number; text?: string; url?: string }[] | { id?: number; text?: string; url?: string }> | undefined
   const userSourcesFlat = userSourcesFlatFromTemplate ?? normalizeUserSources(userSources)
   
@@ -2578,39 +2578,39 @@ const renderSourcesCollapsible = (
     partials: normalizedPartials,
     labels: DEFAULT_SOURCE_LABELS,
     descriptions: SOURCE_DESCRIPTIONS,
-    title: '출처',
-    description: '이 페이지에 노출된 주요 출처를 확인할 수 있습니다.'
+    title: '異쒖쿂',
+    description: '???섏씠吏???몄텧??二쇱슂 異쒖쿂瑜??뺤씤?????덉뒿?덈떎.'
   })
 
-  // 사용자 출처 HTML 생성 — 필드별 그룹핑
+  // ?ъ슜??異쒖쿂 HTML ?앹꽦 ???꾨뱶蹂?洹몃９??
   const fieldLabels: Record<string, string> = {
-    'summary': '직업 설명', 'heroCategory': '직업 분류', 'heroTags': '태그',
-    'overviewWork.main': '수행 직무', 'overviewWork.workStrong': '작업강도',
-    'overviewWork.workPlace': '작업장소', 'overviewWork.physicalAct': '육체활동',
-    'overviewAbilities.abilityList': '핵심 역량', 'overviewAbilities.technKnow': '활용 기술',
-    'overviewAptitude.aptitudeList': '적성', 'overviewAptitude.interestList': '흥미',
-    'trivia': '여담', 'detailReady.curriculum': '정규 교육과정',
-    'detailReady.recruit': '채용 정보', 'detailReady.training': '필요 교육/훈련',
-    'detailReady.researchList': '진로 탐색 활동', 'sidebarJobs': '관련 직업',
-    'way': '되는 방법', 'overviewSalary.sal': '임금 정보',
-    'overviewProspect.main': '전망 정보', 'sidebarMajors': '관련 학과', 'sidebarCerts': '추천 자격증',
-    'detailWlb.wlbDetail': '워라밸 지수 상세', 'detailWlb.socialDetail': '사회적 평가 상세',
-    'overviewWork.mentalAct': '정신활동',
-    'general': '참고 자료'
+    'summary': '吏곸뾽 ?ㅻ챸', 'heroCategory': '吏곸뾽 遺꾨쪟', 'heroTags': '?쒓렇',
+    'overviewWork.main': '?섑뻾 吏곷Т', 'overviewWork.workStrong': '?묒뾽媛뺣룄',
+    'overviewWork.workPlace': '?묒뾽?μ냼', 'overviewWork.physicalAct': '?≪껜?쒕룞',
+    'overviewAbilities.abilityList': '?듭떖 ??웾', 'overviewAbilities.technKnow': '?쒖슜 湲곗닠',
+    'overviewAptitude.aptitudeList': '?곸꽦', 'overviewAptitude.interestList': '?λ?',
+    'trivia': '?щ떞', 'detailReady.curriculum': '?뺢퇋 援먯쑁怨쇱젙',
+    'detailReady.recruit': '梨꾩슜 ?뺣낫', 'detailReady.training': '?꾩슂 援먯쑁/?덈젴',
+    'detailReady.researchList': '吏꾨줈 ?먯깋 ?쒕룞', 'sidebarJobs': '愿??吏곸뾽',
+    'way': '?섎뒗 諛⑸쾿', 'overviewSalary.sal': '?꾧툑 ?뺣낫',
+    'overviewProspect.main': '?꾨쭩 ?뺣낫', 'sidebarMajors': '愿???숆낵', 'sidebarCerts': '異붿쿇 ?먭꺽利?,
+    'detailWlb.wlbDetail': '?뚮씪諛?吏???곸꽭', 'detailWlb.socialDetail': '?ы쉶???됯? ?곸꽭',
+    'overviewWork.mentalAct': '?뺤떊?쒕룞',
+    'general': '李멸퀬 ?먮즺'
   }
   const fieldSectionMap: Record<string, { tab: string; section: string }> = {
     'summary': { tab: 'overview', section: 'overview' },
-    'overviewWork.main': { tab: 'overview', section: 'overview-주요-업무' },
-    'overviewSalary.sal': { tab: 'overview', section: 'overview-임금-정보' },
-    'overviewProspect.main': { tab: 'overview', section: 'overview-커리어-전망' },
-    'overviewAbilities.abilityList': { tab: 'overview', section: 'overview-적성-및-흥미' },
-    'overviewAbilities.technKnow': { tab: 'overview', section: 'overview-적성-및-흥미' },
-    'detailWlb.wlbDetail': { tab: 'overview', section: 'overview-워라밸-지수' },
-    'detailWlb.socialDetail': { tab: 'overview', section: 'overview-워라밸-지수' },
-    'way': { tab: 'details', section: 'details-직업-준비하기' },
-    'detailReady.curriculum': { tab: 'details', section: 'details-직업-준비하기' },
-    'detailReady.recruit': { tab: 'details', section: 'details-직업-준비하기' },
-    'detailReady.training': { tab: 'details', section: 'details-직업-준비하기' },
+    'overviewWork.main': { tab: 'overview', section: 'overview-二쇱슂-?낅Т' },
+    'overviewSalary.sal': { tab: 'overview', section: 'overview-?꾧툑-?뺣낫' },
+    'overviewProspect.main': { tab: 'overview', section: 'overview-而ㅻ━???꾨쭩' },
+    'overviewAbilities.abilityList': { tab: 'overview', section: 'overview-?곸꽦-諛??λ?' },
+    'overviewAbilities.technKnow': { tab: 'overview', section: 'overview-?곸꽦-諛??λ?' },
+    'detailWlb.wlbDetail': { tab: 'overview', section: 'overview-?뚮씪諛?吏?? },
+    'detailWlb.socialDetail': { tab: 'overview', section: 'overview-?뚮씪諛?吏?? },
+    'way': { tab: 'details', section: 'details-吏곸뾽-以鍮꾪븯湲? },
+    'detailReady.curriculum': { tab: 'details', section: 'details-吏곸뾽-以鍮꾪븯湲? },
+    'detailReady.recruit': { tab: 'details', section: 'details-吏곸뾽-以鍮꾪븯湲? },
+    'detailReady.training': { tab: 'details', section: 'details-吏곸뾽-以鍮꾪븯湲? },
     'trivia': { tab: 'overview', section: 'overview' },
   }
   const linkifyText = (text: string): string => {
@@ -2649,7 +2649,7 @@ const renderSourcesCollapsible = (
     `
   }
 
-  // 필드별 그룹핑
+  // ?꾨뱶蹂?洹몃９??
   const groupedSources = new Map<string, typeof userSourcesFlat>()
   for (const source of userSourcesFlat) {
     const key = source.fieldKey
@@ -2679,7 +2679,7 @@ const renderSourcesCollapsible = (
   const userSourceCount = userSourcesFlat.length
 
   if (!panel || !safeTrim(panel)) {
-    // API 출처가 없어도 사용자 출처가 있으면 표시
+    // API 異쒖쿂媛 ?놁뼱???ъ슜??異쒖쿂媛 ?덉쑝硫??쒖떆
     if (!hasUserSources) {
     return ''
     }
@@ -2687,19 +2687,19 @@ const renderSourcesCollapsible = (
 
   const activeSourceCountFromSources = normalizedSources.length
   const fallbackSourceCount = Object.values(normalizedPartials || {}).filter((value) => Boolean(value)).length
-  // 배지 숫자 = API 출처 + 사용자 출처 합산 (패널에 실제 표시되는 전체 출처 수)
+  // 諛곗? ?レ옄 = API 異쒖쿂 + ?ъ슜??異쒖쿂 ?⑹궛 (?⑤꼸???ㅼ젣 ?쒖떆?섎뒗 ?꾩껜 異쒖쿂 ??
   const activeSourceCount = (activeSourceCountFromSources || fallbackSourceCount) + userSourceCount
-  const badgeLabel = `${activeSourceCount}개`
+  const badgeLabel = `${activeSourceCount}媛?
 
   const toggleId = `source-toggle-${normalizedId}`
   const iconId = `source-icon-${normalizedId}`
 
-  // 사용자 출처 섹션
+  // ?ъ슜??異쒖쿂 ?뱀뀡
   const userSourcesSection = hasUserSources ? `
     <div class="mt-6 pt-4 border-t border-wiki-border/40">
       <h4 class="text-sm font-semibold text-wiki-text mb-3 flex items-center gap-2">
         <i class="fas fa-user-edit text-wiki-primary"></i>
-        사용자 추가 출처
+        ?ъ슜??異붽? 異쒖쿂
       </h4>
       <div>
         ${userSourcesHtml}
@@ -2718,7 +2718,7 @@ const renderSourcesCollapsible = (
       >
         <span class="flex items-center gap-3">
           <i class="fas fa-database text-wiki-secondary text-lg" aria-hidden="true"></i>
-          <span class="text-base">출처</span>
+          <span class="text-base">異쒖쿂</span>
         </span>
         <div class="flex items-center gap-3 text-sm text-wiki-muted">
           <span class="inline-flex items-center gap-1.5 rounded-full border border-wiki-secondary/30 bg-wiki-secondary/10 px-3 py-1.5 text-wiki-secondary font-medium">${escapeHtml(badgeLabel)}</span>
@@ -2754,7 +2754,7 @@ const renderSourcesCollapsible = (
           });
         }
         
-        // 주석 번호 클릭 → 출처 섹션으로 이동
+        // 二쇱꽍 踰덊샇 ?대┃ ??異쒖쿂 ?뱀뀡?쇰줈 ?대룞
         document.querySelectorAll('.user-footnote-ref').forEach(function(ref) {
           ref.addEventListener('click', function(e) {
             e.preventDefault();
@@ -2762,7 +2762,7 @@ const renderSourcesCollapsible = (
             var targetEl = document.getElementById('user-fn-' + globalNum);
             if (!targetEl) return;
 
-            // 출처 섹션 펼치기
+            // 異쒖쿂 ?뱀뀡 ?쇱튂湲?
             var sourceSection = document.querySelector('[data-source-collapsible]');
             if (sourceSection) {
               var toggleBtn = sourceSection.querySelector('button[aria-controls]');
@@ -2771,7 +2771,7 @@ const renderSourcesCollapsible = (
               }
             }
 
-            // 스크롤 이동
+            // ?ㅽ겕濡??대룞
             setTimeout(function() {
               targetEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
               targetEl.classList.add('ring-2', 'ring-wiki-primary');
@@ -2782,7 +2782,7 @@ const renderSourcesCollapsible = (
           });
         });
         
-        // 출처 클릭 → 해당 섹션으로 이동 (탭 전환 포함)
+        // 異쒖쿂 ?대┃ ???대떦 ?뱀뀡?쇰줈 ?대룞 (???꾪솚 ?ы븿)
         document.querySelectorAll('.user-source-item').forEach(function(item) {
           item.addEventListener('click', function() {
             var backTo = this.getAttribute('data-back-to');
@@ -2791,37 +2791,37 @@ const renderSourcesCollapsible = (
             var navSection = this.getAttribute('data-nav-section');
             var targetEl = document.getElementById(backTo);
 
-            // data-nav-section이 있으면 해당 섹션으로 이동 (API로 추가된 출처)
+            // data-nav-section???덉쑝硫??대떦 ?뱀뀡?쇰줈 ?대룞 (API濡?異붽???異쒖쿂)
             if (!targetEl && navSection) {
               targetEl = document.getElementById(navSection);
             }
 
-            // 필드 키에 따라 필요한 탭 결정 (data-nav-tab 우선)
+            // ?꾨뱶 ?ㅼ뿉 ?곕씪 ?꾩슂????寃곗젙 (data-nav-tab ?곗꽑)
             var targetTabId = navTab || null;
             if (!targetTabId && fieldKey) {
-              // 상세정보 필드
+              // ?곸꽭?뺣낫 ?꾨뱶
               var detailFields = ['detailReady.curriculum', 'detailReady.recruit', 'detailReady.training', 'detailReady.researchList'];
               if (fieldKey.startsWith('detail') || fieldKey === 'way' || detailFields.some(function(f) { return fieldKey.startsWith(f) || fieldKey === f; })) {
-                targetTabId = 'details';  // 탭 ID는 'details' (복수형)
+                targetTabId = 'details';  // ??ID??'details' (蹂듭닔??
               } else if (fieldKey.startsWith('overview') || fieldKey === 'summary' || fieldKey === 'heroCategory' || fieldKey === 'heroTags' || fieldKey === 'trivia') {
                 targetTabId = 'overview';
               }
-              // sidebar 필드는 탭 전환 불필요 (항상 표시)
+              // sidebar ?꾨뱶?????꾪솚 遺덊븘??(??긽 ?쒖떆)
             }
             
-            // 탭 전환 함수 (api-client.js의 DetailTabs 로직과 일치)
+            // ???꾪솚 ?⑥닔 (api-client.js??DetailTabs 濡쒖쭅怨??쇱튂)
             function switchToTab(tabId) {
               var tabBtn = document.getElementById('cw-tab-' + tabId);
               var tabPanel = document.getElementById('cw-tab-panel-' + tabId);
               if (!tabBtn || !tabPanel) return;
               
-              // 이미 해당 탭이 활성화되어 있으면 스킵
+              // ?대? ?대떦 ??씠 ?쒖꽦?붾릺???덉쑝硫??ㅽ궢
               if (tabBtn.getAttribute('aria-selected') === 'true') return;
               
               var ACTIVE_CLASSES = ['text-white', 'border-wiki-primary', 'md:bg-wiki-border/30'];
               var INACTIVE_CLASSES = ['text-wiki-muted', 'border-transparent', 'bg-transparent'];
               
-              // 모든 탭 버튼 비활성화
+              // 紐⑤뱺 ??踰꾪듉 鍮꾪솢?깊솕
               document.querySelectorAll('[data-cw-tab-trigger]').forEach(function(btn) {
                 btn.setAttribute('aria-selected', 'false');
                 btn.setAttribute('tabindex', '-1');
@@ -2830,7 +2830,7 @@ const renderSourcesCollapsible = (
                 INACTIVE_CLASSES.forEach(function(cls) { btn.classList.add(cls); });
               });
               
-              // 모든 탭 패널 비활성화
+              // 紐⑤뱺 ???⑤꼸 鍮꾪솢?깊솕
               document.querySelectorAll('[data-cw-tab-panel]').forEach(function(panel) {
                 panel.setAttribute('aria-hidden', 'true');
                 panel.setAttribute('tabindex', '-1');
@@ -2838,32 +2838,32 @@ const renderSourcesCollapsible = (
                 panel.classList.add('is-hidden');
               });
               
-              // 대상 탭 버튼 활성화
+              // ?????踰꾪듉 ?쒖꽦??
               tabBtn.setAttribute('aria-selected', 'true');
               tabBtn.setAttribute('tabindex', '0');
               tabBtn.classList.add('is-active');
               INACTIVE_CLASSES.forEach(function(cls) { tabBtn.classList.remove(cls); });
               ACTIVE_CLASSES.forEach(function(cls) { tabBtn.classList.add(cls); });
               
-              // 대상 탭 패널 활성화
+              // ??????⑤꼸 ?쒖꽦??
               tabPanel.setAttribute('aria-hidden', 'false');
               tabPanel.setAttribute('tabindex', '0');
               tabPanel.classList.add('is-active');
               tabPanel.classList.remove('is-hidden');
               
-              // tabset의 data-active-tab 업데이트
+              // tabset??data-active-tab ?낅뜲?댄듃
               var tabset = document.querySelector('[data-cw-tabset]');
               if (tabset) {
                 tabset.setAttribute('data-active-tab', tabId);
               }
             }
             
-            // 탭 전환 필요 시 전환
+            // ???꾪솚 ?꾩슂 ???꾪솚
             if (targetTabId) {
               switchToTab(targetTabId);
             }
             
-            // 약간의 지연 후 스크롤 (탭 전환 후 렌더링 대기)
+            // ?쎄컙??吏?????ㅽ겕濡?(???꾪솚 ???뚮뜑留??湲?
             setTimeout(function() {
               if (targetEl) {
                 targetEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
@@ -2876,7 +2876,7 @@ const renderSourcesCollapsible = (
           });
         });
         
-        // 데이터 출처 하이라이트 기능 (배타적 + 토글 방식)
+        // ?곗씠??異쒖쿂 ?섏씠?쇱씠??湲곕뒫 (諛고???+ ?좉? 諛⑹떇)
         (function() {
           var activeSource = null;
           var sourceColors = {
@@ -2910,28 +2910,28 @@ const renderSourcesCollapsible = (
               }
             });
             
-            // 해당 출처 항목도 강조
+            // ?대떦 異쒖쿂 ??ぉ??媛뺤“
             var sourceEntry = document.querySelector('[data-source-entry="' + source + '"]');
             if (sourceEntry) {
               sourceEntry.classList.add('ring-2', 'ring-wiki-primary');
             }
           }
           
-          // 출처 항목 클릭 이벤트
+          // 異쒖쿂 ??ぉ ?대┃ ?대깽??
           document.querySelectorAll('[data-source-entry][data-source-has-data="true"]').forEach(function(entry) {
             entry.addEventListener('click', function(e) {
-              // 다른 클릭 이벤트와 충돌 방지
+              // ?ㅻⅨ ?대┃ ?대깽?몄? 異⑸룎 諛⑹?
               if (e.target.closest('.user-source-item')) return;
               
               var source = this.getAttribute('data-source-entry');
               
-              // 배타적 + 토글 방식
+              // 諛고???+ ?좉? 諛⑹떇
               if (activeSource === source) {
-                // 같은 출처 클릭 시 끄기
+                // 媛숈? 異쒖쿂 ?대┃ ???꾧린
                 clearAllHighlights();
                 activeSource = null;
               } else {
-                // 다른 출처 클릭 시 전환
+                // ?ㅻⅨ 異쒖쿂 ?대┃ ???꾪솚
                 clearAllHighlights();
                 highlightSource(source);
                 activeSource = source;
@@ -2979,61 +2979,61 @@ const buildJobDemoComments = (profile: UnifiedJobDetail): SampleCommentPayload[]
     const seeds: CommentSeed[] = [
       {
         author: 'GrowthMarketer_J',
-        content: `${profile.name} 역할의 핵심은 퍼널 전 구간 데이터를 매일 점검하면서 실험 캘린더를 돌리는 겁니다. ARR 20억대 SaaS에서 주 1회 테스트 속도로 ROAS를 2배 끌어올린 경험이 있어요.`,
+        content: `${profile.name} ??븷???듭떖? ?쇰꼸 ??援ш컙 ?곗씠?곕? 留ㅼ씪 ?먭??섎㈃???ㅽ뿕 罹섎┛?붾? ?뚮━??寃곷땲?? ARR 20?듬? SaaS?먯꽌 二?1???뚯뒪???띾룄濡?ROAS瑜?2諛??뚯뼱?щ┛ 寃쏀뿕???덉뼱??`,
         likes: 24,
         hoursAgo: 6,
         role: 'expert',
-        badge: 'TOP 성장 사례'
+        badge: 'TOP ?깆옣 ?щ?'
       },
       {
-        author: '데이터혜윰',
-        content: '데이터레이크를 직접 다루지 못하면 성장 속도가 확 떨어집니다. Looker Studio 대신 dbt + Metabase 조합으로 팀 공용 대시보드 만들었더니 실험 승인 속도가 40% 줄었어요.',
+        author: '?곗씠?고삙??,
+        content: '?곗씠?곕젅?댄겕瑜?吏곸젒 ?ㅻ（吏 紐삵븯硫??깆옣 ?띾룄媛 ???⑥뼱吏묐땲?? Looker Studio ???dbt + Metabase 議고빀?쇰줈 ? 怨듭슜 ??쒕낫??留뚮뱾?덈뜑???ㅽ뿕 ?뱀씤 ?띾룄媛 40% 以꾩뿀?댁슂.',
         likes: 17,
         hoursAgo: 9,
-        badge: '데이터 팀 추천'
+        badge: '?곗씠??? 異붿쿇'
       },
       {
         author: 'Collaborative_CMO',
-        content: '세일즈 팀과 OKR을 묶어야 성과가 나옵니다. 마케팅만 성장 지표를 책임지면 LTV 계산이 느슨해져서 CAC-LTV 밸런스가 망가져요.',
+        content: '?몄씪利??怨?OKR??臾띠뼱???깃낵媛 ?섏샃?덈떎. 留덉??낅쭔 ?깆옣 吏?쒕? 梨낆엫吏硫?LTV 怨꾩궛???먯뒯?댁졇??CAC-LTV 諛몃윴?ㅺ? 留앷??몄슂.',
         likes: 12,
         hoursAgo: 15
       },
       {
-        author: '실험러버',
-        content: '연간 그로스 로드맵을 분기별 미션으로 쪼개두면 좋지만, 실무에서는 긴급 실험 요청이 계속 들어옵니다. 백로그 우선순위 테이블 꼭 준비하세요.',
+        author: '?ㅽ뿕?щ쾭',
+        content: '?곌컙 洹몃줈??濡쒕뱶留듭쓣 遺꾧린蹂?誘몄뀡?쇰줈 履쇨컻?먮㈃ 醫뗭?留? ?ㅻТ?먯꽌??湲닿툒 ?ㅽ뿕 ?붿껌??怨꾩냽 ?ㅼ뼱?듬땲?? 諛깅줈洹??곗꽑?쒖쐞 ?뚯씠釉?瑗?以鍮꾪븯?몄슂.',
         likes: 9,
         dislikes: 2,
         hoursAgo: 20
       },
       {
         author: 'BudgetWatcher',
-        content: '광고 집행을 성장팀이 직접 가져가는 건 동의하지만, PMM과 역할이 겹치면서 혼선이 있었습니다. 역할 정의를 분명히 하지 않으면 팀 갈등 생겨요.',
+        content: '愿묎퀬 吏묓뻾???깆옣???吏곸젒 媛?멸???嫄??숈쓽?섏?留? PMM怨???븷??寃뱀튂硫댁꽌 ?쇱꽑???덉뿀?듬땲?? ??븷 ?뺤쓽瑜?遺꾨챸???섏? ?딆쑝硫?? 媛덈벑 ?앷꺼??',
         dislikes: 5,
         hoursAgo: 26
       },
       {
         author: 'GrowthOps_Junior',
-        content: '입사 후 첫 분기엔 메타 광고보다 CRM 자동화가 더 임팩트 컸어요. 사용자의 라이프사이클을 먼저 이해하면 손쉬운 승리가 생깁니다.',
+        content: '?낆궗 ??泥?遺꾧린??硫뷀? 愿묎퀬蹂대떎 CRM ?먮룞?붽? ???꾪뙥??而몄뼱?? ?ъ슜?먯쓽 ?쇱씠?꾩궗?댄겢??癒쇱? ?댄빐?섎㈃ ?먯돩???밸━媛 ?앷퉩?덈떎.',
         hoursAgo: 30
       },
       {
         author: 'AIStack_Builder',
-        content: 'Generative AI를 마케팅 자동화에 붙일 때는 개인정보 처리 이슈가 있습니다. 법무 검토 프로세스를 미리 만들어두세요.',
+        content: 'Generative AI瑜?留덉????먮룞?붿뿉 遺숈씪 ?뚮뒗 媛쒖씤?뺣낫 泥섎━ ?댁뒋媛 ?덉뒿?덈떎. 踰뺣Т 寃???꾨줈?몄뒪瑜?誘몃━ 留뚮뱾?대몢?몄슂.',
         hoursAgo: 36
       },
       {
-        author: 'UX리서처_겸임',
-        content: 'UX 리서치 결과를 바로 실험 설계에 녹여내는 파트너십이 중요했어요. 세일즈 콜 녹취를 Growth 팀이 직접 코딩하면 인사이트가 빨라집니다.',
+        author: 'UX由ъ꽌泥?寃몄엫',
+        content: 'UX 由ъ꽌移?寃곌낵瑜?諛붾줈 ?ㅽ뿕 ?ㅺ퀎???뱀뿬?대뒗 ?뚰듃?덉떗??以묒슂?덉뼱?? ?몄씪利?肄??뱀랬瑜?Growth ???吏곸젒 肄붾뵫?섎㈃ ?몄궗?댄듃媛 鍮⑤씪吏묐땲??',
         hoursAgo: 44
       },
       {
-        author: 'KPI정비중',
-        content: '전환 지표만 보면 채널 믹스가 왜곡돼서 NPS 등 정성 지표를 함께 봤습니다. OKR 정렬이 안 되면 성장 팀이 소방수로만 움직여요.',
+        author: 'KPI?뺣퉬以?,
+        content: '?꾪솚 吏?쒕쭔 蹂대㈃ 梨꾨꼸 誘뱀뒪媛 ?쒓끝?쇱꽌 NPS ???뺤꽦 吏?쒕? ?④퍡 遊ㅼ뒿?덈떎. OKR ?뺣젹?????섎㈃ ?깆옣 ????뚮갑?섎줈留??吏곸뿬??',
         hoursAgo: 55
       },
       {
         author: 'OnboardingPM',
-        content: '온보딩 플로우와 리텐션 코호트 보는 법을 먼저 익히면 좋습니다. 퀵윈은 A/B 테스트보다도 이메일 시퀀스 최적화에서 나왔어요.',
+        content: '?⑤낫???뚮줈?곗? 由ы뀗??肄뷀샇??蹂대뒗 踰뺤쓣 癒쇱? ?듯엳硫?醫뗭뒿?덈떎. ?듭쐢? A/B ?뚯뒪?몃낫?ㅻ룄 ?대찓???쒗??理쒖쟻?붿뿉???섏솕?댁슂.',
         hoursAgo: 68
       }
     ]
@@ -3041,60 +3041,60 @@ const buildJobDemoComments = (profile: UnifiedJobDetail): SampleCommentPayload[]
   }
 
   if (
-    normalizedName.includes('변호사') ||
+    normalizedName.includes('蹂?몄궗') ||
     normalizedId.includes('lawyer') ||
     normalizedId === 'job:g_k000007482' ||
     normalizedId === 'job:c_375'
   ) {
     const seeds: CommentSeed[] = [
       {
-        author: '법조인턴_민지',
-        content: `${profile.name} 준비하면서 로스쿨 진학보다 사법연수원 동기 네트워킹이 더 큰 자산이었어요. 수습 기간에는 사건 기록 정리가 가장 큰 업무입니다.`,
+        author: '踰뺤“?명꽩_誘쇱?',
+        content: `${profile.name} 以鍮꾪븯硫댁꽌 濡쒖뒪荑?吏꾪븰蹂대떎 ?щ쾿?곗닔???숆린 ?ㅽ듃?뚰궧???????먯궛?댁뿀?댁슂. ?섏뒿 湲곌컙?먮뒗 ?ш굔 湲곕줉 ?뺣━媛 媛?????낅Т?낅땲??`,
         likes: 19,
         hoursAgo: 5,
-        badge: '로스쿨 현장 노트'
+        badge: '濡쒖뒪荑??꾩옣 ?명듃'
       },
       {
         author: 'Litigator_J',
-        content: '대형로펌에서 ${profile.name}로 일하면서 가장 많이 배우는 건 증거 인멸 방지와 증인 신뢰도 관리입니다. 초기 변론에서 스토리라인을 탄탄히 짜두세요.',
+        content: '??뺣줈?뚯뿉??${profile.name}濡??쇳븯硫댁꽌 媛??留롮씠 諛곗슦??嫄?利앷굅 ?몃㈇ 諛⑹?? 利앹씤 ?좊ː??愿由ъ엯?덈떎. 珥덇린 蹂濡좎뿉???ㅽ넗由щ씪?몄쓣 ?꾪깂??吏쒕몢?몄슂.',
         likes: 14,
         hoursAgo: 11,
         role: 'expert',
-        badge: '민사 소송 실무'
+        badge: '誘쇱궗 ?뚯넚 ?ㅻТ'
       },
       {
         author: 'LawTech_AI',
-        content: '최근 AI 리서치툴을 쓰면 판례 검색 시간이 절반으로 줄어요. 다만 개인정보 이슈 때문에 로펌 내 보안 가이드를 따라야 합니다.',
+        content: '理쒓렐 AI 由ъ꽌移섑댋???곕㈃ ?먮? 寃???쒓컙???덈컲?쇰줈 以꾩뼱?? ?ㅻ쭔 媛쒖씤?뺣낫 ?댁뒋 ?뚮Ц??濡쒗럩 ??蹂댁븞 媛?대뱶瑜??곕씪???⑸땲??',
         likes: 11,
         hoursAgo: 16
       },
       {
-        author: '공익변호사_서준',
-        content: '공익 사건이 많으면 매출 KPI를 맞추기 어렵지만 브랜드 평판에는 큰 도움이 됩니다. 시간 배분을 잘해야 번아웃을 피할 수 있어요.',
+        author: '怨듭씡蹂?몄궗_?쒖?',
+        content: '怨듭씡 ?ш굔??留롮쑝硫?留ㅼ텧 KPI瑜?留욎텛湲??대졄吏留?釉뚮옖???됲뙋?먮뒗 ???꾩????⑸땲?? ?쒓컙 諛곕텇???섑빐??踰덉븘?껋쓣 ?쇳븷 ???덉뼱??',
         likes: 7,
         hoursAgo: 23
       },
       {
-        author: '로펌매니저_혜진',
-        content: '로펌에서 ${profile.name} 분들을 지원하려면 일정·자료 관리 시스템을 정비해야 합니다. 특히 변론기일 캘린더는 이중화가 필수예요.',
+        author: '濡쒗럩留ㅻ땲?_?쒖쭊',
+        content: '濡쒗럩?먯꽌 ${profile.name} 遺꾨뱾??吏?먰븯?ㅻ㈃ ?쇱젙쨌?먮즺 愿由??쒖뒪?쒖쓣 ?뺣퉬?댁빞 ?⑸땲?? ?뱁엳 蹂濡좉린??罹섎┛?붾뒗 ?댁쨷?붽? ?꾩닔?덉슂.',
         hoursAgo: 31
       },
       {
-        author: '사법연수원40기',
-        content: '연수원 동기들과 사건 공동 수임을 많이 하게 됩니다. 사건 유형별로 템플릿을 만들어두면 커뮤니케이션이 쉬워요.',
+        author: '?щ쾿?곗닔??0湲?,
+        content: '?곗닔???숆린?ㅺ낵 ?ш굔 怨듬룞 ?섏엫??留롮씠 ?섍쾶 ?⑸땲?? ?ш굔 ?좏삎蹂꾨줈 ?쒗뵆由우쓣 留뚮뱾?대몢硫?而ㅻ??덉??댁뀡???ъ썙??',
         likes: 5,
         hoursAgo: 42
       },
       {
-        author: '법률마케팅전문가_JW',
-        content: '${profile.name} 페이지에 실시간 판례·칼럼을 연결하면 상담 문의 전환율이 30% 이상 올라갑니다. SEO와 연동하면 신규 의뢰 확보에 효과적이에요.',
+        author: '踰뺣쪧留덉??낆쟾臾멸?_JW',
+        content: '${profile.name} ?섏씠吏???ㅼ떆媛??먮?쨌移쇰읆???곌껐?섎㈃ ?곷떞 臾몄쓽 ?꾪솚?⑥씠 30% ?댁긽 ?щ씪媛묐땲?? SEO? ?곕룞?섎㈃ ?좉퇋 ?섎ː ?뺣낫???④낵?곸씠?먯슂.',
         likes: 8,
         hoursAgo: 50,
         badge: 'SEO & Growth'
       },
       {
         author: 'InHouseCounsel_K',
-        content: '인하우스 ${profile.name}는 계약 검토보다 컴플라이언스 프로세스 설계 비중이 늘어나고 있어요. 사내 교육 자료를 미리 만들어두면 협업이 쉽습니다.',
+        content: '?명븯?곗뒪 ${profile.name}??怨꾩빟 寃?좊낫??而댄뵆?쇱씠?몄뒪 ?꾨줈?몄뒪 ?ㅺ퀎 鍮꾩쨷???섏뼱?섍퀬 ?덉뼱?? ?щ궡 援먯쑁 ?먮즺瑜?誘몃━ 留뚮뱾?대몢硫??묒뾽???쎌뒿?덈떎.',
         hoursAgo: 60
       }
     ]
@@ -3104,29 +3104,29 @@ const buildJobDemoComments = (profile: UnifiedJobDetail): SampleCommentPayload[]
   const seeds: CommentSeed[] = [
     {
       author: 'Careerwiki_User',
-      content: `${profile.name} 역할에 관심 있는 분들은 실제 업무 일과표와 필요한 역량을 먼저 정리해보세요.`,
+      content: `${profile.name} ??븷??愿???덈뒗 遺꾨뱾? ?ㅼ젣 ?낅Т ?쇨낵?쒖? ?꾩슂????웾??癒쇱? ?뺣━?대낫?몄슂.`,
       likes: 4,
       hoursAgo: 12
     },
     {
       author: 'SkillMapper',
-      content: '필수 역량과 Nice-to-have 스킬을 분리해두면 학습 로드맵이 선명해집니다. 일주일 단위로 리소스를 배분해보세요.',
+      content: '?꾩닔 ??웾怨?Nice-to-have ?ㅽ궗??遺꾨━?대몢硫??숈뒿 濡쒕뱶留듭씠 ?좊챸?댁쭛?덈떎. ?쇱＜???⑥쐞濡?由ъ냼?ㅻ? 諛곕텇?대낫?몄슂.',
       hoursAgo: 18
     },
     {
       author: 'Mentor_J',
-      content: `${profile.name} 현업 멘토를 인터뷰해 보면 공부해야 할 우선순위를 빠르게 잡을 수 있습니다.`,
+      content: `${profile.name} ?꾩뾽 硫섑넗瑜??명꽣酉고빐 蹂대㈃ 怨듬??댁빞 ???곗꽑?쒖쐞瑜?鍮좊Ⅴ寃??≪쓣 ???덉뒿?덈떎.`,
       likes: 2,
       hoursAgo: 28
     },
     {
       author: 'Transitioner',
-      content: '커리어 전환을 생각한다면 관련 직무의 사이드 프로젝트를 먼저 진행해보세요. 작은 실험이 자신감을 줍니다.',
+      content: '而ㅻ━???꾪솚???앷컖?쒕떎硫?愿??吏곷Т???ъ씠???꾨줈?앺듃瑜?癒쇱? 吏꾪뻾?대낫?몄슂. ?묒? ?ㅽ뿕???먯떊媛먯쓣 以띾땲??',
       hoursAgo: 35
     },
     {
       author: 'CuriousMind',
-      content: 'Careerwiki HowTo 가이드를 읽고 나만의 학습 노트를 만들어 공유하면 커뮤니티 피드백을 받기 좋습니다.',
+      content: 'Careerwiki HowTo 媛?대뱶瑜??쎄퀬 ?섎쭔???숈뒿 ?명듃瑜?留뚮뱾??怨듭쑀?섎㈃ 而ㅻ??덊떚 ?쇰뱶諛깆쓣 諛쏄린 醫뗭뒿?덈떎.',
       hoursAgo: 48
     }
   ]
@@ -3157,15 +3157,15 @@ const renderQuickStats = (
   sourceNote?: (source: DataSource | string) => string,
   telemetryVariant?: string
 ): string => {
-  // 변호사 페이지는 Quick Stats 표시하지 않음
+  // 蹂?몄궗 ?섏씠吏??Quick Stats ?쒖떆?섏? ?딆쓬
   if (isLawyerProfile(profile)) {
     return ''
   }
 
   const stats = [
-    { label: '평균 연봉', value: profile.salary, icon: 'fa-coins', source: 'GOYONG24' as DataSource },
-    { label: '직업 전망', value: profile.prospect, icon: 'fa-chart-line', source: 'GOYONG24' as DataSource },
-    { label: '직무 만족도', value: profile.satisfaction, icon: 'fa-face-smile', source: 'CAREERNET' as DataSource }
+    { label: '?됯퇏 ?곕큺', value: profile.salary, icon: 'fa-coins', source: 'GOYONG24' as DataSource },
+    { label: '吏곸뾽 ?꾨쭩', value: profile.prospect, icon: 'fa-chart-line', source: 'GOYONG24' as DataSource },
+    { label: '吏곷Т 留뚯”??, value: profile.satisfaction, icon: 'fa-face-smile', source: 'CAREERNET' as DataSource }
   ].filter((stat) => typeof stat.value === 'string' && safeTrim(stat.value).length > 0)
 
   if (!stats.length) {
@@ -3226,7 +3226,7 @@ const renderQuickStats = (
   return `
     <section
       class="glass-card px-4 py-6 md:p-7 rounded-3xl border border-wiki-border/70 bg-gradient-to-br from-wiki-bg/90 via-wiki-bg/65 to-wiki-bg/40 space-y-6"
-      aria-label="핵심 지표"
+      aria-label="?듭떖 吏??
       data-cw-telemetry-component="job-quick-stats"
       data-cw-telemetry-scope="job-quick-stats"
       data-cw-telemetry-action="quick-stats"${variantAttr}
@@ -3236,8 +3236,8 @@ const renderQuickStats = (
           <i class="fas fa-gauge-high text-lg" aria-hidden="true"></i>
         </span>
         <div>
-          <h2 class="text-lg sm:text-xl font-bold text-white leading-tight">핵심 지표 요약</h2>
-          <p class="mt-1 content-text text-wiki-muted">주요 성과 지표를 한눈에 확인하세요.</p>
+          <h2 class="text-lg sm:text-xl font-bold text-white leading-tight">?듭떖 吏???붿빟</h2>
+          <p class="mt-1 content-text text-wiki-muted">二쇱슂 ?깃낵 吏?쒕? ?쒕늿???뺤씤?섏꽭??</p>
         </div>
       </header>
       <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4" data-cw-stat-grid>
@@ -3248,7 +3248,7 @@ const renderQuickStats = (
 }
 
 const renderOrganizationsList = (profile: UnifiedJobDetail): string => {
-  // ETL 구조화 필드 sidebarOrgs 사용
+  // ETL 援ъ“???꾨뱶 sidebarOrgs ?ъ슜
   const sidebarOrgs = profile.sidebarOrgs
   if (!sidebarOrgs || sidebarOrgs.length === 0) return ''
   
@@ -3263,7 +3263,7 @@ const renderOrganizationsList = (profile: UnifiedJobDetail): string => {
   const renderOrg = (org: any, isHidden: boolean = false) => {
     const name = escapeHtml(safeTrim(org.name))
     if (org.url) {
-      // URL에 프로토콜이 없으면 https:// 추가
+      // URL???꾨줈?좎퐳???놁쑝硫?https:// 異붽?
       let fullUrl = safeTrim(org.url)
       if (!/^https?:\/\//i.test(fullUrl)) {
         fullUrl = `https://${fullUrl}`
@@ -3299,7 +3299,7 @@ const renderOrganizationsList = (profile: UnifiedJobDetail): string => {
   
   const toggleButton = hasMore ? `
     <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-      <span class="toggle-text">더보기</span>
+      <span class="toggle-text">?붾낫湲?/span>
       <span class="toggle-count text-xs opacity-75">(+${hiddenOrgs.length})</span>
       <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
     </button>
@@ -3328,8 +3328,8 @@ const renderKecoCodeList = (profile: UnifiedJobDetail): string => {
 
 
 export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJobSlugs, relatedHowtos, classificationData, careerTrees }: UnifiedJobDetailTemplateParams): string => {
-  // profile은 merged_profile_json에서 파싱된 데이터 (평탄한 구조 + 계층적 구조 병행)
-  // ETL에서 기본 필드들을 모두 포함하고 있음
+  // profile? merged_profile_json?먯꽌 ?뚯떛???곗씠??(?됲깂??援ъ“ + 怨꾩링??援ъ“ 蹂묓뻾)
+  // ETL?먯꽌 湲곕낯 ?꾨뱶?ㅼ쓣 紐⑤몢 ?ы븿?섍퀬 ?덉쓬
   
   const telemetryVariant = resolveJobTelemetryVariant(profile)
   const telemetryVariantAttr = telemetryVariant ? ` data-cw-telemetry-variant="${escapeHtml(telemetryVariant)}"` : ''
@@ -3344,28 +3344,28 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         const baseLabel = SOURCE_NOTE_LABELS[normalizedSource] ?? DEFAULT_SOURCE_LABELS[normalizedSource] ?? normalizedSource
         const status = sources?.[normalizedSource]
         const statusLabel = describeSourceStatus(normalizedSource, status)
-        return statusLabel ? `${baseLabel} · ${statusLabel}` : baseLabel
+        return statusLabel ? `${baseLabel} 쨌 ${statusLabel}` : baseLabel
       }
       if (typeof source === 'string') {
         return source
       }
-      return 'Careerwiki 통합 데이터'
+      return 'Careerwiki ?듯빀 ?곗씠??
     },
     telemetryVariant
   )
   */
-  // ── 사용자 출처 전역 번호 매핑 (페이지 전체에서 1~N 통합 번호) ──
+  // ?? ?ъ슜??異쒖쿂 ?꾩뿭 踰덊샇 留ㅽ븨 (?섏씠吏 ?꾩껜?먯꽌 1~N ?듯빀 踰덊샇) ??
   const userSourcesFlat = normalizeUserSources((profile as any)._sources)
   // footnoteMap: { fieldKey: { localNum: globalNum } }
-  // 소스 텍스트에서 [N]을 파싱하여 필드별 로컬→전역 매핑
+  // ?뚯뒪 ?띿뒪?몄뿉??[N]???뚯떛?섏뿬 ?꾨뱶蹂?濡쒖뺄?믪쟾??留ㅽ븨
   const footnoteMap: Record<string, Record<string, number>> = {}
-  // sourceTextMap: 전역번호 → 출처 설명 텍스트 (각주 hover 시 표시)
+  // sourceTextMap: ?꾩뿭踰덊샇 ??異쒖쿂 ?ㅻ챸 ?띿뒪??(媛곸＜ hover ???쒖떆)
   const sourceTextMap: Record<number, string> = {}
-  // 소스 키 → 렌더링 필드키 매핑 (소스 저장 키와 formatRichText 호출 키가 다른 경우)
+  // ?뚯뒪 ?????뚮뜑留??꾨뱶??留ㅽ븨 (?뚯뒪 ????ㅼ? formatRichText ?몄텧 ?ㅺ? ?ㅻⅨ 寃쎌슦)
   const sourceKeyAliases: Record<string, string[]> = {
     'detailWlb': ['detailWlb.wlbDetail', 'detailWlb.socialDetail'],
   }
-  // 필드별 로컬 카운터 (source.text에 [N] prefix가 없는 경우 자동 부여)
+  // ?꾨뱶蹂?濡쒖뺄 移댁슫??(source.text??[N] prefix媛 ?녿뒗 寃쎌슦 ?먮룞 遺??
   const fieldLocalCounter: Record<string, number> = {}
   for (const source of userSourcesFlat) {
     const m = source.text.match(/^\[(\d+)\]/)
@@ -3373,15 +3373,15 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     if (m) {
       localNum = m[1]
     } else {
-      // [N] prefix 없는 source → 필드 내 순서대로 로컬 번호 자동 부여
+      // [N] prefix ?녿뒗 source ???꾨뱶 ???쒖꽌?濡?濡쒖뺄 踰덊샇 ?먮룞 遺??
       if (!fieldLocalCounter[source.fieldKey]) fieldLocalCounter[source.fieldKey] = 0
       fieldLocalCounter[source.fieldKey]++
       localNum = String(fieldLocalCounter[source.fieldKey])
     }
 
     if (!footnoteMap[source.fieldKey]) footnoteMap[source.fieldKey] = {}
-    footnoteMap[source.fieldKey][localNum] = source.id // source.id = 전역 번호 (1~N)
-    // 별칭 키에도 동일 매핑 등록 (detailWlb → detailWlb.wlbDetail 등)
+    footnoteMap[source.fieldKey][localNum] = source.id // source.id = ?꾩뿭 踰덊샇 (1~N)
+    // 蹂꾩묶 ?ㅼ뿉???숈씪 留ㅽ븨 ?깅줉 (detailWlb ??detailWlb.wlbDetail ??
     const aliases = sourceKeyAliases[source.fieldKey]
     if (aliases) {
       for (const alias of aliases) {
@@ -3390,34 +3390,34 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       }
     }
 
-    // [N] 접두사 제거한 출처 설명을 전역 번호로 매핑
+    // [N] ?묐몢???쒓굅??異쒖쿂 ?ㅻ챸???꾩뿭 踰덊샇濡?留ㅽ븨
     const cleanDesc = source.text.replace(/^\[\d+\]\s*/, '').trim()
     if (cleanDesc) sourceTextMap[source.id] = cleanDesc
   }
 
-  // 히어로 섹션 데이터 (ETL에서 병합된 필드 사용)
+  // ?덉뼱濡??뱀뀡 ?곗씠??(ETL?먯꽌 蹂묓빀???꾨뱶 ?ъ슜)
   const normalizedSources = Array.isArray(profile.sources) ? profile.sources : []
   const hasOnlyWork24DictionarySources =
     normalizedSources.length > 0 &&
     normalizedSources.every((source) => source === 'WORK24_DJOB')
 
   const heroTitle = profile.heroTitle || profile.name
-  // heroDescription: ETL 데이터(heroIntro)는 첫 줄만, 사용자 편집 데이터(summary)는 전체 사용
+  // heroDescription: ETL ?곗씠??heroIntro)??泥?以꾨쭔, ?ъ슜???몄쭛 ?곗씠??summary)???꾩껜 ?ъ슜
   const heroDescription =
     profile.heroIntro?.split('\n')[0]?.trim() ||
-    (profile.summary ? safeTrim(profile.summary).replace(/\n+/g, ' ') : null) ||  // 줄바꿈 → 공백
+    (profile.summary ? safeTrim(profile.summary).replace(/\n+/g, ' ') : null) ||  // 以꾨컮轅???怨듬갚
     profile.work?.summary?.split('\n')[0]?.trim() ||
     profile.duties?.split('\n')[0]?.trim() ||
     profile.heroIntro?.trim()
 
-  // normalizeBracketLabel 제거됨: ETL에서 이미 정제된 데이터 사용
+  // normalizeBracketLabel ?쒓굅?? ETL?먯꽌 ?대? ?뺤젣???곗씠???ъ슜
   const renderWorkMetaCard = (title: string, icon: string, value: string): string => {
-    // 파이프(|), 쉼표 끝 등 불필요한 구분자 정리
+    // ?뚯씠??|), ?쇳몴 ????遺덊븘?뷀븳 援щ텇???뺣━
     const cleaned = safeTrim(value)
-      ?.replace(/\|+/g, ', ')  // 파이프를 쉼표로 변환
-      .replace(/,\s*,/g, ',')  // 중복 쉼표 제거
-      .replace(/,\s*$/g, '')   // 끝 쉼표 제거
-      .replace(/^\s*,/g, '')   // 시작 쉼표 제거
+      ?.replace(/\|+/g, ', ')  // ?뚯씠?꾨? ?쇳몴濡?蹂??
+      .replace(/,\s*,/g, ',')  // 以묐났 ?쇳몴 ?쒓굅
+      .replace(/,\s*$/g, '')   // ???쇳몴 ?쒓굅
+      .replace(/^\s*,/g, '')   // ?쒖옉 ?쇳몴 ?쒓굅
       .trim()
     if (!cleaned) return ''
     const formatted = cleaned
@@ -3441,23 +3441,23 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     `
   }
   
-  // 히어로 카테고리 렌더링 (DB 분류 우선 → heroCategory 폴백)
+  // ?덉뼱濡?移댄뀒怨좊━ ?뚮뜑留?(DB 遺꾨쪟 ?곗꽑 ??heroCategory ?대갚)
   let categoryHtml = ''
   if (classificationData?.large_category) {
-    // DB 분류 데이터 우선 사용 (클릭 가능한 링크)
+    // DB 遺꾨쪟 ?곗씠???곗꽑 ?ъ슜 (?대┃ 媛?ν븳 留곹겕)
     const largeLink = `/job?category=${encodeURIComponent(classificationData.large_category)}`
     const largePart = `<a href="${largeLink}" class="hover:underline">${escapeHtml(classificationData.large_category)}</a>`
     const mediumPart = classificationData.medium_category ? escapeHtml(classificationData.medium_category) : ''
     const jobNamePart = escapeHtml(safeTrim(profile.name) || '')
 
     categoryHtml = `
-      <nav class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full text-[14px] font-medium" style="background:rgba(99,102,241,0.08);color:rgba(203,213,225,0.85);border:1px solid rgba(99,102,241,0.2);" aria-label="분류">
+      <nav class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full text-[14px] font-medium" style="background:rgba(99,102,241,0.08);color:rgba(203,213,225,0.85);border:1px solid rgba(99,102,241,0.2);" aria-label="遺꾨쪟">
         <i class="fas fa-sitemap text-[12px]" style="color:rgba(129,140,248,0.7);" aria-hidden="true"></i>
-        ${largePart}${mediumPart ? ` <span style="color:rgba(129,140,248,0.4);">›</span> ${mediumPart}` : ''}
+        ${largePart}${mediumPart ? ` <span style="color:rgba(129,140,248,0.4);">??/span> ${mediumPart}` : ''}
       </nav>
     `
   } else if (profile.heroCategory) {
-    // 문자열 형식 지원 (사용자 생성 데이터)
+    // 臾몄옄???뺤떇 吏??(?ъ슜???앹꽦 ?곗씠??
     if (typeof profile.heroCategory === 'string') {
       const categoryValue = safeTrim(profile.heroCategory)
       if (categoryValue) {
@@ -3469,13 +3469,13 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         `
       }
     } else if (profile.heroCategory.type === 'breadcrumb') {
-      // 브레드크럼 스타일 (pill 배지) - 모바일에서 클릭하여 확장
+      // 釉뚮젅?쒗겕???ㅽ???(pill 諛곗?) - 紐⑤컮?쇱뿉???대┃?섏뿬 ?뺤옣
       const largePart = profile.heroCategory.large ? escapeHtml(profile.heroCategory.large) : ''
       const mediumPart = profile.heroCategory.medium ? escapeHtml(profile.heroCategory.medium) : ''
       const smallPart = profile.heroCategory.small ? escapeHtml(profile.heroCategory.small) : ''
       const totalLevels = (largePart ? 1 : 0) + (mediumPart ? 1 : 0) + (smallPart ? 1 : 0)
       
-      // 3개 레벨이 있을 때만 모바일 확장 기능 적용
+      // 3媛??덈꺼???덉쓣 ?뚮쭔 紐⑤컮???뺤옣 湲곕뒫 ?곸슜
       if (totalLevels >= 2) {
         categoryHtml = `
           <style>
@@ -3498,22 +3498,22 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
               .job-bc-mobile[data-lv="3"] .bc-expand { opacity: 0; pointer-events: none; }
             }
           </style>
-          <!-- 데스크톱: 전체 표시 -->
+          <!-- ?곗뒪?ы넲: ?꾩껜 ?쒖떆 -->
           <span class="job-bc-desktop hidden md:inline-flex items-center gap-2 px-3 py-1.5 rounded-full text-[14px] font-medium" style="background:rgba(99,102,241,0.08);color:rgba(203,213,225,0.85);border:1px solid rgba(99,102,241,0.2);">
             <i class="fas fa-sitemap text-[11px]" style="color:rgba(129,140,248,0.7);" aria-hidden="true"></i>
-            ${largePart}${mediumPart ? ` <span style="color:rgba(129,140,248,0.4);">›</span> ${mediumPart}` : ''}${smallPart ? ` <span style="color:rgba(129,140,248,0.4);">›</span> ${smallPart}` : ''}
+            ${largePart}${mediumPart ? ` <span style="color:rgba(129,140,248,0.4);">??/span> ${mediumPart}` : ''}${smallPart ? ` <span style="color:rgba(129,140,248,0.4);">??/span> ${smallPart}` : ''}
           </span>
-          <!-- 모바일: 클릭하여 단계적 확장 -->
+          <!-- 紐⑤컮?? ?대┃?섏뿬 ?④퀎???뺤옣 -->
           <span class="job-bc-mobile inline-flex md:hidden items-center gap-1.5 px-3 py-1.5 rounded-full text-[14px] font-medium cursor-pointer select-none" style="background:rgba(99,102,241,0.08);color:rgba(203,213,225,0.85);border:1px solid rgba(99,102,241,0.2);" data-lv="1" data-max="${totalLevels}" onclick="(function(e){var el=e.currentTarget;if(!el)return;var lv=parseInt(el.getAttribute('data-lv')||'1',10);var mx=parseInt(el.getAttribute('data-max')||'1',10);var nxt=lv>=mx?1:lv+1;el.setAttribute('data-lv',String(nxt));})(event)">
             <i class="fas fa-sitemap text-[11px]" style="color:rgba(129,140,248,0.7);" aria-hidden="true"></i>
             <span class="bc-l">${largePart}</span>
-            ${mediumPart ? `<span class="bc-sep-m" style="color:rgba(129,140,248,0.4);">›</span><span class="bc-m">${mediumPart}</span>` : ''}
-            ${smallPart ? `<span class="bc-sep-s" style="color:rgba(129,140,248,0.4);">›</span><span class="bc-s">${smallPart}</span>` : ''}
+            ${mediumPart ? `<span class="bc-sep-m" style="color:rgba(129,140,248,0.4);">??/span><span class="bc-m">${mediumPart}</span>` : ''}
+            ${smallPart ? `<span class="bc-sep-s" style="color:rgba(129,140,248,0.4);">??/span><span class="bc-s">${smallPart}</span>` : ''}
             <i class="fas fa-chevron-right bc-expand text-[9px] text-wiki-muted/70 ml-0.5" aria-hidden="true"></i>
           </span>
         `
       } else {
-        // 1개 레벨만 있으면 기존 방식
+        // 1媛??덈꺼留??덉쑝硫?湲곗〈 諛⑹떇
         categoryHtml = `
           <span class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full text-xs font-medium" style="background:rgba(99,102,241,0.08);color:rgba(203,213,225,0.85);border:1px solid rgba(99,102,241,0.2);">
             <i class="fas fa-sitemap text-[11px]" style="color:rgba(129,140,248,0.7);" aria-hidden="true"></i>
@@ -3522,7 +3522,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         `
       }
     } else if (profile.heroCategory.value) {
-      // 단일 값 스타일 (ETL에서 이미 정제된 값 사용)
+      // ?⑥씪 媛??ㅽ???(ETL?먯꽌 ?대? ?뺤젣??媛??ъ슜)
       const categoryValue = safeTrim(profile.heroCategory.value)
       if (categoryValue) {
         categoryHtml = `
@@ -3573,13 +3573,13 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     }
   }
 
-  // 특정 필드가 어느 출처에서 왔는지 판단하는 헬퍼 함수
-  // partials가 있으면 partials 기반, 없으면 profile.sources 기반
+  // ?뱀젙 ?꾨뱶媛 ?대뒓 異쒖쿂?먯꽌 ?붾뒗吏 ?먮떒?섎뒗 ?ы띁 ?⑥닔
+  // partials媛 ?덉쑝硫?partials 湲곕컲, ?놁쑝硫?profile.sources 湲곕컲
   const normalizedProfileSources = Array.isArray(profile.sources) ? profile.sources : []
   const getFieldSources = (fieldExtractor: (p: UnifiedJobDetail | null | undefined) => any): string[] => {
     const sources: string[] = []
     
-    // partials 기반 판단 (더 정확함)
+    // partials 湲곕컲 ?먮떒 (???뺥솗??
     if (partials?.CAREERNET && fieldExtractor(partials.CAREERNET)) {
       sources.push('CAREERNET')
     }
@@ -3590,23 +3590,23 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       sources.push('WORK24_DJOB')
     }
     
-    // partials가 비어있으면 profile.sources 기반으로 폴백
+    // partials媛 鍮꾩뼱?덉쑝硫?profile.sources 湲곕컲?쇰줈 ?대갚
     if (sources.length === 0 && normalizedProfileSources.length > 0) {
-      // 해당 필드가 profile에 있으면 profile.sources 사용
+      // ?대떦 ?꾨뱶媛 profile???덉쑝硫?profile.sources ?ъ슜
       if (fieldExtractor(profile)) {
         normalizedProfileSources.forEach(s => {
           if (s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_JOB' || s === 'WORK24_DJOB') {
-            // WORK24_JOB은 GOYONG24로 통합
+            // WORK24_JOB? GOYONG24濡??듯빀
             sources.push(s === 'WORK24_JOB' ? 'GOYONG24' : s)
           }
         })
       }
     }
     
-    return [...new Set(sources)]  // 중복 제거
+    return [...new Set(sources)]  // 以묐났 ?쒓굅
   }
 
-  // Type C: 업무 설명 (ETL 구조화 필드 overviewWork 사용)
+  // Type C: ?낅Т ?ㅻ챸 (ETL 援ъ“???꾨뱶 overviewWork ?ъ슜)
   const overviewWork = profile.overviewWork
   const workMainDesc = overviewWork?.main
   const workStrong = overviewWork?.workStrong
@@ -3617,45 +3617,45 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     const introBlocks = []
     const workMetaCards: string[] = []
     
-    // 주요 업무 설명 (ETL에서 우선순위로 선택된 값)
-    // 문자열 또는 배열 모두 지원 (편집 시 배열로 저장될 수 있음)
+    // 二쇱슂 ?낅Т ?ㅻ챸 (ETL?먯꽌 ?곗꽑?쒖쐞濡??좏깮??媛?
+    // 臾몄옄???먮뒗 諛곗뿴 紐⑤몢 吏??(?몄쭛 ??諛곗뿴濡???λ맆 ???덉쓬)
     if (workMainDesc) {
       let workMainContent = ''
       if (typeof workMainDesc === 'string' && safeTrim(workMainDesc)) {
         workMainContent = renderDutyBulletList(workMainDesc)
       } else if (Array.isArray(workMainDesc) && workMainDesc.length > 0) {
-        // 배열인 경우 불릿 리스트로 렌더링
+        // 諛곗뿴??寃쎌슦 遺덈┸ 由ъ뒪?몃줈 ?뚮뜑留?
         const validItems = workMainDesc.filter((item: any) => typeof item === 'string' && safeTrim(item))
         if (validItems.length > 0) {
           workMainContent = `<ul class="space-y-2">${validItems.map((item: string) => 
             `<li class="flex items-start gap-2 text-base text-wiki-text">
-              <span class="text-wiki-secondary mt-1">▶</span>
+              <span class="text-wiki-secondary mt-1">??/span>
               <span>${escapeHtml(safeTrim(item))}</span>
             </li>`
           ).join('')}</ul>`
         }
       }
       if (workMainContent) {
-        introBlocks.push(`<div><h4 class="content-heading">수행 직무</h4>${workMainContent}</div>`)
+        introBlocks.push(`<div><h4 class="content-heading">?섑뻾 吏곷Т</h4>${workMainContent}</div>`)
       }
     }
     
-    // 작업강도
+    // ?묒뾽媛뺣룄
     if (workStrong && safeTrim(workStrong)) {
-      workMetaCards.push(renderWorkMetaCard('작업강도', 'fa-dumbbell', workStrong))
+      workMetaCards.push(renderWorkMetaCard('?묒뾽媛뺣룄', 'fa-dumbbell', workStrong))
     }
     
-    // 작업장소
+    // ?묒뾽?μ냼
     if (workPlace && safeTrim(workPlace)) {
-      workMetaCards.push(renderWorkMetaCard('작업장소', 'fa-location-dot', workPlace))
+      workMetaCards.push(renderWorkMetaCard('?묒뾽?μ냼', 'fa-location-dot', workPlace))
     }
 
-    // 육체활동
+    // ?≪껜?쒕룞
     if (physicalAct && safeTrim(physicalAct)) {
-      workMetaCards.push(renderWorkMetaCard('육체활동', 'fa-person-running', physicalAct))
+      workMetaCards.push(renderWorkMetaCard('?≪껜?쒕룞', 'fa-person-running', physicalAct))
     }
 
-    // 카드가 있으면 그리드로 렌더링 (3개까지 지원)
+    // 移대뱶媛 ?덉쑝硫?洹몃━?쒕줈 ?뚮뜑留?(3媛쒓퉴吏 吏??
     if (workMetaCards.length > 0) {
       const gridCols = workMetaCards.length === 3 ? 'sm:grid-cols-3' : 'sm:grid-cols-2'
       introBlocks.push(`
@@ -3666,46 +3666,46 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     }
     
   if (introBlocks.length > 0) {
-    // 주요 업무 출처 판단: overviewWork 필드가 있는 출처
+    // 二쇱슂 ?낅Т 異쒖쿂 ?먮떒: overviewWork ?꾨뱶媛 ?덈뒗 異쒖쿂
     const workSources = getFieldSources(p => p?.overviewWork?.main || p?.duties || p?.work?.summary)
-    pushOverviewCard('주요 업무', 'fa-rocket', introBlocks.join(''), workSources.length > 0 ? workSources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
+    pushOverviewCard('二쇱슂 ?낅Т', 'fa-rocket', introBlocks.join(''), workSources.length > 0 ? workSources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
   }
   }
 
   const normalizedWorkMainDesc = normalizeComparableText(workMainDesc)
 
-  // Type B: 전망 (ETL 구조화 필드 overviewProspect 사용)
+  // Type B: ?꾨쭩 (ETL 援ъ“???꾨뱶 overviewProspect ?ъ슜)
   const overviewProspect = profile.overviewProspect
   const prospectPrimary = overviewProspect?.main
   
-  // 재직자가 생각하는 일자리 전망 데이터
+  // ?ъ쭅?먭? ?앷컖?섎뒗 ?쇱옄由??꾨쭩 ?곗씠??
   const jobSumProspect = overviewProspect?.jobSumProspect
   const jobProspectInqYr = jobSumProspect && jobSumProspect.length > 0 
     ? jobSumProspect[0].jobProspectInqYr 
     : null
   
   let prospectChartHtml = ''
-  // 통일 포맷 전망 차트 (사용자 편집) 확인
+  // ?듭씪 ?щ㎎ ?꾨쭩 李⑦듃 (?ъ슜???몄쭛) ?뺤씤
   const prospectChartUnified = (profile as any).prospectChart
   if (isUnifiedChartData(prospectChartUnified)) {
     prospectChartHtml = renderUnifiedChart(prospectChartUnified, 'prospect')
   } else if (jobSumProspect && Array.isArray(jobSumProspect) && jobSumProspect.length > 0) {
-    // 전망 레이블 매핑
+    // ?꾨쭩 ?덉씠釉?留ㅽ븨
     const labelMap: Record<string, string> = {
-      '증가': '증가',
-      '다소 증가': '다소 증가',
-      '유지': '유지',
-      '다소 감소': '다소 감소',
-      '감소': '감소'
+      '利앷?': '利앷?',
+      '?ㅼ냼 利앷?': '?ㅼ냼 利앷?',
+      '?좎?': '?좎?',
+      '?ㅼ냼 媛먯냼': '?ㅼ냼 媛먯냼',
+      '媛먯냼': '媛먯냼'
     }
     
-    // 바 색상 (스크린샷 참고 - 파란색 계열)
+    // 諛??됱긽 (?ㅽ겕由곗꺑 李멸퀬 - ?뚮???怨꾩뿴)
     const barColor = 'rgba(59, 130, 246, 0.85)'
     
-    // Chart.js용 고유 ID
+    // Chart.js??怨좎쑀 ID
     const chartId = `prospect-chart-${Date.now()}`
     
-    // 데이터 파싱
+    // ?곗씠???뚯떛
     const chartData = jobSumProspect.map(item => ({
       label: labelMap[item.jobProspectNm] || item.jobProspectNm,
       value: Number.parseFloat(item.jobProspectRatio) || 0
@@ -3718,11 +3718,11 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     prospectChartHtml = `
           <div class="bg-white/5 rounded-2xl p-6 border border-wiki-border/50">
         <div class="flex items-center justify-between mb-4">
-          <h4 class="text-base font-bold text-wiki-secondary">재직자가 생각하는 일자리 전망</h4>
-          ${jobProspectInqYr ? `<span class="text-xs text-wiki-muted">&lt;조사년도: ${jobProspectInqYr}년&gt;</span>` : ''}
+          <h4 class="text-base font-bold text-wiki-secondary">?ъ쭅?먭? ?앷컖?섎뒗 ?쇱옄由??꾨쭩</h4>
+          ${jobProspectInqYr ? `<span class="text-xs text-wiki-muted">&lt;議곗궗?꾨룄: ${jobProspectInqYr}??gt;</span>` : ''}
         </div>
         <canvas id="${chartId}" style="max-height: 300px;"></canvas>
-        <p class="text-xs text-wiki-muted mt-3">※ 위의 그래프는 직업당 평균 30명의 재직자가 해당 직업의 향후 5년간 일자리 변화에 대해 응답한 결과입니다. 작업전문가와 재직자들의 입장과 견해에 따라 일자리 전망에 차이가 있으므로, 참고자료로만 활용하시기 바랍니다.</p>
+        <p class="text-xs text-wiki-muted mt-3">???꾩쓽 洹몃옒?꾨뒗 吏곸뾽???됯퇏 30紐낆쓽 ?ъ쭅?먭? ?대떦 吏곸뾽???ν썑 5?꾧컙 ?쇱옄由?蹂?붿뿉 ????묐떟??寃곌낵?낅땲?? ?묒뾽?꾨Ц媛? ?ъ쭅?먮뱾???낆옣怨?寃ы빐???곕씪 ?쇱옄由??꾨쭩??李⑥씠媛 ?덉쑝誘濡? 李멸퀬?먮즺濡쒕쭔 ?쒖슜?섏떆湲?諛붾엻?덈떎.</p>
       </div>
       
       <script>
@@ -3763,7 +3763,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
                     },
                     title: {
                       display: true,
-                      text: '(단위: %)',
+                      text: '(?⑥쐞: %)',
                       color: 'rgba(255, 255, 255, 0.6)',
                       font: { size: 10 },
                       align: 'end'
@@ -3802,7 +3802,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     `
   }
   
-  // CareerNet 원본 데이터일 때만 출처 문구 표시 (chart/구조화된 forecastList가 있을 때만)
+  // CareerNet ?먮낯 ?곗씠?곗씪 ?뚮쭔 異쒖쿂 臾멸뎄 ?쒖떆 (chart/援ъ“?붾맂 forecastList媛 ?덉쓣 ?뚮쭔)
   const prospectSources = getFieldSources(p => p?.overviewProspect?.main)
   const isCareerNetProspect = prospectSources.includes('CAREERNET')
 
@@ -3810,12 +3810,12 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     let prospectHtml = ''
 
     if (Array.isArray(prospectPrimary) && prospectPrimary.length > 0) {
-      // 배열인 경우 각 항목을 블록으로 만들고 첫 단어 들여쓰기
+      // 諛곗뿴??寃쎌슦 媛???ぉ??釉붾줉?쇰줈 留뚮뱾怨?泥??⑥뼱 ?ㅼ뿬?곌린
       const prospectBlocks = prospectPrimary
         .map((item: any) => {
           const text = typeof item === 'string' ? item : item.list_content || ''
           if (!safeTrim(text)) return ''
-          // 첫 단어 들여쓰기를 위해 <span> 추가
+          // 泥??⑥뼱 ?ㅼ뿬?곌린瑜??꾪빐 <span> 異붽?
           return `<div class="mb-3 content-text"><span class="inline-block w-4"></span>${escapeHtml(text)}</div>`
         })
         .filter(Boolean)
@@ -3825,7 +3825,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         prospectHtml = `<div class="space-y-2">${prospectBlocks}</div>`
       }
     } else if (typeof prospectPrimary === 'string' && safeTrim(prospectPrimary)) {
-      // 문자열인 경우 줄바꿈을 블록으로 변환 (실제 내용이 있을 때만)
+      // 臾몄옄?댁씤 寃쎌슦 以꾨컮轅덉쓣 釉붾줉?쇰줈 蹂??(?ㅼ젣 ?댁슜???덉쓣 ?뚮쭔)
       const lines = prospectPrimary.split('\n').filter(line => safeTrim(line))
       const prospectFieldMap = footnoteMap['overviewProspect.main'] || null
       if (lines.length > 1) {
@@ -3833,7 +3833,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
           const safeLine = escapeHtml(line).replace(/\[(\d+)\]/g, (_m: string, localNum: string) => {
             const globalNum = prospectFieldMap ? (prospectFieldMap[localNum] ?? localNum) : localNum
             const srcDesc = sourceTextMap?.[Number(globalNum)] || ''
-            const titleTxt = srcDesc ? srcDesc.replace(/"/g, '&quot;') : `출처 [${globalNum}]`
+            const titleTxt = srcDesc ? srcDesc.replace(/"/g, '&quot;') : `異쒖쿂 [${globalNum}]`
             return `<sup class="user-footnote-ref cursor-pointer transition" style="font-size:11px;font-weight:600;color:var(--wiki-primary,#8b5cf6);margin-left:1px;vertical-align:super;line-height:1;" data-source-id="${globalNum}" id="user-fnref-${globalNum}" title="${titleTxt}">[${globalNum}]</sup>`
           })
           return `<div class="mb-3 content-text"><span class="inline-block w-4"></span>${safeLine}</div>`
@@ -3860,16 +3860,16 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         .join('')
 
       if (forecastItems) {
-        forecastDetailHtml = `<div class="mt-6"><h4 class="text-base font-bold text-wiki-secondary mb-3">기간별 전망</h4>${forecastItems}</div>`
+        forecastDetailHtml = `<div class="mt-6"><h4 class="text-base font-bold text-wiki-secondary mb-3">湲곌컙蹂??꾨쭩</h4>${forecastItems}</div>`
       }
     }
 
-    // 면책조항: CareerNet 데이터이면서 차트 또는 구조화된 forecastList가 있을 때만 표시
+    // 硫댁콉議고빆: CareerNet ?곗씠?곗씠硫댁꽌 李⑦듃 ?먮뒗 援ъ“?붾맂 forecastList媛 ?덉쓣 ?뚮쭔 ?쒖떆
     const prospectDisclaimer = isCareerNetProspect && (prospectChartHtml !== '' || structuredForecastItems.length > 0)
-      ? `<p class="text-xs text-wiki-muted mt-4 leading-relaxed">※ 위의 일자리 전망은 직업전문가들이 「중장기인력수급전망」, 「정성적 직업전망조사」, 「KNOW 재직자조사」 등 각종 연구와 조사를 기초로 작성하였습니다.</p>`
+      ? `<p class="text-xs text-wiki-muted mt-4 leading-relaxed">???꾩쓽 ?쇱옄由??꾨쭩? 吏곸뾽?꾨Ц媛?ㅼ씠 ?뚯쨷?κ린?몃젰?섍툒?꾨쭩?? ?뚯젙?깆쟻 吏곸뾽?꾨쭩議곗궗?? ?똊NOW ?ъ쭅?먯“?с???媛곸쥌 ?곌뎄? 議곗궗瑜?湲곗큹濡??묒꽦?섏??듬땲??</p>`
       : ''
 
-    // 텍스트가 있으면 먼저 표시, 기간별 전망, 그래프는 뒤에 표시
+    // ?띿뒪?멸? ?덉쑝硫?癒쇱? ?쒖떆, 湲곌컙蹂??꾨쭩, 洹몃옒?꾨뒗 ?ㅼ뿉 ?쒖떆
     const combinedHtml = [
       prospectHtml?.trim() || '',
       forecastDetailHtml?.trim() || '',
@@ -3879,21 +3879,21 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     
     // Only add card if combinedHtml has content
     if (safeTrim(combinedHtml)) {
-      // 전망 출처 판단: overviewProspect 필드가 있는 출처
+      // ?꾨쭩 異쒖쿂 ?먮떒: overviewProspect ?꾨뱶媛 ?덈뒗 異쒖쿂
       const prospectSources = getFieldSources(p => p?.overviewProspect?.main || p?.prospect)
-      pushOverviewCard('커리어 전망', 'fa-chart-line', combinedHtml, prospectSources.length > 0 ? prospectSources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
+      pushOverviewCard('而ㅻ━???꾨쭩', 'fa-chart-line', combinedHtml, prospectSources.length > 0 ? prospectSources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
     }
   }
 
-  // 한국의 직업지표 (indicatorChart)는 상세정보 탭으로 이동됨
+  // ?쒓뎅??吏곸뾽吏??(indicatorChart)???곸꽭?뺣낫 ??쑝濡??대룞??
 
-  // Type C: 능력 & 지식 (ETL 구조화 필드 overviewAbilities 사용)
+  // Type C: ?λ젰 & 吏??(ETL 援ъ“???꾨뱶 overviewAbilities ?ъ슜)
   const overviewAbilities = profile.overviewAbilities
   const abilitiesSimple = overviewAbilities?.abilityList
   
   const abilityBlocks = []
   
-  // 능력 섹션 (큼지막한 네모 박스 아이콘 - Lucide)
+  // ?λ젰 ?뱀뀡 (?쇱?留됲븳 ?ㅻえ 諛뺤뒪 ?꾩씠肄?- Lucide)
   if (abilitiesSimple && Array.isArray(abilitiesSimple) && abilitiesSimple.length > 0) {
     const abilityList = abilitiesSimple
       .slice(0, 10)
@@ -3909,31 +3909,31 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         </div>`
       })
       .join('')
-    abilityBlocks.push(`<div><h3 class="content-heading">핵심 역량</h3><div class="grid grid-cols-1 md:grid-cols-2 gap-4">${abilityList}</div></div>`)
+    abilityBlocks.push(`<div><h3 class="content-heading">?듭떖 ??웾</h3><div class="grid grid-cols-1 md:grid-cols-2 gap-4">${abilityList}</div></div>`)
   }
   
-  // 정규교육과정 - 상세정보 탭의 직업 준비하기 섹션으로 이동 (중복 제거)
+  // ?뺢퇋援먯쑁怨쇱젙 - ?곸꽭?뺣낫 ??쓽 吏곸뾽 以鍮꾪븯湲??뱀뀡?쇰줈 ?대룞 (以묐났 ?쒓굅)
   
-  // 활용 기술 (overviewAbilities.technKnow)
+  // ?쒖슜 湲곗닠 (overviewAbilities.technKnow)
   const technKnow = overviewAbilities?.technKnow
   if (typeof technKnow === 'string' && technKnow?.trim()) {
-    abilityBlocks.push(`<div class="mt-6"><h3 class="content-heading">활용 기술</h3>${formatRichText(technKnow, 'overviewAbilities.technKnow', footnoteMap, sourceTextMap)}</div>`)
+    abilityBlocks.push(`<div class="mt-6"><h3 class="content-heading">?쒖슜 湲곗닠</h3>${formatRichText(technKnow, 'overviewAbilities.technKnow', footnoteMap, sourceTextMap)}</div>`)
   }
 
   const trainingMetaCards: string[] = []
   const eduLevel = overviewAbilities?.eduLevel
   const skillYear = overviewAbilities?.skillYear
   if (typeof eduLevel === 'string' && safeTrim(eduLevel)) {
-    trainingMetaCards.push(renderWorkMetaCard('정규교육', 'fa-graduation-cap', eduLevel))
+    trainingMetaCards.push(renderWorkMetaCard('?뺢퇋援먯쑁', 'fa-graduation-cap', eduLevel))
   }
   if (typeof skillYear === 'string' && safeTrim(skillYear)) {
-    trainingMetaCards.push(renderWorkMetaCard('숙련기간', 'fa-hourglass-half', skillYear))
+    trainingMetaCards.push(renderWorkMetaCard('?숇젴湲곌컙', 'fa-hourglass-half', skillYear))
   }
 
   if (trainingMetaCards.length > 0) {
     abilityBlocks.push(`
       <div class="mt-6">
-        <h3 class="content-heading">교육 및 숙련도</h3>
+        <h3 class="content-heading">援먯쑁 諛??숇젴??/h3>
         <div class="mt-4 grid gap-4 sm:grid-cols-2">
           ${trainingMetaCards.join('')}
         </div>
@@ -3942,14 +3942,14 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
   }
   
   if (abilityBlocks.length > 0) {
-    // 능력 출처 판단: overviewAbilities 필드가 있는 출처
-    // '과정' 탭으로 이동됨
+    // ?λ젰 異쒖쿂 ?먮떒: overviewAbilities ?꾨뱶媛 ?덈뒗 異쒖쿂
+    // '怨쇱젙' ??쑝濡??대룞??
     const abilitySources = getFieldSources(p => p?.overviewAbilities?.abilityList || p?.abilities)
-    pushDetailCard('핵심 능력·자격', 'fa-bolt', abilityBlocks.join(''), abilitySources.length > 0 ? abilitySources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
+    pushDetailCard('?듭떖 ?λ젰쨌?먭꺽', 'fa-bolt', abilityBlocks.join(''), abilitySources.length > 0 ? abilitySources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
   }
 
-  // 직업 만족도 먼저 추가 (있을 경우)
-  // 직업 만족도 등급 계산 함수
+  // 吏곸뾽 留뚯”??癒쇱? 異붽? (?덉쓣 寃쎌슦)
+  // 吏곸뾽 留뚯”???깃툒 怨꾩궛 ?⑥닔
   const getSatisfactionGrade = (satisfaction: string | number | undefined) => {
     if (!satisfaction) return null
     
@@ -3959,7 +3959,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     
     if (score >= 80) {
       return {
-        level: '매우 좋음',
+        level: '留ㅼ슦 醫뗭쓬',
         icon: 'fa-face-grin-stars',
         color: 'text-green-400',
         bgColor: 'bg-green-500/20',
@@ -3967,7 +3967,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       }
     } else if (score >= 60) {
       return {
-        level: '좋음',
+        level: '醫뗭쓬',
         icon: 'fa-face-smile',
         color: 'text-blue-400',
         bgColor: 'bg-blue-500/20',
@@ -3975,7 +3975,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       }
     } else if (score >= 40) {
       return {
-        level: '보통',
+        level: '蹂댄넻',
         icon: 'fa-face-meh',
         color: 'text-yellow-400',
         bgColor: 'bg-yellow-500/20',
@@ -3983,7 +3983,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       }
     } else if (score >= 20) {
       return {
-        level: '별로',
+        level: '蹂꾨줈',
         icon: 'fa-face-frown',
         color: 'text-orange-400',
         bgColor: 'bg-orange-500/20',
@@ -3991,7 +3991,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       }
     } else {
       return {
-        level: '매우 별로',
+        level: '留ㅼ슦 蹂꾨줈',
         icon: 'fa-face-sad-tear',
         color: 'text-red-400',
         bgColor: 'bg-red-500/20',
@@ -4000,11 +4000,11 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     }
   }
 
-  // 적성 및 흥미 (ETL 구조화 필드 overviewAptitude 사용)
+  // ?곸꽦 諛??λ? (ETL 援ъ“???꾨뱶 overviewAptitude ?ъ슜)
   const overviewAptitude = profile.overviewAptitude
   const traitBlocks: string[] = []
   
-  // 직업 만족도 카드 (개선된 UI)
+  // 吏곸뾽 留뚯”??移대뱶 (媛쒖꽑??UI)
   const satisfactionValue = overviewAptitude?.satisfaction?.value
   if (satisfactionValue) {
     const score = typeof satisfactionValue === 'number' 
@@ -4019,7 +4019,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
             <i class="fas ${grade.icon} text-3xl ${grade.color}"></i>
           </div>
           <div class="flex-1">
-            <h3 class="text-sm font-semibold ${grade.color} uppercase tracking-wide mb-1">직업 만족도</h3>
+            <h3 class="text-sm font-semibold ${grade.color} uppercase tracking-wide mb-1">吏곸뾽 留뚯”??/h3>
             <div class="flex items-baseline gap-2">
               <span class="text-3xl font-bold text-white">${score.toFixed(1)}%</span>
               <span class="text-lg font-semibold ${grade.color}">${grade.level}</span>
@@ -4027,13 +4027,13 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
           </div>
         </div>
         <p class="text-xs text-wiki-muted mt-3 leading-relaxed">
-          ※ 직업만족도는 해당 직업의 일자리 증가 가능성, 발전가능성 및 고용안정에 대해 재직자가 느끼는 생각을 종합하여 100점 만점으로 환산한 값입니다.
+          ??吏곸뾽留뚯”?꾨뒗 ?대떦 吏곸뾽???쇱옄由?利앷? 媛?μ꽦, 諛쒖쟾媛?μ꽦 諛?怨좎슜?덉젙??????ъ쭅?먭? ?먮겮???앷컖??醫낇빀?섏뿬 100??留뚯젏?쇰줈 ?섏궛??媛믪엯?덈떎.
         </p>
       `)
     }
   }
   
-  // 적성 (aptitudeList 배열에서 텍스트 추출)
+  // ?곸꽦 (aptitudeList 諛곗뿴?먯꽌 ?띿뒪??異붿텧)
   const aptitudeList = overviewAptitude?.aptitudeList
   if (aptitudeList && Array.isArray(aptitudeList) && aptitudeList.length > 0) {
     const aptitudeText = aptitudeList
@@ -4042,11 +4042,11 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       .join(', ')
     if (safeTrim(aptitudeText)) {
     const divClass = traitBlocks.length > 0 ? 'mt-6' : ''
-      traitBlocks.push(`<div class="${divClass}"><h3 class="content-heading text-wiki-secondary text-base font-bold mb-3">적성</h3>${formatAsBulletList(aptitudeText)}</div>`)
+      traitBlocks.push(`<div class="${divClass}"><h3 class="content-heading text-wiki-secondary text-base font-bold mb-3">?곸꽦</h3>${formatAsBulletList(aptitudeText)}</div>`)
   }
   }
   
-  // 흥미 (interestList 배열에서 텍스트 추출)
+  // ?λ? (interestList 諛곗뿴?먯꽌 ?띿뒪??異붿텧)
   const interestList = overviewAptitude?.interestList
   if (interestList && Array.isArray(interestList) && interestList.length > 0) {
     const interestText = interestList
@@ -4055,56 +4055,56 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       .join(', ')
     if (safeTrim(interestText)) {
     const divClass = traitBlocks.length > 0 ? 'mt-6' : ''
-      traitBlocks.push(`<div class="${divClass}"><h3 class="content-heading text-wiki-secondary text-base font-bold mb-3">흥미</h3>${formatAsBulletList(interestText)}</div>`)
+      traitBlocks.push(`<div class="${divClass}"><h3 class="content-heading text-wiki-secondary text-base font-bold mb-3">?λ?</h3>${formatAsBulletList(interestText)}</div>`)
     }
   }
   
   if (traitBlocks.length) {
-    // 적성/흥미 출처 판단
+    // ?곸꽦/?λ? 異쒖쿂 ?먮떒
     const traitSources = getFieldSources(p => p?.personality || p?.interests || p?.values)
-    pushOverviewCard('적성 및 흥미', 'fa-heart', traitBlocks.join(''), traitSources.length > 0 ? traitSources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
+    pushOverviewCard('?곸꽦 諛??λ?', 'fa-heart', traitBlocks.join(''), traitSources.length > 0 ? traitSources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
   }
 
-  // 관련 영상 (youtubeLinks) - 유튜브 임베드 카드
+  // 愿???곸긽 (youtubeLinks) - ?좏뒠釉??꾨쿋??移대뱶
   const youtubeLinks = (profile as any).youtubeLinks
   if (Array.isArray(youtubeLinks) && youtubeLinks.length > 0) {
     const validLinks = youtubeLinks.filter((l: any) => l?.url && extractYouTubeVideoId(l.url))
     if (validLinks.length > 0) {
       const ytHtml = renderYouTubeSection(validLinks)
-      pushOverviewCard('관련 영상', 'fa-video', ytHtml)
+      pushOverviewCard('愿???곸긽', 'fa-video', ytHtml)
     }
   }
 
-  // 여담 (trivia) - 리스트 형식
+  // ?щ떞 (trivia) - 由ъ뒪???뺤떇
   let triviaItems: string[] = []
   let rawTrivia = profile.trivia
-  // 방어: JSON 배열 문자열 '["텍스트"]' 형태면 파싱
+  // 諛⑹뼱: JSON 諛곗뿴 臾몄옄??'["?띿뒪??]' ?뺥깭硫??뚯떛
   if (typeof rawTrivia === 'string' && rawTrivia.trim().startsWith('[')) {
     try {
       const parsed = JSON.parse(rawTrivia)
       if (Array.isArray(parsed)) rawTrivia = parsed
-    } catch { /* 파싱 실패 시 원본 사용 */ }
+    } catch { /* ?뚯떛 ?ㅽ뙣 ???먮낯 ?ъ슜 */ }
   }
   if (Array.isArray(rawTrivia)) {
     triviaItems = (rawTrivia as any[]).filter((item: any) => typeof item === 'string' && safeTrim(item))
   } else if (typeof rawTrivia === 'string' && safeTrim(rawTrivia)) {
-    triviaItems = rawTrivia.split(/\n|•/).map((s: string) => s.trim()).filter(Boolean)
+    triviaItems = rawTrivia.split(/\n|??).map((s: string) => s.trim()).filter(Boolean)
   }
   
   if (triviaItems.length > 0) {
     const triviaList = triviaItems
       .map(item => {
         const rendered = formatRichText(item, 'trivia', footnoteMap, sourceTextMap)
-        return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${rendered}</span></li>`
+        return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${rendered}</span></li>`
       })
       .join('')
-    pushOverviewCard('여담', 'fa-comment-dots', `<ul class="space-y-2">${triviaList}</ul>`)
+    pushOverviewCard('?щ떞', 'fa-comment-dots', `<ul class="space-y-2">${triviaList}</ul>`)
   }
 
-  // Type B: 임금 (ETL 구조화 필드 overviewSalary 사용)
+  // Type B: ?꾧툑 (ETL 援ъ“???꾨뱶 overviewSalary ?ъ슜)
   const overviewSalary = profile.overviewSalary
   const salaryPrimary = overviewSalary?.sal || overviewSalary?.wage
-  const salaryAnchor = anchorIdFactory('overview', '임금 정보')
+  const salaryAnchor = anchorIdFactory('overview', '?꾧툑 ?뺣낫')
   const salarySources = getFieldSources(p => p?.overviewSalary?.sal || p?.overviewSalary?.wage || p?.salary)
   const salaryCard = renderSalaryCard(salaryPrimary, {
     anchorId: salaryAnchor,
@@ -4113,10 +4113,10 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     dataSources: salarySources
   }, footnoteMap, sourceTextMap, overviewSalary?.wage)
   if (salaryCard) {
-    overviewCards.push({ id: salaryAnchor, label: '임금 정보', icon: 'fa-coins', markup: salaryCard })
+    overviewCards.push({ id: salaryAnchor, label: '?꾧툑 ?뺣낫', icon: 'fa-coins', markup: salaryCard })
   }
 
-  // 한국의 직업지표 (ETL 구조화 필드 detailIndicators 사용) - 소개 탭
+  // ?쒓뎅??吏곸뾽吏??(ETL 援ъ“???꾨뱶 detailIndicators ?ъ슜) - ?뚭컻 ??
   const indicatorChartData = profile.detailIndicators
   if (indicatorChartData) {
     let indicatorHtml = ''
@@ -4127,25 +4127,25 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     }
     if (indicatorHtml) {
       const indicatorSources = getFieldSources(p => p?.detailIndicators)
-      pushOverviewCard('한국의 직업지표', 'fa-chart-bar', indicatorHtml, indicatorSources.length > 0 ? indicatorSources : ['GOYONG24'])
+      pushOverviewCard('?쒓뎅??吏곸뾽吏??, 'fa-chart-bar', indicatorHtml, indicatorSources.length > 0 ? indicatorSources : ['GOYONG24'])
     }
   }
 
-  // 학력·전공 분포 통합 (ETL 구조화 필드 detailEducation 사용) - 소개 탭
+  // ?숇젰쨌?꾧났 遺꾪룷 ?듯빀 (ETL 援ъ“???꾨뱶 detailEducation ?ъ슜) - ?뚭컻 ??
   const detailEducation = profile.detailEducation
   const educationDistribution = detailEducation?.educationDistribution
   const majorDistribution = detailEducation?.majorDistribution
-  const combinedAnchor = anchorIdFactory('overview', '학력·전공 분포')
+  const combinedAnchor = anchorIdFactory('overview', '?숇젰쨌?꾧났 遺꾪룷')
   const educationSources = getFieldSources(p => p?.detailEducation?.educationDistribution || p?.detailEducation?.majorDistribution)
 
-  // 통일 포맷이면 개별 렌더링, 아니면 레거시 결합 렌더링
+  // ?듭씪 ?щ㎎?대㈃ 媛쒕퀎 ?뚮뜑留? ?꾨땲硫??덇굅??寃고빀 ?뚮뜑留?
   if (isUnifiedChartData(educationDistribution) || isUnifiedChartData(majorDistribution)) {
     const parts: string[] = []
     if (isUnifiedChartData(educationDistribution)) parts.push(renderUnifiedChart(educationDistribution, 'edu-dist'))
     if (isUnifiedChartData(majorDistribution)) parts.push(renderUnifiedChart(majorDistribution, 'major-dist'))
     if (parts.length > 0) {
       const distHtml = `<div class="space-y-4">${parts.join('')}</div>`
-      overviewCards.push({ id: combinedAnchor, label: '학력·전공 분포', icon: 'fa-graduation-cap', markup: buildCard('학력·전공 분포', 'fa-graduation-cap', distHtml, { anchorId: combinedAnchor, telemetryScope: 'job-overview-card', dataSources: educationSources.length > 0 ? educationSources : ['GOYONG24'] }) })
+      overviewCards.push({ id: combinedAnchor, label: '?숇젰쨌?꾧났 遺꾪룷', icon: 'fa-graduation-cap', markup: buildCard('?숇젰쨌?꾧났 遺꾪룷', 'fa-graduation-cap', distHtml, { anchorId: combinedAnchor, telemetryScope: 'job-overview-card', dataSources: educationSources.length > 0 ? educationSources : ['GOYONG24'] }) })
     }
   } else {
     const combinedCard = renderCombinedDistributionCharts(
@@ -4159,11 +4159,11 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       }
     )
     if (combinedCard) {
-      overviewCards.push({ id: combinedAnchor, label: '학력·전공 분포', icon: 'fa-graduation-cap', markup: combinedCard })
+      overviewCards.push({ id: combinedAnchor, label: '?숇젰쨌?꾧났 遺꾪룷', icon: 'fa-graduation-cap', markup: combinedCard })
     }
   }
 
-  // 워라밸 & 사회적 기여도 (ETL 구조화 필드 detailWlb 사용) - 소개 탭
+  // ?뚮씪諛?& ?ы쉶??湲곗뿬??(ETL 援ъ“???꾨뱶 detailWlb ?ъ슜) - ?뚭컻 ??
   const detailWlb = profile.detailWlb
   const wlb = detailWlb?.wlb
   const social = detailWlb?.social
@@ -4172,7 +4172,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
   if (wlb || social || (typeof wlbDetail === 'string' && wlbDetail.trim()) || (typeof socialDetail === 'string' && socialDetail.trim())) {
     const wlbCards = []
 
-    // 워라밸 카드 (짧은 등급만)
+    // ?뚮씪諛?移대뱶 (吏㏃? ?깃툒留?
     if (wlb) {
       wlbCards.push(`
         <div class="flex items-center gap-4 p-5 rounded-2xl border border-purple-500/30 bg-purple-500/5">
@@ -4180,14 +4180,14 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
             <i class="fas fa-balance-scale text-3xl text-purple-400"></i>
           </div>
           <div class="flex-1 min-w-0">
-            <h3 class="content-heading" style="border:none; padding:0; margin-bottom:8px;">워라밸 지수</h3>
+            <h3 class="content-heading" style="border:none; padding:0; margin-bottom:8px;">?뚮씪諛?吏??/h3>
             <p class="content-text text-white leading-relaxed">${escapeHtml(wlb)}</p>
           </div>
         </div>
       `)
     }
 
-    // 사회적 기여도 카드 (짧은 등급만)
+    // ?ы쉶??湲곗뿬??移대뱶 (吏㏃? ?깃툒留?
     if (social) {
       wlbCards.push(`
         <div class="flex items-center gap-4 p-5 rounded-2xl border border-green-500/30 bg-green-500/5">
@@ -4195,34 +4195,34 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
             <i class="fas fa-hands-helping text-3xl text-green-400"></i>
           </div>
           <div class="flex-1 min-w-0">
-            <h3 class="content-heading" style="border:none; padding:0; margin-bottom:8px;">사회적 기여도</h3>
+            <h3 class="content-heading" style="border:none; padding:0; margin-bottom:8px;">?ы쉶??湲곗뿬??/h3>
             <p class="content-text text-white leading-relaxed">${escapeHtml(social)}</p>
           </div>
         </div>
       `)
     }
 
-    // 그리드 레이아웃 + 상세 설명 (wlbDetail/socialDetail)
+    // 洹몃━???덉씠?꾩썐 + ?곸꽭 ?ㅻ챸 (wlbDetail/socialDetail)
     let gridLayout = `<div class="grid grid-cols-1 lg:grid-cols-2 gap-4">${wlbCards.join('')}</div>`
 
-    // 상세 설명이 있으면 카드 아래에 표시
+    // ?곸꽭 ?ㅻ챸???덉쑝硫?移대뱶 ?꾨옒???쒖떆
     const detailBlocks: string[] = []
     if (typeof wlbDetail === 'string' && wlbDetail.trim()) {
-      detailBlocks.push(`<div><h4 class="content-heading">워라밸</h4>${formatRichText(wlbDetail, 'detailWlb.wlbDetail', footnoteMap, sourceTextMap)}</div>`)
+      detailBlocks.push(`<div><h4 class="content-heading">?뚮씪諛?/h4>${formatRichText(wlbDetail, 'detailWlb.wlbDetail', footnoteMap, sourceTextMap)}</div>`)
     }
     if (typeof socialDetail === 'string' && socialDetail.trim()) {
-      detailBlocks.push(`<div><h4 class="content-heading">사회적 기여</h4>${formatRichText(socialDetail, 'detailWlb.socialDetail', footnoteMap, sourceTextMap)}</div>`)
+      detailBlocks.push(`<div><h4 class="content-heading">?ы쉶??湲곗뿬</h4>${formatRichText(socialDetail, 'detailWlb.socialDetail', footnoteMap, sourceTextMap)}</div>`)
     }
     if (detailBlocks.length > 0) {
       gridLayout += `<div class="mt-4 space-y-4">${detailBlocks.join('')}</div>`
     }
 
-    // 워라밸/사회적 평가는 주로 고용24에서 제공
+    // ?뚮씪諛??ы쉶???됯???二쇰줈 怨좎슜24?먯꽌 ?쒓났
     const wlbSources = getFieldSources(p => p?.detailWlb?.wlb || p?.detailWlb?.social)
-    pushOverviewCard('워라밸 & 사회적 평가', 'fa-heart', gridLayout, wlbSources.length > 0 ? wlbSources : ['GOYONG24'])
+    pushOverviewCard('?뚮씪諛?& ?ы쉶???됯?', 'fa-heart', gridLayout, wlbSources.length > 0 ? wlbSources : ['GOYONG24'])
   }
 
-  // 커스텀 차트 (사용자 추가)
+  // 而ㅼ뒪? 李⑦듃 (?ъ슜??異붽?)
   const customCharts = (profile as any).customCharts
   if (Array.isArray(customCharts) && customCharts.length > 0) {
     for (let ci = 0; ci < customCharts.length; ci++) {
@@ -4230,28 +4230,28 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       if (isUnifiedChartData(cc)) {
         const ccHtml = renderUnifiedChart(cc, `custom-${ci}`)
         if (ccHtml) {
-          pushOverviewCard(cc.title || `커스텀 차트 ${ci + 1}`, 'fa-chart-bar', ccHtml)
+          pushOverviewCard(cc.title || `而ㅼ뒪? 李⑦듃 ${ci + 1}`, 'fa-chart-bar', ccHtml)
         }
       }
     }
   }
 
-  // 소개 탭 섹션 순서 재정렬
+  // ?뚭컻 ???뱀뀡 ?쒖꽌 ?ъ젙??
   const overviewSectionOrder = [
-    '주요 업무',
-    '커리어 전망',
-    '한국의 직업지표',
-    '학력·전공 분포',
-    '워라밸 & 사회적 평가',
-    '적성 및 흥미',
-    '임금 정보',
-    '관련 영상',
-    '여담'
+    '二쇱슂 ?낅Т',
+    '而ㅻ━???꾨쭩',
+    '?쒓뎅??吏곸뾽吏??,
+    '?숇젰쨌?꾧났 遺꾪룷',
+    '?뚮씪諛?& ?ы쉶???됯?',
+    '?곸꽦 諛??λ?',
+    '?꾧툑 ?뺣낫',
+    '愿???곸긽',
+    '?щ떞'
   ]
   overviewCards.sort((a, b) => {
     const aIndex = overviewSectionOrder.indexOf(a.label)
     const bIndex = overviewSectionOrder.indexOf(b.label)
-    // 순서 목록에 없는 항목은 맨 뒤로
+    // ?쒖꽌 紐⑸줉???녿뒗 ??ぉ? 留??ㅻ줈
     const aOrder = aIndex === -1 ? 999 : aIndex
     const bOrder = bIndex === -1 ? 999 : bIndex
     return aOrder - bOrder
@@ -4259,21 +4259,21 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
 
   const overviewContent = overviewCards.length
     ? `<div class="space-y-6" data-cw-job-overview>
-        ${renderSectionToc('overview', '목차', overviewCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('overview', '紐⑹감', overviewCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${overviewCards.map((card) => card.markup).join('')}
       </div>`
-    : `<p class="text-sm text-wiki-muted">소개 정보가 준비 중입니다.</p>`
+    : `<p class="text-sm text-wiki-muted">?뚭컻 ?뺣낫媛 以鍮?以묒엯?덈떎.</p>`
 
-  // ===== 업무특성 탭 시작 (ETL 구조화 필드 char* 사용) =====
+  // ===== ?낅Т?뱀꽦 ???쒖옉 (ETL 援ъ“???꾨뱶 char* ?ъ슜) =====
   const characteristicsCards: Array<{ id: string; label: string; icon: string; markup: string }> = []
   const pushCharacteristicsCard = (label: string, icon: string, markup: string, dataSources?: string[]) => {
     const id = anchorIdFactory('characteristics', label)
     characteristicsCards.push({ id, label, icon, markup: buildCard(label, icon, markup, { anchorId: id, telemetryScope: 'job-characteristics-card', dataSources }) })
   }
 
-  // 지식 (charKnowledge) - 제거됨 (뒤의 고용24 지식 비교 테이블로 대체)
+  // 吏??(charKnowledge) - ?쒓굅??(?ㅼ쓽 怨좎슜24 吏??鍮꾧탳 ?뚯씠釉붾줈 ?泥?
   
-  // 근무 환경 (charEnvironment)
+  // 洹쇰Т ?섍꼍 (charEnvironment)
   const charEnvironment = profile.charEnvironment
   if (charEnvironment?.jobsEnv && Array.isArray(charEnvironment.jobsEnv) && charEnvironment.jobsEnv.length > 0) {
     const envText = charEnvironment.jobsEnv
@@ -4282,11 +4282,11 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       .join(', ')
     if (safeTrim(envText)) {
       const envSources = getFieldSources(p => p?.charEnvironment?.jobsEnv)
-      pushCharacteristicsCard('근무 환경', 'fa-building', formatRichText(envText), envSources.length > 0 ? envSources : ['GOYONG24'])
+      pushCharacteristicsCard('洹쇰Т ?섍꼍', 'fa-building', formatRichText(envText), envSources.length > 0 ? envSources : ['GOYONG24'])
   }
   }
 
-  // 업무 활동 중요도/수준 (charStatus - actv 데이터)
+  // ?낅Т ?쒕룞 以묒슂???섏? (charStatus - actv ?곗씠??
   const charStatus = profile.charStatus
   if (charStatus && (charStatus.jobActvImprtnc || charStatus.jobActvLvl)) {
     const activityBlocks: string[] = []
@@ -4297,7 +4297,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         .filter(Boolean)
         .join(', ')
       if (safeTrim(impText)) {
-        activityBlocks.push(`<div><h3 class="content-heading text-wiki-muted uppercase tracking-wide font-semibold mb-2">활동 중요도</h3>${formatRichText(impText)}</div>`)
+        activityBlocks.push(`<div><h3 class="content-heading text-wiki-muted uppercase tracking-wide font-semibold mb-2">?쒕룞 以묒슂??/h3>${formatRichText(impText)}</div>`)
       }
     }
     
@@ -4307,38 +4307,38 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
         .filter(Boolean)
         .join(', ')
       if (safeTrim(lvlText)) {
-        activityBlocks.push(`<div class="${activityBlocks.length > 0 ? 'mt-4' : ''}"><h3 class="content-heading text-wiki-muted uppercase tracking-wide font-semibold mb-2">활동 수준</h3>${formatRichText(lvlText)}</div>`)
+        activityBlocks.push(`<div class="${activityBlocks.length > 0 ? 'mt-4' : ''}"><h3 class="content-heading text-wiki-muted uppercase tracking-wide font-semibold mb-2">?쒕룞 ?섏?</h3>${formatRichText(lvlText)}</div>`)
       }
     }
     
     if (activityBlocks.length > 0) {
       const statusSources = getFieldSources(p => p?.charStatus)
-      pushCharacteristicsCard('업무 수행 지표', 'fa-chart-area', activityBlocks.join(''), statusSources.length > 0 ? statusSources : ['GOYONG24'])
+      pushCharacteristicsCard('?낅Т ?섑뻾 吏??, 'fa-chart-area', activityBlocks.join(''), statusSources.length > 0 ? statusSources : ['GOYONG24'])
     }
   }
 
-  // 흥미 (charInterest) - 제거됨 (뒤의 고용24 흥미 비교 테이블로 대체)
+  // ?λ? (charInterest) - ?쒓굅??(?ㅼ쓽 怨좎슜24 ?λ? 鍮꾧탳 ?뚯씠釉붾줈 ?泥?
   
-  // 가치관 (charValues) - 제거됨 (뒤의 고용24 가치관 비교 테이블로 대체)
+  // 媛移섍? (charValues) - ?쒓굅??(?ㅼ쓽 怨좎슜24 媛移섍? 鍮꾧탳 ?뚯씠釉붾줈 ?泥?
 
-  // 한국의 직업지표, 학력·전공 분포, 워라밸 & 사회적 평가는 소개 탭으로 이동됨 (line 3868~)
+  // ?쒓뎅??吏곸뾽吏?? ?숇젰쨌?꾧났 遺꾪룷, ?뚮씪諛?& ?ы쉶???됯????뚭컻 ??쑝濡??대룞??(line 3868~)
   
-  // 직업 준비하기 (ETL 구조화 필드 detailReady 사용)
+  // 吏곸뾽 以鍮꾪븯湲?(ETL 援ъ“???꾨뱶 detailReady ?ъ슜)
   const detailReady = profile.detailReady
   
-  // 필요기술 및 지식은 overviewAbilities.technKnow로 이동됨 (개요 탭에서 표시)
+  // ?꾩슂湲곗닠 諛?吏?앹? overviewAbilities.technKnow濡??대룞??(媛쒖슂 ??뿉???쒖떆)
   
-  // 5.5. 직업 준비하기
+  // 5.5. 吏곸뾽 以鍮꾪븯湲?
   {
     const readyBlocks: string[] = []
 
-    // 사용자 기여 way 텍스트 (인라인 각주 포함) — 맨 위에 표시
+    // ?ъ슜??湲곗뿬 way ?띿뒪??(?몃씪??媛곸＜ ?ы븿) ??留??꾩뿉 ?쒖떆
     const userWay = (profile as any).way as string | undefined
     if (userWay && safeTrim(userWay)) {
-      readyBlocks.push(`<div><h3 class="content-heading">되는 방법</h3>${formatRichText(userWay, 'way', footnoteMap, sourceTextMap)}</div>`)
+      readyBlocks.push(`<div><h3 class="content-heading">?섎뒗 諛⑸쾿</h3>${formatRichText(userWay, 'way', footnoteMap, sourceTextMap)}</div>`)
     }
 
-    // 헬퍼: 객체 또는 문자열에서 텍스트 추출
+    // ?ы띁: 媛앹껜 ?먮뒗 臾몄옄?댁뿉???띿뒪??異붿텧
     const extractReadyItem = (item: any, key: string): string => {
       if (typeof item === 'string') return item
       if (item && typeof item === 'object') return item[key] || item.name || item.value || item.text || ''
@@ -4346,23 +4346,23 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     }
 
   if (detailReady) {
-    // 정규 교육과정 (제일 먼저 표시)
+    // ?뺢퇋 援먯쑁怨쇱젙 (?쒖씪 癒쇱? ?쒖떆)
     if (detailReady.curriculum && Array.isArray(detailReady.curriculum) && detailReady.curriculum.length > 0) {
       const currList = detailReady.curriculum
         .map(item => extractReadyItem(item, 'curriculum'))
         .filter(text => !!safeTrim(text))
-        .map(text => `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${applyInlineFootnotes(text, 'detailReady.curriculum', footnoteMap, sourceTextMap)}</span></li>`)
+        .map(text => `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${applyInlineFootnotes(text, 'detailReady.curriculum', footnoteMap, sourceTextMap)}</span></li>`)
         .join('')
       if (currList) {
-        readyBlocks.push(`<div><h3 class="content-heading">정규 교육과정</h3><ul class="space-y-2">${currList}</ul></div>`)
+        readyBlocks.push(`<div><h3 class="content-heading">?뺢퇋 援먯쑁怨쇱젙</h3><ul class="space-y-2">${currList}</ul></div>`)
       }
     }
     
-    // 채용 정보 — linkList {text, url} 객체 또는 string 지원
+    // 梨꾩슜 ?뺣낫 ??linkList {text, url} 媛앹껜 ?먮뒗 string 吏??
     if (detailReady.recruit && Array.isArray(detailReady.recruit) && detailReady.recruit.length > 0) {
       const recruitList = detailReady.recruit
         .map(item => {
-          // {text, url} 객체인 경우 — 직접 링크 렌더링
+          // {text, url} 媛앹껜??寃쎌슦 ??吏곸젒 留곹겕 ?뚮뜑留?
           if (item && typeof item === 'object' && item.text) {
             const text = safeTrim(item.text) || ''
             if (!text) return ''
@@ -4371,54 +4371,54 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
               let domain = ''
               try { domain = new URL(url).hostname.replace('www.', '') } catch {}
               const domainHtml = domain ? ` <span class="text-wiki-muted text-xs">(${escapeHtml(domain)})</span>` : ''
-              return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span><a href="${escapeHtml(url)}" target="_blank" rel="noopener noreferrer" class="text-wiki-primary hover:underline">${applyInlineFootnotes(text, 'detailReady.recruit', footnoteMap, sourceTextMap)}</a>${domainHtml}</span></li>`
+              return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span><a href="${escapeHtml(url)}" target="_blank" rel="noopener noreferrer" class="text-wiki-primary hover:underline">${applyInlineFootnotes(text, 'detailReady.recruit', footnoteMap, sourceTextMap)}</a>${domainHtml}</span></li>`
             }
-            return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${applyInlineFootnotes(text, 'detailReady.recruit', footnoteMap, sourceTextMap)}</span></li>`
+            return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${applyInlineFootnotes(text, 'detailReady.recruit', footnoteMap, sourceTextMap)}</span></li>`
           }
-          // string인 경우 — plain text + 인라인 각주 [N] 렌더링
+          // string??寃쎌슦 ??plain text + ?몃씪??媛곸＜ [N] ?뚮뜑留?
           const text = extractReadyItem(item, 'recruit')
           if (!safeTrim(text)) return ''
-          return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${applyInlineFootnotes(text, 'detailReady.recruit', footnoteMap, sourceTextMap)}</span></li>`
+          return `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${applyInlineFootnotes(text, 'detailReady.recruit', footnoteMap, sourceTextMap)}</span></li>`
         })
         .filter(html => !!html)
         .join('')
       if (recruitList) {
-        readyBlocks.push(`<div class="${readyBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">채용 정보</h3><ul class="space-y-2">${recruitList}</ul></div>`)
+        readyBlocks.push(`<div class="${readyBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">梨꾩슜 ?뺣낫</h3><ul class="space-y-2">${recruitList}</ul></div>`)
       }
     }
     
-    // 자격증
+    // ?먭꺽利?
     if (detailReady.certificate && Array.isArray(detailReady.certificate) && detailReady.certificate.length > 0) {
       const certList = detailReady.certificate
         .map(item => extractReadyItem(item, 'certificate'))
         .filter(text => !!safeTrim(text))
-        .map(text => `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${applyInlineFootnotes(text, 'detailReady.certificate', footnoteMap, sourceTextMap)}</span></li>`)
+        .map(text => `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${applyInlineFootnotes(text, 'detailReady.certificate', footnoteMap, sourceTextMap)}</span></li>`)
         .join('')
       if (certList) {
-        readyBlocks.push(`<div class="${readyBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">추천 자격증</h3><ul class="space-y-2">${certList}</ul></div>`)
+        readyBlocks.push(`<div class="${readyBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">異붿쿇 ?먭꺽利?/h3><ul class="space-y-2">${certList}</ul></div>`)
       }
     }
     
-    // 교육/훈련
+    // 援먯쑁/?덈젴
     if (detailReady.training && Array.isArray(detailReady.training) && detailReady.training.length > 0) {
       const trainingList = detailReady.training
         .map(item => extractReadyItem(item, 'training'))
         .filter(text => !!safeTrim(text))
-        .map(text => `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${applyInlineFootnotes(text, 'detailReady.training', footnoteMap, sourceTextMap)}</span></li>`)
+        .map(text => `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${applyInlineFootnotes(text, 'detailReady.training', footnoteMap, sourceTextMap)}</span></li>`)
         .join('')
       if (trainingList) {
-        readyBlocks.push(`<div class="${readyBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">필요 교육/훈련</h3><ul class="space-y-2">${trainingList}</ul></div>`)
+        readyBlocks.push(`<div class="${readyBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">?꾩슂 援먯쑁/?덈젴</h3><ul class="space-y-2">${trainingList}</ul></div>`)
       }
     }
     
-    // 진로 탐색 활동
+    // 吏꾨줈 ?먯깋 ?쒕룞
     const researchList = detailReady.researchList
     if (researchList && Array.isArray(researchList) && researchList.length > 0) {
       const bullets = renderResearchBullets(researchList)
       if (bullets) {
         readyBlocks.push(
           `<div class="${readyBlocks.length > 0 ? 'mt-8' : ''}">
-            <h3 class="content-heading">진로 탐색 활동</h3>
+            <h3 class="content-heading">吏꾨줈 ?먯깋 ?쒕룞</h3>
             ${bullets}
           </div>`
         )
@@ -4428,24 +4428,24 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     } // end if (detailReady)
 
     if (readyBlocks.length > 0) {
-      // 직업 준비하기 출처 판단
+      // 吏곸뾽 以鍮꾪븯湲?異쒖쿂 ?먮떒
       const readySources = getFieldSources(p => p?.detailReady?.curriculum || p?.detailReady?.training || p?.way)
-      pushDetailCard('직업 준비하기', 'fa-clipboard-check', readyBlocks.join(''), readySources.length > 0 ? readySources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
+      pushDetailCard('吏곸뾽 以鍮꾪븯湲?, 'fa-clipboard-check', readyBlocks.join(''), readySources.length > 0 ? readySources : normalizedProfileSources.filter(s => s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_DJOB'))
     }
   }
 
-  // ── 커리어트리 섹션 ──
+  // ?? 而ㅻ━?댄듃由??뱀뀡 ??
   if (careerTrees && careerTrees.length > 0) {
     const jobSlug = (profile as any).slug || composeDetailSlug('job', profile.name, profile.id)
     const careerTreeBody = renderCareerTreeSection(careerTrees, jobSlug)
     if (careerTreeBody) {
-      pushDetailCard('커리어트리', 'fa-route', careerTreeBody)
+      pushDetailCard('而ㅻ━?댄듃由?, 'fa-route', careerTreeBody)
     }
   }
 
-  // 한국의 직업지표는 개요 탭으로 이동 (중복 제거)
+  // ?쒓뎅??吏곸뾽吏?쒕뒗 媛쒖슂 ??쑝濡??대룞 (以묐났 ?쒓굅)
 
-  // 1. Type C: 업무수행능력 분석 (고용24 원본 필드 → merged profile)
+  // 1. Type C: ?낅Т?섑뻾?λ젰 遺꾩꽍 (怨좎슜24 ?먮낯 ?꾨뱶 ??merged profile)
   const abilityComparison =
     profile.goyong24Only?.workEnvironment ||
     profile.ablKnwEnv
@@ -4462,41 +4462,41 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       (abilityLevel && (abilityLevel.withinJob || abilityLevel.betweenJobs)))) {
     const abilityBlocks = []
     
-    // 업무수행능력 중요도
+    // ?낅Т?섑뻾?λ젰 以묒슂??
     if (abilityImportance && (abilityImportance.withinJob || abilityImportance.betweenJobs)) {
       const importanceHtml = renderComparisonTable(
         abilityImportance.withinJob,
         abilityImportance.betweenJobs,
-        '업무수행능력',
+        '?낅Т?섑뻾?λ젰',
         'fa-brain',
         'importance'
       )
       if (importanceHtml) {
-        abilityBlocks.push(`<div><h3 class="content-heading">업무수행능력 중요도</h3>${importanceHtml}</div>`)
+        abilityBlocks.push(`<div><h3 class="content-heading">?낅Т?섑뻾?λ젰 以묒슂??/h3>${importanceHtml}</div>`)
       }
     }
     
-    // 업무수행능력 수준
+    // ?낅Т?섑뻾?λ젰 ?섏?
     if (abilityLevel && (abilityLevel.withinJob || abilityLevel.betweenJobs)) {
       const levelHtml = renderComparisonTable(
         abilityLevel.withinJob,
         abilityLevel.betweenJobs,
-        '업무수행능력',
+        '?낅Т?섑뻾?λ젰',
         'fa-star-half-alt',
         'level'
       )
       if (levelHtml) {
-        abilityBlocks.push(`<div class="${abilityBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">업무수행능력 수준</h3>${levelHtml}</div>`)
+        abilityBlocks.push(`<div class="${abilityBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">?낅Т?섑뻾?λ젰 ?섏?</h3>${levelHtml}</div>`)
       }
     }
     
     if (abilityBlocks.length > 0) {
       const abilitySources = getFieldSources(p => p?.abilities || p?.goyong24Only?.workEnvironment)
-      pushCharacteristicsCard('업무수행능력', 'fa-brain', abilityBlocks.join(''), abilitySources.length > 0 ? abilitySources : ['GOYONG24'])
+      pushCharacteristicsCard('?낅Т?섑뻾?λ젰', 'fa-brain', abilityBlocks.join(''), abilitySources.length > 0 ? abilitySources : ['GOYONG24'])
     }
   }
 
-  // 2. Type C: 지식 분석
+  // 2. Type C: 吏??遺꾩꽍
   const knowledgeComparison = abilityComparison
   const knowledgeImportance = knowledgeComparison ? {
     withinJob: knowledgeComparison.KnwldgCmpr,
@@ -4508,7 +4508,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
   } : null
   const knowledgeSimpleData = profile.knowledge
   
-  // detailedComparison 또는 simple 데이터가 있으면 지식 섹션 표시
+  // detailedComparison ?먮뒗 simple ?곗씠?곌? ?덉쑝硫?吏???뱀뀡 ?쒖떆
   const hasKnowledgeDetailed = (knowledgeImportance && (knowledgeImportance.withinJob || knowledgeImportance.betweenJobs)) ||
                                 (knowledgeLevel && (knowledgeLevel.withinJob || knowledgeLevel.betweenJobs))
   const hasKnowledgeSimple = knowledgeSimpleData && Array.isArray(knowledgeSimpleData) && knowledgeSimpleData.length > 0
@@ -4516,40 +4516,40 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
   if (hasKnowledgeDetailed || hasKnowledgeSimple) {
     const knowledgeBlocks = []
     
-    // 지식 중요도
+    // 吏??以묒슂??
     if (knowledgeImportance && (knowledgeImportance.withinJob || knowledgeImportance.betweenJobs)) {
       const importanceHtml = renderComparisonTable(
         knowledgeImportance.withinJob,
         knowledgeImportance.betweenJobs,
-        '지식',
+        '吏??,
         'fa-book',
         'importance'
       )
       if (importanceHtml) {
-        knowledgeBlocks.push(`<div><h3 class="content-heading">지식 중요도</h3>${importanceHtml}</div>`)
+        knowledgeBlocks.push(`<div><h3 class="content-heading">吏??以묒슂??/h3>${importanceHtml}</div>`)
       }
     }
     
-    // 지식 수준
+    // 吏???섏?
     if (knowledgeLevel && (knowledgeLevel.withinJob || knowledgeLevel.betweenJobs)) {
       const levelHtml = renderComparisonTable(
         knowledgeLevel.withinJob,
         knowledgeLevel.betweenJobs,
-        '지식',
+        '吏??,
         'fa-layer-group',
         'level'
       )
       if (levelHtml) {
-        knowledgeBlocks.push(`<div class="${knowledgeBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">지식 수준</h3>${levelHtml}</div>`)
+        knowledgeBlocks.push(`<div class="${knowledgeBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">吏???섏?</h3>${levelHtml}</div>`)
       }
     }
     
-    // simple 데이터가 있고 detailed 데이터가 없을 때 simple 데이터를 표로 표시
+    // simple ?곗씠?곌? ?덇퀬 detailed ?곗씠?곌? ?놁쓣 ??simple ?곗씠?곕? ?쒕줈 ?쒖떆
     if ((!knowledgeImportance || (!knowledgeImportance.withinJob && !knowledgeImportance.betweenJobs)) &&
         (!knowledgeLevel || (!knowledgeLevel.withinJob && !knowledgeLevel.betweenJobs)) &&
         knowledgeSimpleData && Array.isArray(knowledgeSimpleData) && knowledgeSimpleData.length > 0) {
       
-      // 커리어넷 지식 데이터를 표로 변환
+      // 而ㅻ━?대꽬 吏???곗씠?곕? ?쒕줈 蹂??
       const validItems = knowledgeSimpleData
         .map((item: any) => {
           const name = item.knowledge || item.list_content || item.knowledge_name || item.inform || ''
@@ -4582,7 +4582,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
               id="${tableId}-toggle"
               class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-wiki-primary hover:text-wiki-secondary border border-wiki-primary/50 hover:border-wiki-secondary/50 rounded-lg transition-all cursor-pointer"
             >
-              <span id="${tableId}-text">전체보기 (${validItems.length - 5}개 항목)</span>
+              <span id="${tableId}-text">?꾩껜蹂닿린 (${validItems.length - 5}媛???ぉ)</span>
               <i id="${tableId}-icon" class="fas fa-chevron-down transition-transform"></i>
             </div>
           </div>
@@ -4594,8 +4594,8 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
               <table class="w-full" id="${tableId}">
                 <thead class="bg-wiki-card/50 border-b-2 border-wiki-primary/30">
                   <tr>
-                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="width: 120px; font-size: 16px;">중요도</th>
-                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="font-size: 16px;">지식</th>
+                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="width: 120px; font-size: 16px;">以묒슂??/th>
+                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="font-size: 16px;">吏??/th>
                   </tr>
                 </thead>
                 <tbody>
@@ -4606,17 +4606,17 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
             ${toggleIndicator}
           </div>
         `
-        knowledgeBlocks.push(`<div><h3 class="content-heading">필수 지식 중요도</h3>${knowledgeTable}</div>`)
+        knowledgeBlocks.push(`<div><h3 class="content-heading">?꾩닔 吏??以묒슂??/h3>${knowledgeTable}</div>`)
       }
     }
     
     if (knowledgeBlocks.length > 0) {
       const knowledgeSources = getFieldSources(p => p?.knowledge || p?.goyong24Only?.workEnvironment)
-      pushCharacteristicsCard('지식', 'fa-book', knowledgeBlocks.join(''), knowledgeSources.length > 0 ? knowledgeSources : ['GOYONG24'])
+      pushCharacteristicsCard('吏??, 'fa-book', knowledgeBlocks.join(''), knowledgeSources.length > 0 ? knowledgeSources : ['GOYONG24'])
     }
   }
 
-  // 3. Type D: 업무환경 분석
+  // 3. Type D: ?낅Т?섍꼍 遺꾩꽍
   const workEnvironmentData =
     abilityComparison ||
     profile.goyong24Only?.workEnvironment ||
@@ -4629,22 +4629,22 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       (workContext && Array.isArray(workContext) && workContext.length > 0)) {
     const workEnvBlocks = []
     
-    // 고용24 업무환경 데이터 (직업 내/간 비교)
+    // 怨좎슜24 ?낅Т?섍꼍 ?곗씠??(吏곸뾽 ??媛?鍮꾧탳)
     if (workEnvironmentData && (workEnvironmentData.jobsEnvCmpr || workEnvironmentData.jobsEnv)) {
       const workEnvHtml = renderComparisonTable(
-        workEnvironmentData.jobsEnvCmpr, // 직업 내 비교
-        workEnvironmentData.jobsEnv,     // 직업 간 비교
-        '업무환경',
+        workEnvironmentData.jobsEnvCmpr, // 吏곸뾽 ??鍮꾧탳
+        workEnvironmentData.jobsEnv,     // 吏곸뾽 媛?鍮꾧탳
+        '?낅Т?섍꼍',
         'fa-building'
       )
       if (workEnvHtml) {
-        workEnvBlocks.push(`<div><h3 class="content-heading">업무환경 조건</h3>${workEnvHtml}</div>`)
+        workEnvBlocks.push(`<div><h3 class="content-heading">?낅Т?섍꼍 議곌굔</h3>${workEnvHtml}</div>`)
       }
     }
     
-    // 커리어넷 업무환경 데이터 (중요도 표)
+    // 而ㅻ━?대꽬 ?낅Т?섍꼍 ?곗씠??(以묒슂????
     if (workContext && Array.isArray(workContext) && workContext.length > 0) {
-      // 유효한 데이터만 필터링
+      // ?좏슚???곗씠?곕쭔 ?꾪꽣留?
       const validItems = workContext
         .map((item) => {
           const name = item.environment || item.inform || ''
@@ -4677,7 +4677,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
               id="${tableId}-toggle"
               class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-wiki-primary hover:text-wiki-secondary border border-wiki-primary/50 hover:border-wiki-secondary/50 rounded-lg transition-all cursor-pointer"
             >
-              <span id="${tableId}-text">전체보기 (${validItems.length - 5}개 항목)</span>
+              <span id="${tableId}-text">?꾩껜蹂닿린 (${validItems.length - 5}媛???ぉ)</span>
               <i id="${tableId}-icon" class="fas fa-chevron-down transition-transform"></i>
             </div>
           </div>
@@ -4689,8 +4689,8 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
               <table class="w-full" id="${tableId}">
                 <thead class="bg-wiki-card/50 border-b-2 border-wiki-primary/30">
                   <tr>
-                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="width: 120px; font-size: 16px;">중요도</th>
-                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="font-size: 16px;">업무환경</th>
+                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="width: 120px; font-size: 16px;">以묒슂??/th>
+                    <th class="px-2 py-2 md:px-4 md:py-3 text-center font-semibold text-wiki-text" style="font-size: 16px;">?낅Т?섍꼍</th>
                   </tr>
                 </thead>
                 <tbody>
@@ -4701,17 +4701,17 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
             ${toggleIndicator}
           </div>
         `
-        workEnvBlocks.push(`<div class="${workEnvBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">업무환경 중요도</h3>${contextTable}</div>`)
+        workEnvBlocks.push(`<div class="${workEnvBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">?낅Т?섍꼍 以묒슂??/h3>${contextTable}</div>`)
       }
     }
     
     if (workEnvBlocks.length > 0) {
-      pushCharacteristicsCard('업무환경', 'fa-building', workEnvBlocks.join(''), ['GOYONG24'])
+      pushCharacteristicsCard('?낅Т?섍꼍', 'fa-building', workEnvBlocks.join(''), ['GOYONG24'])
     }
   }
 
   
-  // 4. Type D: 고용24 전용 - 성격 특성 비교
+  // 4. Type D: 怨좎슜24 ?꾩슜 - ?깃꺽 ?뱀꽦 鍮꾧탳
   const personalityData =
     profile.goyong24Only?.personality ||
     profile.chrIntrVals
@@ -4719,15 +4719,15 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     const personalityHtml = renderComparisonTable(
       personalityData.jobChrCmpr,
       personalityData.jobChr,
-      '성격',
+      '?깃꺽',
       'fa-user-check'
     )
     if (personalityHtml) {
-      pushCharacteristicsCard('성격', 'fa-user-check', personalityHtml, ['GOYONG24'])
+      pushCharacteristicsCard('?깃꺽', 'fa-user-check', personalityHtml, ['GOYONG24'])
     }
   }
 
-  // 5. Type D: 고용24 전용 - 흥미 분야 비교
+  // 5. Type D: 怨좎슜24 ?꾩슜 - ?λ? 遺꾩빞 鍮꾧탳
   const interestData =
     profile.goyong24Only?.interest ||
     profile.chrIntrVals
@@ -4735,15 +4735,15 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     const interestHtml = renderComparisonTable(
       interestData.jobIntrstCmpr,
       interestData.jobIntrst,
-      '흥미',
+      '?λ?',
       'fa-heart'
     )
     if (interestHtml) {
-      pushCharacteristicsCard('흥미', 'fa-heart', interestHtml, ['GOYONG24'])
+      pushCharacteristicsCard('?λ?', 'fa-heart', interestHtml, ['GOYONG24'])
     }
   }
 
-  // 6. Type D: 고용24 전용 - 가치관 비교
+  // 6. Type D: 怨좎슜24 ?꾩슜 - 媛移섍? 鍮꾧탳
   const valuesData =
     profile.goyong24Only?.values ||
     profile.chrIntrVals
@@ -4751,15 +4751,15 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     const valuesHtml = renderComparisonTable(
       valuesData.jobValsCmpr,
       valuesData.jobVals,
-      '가치관',
+      '媛移섍?',
       'fa-star'
     )
     if (valuesHtml) {
-      pushCharacteristicsCard('가치관', 'fa-star', valuesHtml, ['GOYONG24'])
+      pushCharacteristicsCard('媛移섍?', 'fa-star', valuesHtml, ['GOYONG24'])
     }
   }
 
-  // 7. Type D: 고용24 전용 - 업무활동 분석
+  // 7. Type D: 怨좎슜24 ?꾩슜 - ?낅Т?쒕룞 遺꾩꽍
   const activityData =
     profile.goyong24Only?.activity ||
     profile.actv
@@ -4776,36 +4776,36 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       (activityLevel && (activityLevel.withinJob || activityLevel.betweenJobs))) {
     const activityBlocks = []
     
-    // 업무활동 중요도
+    // ?낅Т?쒕룞 以묒슂??
     if (activityImportance && (activityImportance.withinJob || activityImportance.betweenJobs)) {
       const importanceHtml = renderComparisonTable(
         activityImportance.withinJob,
         activityImportance.betweenJobs,
-        '업무활동',
+        '?낅Т?쒕룞',
         'fa-chart-line',
         'importance'
       )
       if (importanceHtml) {
-        activityBlocks.push(`<div><h3 class="content-heading">업무활동 중요도</h3>${importanceHtml}</div>`)
+        activityBlocks.push(`<div><h3 class="content-heading">?낅Т?쒕룞 以묒슂??/h3>${importanceHtml}</div>`)
       }
     }
     
-    // 업무활동 수준
+    // ?낅Т?쒕룞 ?섏?
     if (activityLevel && (activityLevel.withinJob || activityLevel.betweenJobs)) {
       const levelHtml = renderComparisonTable(
         activityLevel.withinJob,
         activityLevel.betweenJobs,
-        '업무활동',
+        '?낅Т?쒕룞',
         'fa-signal',
         'level'
       )
       if (levelHtml) {
-        activityBlocks.push(`<div class="${activityBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">업무활동 수준</h3>${levelHtml}</div>`)
+        activityBlocks.push(`<div class="${activityBlocks.length > 0 ? 'mt-8' : ''}"><h3 class="content-heading">?낅Т?쒕룞 ?섏?</h3>${levelHtml}</div>`)
       }
     }
     
     if (activityBlocks.length > 0) {
-      pushCharacteristicsCard('업무활동', 'fa-tasks', activityBlocks.join(''), ['GOYONG24'])
+      pushCharacteristicsCard('?낅Т?쒕룞', 'fa-tasks', activityBlocks.join(''), ['GOYONG24'])
     }
   }
 
@@ -4816,11 +4816,11 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
             <span class="flex items-center justify-center w-9 h-9 md:w-10 md:h-10 rounded-lg bg-wiki-primary/20 flex-shrink-0">
               <i class="fas fa-chart-pie text-wiki-primary text-base md:text-lg" aria-hidden="true"></i>
             </span>
-            업무특성
+            ?낅Т?뱀꽦
           </h2>
-          <p class="text-sm md:text-base text-wiki-muted leading-relaxed px-0.5">직업 수행에 필요한 능력, 지식, 성격 등 상세 분석</p>
+          <p class="text-sm md:text-base text-wiki-muted leading-relaxed px-0.5">吏곸뾽 ?섑뻾???꾩슂???λ젰, 吏?? ?깃꺽 ???곸꽭 遺꾩꽍</p>
         </div>
-        ${renderSectionToc('characteristics', '목차', characteristicsCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('characteristics', '紐⑹감', characteristicsCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${characteristicsCards.map((card) => card.markup).join('')}
       </div>
       <script>
@@ -4831,40 +4831,40 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
           const toggleIcon = document.getElementById(tableId + '-icon');
           if (!table || !toggleButton) return;
           
-          // data-expandable-row 속성을 가진 행들만 선택 (5개 이후의 행들)
+          // data-expandable-row ?띿꽦??媛吏??됰뱾留??좏깮 (5媛??댄썑???됰뱾)
           const expandableRows = table.querySelectorAll('tr[data-expandable-row]');
-          if (expandableRows.length === 0) return; // 5개 이하면 토글 필요 없음
+          if (expandableRows.length === 0) return; // 5媛??댄븯硫??좉? ?꾩슂 ?놁쓬
           
           const hiddenRows = table.querySelectorAll('tr[data-expandable-row].hidden');
           const isExpanded = hiddenRows.length === 0;
           
-          // 펼쳐질 항목 수 (항상 5개는 보이므로 expandable 행들만 카운트)
+          // ?쇱퀜吏???ぉ ??(??긽 5媛쒕뒗 蹂댁씠誘濡?expandable ?됰뱾留?移댁슫??
           const expandableCount = expandableRows.length;
           
-          // 전체 항목 수 (고정 5개 + expandable)
+          // ?꾩껜 ??ぉ ??(怨좎젙 5媛?+ expandable)
           const totalCount = 5 + expandableCount;
           
           if (isExpanded) {
-            // 접기 - expandable 행들만 숨기기
+            // ?묎린 - expandable ?됰뱾留??④린湲?
             expandableRows.forEach(row => {
               row.classList.add('hidden');
             });
             
-            // 버튼 상태 업데이트
+            // 踰꾪듉 ?곹깭 ?낅뜲?댄듃
             if (toggleText) {
-              toggleText.textContent = '전체보기 (' + expandableCount + '개 항목)';
+              toggleText.textContent = '?꾩껜蹂닿린 (' + expandableCount + '媛???ぉ)';
             }
             if (toggleIcon) {
               toggleIcon.classList.remove('fa-chevron-up');
               toggleIcon.classList.add('fa-chevron-down');
             }
             
-            // 항목이 10개를 초과하는 경우에만 스크롤 이동
+            // ??ぉ??10媛쒕? 珥덇낵?섎뒗 寃쎌슦?먮쭔 ?ㅽ겕濡??대룞
             if (totalCount > 10) {
-              // 접기 전에 테이블 컨테이너의 위치를 저장
+              // ?묎린 ?꾩뿉 ?뚯씠釉?而⑦뀒?대꼫???꾩튂瑜????
               let tableContainer = table.closest('div[class*="bg-white"]');
               if (!tableContainer) {
-                // 다른 방법으로 컨테이너 찾기
+                // ?ㅻⅨ 諛⑸쾿?쇰줈 而⑦뀒?대꼫 李얘린
                 tableContainer = table.closest('div[class*="rounded"]');
               }
               
@@ -4873,19 +4873,19 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
                 const scrollY = window.scrollY || window.pageYOffset;
                 const tableY = tableRect.top + scrollY;
                 
-                // 테이블 시작 위치로 즉시 스크롤 (부드러운 효과 없음)
-                window.scrollTo(0, tableY - 120); // 테이블 위치에서 120px 위로 (여유 공간)
+                // ?뚯씠釉??쒖옉 ?꾩튂濡?利됱떆 ?ㅽ겕濡?(遺?쒕윭???④낵 ?놁쓬)
+                window.scrollTo(0, tableY - 120); // ?뚯씠釉??꾩튂?먯꽌 120px ?꾨줈 (?ъ쑀 怨듦컙)
               }
             }
           } else {
-            // 펼치기 - expandable 행들 보이기
+            // ?쇱튂湲?- expandable ?됰뱾 蹂댁씠湲?
             expandableRows.forEach(row => {
               row.classList.remove('hidden');
             });
             
-            // 버튼 상태 업데이트
+            // 踰꾪듉 ?곹깭 ?낅뜲?댄듃
             if (toggleText) {
-              toggleText.textContent = '접기';
+              toggleText.textContent = '?묎린';
             }
             if (toggleIcon) {
               toggleIcon.classList.remove('fa-chevron-down');
@@ -4894,21 +4894,21 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
           }
         }
       </script>`
-    : `<p class="text-sm text-wiki-muted">업무특성 정보가 준비 중입니다.</p>`
-  // ===== 업무특성 탭 끝 =====
+    : `<p class="text-sm text-wiki-muted">?낅Т?뱀꽦 ?뺣낫媛 以鍮?以묒엯?덈떎.</p>`
+  // ===== ?낅Т?뱀꽦 ????=====
 
-  // 직업 분류 체계 섹션 제거됨 (히어로 섹션 브레드크럼으로 이동)
-  // 한국표준직업분류 코드 섹션 제거됨
+  // 吏곸뾽 遺꾨쪟 泥닿퀎 ?뱀뀡 ?쒓굅??(?덉뼱濡??뱀뀡 釉뚮젅?쒗겕?쇱쑝濡??대룞)
+  // ?쒓뎅?쒖?吏곸뾽遺꾨쪟 肄붾뱶 ?뱀뀡 ?쒓굅??
   // const kecoList = renderKecoCodeList(profile)
   // if (kecoList) {
-  //   pushDetailCard('한국표준직업분류 코드', 'fa-layer-group', kecoList)
+  //   pushDetailCard('?쒓뎅?쒖?吏곸뾽遺꾨쪟 肄붾뱶', 'fa-layer-group', kecoList)
   // }
 
-  // 과정 탭 섹션 순서 재정렬
+  // 怨쇱젙 ???뱀뀡 ?쒖꽌 ?ъ젙??
   const detailSectionOrder = [
-    '핵심 능력·자격',
-    '직업 준비하기',
-    '커리어트리'
+    '?듭떖 ?λ젰쨌?먭꺽',
+    '吏곸뾽 以鍮꾪븯湲?,
+    '而ㅻ━?댄듃由?
   ]
   detailCards.sort((a, b) => {
     const aIndex = detailSectionOrder.indexOf(a.label)
@@ -4920,26 +4920,26 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
 
   const detailContent = detailCards.length
     ? `<div class="space-y-6" data-cw-job-details>
-        ${renderSectionToc('details', '목차', detailCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('details', '紐⑹감', detailCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${detailCards.map((card) => card.markup).join('')}
       </div>`
-    : `<p class="text-sm text-wiki-muted">상세 정보가 준비 중입니다.</p>`
+    : `<p class="text-sm text-wiki-muted">?곸꽭 ?뺣낫媛 以鍮?以묒엯?덈떎.</p>`
 
-  // 탭이 비어있으면 숨기기 (내용이 없거나 "준비 중" 메시지만 있는 경우)
-  const hasOverviewContent = overviewContent && !overviewContent.includes('준비 중') && overviewContent.trim().length > 50
-  const hasDetailContent = detailContent && !detailContent.includes('준비 중') && detailContent.trim().length > 50
-  const hasCharacteristicsContent = characteristicsContent && !characteristicsContent.includes('준비 중') && characteristicsContent.trim().length > 50
+  // ??씠 鍮꾩뼱?덉쑝硫??④린湲?(?댁슜???녾굅??"以鍮?以? 硫붿떆吏留??덈뒗 寃쎌슦)
+  const hasOverviewContent = overviewContent && !overviewContent.includes('以鍮?以?) && overviewContent.trim().length > 50
+  const hasDetailContent = detailContent && !detailContent.includes('以鍮?以?) && detailContent.trim().length > 50
+  const hasCharacteristicsContent = characteristicsContent && !characteristicsContent.includes('以鍮?以?) && characteristicsContent.trim().length > 50
   
   const tabEntries: TabEntry[] = []
   if (hasOverviewContent) {
-    tabEntries.push({ id: 'overview', label: '소개', icon: 'fa-circle-info', content: overviewContent })
+    tabEntries.push({ id: 'overview', label: '?뚭컻', icon: 'fa-circle-info', content: overviewContent })
   }
   if (hasDetailContent) {
-    tabEntries.push({ id: 'details', label: '과정', icon: 'fa-layer-group', content: detailContent })
+    tabEntries.push({ id: 'details', label: '怨쇱젙', icon: 'fa-layer-group', content: detailContent })
   }
   if (hasCharacteristicsContent) {
-    // 업무특성 탭: 아이콘만 표시, 오른쪽 끝 배치, 호버 시 라벨 표시
-    tabEntries.push({ id: 'characteristics', label: '업무특성', icon: 'fa-chart-pie', content: characteristicsContent, iconOnly: true })
+    // ?낅Т?뱀꽦 ?? ?꾩씠肄섎쭔 ?쒖떆, ?ㅻⅨ履???諛곗튂, ?몃쾭 ???쇰꺼 ?쒖떆
+    tabEntries.push({ id: 'characteristics', label: '?낅Т?뱀꽦', icon: 'fa-chart-pie', content: characteristicsContent, iconOnly: true })
   }
 
   const entitySlug = composeDetailSlug('job', profile.name, profile.id)
@@ -4975,19 +4975,19 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     entitySources: profile.sources,
     tabs: tabEntries,
     tabFallback: {
-      title: '데이터 준비 중',
+      title: '?곗씠??以鍮?以?,
       icon: 'fa-circle-info',
-      message: '이 직업은 아직 상세 정보가 충분하지 않습니다. 데이터가 수집되는 대로 탭이 활성화됩니다.'
+      message: '??吏곸뾽? ?꾩쭅 ?곸꽭 ?뺣낫媛 異⑸텇?섏? ?딆뒿?덈떎. ?곗씠?곌? ?섏쭛?섎뒗 ?濡???씠 ?쒖꽦?붾맗?덈떎.'
     },
     ctaLinks: [],
     comments: {
-      title: `${profile.name} 커뮤니티`,
-      description: `${profile.name}에 대해 자유롭게 이야기해주세요.`,
-      feedbackLabel: '피드백 남기기',
-      notifyLabel: '알림 받기',
-      emptyLabel: `아직 등록된 댓글이 없습니다. 가장 먼저 의견을 남겨주세요.`,
+      title: `${profile.name} 而ㅻ??덊떚`,
+      description: `${profile.name}??????먯쑀濡?쾶 ?댁빞湲고빐二쇱꽭??`,
+      feedbackLabel: '?쇰뱶諛??④린湲?,
+      notifyLabel: '?뚮┝ 諛쏄린',
+      emptyLabel: `?꾩쭅 ?깅줉???볤????놁뒿?덈떎. 媛??癒쇱? ?섍껄???④꺼二쇱꽭??`,
       sampleComments,
-      showForm: true  // 익명 사용자도 댓글 작성 가능
+      showForm: true  // ?듬챸 ?ъ슜?먮룄 ?볤? ?묒꽦 媛??
     },
     partials,
     sources,
@@ -4995,25 +4995,26 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
   })
 
   const sidebarContent = renderJobSidebar(profile, existingJobSlugs, relatedHowtos)
-  // userSourcesFlat은 함수 초반에서 이미 생성됨 (전역 번호 매핑용)
+  // userSourcesFlat? ?⑥닔 珥덈컲?먯꽌 ?대? ?앹꽦??(?꾩뿭 踰덊샇 留ㅽ븨??
   const sourcesCollapsible = renderSourcesCollapsible(profile, sources, partials, userSourcesFlat)
   const heroImageUrl = (profile as any).image_url
-  const heroImageAlt = (profile as any).image_alt ?? `${profile.name} 직업 종사자가 업무를 수행하는 모습`
-  // slug 추출 (이미지 재생성용)
+  const heroImageAlt = (profile as any).image_alt ?? `${profile.name} 吏곸뾽 醫낆궗?먭? ?낅Т瑜??섑뻾?섎뒗 紐⑥뒿`
+  // slug 異붿텧 (?대?吏 ?ъ깮?깆슜)
   const jobSlug = (profile as any).slug || profile.name
-  // 🔍 디버그 HTML 코멘트
+  const imageGenerationMeta = (profile as any).image_generation_meta || null
+  // ?뵇 ?붾쾭洹?HTML 肄붾찘??
   const debugComment = `<!-- DEBUG: image_url=${heroImageUrl || 'undefined'}, hasImageUrl=${!!heroImageUrl} -->`
   const heroImage = heroImageUrl && heroImageUrl.trim() !== '' 
-    ? renderHeroImage(profile.name, { imageUrl: heroImageUrl, imageAlt: heroImageAlt, dataAttribute: 'data-job-hero-image', context: 'job', slug: jobSlug })
+    ? renderHeroImage(profile.name, { imageUrl: heroImageUrl, imageAlt: heroImageAlt, dataAttribute: 'data-job-hero-image', context: 'job', slug: jobSlug, generationMeta: imageGenerationMeta })
     : debugComment
   const hasSidebar = safeTrim(sidebarContent).length > 0
   
-  // 히어로 태그 렌더링 (ETL에서 병합된 heroTags 또는 user_contributed heroTags 사용)
-  // 불필요한 태그 필터링 ("등", "외", "기타" 등)
-  const invalidTags = new Set(['등', '외', '기타', '및', '그외', '그 외', '기타 등', '등등'])
+  // ?덉뼱濡??쒓렇 ?뚮뜑留?(ETL?먯꽌 蹂묓빀??heroTags ?먮뒗 user_contributed heroTags ?ъ슜)
+  // 遺덊븘?뷀븳 ?쒓렇 ?꾪꽣留?("??, "??, "湲고?" ??
+  const invalidTags = new Set(['??, '??, '湲고?', '諛?, '洹몄쇅', '洹???, '湲고? ??, '?깅벑'])
   const isValidTag = (tag: string): boolean => {
     const t = safeTrim(tag)
-    if (!t) return false  // 빈 문자열 제외
+    if (!t) return false  // 鍮?臾몄옄???쒖쇅
     if (invalidTags.has(t)) return false
     if (/^[0-9]+$/.test(t)) return false
     return true
@@ -5021,7 +5022,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
   
   let heroTagsMarkup = ''
   if (profile.heroTags && Array.isArray(profile.heroTags) && profile.heroTags.length > 0) {
-    // 객체 안전 처리: 객체가 섞여 있을 수 있으므로 문자열로 변환 후 필터
+    // 媛앹껜 ?덉쟾 泥섎━: 媛앹껜媛 ?욎뿬 ?덉쓣 ???덉쑝誘濡?臾몄옄?대줈 蹂?????꾪꽣
     const validTags = profile.heroTags.map((tag: any) => extractDisplayText(tag)).filter(tag => isValidTag(tag))
     const visibleLimit = 15
     const hasMore = validTags.length > visibleLimit
@@ -5044,13 +5045,13 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
     
     const moreButton = hasMore
       ? `<button type="button" id="${containerId}-more" class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-wiki-bg/80 text-xs text-wiki-muted font-medium border-2 border-dashed border-wiki-border hover:border-wiki-primary/50 hover:text-wiki-primary hover:bg-wiki-primary/5 transition-all duration-200" onclick="document.getElementById('${containerId}-hidden').classList.remove('hidden'); this.classList.add('hidden'); document.getElementById('${containerId}-less').classList.remove('hidden');">
-          <i class="fas fa-plus text-[10px]" aria-hidden="true"></i>${hiddenTags.length}개 더보기
+          <i class="fas fa-plus text-[10px]" aria-hidden="true"></i>${hiddenTags.length}媛??붾낫湲?
         </button>`
       : ''
     
     const lessButton = hasMore
       ? `<button type="button" id="${containerId}-less" class="hidden inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-wiki-bg/80 text-xs text-wiki-muted font-medium border-2 border-dashed border-wiki-border hover:border-wiki-primary/50 hover:text-wiki-primary hover:bg-wiki-primary/5 transition-all duration-200" onclick="document.getElementById('${containerId}-hidden').classList.add('hidden'); this.classList.add('hidden'); document.getElementById('${containerId}-more').classList.remove('hidden');">
-          <i class="fas fa-minus text-[10px]" aria-hidden="true"></i>접기
+          <i class="fas fa-minus text-[10px]" aria-hidden="true"></i>?묎린
         </button>`
       : ''
     
@@ -5077,7 +5078,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
       `
     : `<div class="space-y-6" data-job-layout>${tabLayout}</div>`
 
-  // 법조 직업 매트릭스 섹션 제거됨
+  // 踰뺤“ 吏곸뾽 留ㅽ듃由?뒪 ?뱀뀡 ?쒓굅??
   const lawyerMatrixSection = ''
 
   const sourcesBlock = sourcesCollapsible
@@ -5104,12 +5105,12 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
                 data-entity-id="${escapeHtml(profile.id)}"
                 data-cw-telemetry-component="job-edit-trigger"
                 data-cw-telemetry-action="edit-open"
-                aria-label="편집 모드"
-                title="이 페이지 편집하기"
+                aria-label="?몄쭛 紐⑤뱶"
+                title="???섏씠吏 ?몄쭛?섍린"
                 ${telemetryVariantAttr}
               >
                 <i class="fas fa-edit" aria-hidden="true"></i>
-                편집
+                ?몄쭛
               </button>
               <button 
                 type="button" 
@@ -5119,20 +5120,20 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
                 data-entity-id="${escapeHtml(profile.id)}"
                 data-cw-telemetry-component="job-history-trigger"
                 data-cw-telemetry-action="history-open"
-                aria-label="역사"
-                title="이 페이지의 편집 이력 보기"
+                aria-label="??궗"
+                title="???섏씠吏???몄쭛 ?대젰 蹂닿린"
                 ${telemetryVariantAttr}
               >
                 <i class="fas fa-history" aria-hidden="true"></i>
-                역사
+                ??궗
               </button>
               <div class="relative" data-share-root data-cw-telemetry-scope="job-hero-actions">
                 <button type="button" class="px-4 py-2 min-h-[44px] bg-wiki-primary text-white rounded-lg text-sm hover:bg-blue-600 transition inline-flex items-center gap-2" data-share-trigger data-share-path="${escapeHtml(detailPath)}" data-share-title="${escapeHtml(profile.name)}" data-share-og-image="${heroImageUrl ? escapeHtml(heroImageUrl) : '/images/og-default.png'}" data-cw-telemetry-component="job-share-trigger" data-cw-telemetry-action="share-open"${telemetryVariantAttr}>
                   <i class="fas fa-share-nodes" aria-hidden="true"></i>
-                  공유
+                  怨듭쑀
                 </button>
               </div>
-              <!-- 저장 버튼 -->
+              <!-- ???踰꾪듉 -->
               <button 
                 type="button" 
                 class="p-2.5 bg-wiki-card border border-wiki-border/60 text-wiki-text rounded-lg text-sm hover:border-amber-400/50 hover:text-amber-400 transition"
@@ -5140,8 +5141,8 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
                 data-bookmark-type="job"
                 data-bookmark-slug="${escapeHtml(entitySlug)}"
                 data-bookmark-title="${escapeHtml(profile.name)}"
-                aria-label="저장"
-                title="저장함에 추가"
+                aria-label="???
+                title="??ν븿??異붽?"
               >
                 <i class="fas fa-bookmark" aria-hidden="true"></i>
               </button>
@@ -5167,28 +5168,28 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
 
       ${metaScript}
       <script>
-        // 사용자 추가 출처 데이터 (평탄화 배열)
+        // ?ъ슜??異붽? 異쒖쿂 ?곗씠??(?됲깂??諛곗뿴)
         window.__USER_SOURCES__ = ${JSON.stringify(userSourcesFlat || [])};
         
-        // 필드별 주석 번호 매핑
+        // ?꾨뱶蹂?二쇱꽍 踰덊샇 留ㅽ븨
         window.__SOURCE_FIELD_MAP__ = {
-          'summary': ['직업 설명', '.hero-description', '[data-field="summary"]'],
-          'heroCategory': ['직업 분류', '.hero-category', '[data-field="heroCategory"]'],
-          'heroTags': ['태그', '.hero-tags', '[data-field="heroTags"]'],
-          'overviewWork.main': ['수행 직무', 'h4:contains("수행 직무")', '[data-field="overviewWork.main"]'],
-          'overviewWork.workStrong': ['작업강도', ':contains("작업강도")', '[data-field="overviewWork.workStrong"]'],
-          'overviewAbilities.abilityList': ['핵심 역량', 'h4:contains("핵심 역량")', '[data-field="overviewAbilities.abilityList"]'],
-          'trivia': ['여담', 'h3:contains("여담")', '[data-field="trivia"]'],
-          // detailReady.curriculum/recruit/training은 applyInlineFootnotes로 항목 텍스트에 인라인 렌더링
-          // → heading badge 불필요 (등록 시 헤딩에 붙는 버그 방지)
-          'detailReady.researchList': ['진로 탐색 활동', 'h3:contains("진로 탐색 활동")', '[data-field="detailReady.researchList"]'],
-          'detailReady.certificate': ['추천 자격증', 'h3:contains("추천 자격증")', '[data-field="detailReady.certificate"]'],
-          'sidebarJobs': ['관련 직업', 'h3:contains("관련 직업")', '[data-field="sidebarJobs"]'],
-          'sidebarMajors': ['관련 학과', 'h3:contains("관련 전공")', '[data-field="sidebarMajors"]']
+          'summary': ['吏곸뾽 ?ㅻ챸', '.hero-description', '[data-field="summary"]'],
+          'heroCategory': ['吏곸뾽 遺꾨쪟', '.hero-category', '[data-field="heroCategory"]'],
+          'heroTags': ['?쒓렇', '.hero-tags', '[data-field="heroTags"]'],
+          'overviewWork.main': ['?섑뻾 吏곷Т', 'h4:contains("?섑뻾 吏곷Т")', '[data-field="overviewWork.main"]'],
+          'overviewWork.workStrong': ['?묒뾽媛뺣룄', ':contains("?묒뾽媛뺣룄")', '[data-field="overviewWork.workStrong"]'],
+          'overviewAbilities.abilityList': ['?듭떖 ??웾', 'h4:contains("?듭떖 ??웾")', '[data-field="overviewAbilities.abilityList"]'],
+          'trivia': ['?щ떞', 'h3:contains("?щ떞")', '[data-field="trivia"]'],
+          // detailReady.curriculum/recruit/training? applyInlineFootnotes濡???ぉ ?띿뒪?몄뿉 ?몃씪???뚮뜑留?
+          // ??heading badge 遺덊븘??(?깅줉 ???ㅻ뵫??遺숇뒗 踰꾧렇 諛⑹?)
+          'detailReady.researchList': ['吏꾨줈 ?먯깋 ?쒕룞', 'h3:contains("吏꾨줈 ?먯깋 ?쒕룞")', '[data-field="detailReady.researchList"]'],
+          'detailReady.certificate': ['異붿쿇 ?먭꺽利?, 'h3:contains("異붿쿇 ?먭꺽利?)', '[data-field="detailReady.certificate"]'],
+          'sidebarJobs': ['愿??吏곸뾽', 'h3:contains("愿??吏곸뾽")', '[data-field="sidebarJobs"]'],
+          'sidebarMajors': ['愿???숆낵', 'h3:contains("愿???꾧났")', '[data-field="sidebarMajors"]']
         };
       </script>
       <script>
-        // _t 파라미터 제거 (캐시 우회 후 URL 정리)
+        // _t ?뚮씪誘명꽣 ?쒓굅 (罹먯떆 ?고쉶 ??URL ?뺣━)
         (function() {
           if (window.location.search.includes('_t=')) {
             const url = new URL(window.location.href);
@@ -5197,7 +5198,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
           }
         })();
         
-        // 사용자 추가 출처에 주석 번호 추가 및 클릭 이벤트 등록 (배열 지원)
+        // ?ъ슜??異붽? 異쒖쿂??二쇱꽍 踰덊샇 異붽? 諛??대┃ ?대깽???깅줉 (諛곗뿴 吏??
         (function() {
           const sources = window.__USER_SOURCES__ || [];
           const fieldMap = window.__SOURCE_FIELD_MAP__ || {};
@@ -5207,19 +5208,19 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
             const fieldKey = source.fieldKey;
             const mapping = fieldMap[fieldKey];
             if (!mapping) return;
-            // 이미 본문에 인라인 각주(user-fnref-N)가 렌더링되어 있으면 heading badge 불필요
+            // ?대? 蹂몃Ц???몃씪??媛곸＜(user-fnref-N)媛 ?뚮뜑留곷릺???덉쑝硫?heading badge 遺덊븘??
             if (document.getElementById('user-fnref-' + sourceId)) return;
             
             const fieldLabel = mapping[0];
             
-            // content-heading 클래스를 가진 요소 중 해당 필드명을 포함하는 것을 찾기
+            // content-heading ?대옒?ㅻ? 媛吏??붿냼 以??대떦 ?꾨뱶紐낆쓣 ?ы븿?섎뒗 寃껋쓣 李얘린
             const headings = document.querySelectorAll('.content-heading, h3, h4');
             headings.forEach(function(heading) {
               if (heading.textContent && heading.textContent.includes(fieldLabel)) {
-                // 동일 ID가 이미 있으면 패스
+                // ?숈씪 ID媛 ?대? ?덉쑝硫??⑥뒪
                 if (heading.querySelector('#user-fnref-' + sourceId)) return;
                 
-                // 주석 번호 추가
+                // 二쇱꽍 踰덊샇 異붽?
                 const footnoteRef = document.createElement('sup');
                 footnoteRef.className = 'user-footnote-ref ml-1 inline-flex align-middle cursor-pointer';
                 footnoteRef.id = 'user-fnref-' + sourceId;
@@ -5227,8 +5228,8 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
                 footnoteRef.style.cssText = 'font-size:11px;font-weight:600;color:var(--wiki-primary,#8b5cf6);margin-left:1px;vertical-align:super;line-height:1;';
                 footnoteRef.textContent = '[' + sourceId + ']';
                 
-                // flex justify-between로 멀어지는 문제를 피하기 위해 텍스트 컨테이너에 부착
-                // ⚠️ firstElementChild 사용 금지 — 이전 badge <sup>을 가리켜서 중첩됨
+                // flex justify-between濡?硫?댁???臾몄젣瑜??쇳븯湲??꾪빐 ?띿뒪??而⑦뀒?대꼫??遺李?
+                // ?좑툘 firstElementChild ?ъ슜 湲덉? ???댁쟾 badge <sup>??媛由ъ폒??以묒꺽??
                 const attachTarget =
                   heading.querySelector('.section-title-text') ||
                   heading.querySelector('.content-heading-text') ||
@@ -5237,7 +5238,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
 
                 attachTarget.appendChild(footnoteRef);
                 
-                // 클릭 이벤트 등록 - 데이터 출처 섹션으로 이동
+                // ?대┃ ?대깽???깅줉 - ?곗씠??異쒖쿂 ?뱀뀡?쇰줈 ?대룞
                 footnoteRef.addEventListener('click', function(e) {
                   e.preventDefault();
                   e.stopPropagation();
@@ -5245,23 +5246,23 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
                   var targetEl = document.getElementById('user-fn-' + sourceId);
                   if (!targetEl) return;
                   
-                  // 데이터 출처 섹션 찾기 및 펼치기
+                  // ?곗씠??異쒖쿂 ?뱀뀡 李얘린 諛??쇱튂湲?
                   var sourceSection = document.querySelector('[data-source-collapsible]');
                   if (sourceSection) {
                     var toggleBtn = sourceSection.querySelector('button[aria-controls]');
                     var panelId = toggleBtn ? toggleBtn.getAttribute('aria-controls') : null;
                     var panel = panelId ? document.getElementById(panelId) : null;
                     
-                    // 섹션이 접혀있으면 펼치기
+                    // ?뱀뀡???묓??덉쑝硫??쇱튂湲?
                     if (toggleBtn && toggleBtn.getAttribute('aria-expanded') === 'false') {
                       toggleBtn.click();
                     }
                   }
                   
-                  // 스크롤 이동 (펼치기 애니메이션 후)
+                  // ?ㅽ겕濡??대룞 (?쇱튂湲??좊땲硫붿씠????
                   setTimeout(function() {
                     targetEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
-                    // 하이라이트 효과
+                    // ?섏씠?쇱씠???④낵
                     targetEl.classList.add('ring-2', 'ring-wiki-primary');
                     setTimeout(function() {
                       targetEl.classList.remove('ring-2', 'ring-wiki-primary');
@@ -5273,12 +5274,12 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
           });
         })();
         
-        // Lucide 아이콘 초기화
+        // Lucide ?꾩씠肄?珥덇린??
         if (typeof lucide !== 'undefined') {
           lucide.createIcons();
         }
         
-        // 사이드바 접기/펼치기 기능 (연관 직업, 관련 전공)
+        // ?ъ씠?쒕컮 ?묎린/?쇱튂湲?湲곕뒫 (?곌? 吏곸뾽, 愿???꾧났)
         (function() {
           const expandButtons = document.querySelectorAll('.expand-toggle');
           
@@ -5292,20 +5293,20 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
               const toggleCount = this.querySelector('.toggle-count');
               
               if (isExpanded) {
-                // 접기
+                // ?묎린
                 hiddenItems.forEach(item => {
                   item.style.display = 'none';
                 });
-                toggleText.textContent = '더보기';
+                toggleText.textContent = '?붾낫湲?;
                 toggleIcon.style.transform = 'rotate(0deg)';
                 toggleCount.style.display = '';
                 this.setAttribute('data-expanded', 'false');
               } else {
-                // 펼치기
+                // ?쇱튂湲?
                 hiddenItems.forEach(item => {
                   item.style.display = '';
                 });
-                toggleText.textContent = '접기';
+                toggleText.textContent = '?묎린';
                 toggleIcon.style.transform = 'rotate(180deg)';
                 toggleCount.style.display = 'none';
                 this.setAttribute('data-expanded', 'true');
@@ -5320,7 +5321,7 @@ export const renderUnifiedJobDetail = ({ profile, partials, sources, existingJob
 
 
 export const createJobJsonLd = (profile: UnifiedJobDetail, canonicalUrl: string): string => {
-  // ETL 구조화 필드 사용
+  // ETL 援ъ“???꾨뱶 ?ъ슜
   const overviewWork = profile.overviewWork
   const overviewSalary = profile.overviewSalary
   const detailEducation = profile.detailEducation
@@ -5352,3 +5353,4 @@ export const createJobJsonLd = (profile: UnifiedJobDetail, canonicalUrl: string)
   const script = JSON.stringify(jsonLd).replace(/</g, '\\u003c')
   return `<script type="application/ld+json">${script}</script>`
 }
+
diff --git a/src/templates/unifiedMajorDetail.ts b/src/templates/unifiedMajorDetail.ts
index 01525f3..961ae4d 100644
--- a/src/templates/unifiedMajorDetail.ts
+++ b/src/templates/unifiedMajorDetail.ts
@@ -32,28 +32,28 @@ export interface UnifiedMajorDetailTemplateParams {
   profile: UnifiedMajorDetail
   partials?: Partial<Record<DataSource, UnifiedMajorDetail | null>>
   sources?: SourceStatusRecord
-  existingJobSlugs?: Map<string, string>  // 직업명 → slug 매핑 (DB에 존재하는 직업)
-  relatedMajorsByCategory?: RelatedMajorItem[]  // 같은 계열 전공 (자기 자신 제외)
-  relatedHowtos?: Array<{ slug: string; title: string; summary: string }>  // 이 전공을 참조하는 HowTo
-  classificationData?: { large_category?: string }  // major_categories 테이블에서
+  existingJobSlugs?: Map<string, string>  // 吏곸뾽紐???slug 留ㅽ븨 (DB??議댁옱?섎뒗 吏곸뾽)
+  relatedMajorsByCategory?: RelatedMajorItem[]  // 媛숈? 怨꾩뿴 ?꾧났 (?먭린 ?먯떊 ?쒖쇅)
+  relatedHowtos?: Array<{ slug: string; title: string; summary: string }>  // ???꾧났??李몄“?섎뒗 HowTo
+  classificationData?: { large_category?: string }  // major_categories ?뚯씠釉붿뿉??
 }
 
-// 전공 템플릿용 데이터 출처 레이블 (직업 템플릿과 다름)
+// ?꾧났 ?쒗뵆由우슜 ?곗씠??異쒖쿂 ?덉씠釉?(吏곸뾽 ?쒗뵆由욧낵 ?ㅻ쫫)
 const SOURCE_LABELS: Partial<Record<DataSource, string>> = {
   ...DEFAULT_SOURCE_LABELS,
-  CAREERNET: '커리어넷 학과정보',  // 직업백과 → 학과정보
-  GOYONG24: '고용24 학과정보',     // 직업정보 → 학과정보
-  WORK24_MAJOR: '고용24 학과정보'  // 전공용 소스
+  CAREERNET: '而ㅻ━?대꽬 ?숆낵?뺣낫',  // 吏곸뾽諛깃낵 ???숆낵?뺣낫
+  GOYONG24: '怨좎슜24 ?숆낵?뺣낫',     // 吏곸뾽?뺣낫 ???숆낵?뺣낫
+  WORK24_MAJOR: '怨좎슜24 ?숆낵?뺣낫'  // ?꾧났???뚯뒪
 }
 
 const SOURCE_DESCRIPTIONS: Partial<Record<DataSource, string>> = {
-  CAREERNET: '교육부 산하 진로·진학 정보 플랫폼\n통합 데이터에 포함되었습니다.',
-  GOYONG24: '고용노동부 고용24 학과 정보\n통합 데이터에 포함되었습니다.',
-  WORK24_MAJOR: '고용노동부 고용24 학과 정보\n통합 데이터에 포함되었습니다.'
+  CAREERNET: '援먯쑁遺 ?고븯 吏꾨줈쨌吏꾪븰 ?뺣낫 ?뚮옯??n?듯빀 ?곗씠?곗뿉 ?ы븿?섏뿀?듬땲??',
+  GOYONG24: '怨좎슜?몃룞遺 怨좎슜24 ?숆낵 ?뺣낫\n?듯빀 ?곗씠?곗뿉 ?ы븿?섏뿀?듬땲??',
+  WORK24_MAJOR: '怨좎슜?몃룞遺 怨좎슜24 ?숆낵 ?뺣낫\n?듯빀 ?곗씠?곗뿉 ?ы븿?섏뿀?듬땲??'
 }
 
 /**
- * 안전한 trim 함수 - undefined/null 체크
+ * ?덉쟾??trim ?⑥닔 - undefined/null 泥댄겕
  */
 const safeTrim = (value: any): string => {
   if (value === null || value === undefined) return ''
@@ -62,9 +62,9 @@ const safeTrim = (value: any): string => {
 }
 
 /**
- * 배열 항목에서 표시 텍스트 안전 추출
- * 객체인 경우 주요 키를 우선순위대로 시도하여 문자열 반환
- * DB에 저장된 "[object Object]" 문자열도 필터링
+ * 諛곗뿴 ??ぉ?먯꽌 ?쒖떆 ?띿뒪???덉쟾 異붿텧
+ * 媛앹껜??寃쎌슦 二쇱슂 ?ㅻ? ?곗꽑?쒖쐞?濡??쒕룄?섏뿬 臾몄옄??諛섑솚
+ * DB????λ맂 "[object Object]" 臾몄옄?대룄 ?꾪꽣留?
  */
 const extractDisplayText = (item: any): string => {
   if (item === null || item === undefined) return ''
@@ -83,7 +83,7 @@ const extractDisplayText = (item: any): string => {
 }
 
 /**
- * 취업률 포맷 함수: "70% 이상" 같은 텍스트에서 숫자 추출 후 소수점 1자리까지 반올림
+ * 痍⑥뾽瑜??щ㎎ ?⑥닔: "70% ?댁긽" 媛숈? ?띿뒪?몄뿉???レ옄 異붿텧 ???뚯닔??1?먮━源뚯? 諛섏삱由?
  */
 const formatEmploymentRate = (rate: string | undefined): string | undefined => {
   if (!rate) return undefined
@@ -97,19 +97,19 @@ const formatEmploymentRate = (rate: string | undefined): string | undefined => {
 }
 
 /**
- * chartData.employment_rate에서 정확한 취업률 추출 (전체 값 우선)
- * 없으면 employmentRate 필드를 폴백으로 사용
+ * chartData.employment_rate?먯꽌 ?뺥솗??痍⑥뾽瑜?異붿텧 (?꾩껜 媛??곗꽑)
+ * ?놁쑝硫?employmentRate ?꾨뱶瑜??대갚?쇰줈 ?ъ슜
  */
 const getAccurateEmploymentRate = (profile: UnifiedMajorDetail): string | undefined => {
-  // chartData 정규화 (배열인 경우 첫 번째 요소 사용)
+  // chartData ?뺢퇋??(諛곗뿴??寃쎌슦 泥?踰덉㎏ ?붿냼 ?ъ슜)
   let chartData = (profile as any).chartData
   if (Array.isArray(chartData)) {
     chartData = chartData[0]
   }
   
-  // chartData.employment_rate에서 정확한 값 추출
+  // chartData.employment_rate?먯꽌 ?뺥솗??媛?異붿텧
   if (chartData?.employment_rate && Array.isArray(chartData.employment_rate)) {
-    const totalItem = chartData.employment_rate.find((e: any) => e.item === '전체')
+    const totalItem = chartData.employment_rate.find((e: any) => e.item === '?꾩껜')
     if (totalItem?.data) {
       const rate = parseFloat(totalItem.data)
       if (!isNaN(rate)) {
@@ -117,18 +117,18 @@ const getAccurateEmploymentRate = (profile: UnifiedMajorDetail): string | undefi
       }
     }
   }
-  // 폴백: employmentRate 필드 (대략적인 값)
+  // ?대갚: employmentRate ?꾨뱶 (??듭쟻??媛?
   if (profile.employmentRate) {
     return formatEmploymentRate(profile.employmentRate)
   }
   return undefined
 }
 
-// 목차 렌더링 함수
+// 紐⑹감 ?뚮뜑留??⑥닔
 type TocItem = { id: string; label: string; icon: string }
 
 const renderSectionToc = (sectionKey: 'overview' | 'curriculum' | 'career' | 'universities' | 'network' | 'details', heading: string, items: TocItem[]): string => {
-  // 항목이 1개 이하면 목차 숨김 (목차가 의미 없음)
+  // ??ぉ??1媛??댄븯硫?紐⑹감 ?④? (紐⑹감媛 ?섎? ?놁쓬)
   if (items.length <= 1) {
     return ''
   }
@@ -175,40 +175,40 @@ const renderSectionToc = (sectionKey: 'overview' | 'curriculum' | 'career' | 'un
   `
 }
 
-// anchor ID 생성 팩토리
+// anchor ID ?앹꽦 ?⑺넗由?
 const anchorIdFactory = (sectionKey: string, label: string): string => {
-  const normalized = label.toLowerCase().replace(/[^a-z0-9가-힣]+/g, '-').replace(/(^-|-$)/g, '')
+  const normalized = label.toLowerCase().replace(/[^a-z0-9媛-??+/g, '-').replace(/(^-|-$)/g, '')
   return `${sectionKey}-${normalized}`
 }
 
 /**
- * 대학 유형 정규화 (고용24 우선, 없으면 기타 로직 사용)
+ * ????좏삎 ?뺢퇋??(怨좎슜24 ?곗꽑, ?놁쑝硫?湲고? 濡쒖쭅 ?ъ슜)
  */
-const normalizeUniversityType = (type?: string): '대학교' | '전문대학' | '기타' => {
-  if (!type) return '기타'
+const normalizeUniversityType = (type?: string): '??숆탳' | '?꾨Ц??? | '湲고?' => {
+  if (!type) return '湲고?'
   
   const normalized = type.trim()
   
-  // 정확한 매칭 (고용24 + 커리어넷)
-  if (normalized === '4년제' || normalized === '일반대' || normalized === '대학교') return '대학교'
-  if (normalized === '전문대' || normalized === '전문대학' || normalized === '2년제') return '전문대학'
+  // ?뺥솗??留ㅼ묶 (怨좎슜24 + 而ㅻ━?대꽬)
+  if (normalized === '4?꾩젣' || normalized === '?쇰컲?' || normalized === '??숆탳') return '??숆탳'
+  if (normalized === '?꾨Ц?' || normalized === '?꾨Ц??? || normalized === '2?꾩젣') return '?꾨Ц???
   
-  // 패턴 매칭
+  // ?⑦꽩 留ㅼ묶
   const lower = normalized.toLowerCase()
-  if (lower.includes('4년') || lower.includes('일반')) return '대학교'
-  if (lower.includes('전문') || lower.includes('2년')) return '전문대학'
+  if (lower.includes('4??) || lower.includes('?쇰컲')) return '??숆탳'
+  if (lower.includes('?꾨Ц') || lower.includes('2??)) return '?꾨Ц???
   
-  // 기타
-  return '기타'
+  // 湲고?
+  return '湲고?'
 }
 
 /**
- * 지역별 + 유형별 대학 집계
+ * 吏??퀎 + ?좏삎蹂????吏묎퀎
  */
 interface UniversityByRegionAndType {
   universities: UnifiedMajorDetail['universities']
-  regular: UnifiedMajorDetail['universities']  // 대학교(4년제)
-  college: UnifiedMajorDetail['universities']  // 전문대학
+  regular: UnifiedMajorDetail['universities']  // ??숆탳(4?꾩젣)
+  college: UnifiedMajorDetail['universities']  // ?꾨Ц???
   regionCounts: Record<string, { regular: number; college: number }>
 }
 
@@ -228,7 +228,7 @@ const aggregateUniversitiesByRegion = (universities?: UnifiedMajorDetail['univer
   let processedCount = 0
   
   universities.forEach(uni => {
-    // Area가 없는 대학은 제외
+    // Area媛 ?녿뒗 ??숈? ?쒖쇅
     if (!uni.area) {
       skippedCount++
       return
@@ -241,16 +241,16 @@ const aggregateUniversitiesByRegion = (universities?: UnifiedMajorDetail['univer
     
     if (!region) return
     
-    // 지역별 카운트 초기화
+    // 吏??퀎 移댁슫??珥덇린??
     if (!result.regionCounts[region]) {
       result.regionCounts[region] = { regular: 0, college: 0 }
     }
     
-    // 유형별 분류
-    if (uniType === '대학교') {
+    // ?좏삎蹂?遺꾨쪟
+    if (uniType === '??숆탳') {
       if (result.regular) result.regular.push(uni)
       result.regionCounts[region].regular++
-    } else if (uniType === '전문대학') {
+    } else if (uniType === '?꾨Ц???) {
       if (result.college) result.college.push(uni)
       result.regionCounts[region].college++
     }
@@ -260,11 +260,11 @@ const aggregateUniversitiesByRegion = (universities?: UnifiedMajorDetail['univer
 }
 
 /**
- * 대학 목록 렌더링 (단일 카드)
+ * ???紐⑸줉 ?뚮뜑留?(?⑥씪 移대뱶)
  */
 const renderUniversityList = (universities: UnifiedMajorDetail['universities'], emptyMessage?: string): string => {
   if (!universities || universities.length === 0) {
-    return ''  // 데이터 없으면 빈 문자열 반환
+    return ''  // ?곗씠???놁쑝硫?鍮?臾몄옄??諛섑솚
   }
   
   const items = universities
@@ -275,15 +275,15 @@ const renderUniversityList = (universities: UnifiedMajorDetail['universities'],
       const department = uni.department ? `<p class="text-xs text-wiki-muted mt-1">${escapeHtml(uni.department)}</p>` : ''
       const type = uni.universityType ? `<span class="px-2 py-1 text-[10px] rounded bg-wiki-primary/10 text-wiki-primary">${escapeHtml(uni.universityType)}</span>` : ''
       const link = uni.url
-        ? `<a href="${escapeHtml(uni.url)}" target="_blank" rel="noopener" class="text-xs text-wiki-primary hover:text-wiki-secondary flex items-center gap-1">웹사이트<i class="fas fa-arrow-up-right-from-square"></i></a>`
+        ? `<a href="${escapeHtml(uni.url)}" target="_blank" rel="noopener" class="text-xs text-wiki-primary hover:text-wiki-secondary flex items-center gap-1">?뱀궗?댄듃<i class="fas fa-arrow-up-right-from-square"></i></a>`
         : ''
-      // 지역 정규화: area가 있으면 정규화, 없으면 대학명에서 추론
+      // 吏???뺢퇋?? area媛 ?덉쑝硫??뺢퇋?? ?놁쑝硫???숇챸?먯꽌 異붾줎
       let regionName = normalizeRegionName(uni.area)
       if (!regionName && uni.name) {
         regionName = inferRegionFromUniversityName(uni.name) || ''
       }
-      // 여전히 없으면 "기타" (지도에 표시되지 않지만 필터링은 작동)
-      const finalRegion = regionName || '기타'
+      // ?ъ쟾???놁쑝硫?"湲고?" (吏?꾩뿉 ?쒖떆?섏? ?딆?留??꾪꽣留곸? ?묐룞)
+      const finalRegion = regionName || '湲고?'
       
       return `
         <div class="bg-wiki-bg/60 border border-wiki-border rounded-lg px-4 py-4 md:px-4 university-card hover:border-wiki-primary/40 hover:bg-wiki-bg/80 transition-all duration-200" data-region="${escapeHtml(finalRegion)}">
@@ -301,14 +301,14 @@ const renderUniversityList = (universities: UnifiedMajorDetail['universities'],
     })
   
   if (items.length === 0) {
-    return ''  // 유효한 대학 없으면 빈 문자열 반환
+    return ''  // ?좏슚??????놁쑝硫?鍮?臾몄옄??諛섑솚
   }
   
   return `<div class="grid gap-4 sm:grid-cols-2">${items.join('')}</div>`
 }
 
 /**
- * 메인 개설 대학 렌더링 (탭 + 지도 + 목록)
+ * 硫붿씤 媛쒖꽕 ????뚮뜑留?(??+ 吏??+ 紐⑸줉)
  */
 const renderUniversities = (universities?: UnifiedMajorDetail['universities']): string => {
   if (!universities || universities.length === 0) {
@@ -317,86 +317,86 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
   
   const aggregated = aggregateUniversitiesByRegion(universities)
   
-  // 지역 데이터 생성 (지도용)
+  // 吏???곗씠???앹꽦 (吏?꾩슜)
   const regularRegions: RegionData[] = Object.entries(aggregated.regionCounts)
     .map(([id, counts]) => ({ id, name: id, count: counts.regular }))
   
   const collegeRegions: RegionData[] = Object.entries(aggregated.regionCounts)
     .map(([id, counts]) => ({ id, name: id, count: counts.college }))
   
-  // 대학교 탭 컨텐츠
+  // ??숆탳 ??而⑦뀗痢?
   const regularMapHtml = renderKoreaMap({ regions: regularRegions })
-  const regularListHtml = renderUniversityList(aggregated.regular, '4년제 대학 정보가 없습니다.')
+  const regularListHtml = renderUniversityList(aggregated.regular, '4?꾩젣 ????뺣낫媛 ?놁뒿?덈떎.')
   
   const regularTabContent = `
     <div class="space-y-6">
-      <!-- 지역별 분포 지도 -->
+      <!-- 吏??퀎 遺꾪룷 吏??-->
       <div class="bg-wiki-bg/30 border border-wiki-border/50 rounded-xl p-4 md:p-6 shadow-lg">
         <h4 class="text-sm md:text-base font-bold text-wiki-text mb-4 flex items-center gap-2">
           <i class="fas fa-map text-wiki-primary text-base"></i>
-          지역별 분포
+          吏??퀎 遺꾪룷
         </h4>
         ${regularMapHtml}
         <div class="flex items-center justify-center gap-2 mt-4 px-2">
           <i class="fas fa-info-circle text-wiki-primary/60 text-xs"></i>
-          <p class="text-xs text-wiki-muted text-center">지역을 클릭하면 해당 지역의 대학만 표시됩니다</p>
+          <p class="text-xs text-wiki-muted text-center">吏??쓣 ?대┃?섎㈃ ?대떦 吏??쓽 ??숇쭔 ?쒖떆?⑸땲??/p>
         </div>
         <div class="flex justify-center mt-3">
           <button type="button" class="reset-region-filter px-4 py-2 text-xs bg-wiki-primary/10 text-wiki-primary rounded-lg hover:bg-wiki-primary/20 transition-all duration-200 flex items-center gap-2" style="display: none;">
             <i class="fas fa-times"></i>
-            <span>필터 해제</span>
+            <span>?꾪꽣 ?댁젣</span>
           </button>
         </div>
       </div>
       
-      <!-- 대학 목록 -->
+      <!-- ???紐⑸줉 -->
       <div class="university-list-container" data-tab-type="regular">
         ${regularListHtml}
       </div>
     </div>
   `
   
-  // 전문대학 탭 컨텐츠
+  // ?꾨Ц?????而⑦뀗痢?
   const collegeMapHtml = renderKoreaMap({ regions: collegeRegions })
-  const collegeListHtml = renderUniversityList(aggregated.college, '전문대학 정보가 없습니다.')
+  const collegeListHtml = renderUniversityList(aggregated.college, '?꾨Ц????뺣낫媛 ?놁뒿?덈떎.')
   
   const collegeTabContent = `
     <div class="space-y-6">
-      <!-- 지역별 분포 지도 -->
+      <!-- 吏??퀎 遺꾪룷 吏??-->
       <div class="bg-wiki-bg/30 border border-wiki-border/50 rounded-xl p-4 md:p-6 shadow-lg">
         <h4 class="text-sm md:text-base font-bold text-wiki-text mb-4 flex items-center gap-2">
           <i class="fas fa-map text-wiki-primary text-base"></i>
-          지역별 분포
+          吏??퀎 遺꾪룷
         </h4>
         ${collegeMapHtml}
         <div class="flex items-center justify-center gap-2 mt-4 px-2">
           <i class="fas fa-info-circle text-wiki-primary/60 text-xs"></i>
-          <p class="text-xs text-wiki-muted text-center">지역을 클릭하면 해당 지역의 대학만 표시됩니다</p>
+          <p class="text-xs text-wiki-muted text-center">吏??쓣 ?대┃?섎㈃ ?대떦 吏??쓽 ??숇쭔 ?쒖떆?⑸땲??/p>
         </div>
         <div class="flex justify-center mt-3">
           <button type="button" class="reset-region-filter px-4 py-2 text-xs bg-wiki-primary/10 text-wiki-primary rounded-lg hover:bg-wiki-primary/20 transition-all duration-200 flex items-center gap-2" style="display: none;">
             <i class="fas fa-times"></i>
-            <span>필터 해제</span>
+            <span>?꾪꽣 ?댁젣</span>
           </button>
         </div>
       </div>
       
-      <!-- 대학 목록 -->
+      <!-- ???紐⑸줉 -->
       <div class="university-list-container" data-tab-type="college">
         ${collegeListHtml}
       </div>
     </div>
   `
   
-  // 서브탭 UI
+  // ?쒕툕??UI
   const hasRegular = (aggregated.regular?.length ?? 0) > 0
   const hasCollege = (aggregated.college?.length ?? 0) > 0
   
   if (!hasRegular && !hasCollege) {
-    return ''  // 데이터 없으면 빈 문자열 반환 (탭 숨김)
+    return ''  // ?곗씠???놁쑝硫?鍮?臾몄옄??諛섑솚 (???④?)
   }
   
-  // 공통 스타일과 스크립트
+  // 怨듯넻 ?ㅽ??쇨낵 ?ㅽ겕由쏀듃
   const commonStyleAndScript = `
     <style>
       @keyframes fadeIn {
@@ -417,7 +417,7 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
     
     <script>
       (function() {
-        // 서브탭 전환
+        // ?쒕툕???꾪솚
         const tabButtons = document.querySelectorAll('.uni-subtab-btn');
         const tabContents = document.querySelectorAll('.uni-subtab-content');
         
@@ -425,12 +425,12 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
           btn.addEventListener('click', function() {
             const targetTab = this.getAttribute('data-subtab');
             
-            // 모든 버튼/컨텐츠 비활성화
+            // 紐⑤뱺 踰꾪듉/而⑦뀗痢?鍮꾪솢?깊솕
             tabButtons.forEach(b => {
               b.classList.remove('border-wiki-primary', 'text-wiki-text');
               b.classList.add('border-transparent', 'text-wiki-muted');
               b.setAttribute('aria-selected', 'false');
-              // 배지 색상도 변경
+              // 諛곗? ?됱긽??蹂寃?
               const badge = b.querySelector('span');
               if (badge) {
                 badge.classList.remove('bg-wiki-primary/20', 'text-wiki-primary');
@@ -439,11 +439,11 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
             });
             tabContents.forEach(c => c.classList.add('hidden'));
             
-            // 선택된 탭 활성화
+            // ?좏깮?????쒖꽦??
             this.classList.remove('border-transparent', 'text-wiki-muted');
             this.classList.add('border-wiki-primary', 'text-wiki-text');
             this.setAttribute('aria-selected', 'true');
-            // 배지 색상 변경
+            // 諛곗? ?됱긽 蹂寃?
             const activeBadge = this.querySelector('span');
             if (activeBadge) {
               activeBadge.classList.remove('bg-wiki-border/40', 'text-wiki-muted');
@@ -457,9 +457,9 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
           });
         });
         
-        // 지역 클릭 필터링 (모든 경우 처리: 탭 1개 또는 2개)
+        // 吏???대┃ ?꾪꽣留?(紐⑤뱺 寃쎌슦 泥섎━: ??1媛??먮뒗 2媛?
         document.querySelectorAll('[data-korea-map]').forEach(mapWrapper => {
-          // 항상 .uni-subtab-content 안에 있으므로 이것을 찾음
+          // ??긽 .uni-subtab-content ?덉뿉 ?덉쑝誘濡??닿쾬??李얠쓬
           const container = mapWrapper.closest('.uni-subtab-content');
           if (!container) return;
           
@@ -476,10 +476,10 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
             regionLink.addEventListener('click', function(e) {
               e.preventDefault();
               
-              // 데이터 없는 지역은 클릭 무시
+              // ?곗씠???녿뒗 吏??? ?대┃ 臾댁떆
               if (this.classList.contains('no-data')) return;
               
-              // "전체" 버튼 클릭 시
+              // "?꾩껜" 踰꾪듉 ?대┃ ??
               if (region === 'ALL') {
                 activeRegion = null;
                 resetBtn.style.display = 'none';
@@ -493,7 +493,7 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
                 return;
               }
               
-              // 같은 지역 재클릭 시
+              // 媛숈? 吏???ы겢由???
               if (activeRegion === region) {
                 activeRegion = null;
                 resetBtn.style.display = 'none';
@@ -507,7 +507,7 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
                 return;
               }
               
-              // 새 지역 선택
+              // ??吏???좏깮
               activeRegion = region;
               resetBtn.style.display = 'block';
               
@@ -516,7 +516,7 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
               });
               this.classList.add('active');
               
-              // 대학 카드 필터링
+              // ???移대뱶 ?꾪꽣留?
               listContainer.querySelectorAll('.university-card').forEach(card => {
                 const cardRegion = card.getAttribute('data-region');
                 if (cardRegion === region) {
@@ -545,11 +545,11 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
     </script>
   `
   
-  // 전문대학만 있을 때: 전문대학 탭만 표시
+  // ?꾨Ц??숇쭔 ?덉쓣 ?? ?꾨Ц?????쭔 ?쒖떆
   if (!hasRegular && hasCollege) {
     return `
       <div class="university-tabs-wrapper w-full">
-        <!-- 서브 탭 네비게이션 -->
+        <!-- ?쒕툕 ???ㅻ퉬寃뚯씠??-->
         <div class="flex gap-2 border-b border-wiki-border/50 mb-6" role="tablist">
           <button
             type="button"
@@ -559,12 +559,12 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
             aria-selected="true"
           >
             <i class="fas fa-building mr-2"></i>
-            전문대학
+            ?꾨Ц???
             <span class="ml-2 px-2 py-0.5 text-xs rounded-full bg-wiki-primary/20 text-wiki-primary">${aggregated.college?.length ?? 0}</span>
           </button>
         </div>
         
-        <!-- 전문대학 탭 컨텐츠 -->
+        <!-- ?꾨Ц?????而⑦뀗痢?-->
         <div class="uni-subtab-content" data-subtab-content="college">
           ${collegeTabContent}
         </div>
@@ -573,11 +573,11 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
     `
   }
   
-  // 대학교만 있을 때: 대학교 탭만 표시
+  // ??숆탳留??덉쓣 ?? ??숆탳 ??쭔 ?쒖떆
   if (hasRegular && !hasCollege) {
     return `
       <div class="university-tabs-wrapper w-full">
-        <!-- 서브 탭 네비게이션 -->
+        <!-- ?쒕툕 ???ㅻ퉬寃뚯씠??-->
         <div class="flex gap-2 border-b border-wiki-border/50 mb-6" role="tablist">
           <button
             type="button"
@@ -587,12 +587,12 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
             aria-selected="true"
           >
             <i class="fas fa-university mr-2"></i>
-            대학교
+            ??숆탳
             <span class="ml-2 px-2 py-0.5 text-xs rounded-full bg-wiki-primary/20 text-wiki-primary">${aggregated.regular?.length ?? 0}</span>
           </button>
         </div>
         
-        <!-- 대학교 탭 컨텐츠 -->
+        <!-- ??숆탳 ??而⑦뀗痢?-->
         <div class="uni-subtab-content" data-subtab-content="regular">
           ${regularTabContent}
         </div>
@@ -601,10 +601,10 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
     `
   }
   
-  // 둘 다 있을 때: 두 탭을 균등하게 나눔 (flex-1)
+  // ?????덉쓣 ?? ????쓣 洹좊벑?섍쾶 ?섎닎 (flex-1)
   return `
     <div class="university-tabs-wrapper w-full">
-      <!-- 서브 탭 네비게이션 -->
+      <!-- ?쒕툕 ???ㅻ퉬寃뚯씠??-->
       <div class="flex gap-2 border-b border-wiki-border/50 mb-6" role="tablist">
         <button
           type="button"
@@ -614,7 +614,7 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
           aria-selected="true"
         >
           <i class="fas fa-university mr-2"></i>
-          대학교
+          ??숆탳
           <span class="ml-2 px-2 py-0.5 text-xs rounded-full bg-wiki-primary/20 text-wiki-primary">${aggregated.regular?.length ?? 0}</span>
         </button>
         <button
@@ -625,17 +625,17 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
           aria-selected="false"
         >
           <i class="fas fa-building mr-2"></i>
-          전문대학
+          ?꾨Ц???
           <span class="ml-2 px-2 py-0.5 text-xs rounded-full bg-wiki-border/40 text-wiki-muted">${aggregated.college?.length ?? 0}</span>
         </button>
       </div>
       
-      <!-- 대학교 탭 컨텐츠 -->
+      <!-- ??숆탳 ??而⑦뀗痢?-->
       <div class="uni-subtab-content" data-subtab-content="regular">
         ${regularTabContent}
       </div>
       
-      <!-- 전문대학 탭 컨텐츠 -->
+      <!-- ?꾨Ц?????而⑦뀗痢?-->
       <div class="uni-subtab-content hidden" data-subtab-content="college">
         ${collegeTabContent}
       </div>
@@ -644,12 +644,12 @@ const renderUniversities = (universities?: UnifiedMajorDetail['universities']):
   `
 }
 
-// 숫자 포맷팅 (천 단위 구분 + '명' 붙이기)
+// ?レ옄 ?щ㎎??(泥??⑥쐞 援щ텇 + '紐? 遺숈씠湲?
 const formatPersonCount = (num: string | number | undefined | null): string => {
   if (num === undefined || num === null || num === '-' || num === '') return '-'
   const n = typeof num === 'string' ? parseInt(num, 10) : num
   if (isNaN(n)) return '-'
-  return n.toLocaleString('ko-KR') + '명'
+  return n.toLocaleString('ko-KR') + '紐?
 }
 
 const renderRecruitmentTable = (stats?: UnifiedMajorDetail['recruitmentStatus']): string => {
@@ -676,11 +676,11 @@ const renderRecruitmentTable = (stats?: UnifiedMajorDetail['recruitmentStatus'])
       <table class="min-w-full divide-y divide-wiki-border content-text">
         <thead class="bg-wiki-bg/80 text-xs uppercase tracking-wide text-wiki-muted">
           <tr>
-            <th class="px-4 py-3 text-left">연도</th>
-            <th class="px-4 py-3 text-left">대학 유형</th>
-            <th class="px-4 py-3 text-left">정원</th>
-            <th class="px-4 py-3 text-left">지원자</th>
-            <th class="px-4 py-3 text-left">졸업자</th>
+            <th class="px-4 py-3 text-left">?곕룄</th>
+            <th class="px-4 py-3 text-left">????좏삎</th>
+            <th class="px-4 py-3 text-left">?뺤썝</th>
+            <th class="px-4 py-3 text-left">吏?먯옄</th>
+            <th class="px-4 py-3 text-left">議몄뾽??/th>
           </tr>
         </thead>
         <tbody class="divide-y divide-wiki-border/70">
@@ -695,9 +695,9 @@ const renderRecruitmentTable = (stats?: UnifiedMajorDetail['recruitmentStatus'])
 const renderMetaHighlights = (profile: UnifiedMajorDetail): string => {
   const accurateEmploymentRate = getAccurateEmploymentRate(profile)
   const highlights = [
-    accurateEmploymentRate ? { label: '취업률', value: accurateEmploymentRate, icon: 'fa-briefcase', accent: 'text-green-400' } : null,
-    profile.salaryAfterGraduation ? { label: '졸업 후 평균 월급', value: profile.salaryAfterGraduation, icon: 'fa-coins', accent: 'text-yellow-300' } : null,
-    profile.categoryName ? { label: '계열/분야', value: profile.categoryName, icon: 'fa-layer-group', accent: 'text-wiki-secondary' } : null
+    accurateEmploymentRate ? { label: '痍⑥뾽瑜?, value: accurateEmploymentRate, icon: 'fa-briefcase', accent: 'text-green-400' } : null,
+    profile.salaryAfterGraduation ? { label: '議몄뾽 ???됯퇏 ?붽툒', value: profile.salaryAfterGraduation, icon: 'fa-coins', accent: 'text-yellow-300' } : null,
+    profile.categoryName ? { label: '怨꾩뿴/遺꾩빞', value: profile.categoryName, icon: 'fa-layer-group', accent: 'text-wiki-secondary' } : null
   ].filter(Boolean) as Array<{ label: string; value: string; icon: string; accent: string }>
 
   if (highlights.length === 0) return ''
@@ -740,31 +740,31 @@ const renderMajorSourcesCollapsible = (
     partials,
     labels: SOURCE_LABELS,
     descriptions: SOURCE_DESCRIPTIONS,
-    title: '출처',
-    description: '이 페이지에 노출된 주요 출처를 확인할 수 있습니다.'
+    title: '異쒖쿂',
+    description: '???섏씠吏???몄텧??二쇱슂 異쒖쿂瑜??뺤씤?????덉뒿?덈떎.'
   })
 
-  // 사용자가 추가한 출처 (_sources) - 직업 템플릿과 동일
+  // ?ъ슜?먭? 異붽???異쒖쿂 (_sources) - 吏곸뾽 ?쒗뵆由욧낵 ?숈씪
   const userSources = (profile as any)._sources as Record<string, { id?: number; text?: string; url?: string }[] | { id?: number; text?: string; url?: string }> | undefined
   
-  // 필드 키를 읽기 쉬운 레이블로 변환
+  // ?꾨뱶 ?ㅻ? ?쎄린 ?ъ슫 ?덉씠釉붾줈 蹂??
   const fieldLabels: Record<string, string> = {
-    'overview.summary': '전공 개요',
-    'summary': '전공 설명',
-    'property': '전공 특성',
-    'aptitude': '이 전공에 어울리는 사람',
-    'enterField': '졸업 후 진출 분야',
-    'trivia': '여담',
-    'whatStudy': '배우는 내용',
-    'mainSubject': '주요 교과목',
-    'relateSubject': '고교 추천 교과목',
-    'careerAct': '진로 탐색 활동',
-    'sidebarJobs': '관련 직업',
-    'sidebarMajors': '관련 전공',
-    'sidebarHowtos': '관련 HowTo'
+    'overview.summary': '?꾧났 媛쒖슂',
+    'summary': '?꾧났 ?ㅻ챸',
+    'property': '?꾧났 ?뱀꽦',
+    'aptitude': '???꾧났???댁슱由щ뒗 ?щ엺',
+    'enterField': '議몄뾽 ??吏꾩텧 遺꾩빞',
+    'trivia': '?щ떞',
+    'whatStudy': '諛곗슦???댁슜',
+    'mainSubject': '二쇱슂 援먭낵紐?,
+    'relateSubject': '怨좉탳 異붿쿇 援먭낵紐?,
+    'careerAct': '吏꾨줈 ?먯깋 ?쒕룞',
+    'sidebarJobs': '愿??吏곸뾽',
+    'sidebarMajors': '愿???꾧났',
+    'sidebarHowtos': '愿??HowTo'
   }
   
-  // 사용자 출처 HTML 생성 (배열 지원)
+  // ?ъ슜??異쒖쿂 HTML ?앹꽦 (諛곗뿴 吏??
   let userSourcesHtml = ''
   if (userSourcesFlat.length > 0) {
     const sourceItems: string[] = []
@@ -791,24 +791,24 @@ const renderMajorSourcesCollapsible = (
   const hasUserSources = userSourcesFlat.length > 0
   
   if (!panel || !safeTrim(panel)) {
-    // API 출처가 없어도 사용자 출처가 있으면 표시
+    // API 異쒖쿂媛 ?놁뼱???ъ슜??異쒖쿂媛 ?덉쑝硫??쒖떆
     if (!hasUserSources) {
       return ''
     }
   }
 
   const activeSourceCount = (partials ? Object.values(partials).filter((value) => Boolean(value)).length : 0) + userSourcesFlat.length
-  const badgeLabel = activeSourceCount > 0 ? `${activeSourceCount}개` : '확인하기'
+  const badgeLabel = activeSourceCount > 0 ? `${activeSourceCount}媛? : '?뺤씤?섍린'
 
   const toggleId = `source-toggle-${normalizedId}`
   const iconId = `source-icon-${normalizedId}`
 
-  // 사용자 출처 섹션
+  // ?ъ슜??異쒖쿂 ?뱀뀡
   const userSourcesSection = hasUserSources ? `
     <div class="mt-6 pt-4 border-t border-wiki-border/40">
       <h4 class="text-sm font-semibold text-wiki-text mb-3 flex items-center gap-2">
         <i class="fas fa-user-edit text-wiki-secondary"></i>
-        사용자 추가 출처
+        ?ъ슜??異붽? 異쒖쿂
       </h4>
       <ol class="space-y-2">
         ${userSourcesHtml}
@@ -827,7 +827,7 @@ const renderMajorSourcesCollapsible = (
       >
         <span class="flex items-center gap-3">
           <i class="fas fa-database text-wiki-secondary text-lg" aria-hidden="true"></i>
-          <span class="text-base">출처</span>
+          <span class="text-base">異쒖쿂</span>
         </span>
         <div class="flex items-center gap-3 text-sm text-wiki-muted">
           <span class="inline-flex items-center gap-1.5 rounded-full border border-wiki-secondary/30 bg-wiki-secondary/10 px-3 py-1.5 text-wiki-secondary font-medium">${escapeHtml(badgeLabel)}</span>
@@ -863,7 +863,7 @@ const renderMajorSourcesCollapsible = (
           });
         }
         
-        // 사용자 출처 클릭 시 해당 필드로 이동
+        // ?ъ슜??異쒖쿂 ?대┃ ???대떦 ?꾨뱶濡??대룞
         document.querySelectorAll('.user-source-item').forEach(function(item) {
           item.addEventListener('click', function() {
             const backTo = this.getAttribute('data-back-to');
@@ -871,25 +871,25 @@ const renderMajorSourcesCollapsible = (
             if (backTo) {
               const targetEl = document.getElementById(backTo);
               if (targetEl) {
-                // 탭 전환이 필요한지 확인
+                // ???꾪솚???꾩슂?쒖? ?뺤씤
                 let targetTabId = 'overview';
                 if (fieldKey && (fieldKey.startsWith('what') || fieldKey.startsWith('main') || fieldKey.startsWith('relate') || fieldKey.startsWith('career'))) {
                   targetTabId = 'details';
                 }
                 
-                // 현재 활성 탭 확인
+                // ?꾩옱 ?쒖꽦 ???뺤씤
                 const activeTab = document.querySelector('[data-major-tab-btn][aria-selected="true"]');
                 const currentTabId = activeTab?.getAttribute('data-target');
                 
                 if (currentTabId !== targetTabId) {
-                  // 탭 전환
+                  // ???꾪솚
                   const targetTabBtn = document.querySelector('[data-major-tab-btn][data-target="' + targetTabId + '"]');
                   if (targetTabBtn) {
                     targetTabBtn.click();
                   }
                 }
                 
-                // 스크롤 이동
+                // ?ㅽ겕濡??대룞
                 setTimeout(function() {
                   targetEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
                 }, 100);
@@ -898,7 +898,7 @@ const renderMajorSourcesCollapsible = (
           });
         });
         
-        // 데이터 출처 하이라이트 기능 (배타적 + 토글 방식)
+        // ?곗씠??異쒖쿂 ?섏씠?쇱씠??湲곕뒫 (諛고???+ ?좉? 諛⑹떇)
         (function() {
           var activeSource = null;
           var sourceColors = {
@@ -931,28 +931,28 @@ const renderMajorSourcesCollapsible = (
               }
             });
             
-            // 해당 출처 항목도 강조
+            // ?대떦 異쒖쿂 ??ぉ??媛뺤“
             var sourceEntry = document.querySelector('[data-source-entry="' + source + '"]');
             if (sourceEntry) {
               sourceEntry.classList.add('ring-2', 'ring-wiki-primary');
             }
           }
           
-          // 출처 항목 클릭 이벤트
+          // 異쒖쿂 ??ぉ ?대┃ ?대깽??
           document.querySelectorAll('[data-source-entry][data-source-has-data="true"]').forEach(function(entry) {
             entry.addEventListener('click', function(e) {
-              // 다른 클릭 이벤트와 충돌 방지
+              // ?ㅻⅨ ?대┃ ?대깽?몄? 異⑸룎 諛⑹?
               if (e.target.closest('.user-source-item')) return;
               
               var source = this.getAttribute('data-source-entry');
               
-              // 배타적 + 토글 방식
+              // 諛고???+ ?좉? 諛⑹떇
               if (activeSource === source) {
-                // 같은 출처 클릭 시 끄기
+                // 媛숈? 異쒖쿂 ?대┃ ???꾧린
                 clearAllHighlights();
                 activeSource = null;
               } else {
-                // 다른 출처 클릭 시 전환
+                // ?ㅻⅨ 異쒖쿂 ?대┃ ???꾪솚
                 clearAllHighlights();
                 highlightSource(source);
                 activeSource = source;
@@ -966,30 +966,31 @@ const renderMajorSourcesCollapsible = (
 }
 
 export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJobSlugs, relatedMajorsByCategory, relatedHowtos, classificationData }: UnifiedMajorDetailTemplateParams): string => {
-  // categoryName 정리: ETL에서 이미 처리된 categoryDisplay 사용, 없으면 폴백
+  // categoryName ?뺣━: ETL?먯꽌 ?대? 泥섎━??categoryDisplay ?ъ슜, ?놁쑝硫??대갚
   const cleanCategoryName = (profile as any).categoryDisplay 
     || (profile.categoryName && profile.categoryName.split(',').length <= 2 ? profile.categoryName : undefined)
 
-  // 1. 히어로 요약: ETL에서 병합된 heroSummary 사용 (첫 문장만), 없으면 폴백
+  // 1. ?덉뼱濡??붿빟: ETL?먯꽌 蹂묓빀??heroSummary ?ъ슜 (泥?臾몄옣留?, ?놁쑝硫??대갚
   const careernetSummary = partials?.CAREERNET?.summary
   const goyong24Summary = partials?.GOYONG24?.summary
   const heroDescription = (profile as any).heroSummary 
     || (profile.summary || careernetSummary || goyong24Summary)?.split('\n')[0]?.trim()
   
   const heroImageUrl = (profile as any).image_url
-  const heroImageAlt = (profile as any).image_alt ?? `${profile.name} 전공 관련 학습 환경`
-  // slug 추출 (이미지 재생성용)
+  const heroImageAlt = (profile as any).image_alt ?? `${profile.name} ?꾧났 愿???숈뒿 ?섍꼍`
+  // slug 異붿텧 (?대?吏 ?ъ깮?깆슜)
   const majorSlug = (profile as any).slug || profile.name
+  const imageGenerationMeta = (profile as any).image_generation_meta || null
   const heroImage = heroImageUrl && heroImageUrl.trim() !== '' 
-    ? renderHeroImage(profile.name, { imageUrl: heroImageUrl, imageAlt: heroImageAlt, dataAttribute: 'data-major-hero-image', context: 'major', slug: majorSlug })
+    ? renderHeroImage(profile.name, { imageUrl: heroImageUrl, imageAlt: heroImageAlt, dataAttribute: 'data-major-hero-image', context: 'major', slug: majorSlug, generationMeta: imageGenerationMeta })
     : ''
 
-  // chartData 정규화: 문자열로 저장된 경우 파싱, 배열이면 첫 번째 요소 추출
+  // chartData ?뺢퇋?? 臾몄옄?대줈 ??λ맂 寃쎌슦 ?뚯떛, 諛곗뿴?대㈃ 泥?踰덉㎏ ?붿냼 異붿텧
   let chartData: any = null
   try {
     const rawChartData = profile.chartData
     if (typeof rawChartData === 'string') {
-      // JSON 문자열로 저장된 경우 파싱
+      // JSON 臾몄옄?대줈 ??λ맂 寃쎌슦 ?뚯떛
       const parsed = JSON.parse(rawChartData)
       chartData = Array.isArray(parsed) ? parsed[0] : parsed
     } else if (Array.isArray(rawChartData)) {
@@ -1001,10 +1002,10 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     chartData = null
   }
 
-  // 고유한 차트 ID 생성용 카운터
+  // 怨좎쑀??李⑦듃 ID ?앹꽦??移댁슫??
   let chartIdCounter = 0
   const generateChartId = (prefix: string) => {
-    const safeId = (profile.id || 'default').replace(/[^a-zA-Z0-9가-힣-]/g, '-')
+    const safeId = (profile.id || 'default').replace(/[^a-zA-Z0-9媛-??]/g, '-')
     return `${prefix}-${safeId}-${++chartIdCounter}`
   }
 
@@ -1014,13 +1015,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     overviewCards.push({ id, label, icon, markup: buildCard(label, icon, markup, { anchorId: id, dataSources }) })
   }
 
-  // 특정 필드가 어느 출처에서 왔는지 판단하는 헬퍼 함수
-  // partials가 있으면 partials 기반, 없으면 profile.sources 기반
+  // ?뱀젙 ?꾨뱶媛 ?대뒓 異쒖쿂?먯꽌 ?붾뒗吏 ?먮떒?섎뒗 ?ы띁 ?⑥닔
+  // partials媛 ?덉쑝硫?partials 湲곕컲, ?놁쑝硫?profile.sources 湲곕컲
   const normalizedProfileSources = Array.isArray(profile.sources) ? profile.sources : []
   const getFieldSources = (fieldExtractor: (p: UnifiedMajorDetail | null | undefined) => any): string[] => {
     const sources: string[] = []
     
-    // partials 기반 판단 (더 정확함)
+    // partials 湲곕컲 ?먮떒 (???뺥솗??
     if (partials?.CAREERNET && fieldExtractor(partials.CAREERNET)) {
       sources.push('CAREERNET')
     }
@@ -1028,9 +1029,9 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       sources.push('GOYONG24')
     }
     
-    // partials가 비어있으면 profile.sources 기반으로 폴백
+    // partials媛 鍮꾩뼱?덉쑝硫?profile.sources 湲곕컲?쇰줈 ?대갚
     if (sources.length === 0 && normalizedProfileSources.length > 0) {
-      // 해당 필드가 profile에 있으면 profile.sources 사용
+      // ?대떦 ?꾨뱶媛 profile???덉쑝硫?profile.sources ?ъ슜
       if (fieldExtractor(profile)) {
         normalizedProfileSources.forEach(s => {
           if (s === 'CAREERNET' || s === 'GOYONG24' || s === 'WORK24_MAJOR') {
@@ -1040,37 +1041,37 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       }
     }
     
-    return [...new Set(sources)]  // 중복 제거
+    return [...new Set(sources)]  // 以묐났 ?쒓굅
   }
 
-  // 1. 전공 개요: 고용24만 사용, 둘 다 있으면 개요에만 표시 (히어로와 중복 방지)
+  // 1. ?꾧났 媛쒖슂: 怨좎슜24留??ъ슜, ?????덉쑝硫?媛쒖슂?먮쭔 ?쒖떆 (?덉뼱濡쒖? 以묐났 諛⑹?)
   const hasCareernetOnly = careernetSummary && !goyong24Summary
   const hasGoyong24Only = goyong24Summary && !careernetSummary
   const hasBothSummaries = careernetSummary && goyong24Summary
   
-  // 전공 개요: overview.summary 우선, 없으면 summary, 없으면 고용24/커리어넷
+  // ?꾧났 媛쒖슂: overview.summary ?곗꽑, ?놁쑝硫?summary, ?놁쑝硫?怨좎슜24/而ㅻ━?대꽬
   const overviewSummary = (profile as any)?.overview?.summary || profile.summary
   if (overviewSummary?.trim()) {
     const summarySources = getFieldSources(p => p?.summary)
-    pushOverviewCard('전공 개요', 'fa-circle-info', formatRichText(overviewSummary), summarySources)
+    pushOverviewCard('?꾧났 媛쒖슂', 'fa-circle-info', formatRichText(overviewSummary), summarySources)
   } else if (goyong24Summary?.trim()) {
-    pushOverviewCard('전공 개요', 'fa-circle-info', formatRichText(goyong24Summary), ['GOYONG24'])
+    pushOverviewCard('?꾧났 媛쒖슂', 'fa-circle-info', formatRichText(goyong24Summary), ['GOYONG24'])
   } else if (hasCareernetOnly && careernetSummary?.trim()) {
-    pushOverviewCard('전공 개요', 'fa-circle-info', formatRichText(careernetSummary), ['CAREERNET'])
+    pushOverviewCard('?꾧났 媛쒖슂', 'fa-circle-info', formatRichText(careernetSummary), ['CAREERNET'])
   }
   
-  // 전공 특성 (property)
+  // ?꾧났 ?뱀꽦 (property)
   if (profile.property?.trim()) {
     const propertySources = getFieldSources(p => p?.property)
-    pushOverviewCard('전공 특성', 'fa-star', formatRichText(profile.property), propertySources)
+    pushOverviewCard('?꾧났 ?뱀꽦', 'fa-star', formatRichText(profile.property), propertySources)
   }
   
   if (profile.aptitude?.trim()) {
     const aptitudeSources = getFieldSources(p => p?.aptitude)
-    pushOverviewCard('이 전공에 어울리는 사람', 'fa-user-check', formatRichText(profile.aptitude), aptitudeSources)
+    pushOverviewCard('???꾧났???댁슱由щ뒗 ?щ엺', 'fa-user-check', formatRichText(profile.aptitude), aptitudeSources)
   }
 
-  // 졸업 후 진출 분야 (enterField + chartData.field + careerFields) - 개요로 이동
+  // 議몄뾽 ??吏꾩텧 遺꾩빞 (enterField + chartData.field + careerFields) - 媛쒖슂濡??대룞
   const normalizePairList = (list: any): Array<{ title: string; description: string }> => {
     if (!list) return []
     if (typeof list === 'string') {
@@ -1091,21 +1092,21 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
   const hasFieldChart = !hasFieldChartUnified && chartData?.field && Array.isArray(chartData.field) && chartData.field.length > 0
   const normalizedEnterField = normalizePairList(profile.enterField)
   const hasEnterField = normalizedEnterField.length > 0
-  const hasCareerFields = !!(profile as any).careerFields?.trim() // 이색학과 진출 분야
+  const hasCareerFields = !!(profile as any).careerFields?.trim() // ?댁깋?숆낵 吏꾩텧 遺꾩빞
 
   if (hasEnterField || hasFieldChart || hasFieldChartUnified || hasCareerFields) {
     const enterFieldParts: string[] = []
 
-    // 1-a. 통일 포맷 차트 (사용자 편집)
+    // 1-a. ?듭씪 ?щ㎎ 李⑦듃 (?ъ슜???몄쭛)
     if (hasFieldChartUnified) {
       enterFieldParts.push(renderUnifiedChart(chartData.field, 'field'))
     }
-    // 1-b. 레거시 차트 (chartData.field)
+    // 1-b. ?덇굅??李⑦듃 (chartData.field)
     else if (hasFieldChart) {
       const chartId = generateChartId('field-chart')
       const fieldLabels = chartData!.field!.map((item: { item?: string; data?: string }) => item.item || '')
       const fieldValues = chartData!.field!.map((item: { item?: string; data?: string }) => parseFloat(item.data || '0'))
-      // 값이 0이 아닌 항목만 필터링
+      // 媛믪씠 0???꾨땶 ??ぉ留??꾪꽣留?
       const filteredData = fieldLabels.map((label: string, i: number) => ({ label, value: fieldValues[i] }))
         .filter((d: { label: string; value: number }) => d.value > 0)
         .sort((a: { label: string; value: number }, b: { label: string; value: number }) => b.value - a.value)
@@ -1126,7 +1127,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           'rgba(168, 85, 247, 0.85)'    // purple
         ]
         
-        // 커스텀 범례 생성 - 상위 5개만 표시, 텍스트 전체 표시 + 퍼센트 오른쪽 정렬
+        // 而ㅼ뒪? 踰붾? ?앹꽦 - ?곸쐞 5媛쒕쭔 ?쒖떆, ?띿뒪???꾩껜 ?쒖떆 + ?쇱꽱???ㅻⅨ履??뺣젹
         const topItems = filteredLabels.slice(0, 5).map((label: string, i: number) => {
           const value = filteredValues[i] || 0
           return `<div class="flex items-center gap-2 sm:gap-3 px-2.5 sm:px-4 py-2 sm:py-2.5 rounded-lg bg-white/5 border border-white/10 min-w-0 sm:min-w-[220px]">
@@ -1141,7 +1142,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
             <div class="bg-wiki-bg/40 rounded-2xl p-6 border border-wiki-border/40">
               <h5 class="text-[16px] font-bold text-wiki-secondary mb-5 flex items-center gap-2">
                 <i class="fas fa-chart-pie text-sm"></i>
-                졸업 후 첫 직업 분야 비율
+                議몄뾽 ??泥?吏곸뾽 遺꾩빞 鍮꾩쑉
               </h5>
               <div class="flex flex-col lg:flex-row gap-6 items-center">
                 <div class="relative w-full lg:w-1/2 p-4" style="height: 300px;">
@@ -1149,7 +1150,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                 </div>
                 <div class="flex flex-wrap lg:flex-col gap-2 justify-center">${topItems}</div>
               </div>
-              <p class="text-sm text-wiki-muted/60 italic text-right mt-4">자료: 한국고용정보원, 대졸자 직업이동 경로조사(2020), ${escapeHtml(profile.name)}</p>
+              <p class="text-sm text-wiki-muted/60 italic text-right mt-4">?먮즺: ?쒓뎅怨좎슜?뺣낫?? ?議몄옄 吏곸뾽?대룞 寃쎈줈議곗궗(2020), ${escapeHtml(profile.name)}</p>
               <script>
                 (function() {
                   const ctx = document.getElementById('${chartId}');
@@ -1191,7 +1192,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       }
     }
     
-    // 2. 텍스트 항목 (enterField)
+    // 2. ?띿뒪????ぉ (enterField)
     if (hasEnterField) {
       const enterItems = normalizedEnterField
         .map(item => {
@@ -1208,20 +1209,20 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       }
     }
     
-    // 3. 이색학과 진출 분야 (careerFields) - 다른 내용처럼 단순하게 표시
+    // 3. ?댁깋?숆낵 吏꾩텧 遺꾩빞 (careerFields) - ?ㅻⅨ ?댁슜泥섎읆 ?⑥닚?섍쾶 ?쒖떆
     if (hasCareerFields) {
       const careerFieldsText = (profile as any).careerFields.trim()
       enterFieldParts.push(`<p class="text-base text-wiki-text leading-relaxed">${escapeHtml(careerFieldsText)}</p>`)
     }
     
     if (enterFieldParts.length > 0) {
-      // 진출 분야 출처 판단
+      // 吏꾩텧 遺꾩빞 異쒖쿂 ?먮떒
       const enterFieldSources = getFieldSources(p => p?.enterField || (p as any)?.careerFields)
-      pushOverviewCard('졸업 후 진출 분야', 'fa-door-open', enterFieldParts.join(''), enterFieldSources)
+      pushOverviewCard('議몄뾽 ??吏꾩텧 遺꾩빞', 'fa-door-open', enterFieldParts.join(''), enterFieldSources)
     }
   }
 
-  // 4. 핵심 지표 - chartData 차트 + 텍스트 지표
+  // 4. ?듭떖 吏??- chartData 李⑦듃 + ?띿뒪??吏??
   const profileAny = profile as any
   const careernetSalary = (partials?.CAREERNET as any)?.salary
   const goyong24Salary = (partials?.GOYONG24 as any)?.salary
@@ -1239,11 +1240,11 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
   if (hasAnyMetrics || hasChartMetrics) {
     const metricsContent: string[] = []
     
-    // 1. 차트 섹션 (2x2 그리드)
+    // 1. 李⑦듃 ?뱀뀡 (2x2 洹몃━??
     if (hasChartMetrics) {
       const chartCards: string[] = []
       
-      // 진학률 (after_graduation) - 도넛 차트
+      // 吏꾪븰瑜?(after_graduation) - ?꾨꽋 李⑦듃
       if (isUnifiedChartData(chartData?.after_graduation)) {
         chartCards.push(`<div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">${renderUnifiedChart(chartData.after_graduation, 'graduation')}</div>`)
       } else if (chartData?.after_graduation && chartData.after_graduation.length > 0) {
@@ -1264,7 +1265,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">
             <h5 class="text-[16px] font-bold text-wiki-secondary mb-4 flex items-center gap-2">
               <i class="fas fa-graduation-cap text-sm"></i>
-              진학률
+              吏꾪븰瑜?
             </h5>
             <div class="relative p-3" style="height: 200px;">
               <canvas id="${chartId}"></canvas>
@@ -1303,7 +1304,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         `)
       }
       
-      // 취업률 (employment_rate) - 바 차트 + 진학률과 동일한 범례 스타일
+      // 痍⑥뾽瑜?(employment_rate) - 諛?李⑦듃 + 吏꾪븰瑜좉낵 ?숈씪??踰붾? ?ㅽ???
       if (isUnifiedChartData(chartData?.employment_rate)) {
         chartCards.push(`<div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">${renderUnifiedChart(chartData.employment_rate, 'emprate')}</div>`)
       } else if (chartData?.employment_rate && chartData.employment_rate.length > 0) {
@@ -1312,7 +1313,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         const values = chartData.employment_rate.map((item: { item?: string; data?: string }) => parseFloat(item.data || '0'))
         const empColors = ['#10B981', '#3B82F6', '#FBBF24']
         
-        // 진학률과 동일한 범례 스타일
+        // 吏꾪븰瑜좉낵 ?숈씪??踰붾? ?ㅽ???
         const empLegendHtml = labels.map((label: string, i: number) => 
           `<span class="inline-flex items-center gap-1.5 sm:gap-2 text-xs sm:text-[15px]">
             <span class="w-2 h-2 sm:w-2.5 sm:h-2.5 rounded-full" style="background: ${empColors[i % empColors.length]}"></span>
@@ -1325,7 +1326,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">
             <h5 class="text-[16px] font-bold text-wiki-secondary mb-4 flex items-center gap-2">
               <i class="fas fa-briefcase text-sm"></i>
-              취업률
+              痍⑥뾽瑜?
             </h5>
             <div class="relative p-3" style="height: 200px;">
               <canvas id="${chartId}"></canvas>
@@ -1367,7 +1368,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         `)
       }
       
-      // 월평균 임금 분포 (avg_salary) - 도넛 차트
+      // ?뷀룊洹??꾧툑 遺꾪룷 (avg_salary) - ?꾨꽋 李⑦듃
       if (isUnifiedChartData(chartData?.avg_salary)) {
         chartCards.push(`<div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">${renderUnifiedChart(chartData.avg_salary, 'salary')}</div>`)
       } else if (chartData?.avg_salary && chartData.avg_salary.length > 0) {
@@ -1388,7 +1389,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">
             <h5 class="text-[16px] font-bold text-wiki-secondary mb-4 flex items-center gap-2">
               <i class="fas fa-won-sign text-sm"></i>
-              월평균 임금 분포
+              ?뷀룊洹??꾧툑 遺꾪룷
             </h5>
             <div class="relative p-3" style="height: 200px;">
               <canvas id="${chartId}"></canvas>
@@ -1427,14 +1428,14 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         `)
       }
       
-      // 첫 직장 만족도 (satisfaction) - 도넛 차트
+      // 泥?吏곸옣 留뚯”??(satisfaction) - ?꾨꽋 李⑦듃
       if (isUnifiedChartData(chartData?.satisfaction)) {
         chartCards.push(`<div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">${renderUnifiedChart(chartData.satisfaction, 'satisfaction')}</div>`)
       } else if (chartData?.satisfaction && chartData.satisfaction.length > 0) {
         const chartId = generateChartId('satisfaction-chart')
         const labels = chartData.satisfaction.map((item: { item?: string; data?: string }) => item.item || '')
         const values = chartData.satisfaction.map((item: { item?: string; data?: string }) => parseFloat(item.data || '0'))
-        // 매우불만족(빨강) -> 불만족(주황) -> 보통(노랑) -> 만족(연두) -> 매우만족(진한초록)
+        // 留ㅼ슦遺덈쭔議?鍮④컯) -> 遺덈쭔議?二쇳솴) -> 蹂댄넻(?몃옉) -> 留뚯”(?곕몢) -> 留ㅼ슦留뚯”(吏꾪븳珥덈줉)
         const satColors = ['#EF4444', '#F97316', '#FBBF24', '#84CC16', '#166534']
         
         const satLegendHtml = labels.map((label: string, i: number) => 
@@ -1449,7 +1450,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <div class="bg-wiki-bg/40 rounded-2xl p-3 sm:p-5 border border-wiki-border/40">
             <h5 class="text-[16px] font-bold text-wiki-secondary mb-4 flex items-center gap-2">
               <i class="fas fa-face-smile text-sm"></i>
-              첫 직장 만족도
+              泥?吏곸옣 留뚯”??
             </h5>
             <div class="relative p-3" style="height: 200px;">
               <canvas id="${chartId}"></canvas>
@@ -1493,12 +1494,12 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <div class="grid grid-cols-2 gap-2 sm:gap-3 mb-4">
             ${chartCards.join('')}
           </div>
-          <p class="text-xs text-wiki-muted/70 italic text-right mb-4">자료: 한국고용정보원, 대졸자 직업이동 경로조사(2020)</p>
+          <p class="text-xs text-wiki-muted/70 italic text-right mb-4">?먮즺: ?쒓뎅怨좎슜?뺣낫?? ?議몄옄 吏곸뾽?대룞 寃쎈줈議곗궗(2020)</p>
         `)
       }
     }
     
-    // 2. 텍스트 지표 (카드 형태로 강조)
+    // 2. ?띿뒪??吏??(移대뱶 ?뺥깭濡?媛뺤“)
     if (hasAnyMetrics) {
       const textMetrics: string[] = []
       
@@ -1507,17 +1508,17 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       const isSameSalary = salaryValue && salaryAfterValue && 
         salaryValue.replace(/<[^>]*>/g, '').trim() === salaryAfterValue.replace(/<[^>]*>/g, '').trim()
       
-      // 월급 지표
+      // ?붽툒 吏??
       let displaySalary = ''
       if (salaryValue && !isSameSalary) {
         displaySalary = salaryValue.replace(/<[^>]*>/g, '').trim()
-        if (/^\d+(\.\d+)?$/.test(displaySalary)) displaySalary = `${displaySalary}만원`
+        if (/^\d+(\.\d+)?$/.test(displaySalary)) displaySalary = `${displaySalary}留뚯썝`
       } else if (salaryAfterValue) {
         displaySalary = salaryAfterValue.replace(/<[^>]*>/g, '').trim()
-        if (/^\d+(\.\d+)?$/.test(displaySalary)) displaySalary = `${displaySalary}만원`
+        if (/^\d+(\.\d+)?$/.test(displaySalary)) displaySalary = `${displaySalary}留뚯썝`
       } else if (isSameSalary && salaryValue) {
         displaySalary = salaryValue.replace(/<[^>]*>/g, '').trim()
-        if (/^\d+(\.\d+)?$/.test(displaySalary)) displaySalary = `${displaySalary}만원`
+        if (/^\d+(\.\d+)?$/.test(displaySalary)) displaySalary = `${displaySalary}留뚯썝`
       }
       
       if (displaySalary) {
@@ -1527,14 +1528,14 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
               <i class="fas fa-won-sign text-xl text-amber-400"></i>
             </div>
             <div>
-              <p class="text-xs text-wiki-muted mb-1">졸업 후 평균 월급</p>
+              <p class="text-xs text-wiki-muted mb-1">議몄뾽 ???됯퇏 ?붽툒</p>
               <p class="text-xl font-bold text-amber-300">${escapeHtml(displaySalary)}</p>
             </div>
           </div>
         `)
       }
       
-      // 취업률 지표 - chartData.employment_rate 우선 사용
+      // 痍⑥뾽瑜?吏??- chartData.employment_rate ?곗꽑 ?ъ슜
       const displayEmployment = getAccurateEmploymentRate(profile) || ''
       
       if (displayEmployment) {
@@ -1544,7 +1545,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
               <i class="fas fa-briefcase text-xl text-emerald-400"></i>
             </div>
             <div>
-              <p class="text-xs text-wiki-muted mb-1">취업률</p>
+              <p class="text-xs text-wiki-muted mb-1">痍⑥뾽瑜?/p>
               <p class="text-xl font-bold text-emerald-300">${escapeHtml(displayEmployment)}</p>
             </div>
           </div>
@@ -1561,23 +1562,23 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     }
     
     if (metricsContent.length > 0) {
-      // 핵심 지표는 여러 출처에서 올 수 있음
+      // ?듭떖 吏?쒕뒗 ?щ윭 異쒖쿂?먯꽌 ?????덉쓬
       const metricsSources = getFieldSources(p => (p as any)?.salary || (p as any)?.employment)
-      pushOverviewCard('핵심 지표', 'fa-gauge-high', metricsContent.join(''), metricsSources)
+      pushOverviewCard('?듭떖 吏??, 'fa-gauge-high', metricsContent.join(''), metricsSources)
     }
   }
 
-  // 관련 영상 (youtubeLinks) - 유튜브 임베드 카드
+  // 愿???곸긽 (youtubeLinks) - ?좏뒠釉??꾨쿋??移대뱶
   const youtubeLinks = (profile as any).youtubeLinks
   if (Array.isArray(youtubeLinks) && youtubeLinks.length > 0) {
     const validLinks = youtubeLinks.filter((l: any) => l?.url && extractYouTubeVideoId(l.url))
     if (validLinks.length > 0) {
       const ytHtml = renderYouTubeSection(validLinks)
-      pushOverviewCard('관련 영상', 'fa-video', ytHtml)
+      pushOverviewCard('愿???곸긽', 'fa-video', ytHtml)
     }
   }
 
-  // 여담 (trivia) - 개요 탭 마지막에 표시
+  // ?щ떞 (trivia) - 媛쒖슂 ??留덉?留됱뿉 ?쒖떆
   const triviaSource = (profile as any)?.trivia
   if (triviaSource) {
     const normalizeTriviaItem = (item: any): string | null => {
@@ -1595,34 +1596,34 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           .filter((p: any) => p && String(p).trim())
           .map((p: any) => String(p).trim())
       : typeof triviaSource === 'string'
-        ? triviaSource.split(/\n|•/).map(s => s.trim()).filter(Boolean)
+        ? triviaSource.split(/\n|??).map(s => s.trim()).filter(Boolean)
         : []
     
     if (triviaItems.length > 0) {
       const html = `<ul class="space-y-2">${triviaItems
         .map(
           item =>
-            `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${escapeHtml(
+            `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${escapeHtml(
               item
             )}</span></li>`
         )
         .join('')}</ul>`
-      // 여담은 사용자 기여 데이터 (출처 정보 없음)
-      pushOverviewCard('여담', 'fa-comment-dots', html)
+      // ?щ떞? ?ъ슜??湲곗뿬 ?곗씠??(異쒖쿂 ?뺣낫 ?놁쓬)
+      pushOverviewCard('?щ떞', 'fa-comment-dots', html)
     }
   }
 
-  // 개요 탭: 콘텐츠가 없으면 빈 문자열 반환 (탭 자체를 숨김)
+  // 媛쒖슂 ?? 肄섑뀗痢좉? ?놁쑝硫?鍮?臾몄옄??諛섑솚 (???먯껜瑜??④?)
   const overviewContent = overviewCards.length > 0
     ? `<div class="space-y-6">
-        ${renderSectionToc('overview', '목차', overviewCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('overview', '紐⑹감', overviewCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${overviewCards.map((card) => card.markup).join('')}
       </div>`
-    : ''  // 콘텐츠 없으면 탭 숨김
+    : ''  // 肄섑뀗痢??놁쑝硫????④?
   
-  // 2. 적성 리스트 (lstMiddleAptd, lstHighAptd) 제거됨 - 사용자 요청
+  // 2. ?곸꽦 由ъ뒪??(lstMiddleAptd, lstHighAptd) ?쒓굅??- ?ъ슜???붿껌
   
-  // 가치관 리스트 (lstVals)
+  // 媛移섍? 由ъ뒪??(lstVals)
   if (profile.lstVals && Array.isArray(profile.lstVals) && profile.lstVals.length > 0) {
     const valItems = profile.lstVals
       .filter(item => item && (item.item || item.val_name))
@@ -1646,32 +1647,32 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       })
       .join('')
     if (valItems) {
-      // 가치관은 커리어넷에서 제공
-      pushOverviewCard('가치관', 'fa-heart', valItems, ['CAREERNET'])
+      // 媛移섍?? 而ㅻ━?대꽬?먯꽌 ?쒓났
+      pushOverviewCard('媛移섍?', 'fa-heart', valItems, ['CAREERNET'])
     }
   }
 
-  // 5. 상세정보 탭 순서 재구성
+  // 5. ?곸꽭?뺣낫 ???쒖꽌 ?ш뎄??
   const learningCards: Array<{ id: string; label: string; icon: string; markup: string }> = []
   const pushLearningCard = (label: string, icon: string, markup: string) => {
     const id = anchorIdFactory('curriculum', label)
     learningCards.push({ id, label, icon, markup: buildCard(label, icon, markup, { anchorId: id }) })
   }
 
-  // 0) 입학상황 섹션 (chartData.gender + chartData.applicant) - 첫 번째 섹션
+  // 0) ?낇븰?곹솴 ?뱀뀡 (chartData.gender + chartData.applicant) - 泥?踰덉㎏ ?뱀뀡
   if (chartData) {
     const admissionCharts: string[] = []
 
-    // 통일 포맷 감지: gender
+    // ?듭씪 ?щ㎎ 媛먯?: gender
     if (isUnifiedChartData(chartData.gender)) {
       admissionCharts.push(renderUnifiedChart(chartData.gender, 'gender'))
     }
-    // 통일 포맷 감지: applicant
+    // ?듭씪 ?щ㎎ 媛먯?: applicant
     if (isUnifiedChartData(chartData.applicant)) {
       admissionCharts.push(renderUnifiedChart(chartData.applicant, 'applicant'))
     }
 
-    // 레거시: 미리 ID와 데이터 준비
+    // ?덇굅?? 誘몃━ ID? ?곗씠??以鍮?
     const genderChartId = (!isUnifiedChartData(chartData.gender) && chartData.gender && Array.isArray(chartData.gender) && chartData.gender.length > 0)
       ? generateChartId('gender-chart') : null
     const genderLabels = (!isUnifiedChartData(chartData.gender) && chartData.gender?.map) ? chartData.gender.map((item: { item?: string; data?: string }) => item.item || '') : []
@@ -1682,7 +1683,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     const applicantLabels = (!isUnifiedChartData(chartData.applicant) && chartData.applicant?.map) ? chartData.applicant.map((item: { item?: string; data?: string }) => item.item || '') : []
     const applicantValues = (!isUnifiedChartData(chartData.applicant) && chartData.applicant?.map) ? chartData.applicant.map((item: { item?: string; data?: string }) => parseFloat(item.data || '0')) : []
 
-    // 성별 비율 (도넛 차트) - 커스텀 범례로 데이터 표시 (레거시)
+    // ?깅퀎 鍮꾩쑉 (?꾨꽋 李⑦듃) - 而ㅼ뒪? 踰붾?濡??곗씠???쒖떆 (?덇굅??
     if (genderChartId) {
       const genderLegendHtml = genderLabels.map((label: string, i: number) => {
         const value = genderValues[i] || 0
@@ -1698,7 +1699,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         <div class="bg-wiki-bg/40 rounded-2xl p-6 border border-wiki-border/40">
           <h5 class="text-[16px] font-bold text-wiki-secondary mb-4 flex items-center gap-2">
             <i class="fas fa-venus-mars text-sm"></i>
-            입학상황(성별)
+            ?낇븰?곹솴(?깅퀎)
           </h5>
           <div class="relative p-3" style="height: 200px;">
             <canvas id="${genderChartId}"></canvas>
@@ -1708,7 +1709,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       `)
     }
     
-    // 지원자/입학자 (바 차트) - 커스텀 범례로 데이터 표시
+    // 吏?먯옄/?낇븰??(諛?李⑦듃) - 而ㅼ뒪? 踰붾?濡??곗씠???쒖떆
     if (admissionChartId) {
       const applicantLegendHtml = applicantLabels.map((label: string, i: number) => {
         const value = applicantValues[i] || 0
@@ -1716,7 +1717,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         return `<span class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/5 border border-white/10">
           <span class="w-3 h-3 rounded-full" style="background: ${colors[i % colors.length]}"></span>
           <span class="text-[15px] text-white/90 font-medium">${escapeHtml(label)}</span>
-          <span class="text-[15px] text-wiki-secondary font-bold">${value.toLocaleString()}명</span>
+          <span class="text-[15px] text-wiki-secondary font-bold">${value.toLocaleString()}紐?/span>
         </span>`
       }).join('')
       
@@ -1724,7 +1725,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         <div class="bg-wiki-bg/40 rounded-2xl p-6 border border-wiki-border/40">
           <h5 class="text-[16px] font-bold text-wiki-secondary mb-4 flex items-center gap-2">
             <i class="fas fa-user-graduate text-sm"></i>
-            입학상황(지원/입학)
+            ?낇븰?곹솴(吏???낇븰)
           </h5>
           <div class="relative p-3" style="height: 200px;">
             <canvas id="${admissionChartId}"></canvas>
@@ -1734,7 +1735,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       `)
     }
     
-    // 입학상황 차트 초기화 스크립트
+    // ?낇븰?곹솴 李⑦듃 珥덇린???ㅽ겕由쏀듃
     if (admissionCharts.length > 0) {
       admissionCharts.push(`
         <script>
@@ -1786,7 +1787,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                         x: { display: false, beginAtZero: true },
                         y: { display: false }
                       },
-                      plugins: { legend: { display: false }, tooltip: { callbacks: { label: function(c) { return c.parsed.x.toLocaleString() + '명'; } } } }
+                      plugins: { legend: { display: false }, tooltip: { callbacks: { label: function(c) { return c.parsed.x.toLocaleString() + '紐?; } } } }
                     }
                   });
                   } catch(e) {}
@@ -1795,13 +1796,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
               ` : ''}
             }
             
-            // 여러 시점에 시도
+            // ?щ윭 ?쒖젏???쒕룄
             window.addEventListener('load', tryInitCharts);
             setTimeout(tryInitCharts, 500);
             setTimeout(tryInitCharts, 1500);
             setTimeout(tryInitCharts, 3000);
             
-            // 모든 클릭에 반응
+            // 紐⑤뱺 ?대┃??諛섏쓳
             document.addEventListener('click', function() {
               setTimeout(tryInitCharts, 100);
               setTimeout(tryInitCharts, 400);
@@ -1816,13 +1817,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         <div class="grid md:grid-cols-2 gap-6 mb-5">
           ${admissionCharts.join('')}
         </div>
-        <p class="text-sm text-wiki-muted/70 italic text-right">자료: 한국교육개발원, 교육통계(2025), ${escapeHtml(profile.name)}</p>
+        <p class="text-sm text-wiki-muted/70 italic text-right">?먮즺: ?쒓뎅援먯쑁媛쒕컻?? 援먯쑁?듦퀎(2025), ${escapeHtml(profile.name)}</p>
       `
-      pushLearningCard('입학상황', 'fa-chart-pie', admissionContent)
+      pushLearningCard('?낇븰?곹솴', 'fa-chart-pie', admissionContent)
     }
   }
   
-  // 교육과정 카드 (배우는 내용 + 기초/심화 + 대학 상세 + 고교 추천)
+  // 援먯쑁怨쇱젙 移대뱶 (諛곗슦???댁슜 + 湲곗큹/?ы솕 + ????곸꽭 + 怨좉탳 異붿쿇)
   const curriculumSections: string[] = []
   const subjectSections: string[] = []
   
@@ -1833,51 +1834,51 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <span class="flex h-8 w-8 items-center justify-center rounded-full bg-wiki-secondary/15 text-wiki-secondary">
             <i class="fas fa-graduation-cap text-xs"></i>
           </span>
-          배우는 내용
+          諛곗슦???댁슜
         </h4>
         ${formatRichText(profile.whatStudy)}
       </div>
     `)
   }
   
-  // ETL에서 파싱된 basicSubjects/advancedSubjects가 있으면 우선 사용 (이색학과)
+  // ETL?먯꽌 ?뚯떛??basicSubjects/advancedSubjects媛 ?덉쑝硫??곗꽑 ?ъ슜 (?댁깋?숆낵)
   let basicSubjects: string[] = Array.isArray((profile as any).basicSubjects) ? (profile as any).basicSubjects : []
   let advancedSubjects: string[] = Array.isArray((profile as any).advancedSubjects) ? (profile as any).advancedSubjects : []
   
-  // ETL에서 제공된 데이터가 없고 mainSubjects가 있으면 파싱 시도
+  // ETL?먯꽌 ?쒓났???곗씠?곌? ?녾퀬 mainSubjects媛 ?덉쑝硫??뚯떛 ?쒕룄
   if (basicSubjects.length === 0 && advancedSubjects.length === 0 && profile.mainSubjects?.length) {
-    // mainSubjects 항목을 문자열로 안전하게 변환
+    // mainSubjects ??ぉ??臾몄옄?대줈 ?덉쟾?섍쾶 蹂??
     const mainSubjectsStr = profile.mainSubjects.map((s: any) => extractDisplayText(s)).filter(Boolean)
-    // ‡ 구분자가 있는 뭉텅이 데이터 파싱
+    // ??援щ텇?먭? ?덈뒗 萸됲뀉???곗씠???뚯떛
     const firstSubject = mainSubjectsStr[0]
-    if (firstSubject && firstSubject.includes('‡')) {
-      const sections = firstSubject.split('‡').filter(s => s.trim())
+    if (firstSubject && firstSubject.includes('??)) {
+      const sections = firstSubject.split('??).filter(s => s.trim())
 
       sections.forEach(section => {
-        if (section.includes('기초과목')) {
-          // "기초과목 : 심리학, 해부학, ..." 형식에서 과목들 추출
-          const subjects = section.replace(/^.*?기초과목\s*[:：]\s*/i, '')
-            .split(/[,、]\s*/)
+        if (section.includes('湲곗큹怨쇰ぉ')) {
+          // "湲곗큹怨쇰ぉ : ?щ━?? ?대??? ..." ?뺤떇?먯꽌 怨쇰ぉ??異붿텧
+          const subjects = section.replace(/^.*?湲곗큹怨쇰ぉ\s*[:竊?\s*/i, '')
+            .split(/[,??\s*/)
             .map(s => s.trim())
-            .filter(s => s && s !== '등')
+            .filter(s => s && s !== '??)
           basicSubjects.push(...subjects)
-        } else if (section.includes('심화과목')) {
-          // "심화과목 : 임상미생물학, ..." 형식에서 과목들 추출
-          const subjects = section.replace(/^.*?심화과목\s*[:：]\s*/i, '')
-            .split(/[,、]\s*/)
+        } else if (section.includes('?ы솕怨쇰ぉ')) {
+          // "?ы솕怨쇰ぉ : ?꾩긽誘몄깮臾쇳븰, ..." ?뺤떇?먯꽌 怨쇰ぉ??異붿텧
+          const subjects = section.replace(/^.*??ы솕怨쇰ぉ\s*[:竊?\s*/i, '')
+            .split(/[,??\s*/)
             .map(s => s.trim())
-            .filter(s => s && s !== '등')
+            .filter(s => s && s !== '??)
           advancedSubjects.push(...subjects)
         }
       })
     } else {
-      // 기존 로직: 배열로 들어온 경우 (문자열로 변환 후 필터)
-      basicSubjects = mainSubjectsStr.filter(s => s && (s.includes('기초') || s.includes('입문')))
-      advancedSubjects = mainSubjectsStr.filter(s => s && !(s.includes('기초') || s.includes('입문')))
+      // 湲곗〈 濡쒖쭅: 諛곗뿴濡??ㅼ뼱??寃쎌슦 (臾몄옄?대줈 蹂?????꾪꽣)
+      basicSubjects = mainSubjectsStr.filter(s => s && (s.includes('湲곗큹') || s.includes('?낅Ц')))
+      advancedSubjects = mainSubjectsStr.filter(s => s && !(s.includes('湲곗큹') || s.includes('?낅Ц')))
     }
   }
   
-  // 기초/심화 과목
+  // 湲곗큹/?ы솕 怨쇰ぉ
   if (basicSubjects.length > 0) {
     subjectSections.push(`
       <div class="pb-6 mb-8 border-b border-wiki-border/30">
@@ -1885,7 +1886,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <span class="flex h-8 w-8 items-center justify-center rounded-full bg-wiki-primary/15 text-wiki-secondary">
             <i class="fas fa-seedling text-xs"></i>
           </span>
-          기초과목
+          湲곗큹怨쇰ぉ
         </h4>
         <div class="flex flex-wrap gap-2">
           ${basicSubjects.map(subj => `<span class="inline-flex items-center gap-1.5 px-3 py-2 bg-wiki-primary/10 border border-wiki-primary/20 rounded-lg text-sm text-wiki-secondary font-medium">${escapeHtml(subj)}</span>`).join('')}
@@ -1901,7 +1902,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           <span class="flex h-8 w-8 items-center justify-center rounded-full bg-purple-500/15 text-purple-400">
             <i class="fas fa-rocket text-xs"></i>
           </span>
-          심화과목
+          ?ы솕怨쇰ぉ
         </h4>
         <div class="flex flex-wrap gap-2">
           ${advancedSubjects.map(subj => `<span class="inline-flex items-center gap-1.5 px-3 py-2 bg-purple-500/10 border border-purple-500/20 rounded-lg text-sm text-emerald-300 font-medium">${escapeHtml(subj)}</span>`).join('')}
@@ -1910,7 +1911,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     `)
   }
   
-  // 서브섹션: 대학 주요 교과목 상세 (pairList 포함, JSON 문자열 파싱)
+  // ?쒕툕?뱀뀡: ???二쇱슂 援먭낵紐??곸꽭 (pairList ?ы븿, JSON 臾몄옄???뚯떛)
   let mainSubjectArray = (profile as any).mainSubject || (profile as any).main_subject || (profile as any).mainSubjects
   if (typeof mainSubjectArray === 'string') {
     try {
@@ -1948,7 +1949,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
               <span class="flex h-8 w-8 items-center justify-center rounded-full bg-wiki-primary/15 text-wiki-primary">
                 <i class="fas fa-graduation-cap text-xs"></i>
               </span>
-              대학 주요 교과목 상세
+              ???二쇱슂 援먭낵紐??곸꽭
             </h4>
             <div class="grid gap-3">
               ${detailItems}
@@ -1958,7 +1959,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       }
     }
     
-  // 서브섹션: 고교 추천 교과목 (pairList 포함, [출처] 제거)
+  // ?쒕툕?뱀뀡: 怨좉탳 異붿쿇 援먭낵紐?(pairList ?ы븿, [異쒖쿂] ?쒓굅)
   let relateSubjectArray = profile.relateSubject || (profile as any).relate_subject
   if (typeof relateSubjectArray === 'string') {
     try {
@@ -1974,7 +1975,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         if (!item) return ''
         const anyItem = item as any
         const title = anyItem.title || anyItem.subject_name || anyItem.SUBJECT_NM || ''
-        if (!title || title.trim().startsWith('[출처')) return ''
+        if (!title || title.trim().startsWith('[異쒖쿂')) return ''
         let desc = anyItem.description || anyItem.subject_description || anyItem.SUBJECT_SUMRY || ''
         if (typeof desc === 'string') {
           desc = desc
@@ -2001,7 +2002,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
             <span class="flex h-8 w-8 items-center justify-center rounded-full bg-wiki-secondary/15 text-wiki-secondary">
               <i class="fas fa-school text-xs"></i>
             </span>
-            고교 추천 교과목
+            怨좉탳 異붿쿇 援먭낵紐?
           </h4>
           <div class="grid gap-3">${highSchoolItems}</div>
         </div>
@@ -2012,10 +2013,10 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
   const curriculumAll = [...curriculumSections, ...subjectSections]
   if (curriculumAll.length > 0) {
     const wrappedSections = `<div class="[&>div:last-child]:border-b-0 [&>div:last-child]:pb-0 [&>div:last-child]:mb-0">${curriculumAll.join('')}</div>`
-    pushLearningCard('교육과정', 'fa-book-open', wrappedSections)
+    pushLearningCard('援먯쑁怨쇱젙', 'fa-book-open', wrappedSections)
   }
   
-  // 7) 진로 탐색 활동 (careerAct) - pairList 지원
+  // 7) 吏꾨줈 ?먯깋 ?쒕룞 (careerAct) - pairList 吏??
   const normalizedCareerAct = normalizePairList((profile as any).careerAct || (profile as any).career_act)
   if (normalizedCareerAct.length > 0) {
     const actItems = normalizedCareerAct
@@ -2034,11 +2035,11 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       })
       .join('')
     if (actItems) {
-      pushLearningCard('진로 탐색 활동', 'fa-compass', `<div class="grid gap-3">${actItems}</div>`)
+      pushLearningCard('吏꾨줈 ?먯깋 ?쒕룞', 'fa-compass', `<div class="grid gap-3">${actItems}</div>`)
     }
   }
 
-  // 커스텀 차트 (사용자 추가)
+  // 而ㅼ뒪? 李⑦듃 (?ъ슜??異붽?)
   const majorCustomCharts = (profile as any).customCharts
   if (Array.isArray(majorCustomCharts) && majorCustomCharts.length > 0) {
     for (let ci = 0; ci < majorCustomCharts.length; ci++) {
@@ -2046,7 +2047,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       if (isUnifiedChartData(cc)) {
         const ccHtml = renderUnifiedChart(cc, `custom-${ci}`)
         if (ccHtml) {
-          pushLearningCard(cc.title || `커스텀 차트 ${ci + 1}`, 'fa-chart-bar', ccHtml)
+          pushLearningCard(cc.title || `而ㅼ뒪? 李⑦듃 ${ci + 1}`, 'fa-chart-bar', ccHtml)
         }
       }
     }
@@ -2054,10 +2055,10 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
 
   const learningContent = learningCards.length > 0
     ? `<div class="space-y-6">
-        ${renderSectionToc('curriculum', '목차', learningCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('curriculum', '紐⑹감', learningCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${learningCards.map((card) => card.markup).join('')}
       </div>`
-    : `<p class="text-sm text-wiki-muted">커리큘럼 정보가 준비 중입니다.</p>`
+    : `<p class="text-sm text-wiki-muted">而ㅻ━?섎읆 ?뺣낫媛 以鍮?以묒엯?덈떎.</p>`
 
   const careerCards: Array<{ id: string; label: string; icon: string; markup: string }> = []
   const pushCareerCard = (label: string, icon: string, markup: string) => {
@@ -2065,14 +2066,14 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     careerCards.push({ id, label, icon, markup: buildCard(label, icon, markup, { anchorId: id }) })
   }
 
-  // 진로 전망 (jobProspect) - 상세정보 탭 진로 섹션에 표시
+  // 吏꾨줈 ?꾨쭩 (jobProspect) - ?곸꽭?뺣낫 ??吏꾨줈 ?뱀뀡???쒖떆
   const jobProspectSource =
     (profile as any)?.jobProspect ||
     (profile as any)?.overview?.jobProspect ||
     (profile as any)?.prospect
   
   if (jobProspectSource) {
-    // 진로 전망 아이템 정규화 함수
+    // 吏꾨줈 ?꾨쭩 ?꾩씠???뺢퇋???⑥닔
     const normalizeProspectItem = (item: any): string | null => {
       if (typeof item === 'string') return item.trim()
       if (item && typeof item === 'object') {
@@ -2088,29 +2089,29 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           .filter((p: any) => p && String(p).trim())
           .map((p: any) => String(p).trim())
       : typeof jobProspectSource === 'string'
-        ? jobProspectSource.split(/\n|•/).map(s => s.trim()).filter(Boolean)
+        ? jobProspectSource.split(/\n|??).map(s => s.trim()).filter(Boolean)
         : []
     if (prospectItems.length > 0) {
       const html = `<ul class="space-y-2">${prospectItems
         .map(
           item =>
-            `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">•</span><span>${escapeHtml(
+            `<li class="flex items-start gap-2 text-base text-wiki-text"><span class="text-wiki-secondary">??/span><span>${escapeHtml(
               item
             )}</span></li>`
         )
         .join('')}</ul>`
-      pushCareerCard('진로 전망', 'fa-chart-line', html)
+      pushCareerCard('吏꾨줈 ?꾨쭩', 'fa-chart-line', html)
     }
   }
   
-  // chartData 차트는 개요 탭(핵심 지표)과 상세정보 탭(입학상황)으로 이동됨
+  // chartData 李⑦듃??媛쒖슂 ???듭떖 吏??怨??곸꽭?뺣낫 ???낇븰?곹솴)?쇰줈 ?대룞??
 
   const careerContent = careerCards.length > 0
     ? `<div class="space-y-6">
-        ${renderSectionToc('career', '목차', careerCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('career', '紐⑹감', careerCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${careerCards.map((card) => card.markup).join('')}
       </div>`
-    : `<p class="text-sm text-wiki-muted">진로 정보가 준비 중입니다.</p>`
+    : `<p class="text-sm text-wiki-muted">吏꾨줈 ?뺣낫媛 以鍮?以묒엯?덈떎.</p>`
 
   const universityCards: Array<{ id: string; label: string; icon: string; markup: string }> = []
   const pushUniversityCard = (label: string, icon: string, markup: string) => {
@@ -2120,20 +2121,20 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
 
   const universitiesContent = renderUniversities(profile.universities)
   if (universitiesContent) {
-    pushUniversityCard('대학정보', 'fa-building-columns', universitiesContent)
+    pushUniversityCard('??숈젙蹂?, 'fa-building-columns', universitiesContent)
   }
   const recruitmentContent = renderRecruitmentTable(profile.recruitmentStatus)
   if (recruitmentContent) {
-    pushUniversityCard('모집 정원 & 지원 현황', 'fa-users', recruitmentContent)
+    pushUniversityCard('紐⑥쭛 ?뺤썝 & 吏???꾪솴', 'fa-users', recruitmentContent)
   }
 
-  // 대학정보 탭: 콘텐츠가 없으면 빈 문자열 반환 (탭 자체를 숨김)
+  // ??숈젙蹂??? 肄섑뀗痢좉? ?놁쑝硫?鍮?臾몄옄??諛섑솚 (???먯껜瑜??④?)
   const universityContent = universityCards.length > 0
     ? `<div class="space-y-6">
-        ${renderSectionToc('universities', '목차', universityCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('universities', '紐⑹감', universityCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${universityCards.map((card) => card.markup).join('')}
       </div>`
-    : ''  // 콘텐츠 없으면 탭 숨김
+    : ''  // 肄섑뀗痢??놁쑝硫????④?
 
   const networkCards: Array<{ id: string; label: string; icon: string; markup: string }> = []
   const pushNetworkCard = (label: string, icon: string, markup: string) => {
@@ -2141,20 +2142,20 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     networkCards.push({ id, label, icon, markup: buildCard(label, icon, markup, { anchorId: id }) })
   }
 
-  // 추천 유사 전공은 히어로 태그로 이동됨 - 상세정보에서 제거
+  // 異붿쿇 ?좎궗 ?꾧났? ?덉뼱濡??쒓렇濡??대룞??- ?곸꽭?뺣낫?먯꽌 ?쒓굅
 
   const networkContent = networkCards.length > 0
     ? `<div class="space-y-6">
-        ${renderSectionToc('network', '목차', networkCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('network', '紐⑹감', networkCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${networkCards.map((card) => card.markup).join('')}
       </div>`
-    : `<p class="text-sm text-wiki-muted">연결 정보가 준비 중입니다.</p>`
+    : `<p class="text-sm text-wiki-muted">?곌껐 ?뺣낫媛 以鍮?以묒엯?덈떎.</p>`
 
-  // 상세정보 탭: 커리큘럼 + 진로·전망 + 연결정보 통합
+  // ?곸꽭?뺣낫 ?? 而ㅻ━?섎읆 + 吏꾨줈쨌?꾨쭩 + ?곌껐?뺣낫 ?듯빀
   const detailCards: Array<{ id: string; label: string; icon: string; markup: string }> = []
 
-  // 커리큘럼 카드들을 details ID로 다시 빌드
-  // 주의: 정규식에서 g 플래그 제거 - 첫 번째(article) ID만 교체하고 내부 canvas ID는 유지
+  // 而ㅻ━?섎읆 移대뱶?ㅼ쓣 details ID濡??ㅼ떆 鍮뚮뱶
+  // 二쇱쓽: ?뺢퇋?앹뿉??g ?뚮옒洹??쒓굅 - 泥?踰덉㎏(article) ID留?援먯껜?섍퀬 ?대? canvas ID???좎?
   learningCards.forEach(card => {
     const newId = anchorIdFactory('details', card.label)
     const newMarkup = card.markup
@@ -2163,7 +2164,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     detailCards.push({ id: newId, label: card.label, icon: card.icon, markup: newMarkup })
   })
 
-  // 진로·전망 카드들을 details ID로 다시 빌드
+  // 吏꾨줈쨌?꾨쭩 移대뱶?ㅼ쓣 details ID濡??ㅼ떆 鍮뚮뱶
   careerCards.forEach(card => {
     const newId = anchorIdFactory('details', card.label)
     const newMarkup = card.markup
@@ -2172,7 +2173,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     detailCards.push({ id: newId, label: card.label, icon: card.icon, markup: newMarkup })
   })
 
-  // 연결정보 카드들을 details ID로 다시 빌드
+  // ?곌껐?뺣낫 移대뱶?ㅼ쓣 details ID濡??ㅼ떆 鍮뚮뱶
   networkCards.forEach(card => {
     const newId = anchorIdFactory('details', card.label)
     const newMarkup = card.markup
@@ -2181,18 +2182,18 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     detailCards.push({ id: newId, label: card.label, icon: card.icon, markup: newMarkup })
   })
 
-  // 상세정보 탭: 콘텐츠가 없으면 빈 문자열 반환 (탭 자체를 숨김)
+  // ?곸꽭?뺣낫 ?? 肄섑뀗痢좉? ?놁쑝硫?鍮?臾몄옄??諛섑솚 (???먯껜瑜??④?)
   const detailContent = detailCards.length > 0
     ? `<div class="space-y-6">
-        ${renderSectionToc('details', '목차', detailCards.map(({ id, label, icon }) => ({ id, label, icon })))}
+        ${renderSectionToc('details', '紐⑹감', detailCards.map(({ id, label, icon }) => ({ id, label, icon })))}
         ${detailCards.map((card) => card.markup).join('')}
       </div>`
-    : ''  // 콘텐츠 없으면 탭 숨김
+    : ''  // 肄섑뀗痢??놁쑝硫????④?
 
   const tabEntries: TabEntry[] = [
-    { id: 'overview', label: '개요', icon: 'fa-circle-info', content: overviewContent },
-    { id: 'details', label: '상세정보', icon: 'fa-layer-group', content: detailContent },
-    { id: 'universities', label: '대학정보', icon: 'fa-building-columns', content: universityContent }
+    { id: 'overview', label: '媛쒖슂', icon: 'fa-circle-info', content: overviewContent },
+    { id: 'details', label: '?곸꽭?뺣낫', icon: 'fa-layer-group', content: detailContent },
+    { id: 'universities', label: '??숈젙蹂?, icon: 'fa-building-columns', content: universityContent }
   ].filter((entry) => entry.content && entry.content.trim().length > 0)
 
   const entitySlug = composeDetailSlug('major', profile.name, profile.id)
@@ -2218,25 +2219,25 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     entitySources: profile.sources,
     tabs: tabEntries,
     tabFallback: {
-      title: '데이터 준비 중',
+      title: '?곗씠??以鍮?以?,
       icon: 'fa-circle-info',
-      message: '이 전공은 아직 상세 정보가 충분하지 않습니다. 데이터가 수집되는 대로 탭이 활성화됩니다.'
+      message: '???꾧났? ?꾩쭅 ?곸꽭 ?뺣낫媛 異⑸텇?섏? ?딆뒿?덈떎. ?곗씠?곌? ?섏쭛?섎뒗 ?濡???씠 ?쒖꽦?붾맗?덈떎.'
     },
     ctaLinks: [],
     comments: {
-      title: `${profile.name} 커뮤니티`,
-      description: `${profile.name}에 대해 자유롭게 이야기해주세요.`,
-      feedbackLabel: '우선 적용 의견 보내기',
-      notifyLabel: '알림 받기',
-      emptyLabel: `아직 등록된 댓글이 없습니다. ${profile.name}에 대해 자유롭게 이야기해주세요.`,
-      showForm: true  // 익명 사용자도 댓글 작성 가능
+      title: `${profile.name} 而ㅻ??덊떚`,
+      description: `${profile.name}??????먯쑀濡?쾶 ?댁빞湲고빐二쇱꽭??`,
+      feedbackLabel: '?곗꽑 ?곸슜 ?섍껄 蹂대궡湲?,
+      notifyLabel: '?뚮┝ 諛쏄린',
+      emptyLabel: `?꾩쭅 ?깅줉???볤????놁뒿?덈떎. ${profile.name}??????먯쑀濡?쾶 ?댁빞湲고빐二쇱꽭??`,
+      showForm: true  // ?듬챸 ?ъ슜?먮룄 ?볤? ?묒꽦 媛??
     },
     partials,
     sources,
     metaExtra: Object.keys(detailMetaExtra).length ? detailMetaExtra : undefined
   })
 
-  // 사이드바 구성 (직업 템플릿과 동일한 구조)
+  // ?ъ씠?쒕컮 援ъ꽦 (吏곸뾽 ?쒗뵆由욧낵 ?숈씪??援ъ“)
   const normalizeNameSlug = (item: any, defaultType: 'job' | 'major' | 'howto'): { name: string; slug: string } => {
     if (!item) return { name: '', slug: '' }
     if (typeof item === 'string') {
@@ -2264,9 +2265,9 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
 
   const sidebarSections: string[] = []
   
-  // 기본 정보 제거 (사이드바에서 제거)
+  // 湲곕낯 ?뺣낫 ?쒓굅 (?ъ씠?쒕컮?먯꽌 ?쒓굅)
 
-  // 관련 직업 (sidebarJobs 우선, 없으면 relatedJobs) - 5개 이상일 때 접기/펼치기
+  // 愿??吏곸뾽 (sidebarJobs ?곗꽑, ?놁쑝硫?relatedJobs) - 5媛??댁긽?????묎린/?쇱튂湲?
   const sidebarJobsArr = (profile as any)?.sidebarJobs || profile.relatedJobs || []
   if (sidebarJobsArr.length) {
     const limit = 5
@@ -2277,7 +2278,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     const renderJob = (job: any, isHidden: boolean = false) => {
       const { name: jobName, slug: jobSlug } = normalizeNameSlug(job, 'job')
       if (!jobName) return ''
-      // 커리어위키에 존재하는 직업이면 직접 링크, 아니면 검색 페이지로
+      // 而ㅻ━?댁쐞?ㅼ뿉 議댁옱?섎뒗 吏곸뾽?대㈃ 吏곸젒 留곹겕, ?꾨땲硫?寃???섏씠吏濡?
       const existingSlug = existingJobSlugs?.get(jobName) || (jobSlug ? existingJobSlugs?.get(jobSlug) : undefined)
       const href = existingSlug 
         ? `/job/${encodeURIComponent(existingSlug)}`
@@ -2303,13 +2304,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     
     const toggleButton = hasMore ? `
       <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-        <span class="toggle-text">더보기</span>
+        <span class="toggle-text">?붾낫湲?/span>
         <span class="toggle-count text-xs opacity-75">(+${hiddenJobs.length})</span>
         <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
       </button>
     ` : ''
     
-    sidebarSections.push(renderSidebarSection('관련 직업', 'fa-briefcase', `
+    sidebarSections.push(renderSidebarSection('愿??吏곸뾽', 'fa-briefcase', `
       <div class="expandable-list">
         <ul class="space-y-2" role="list">${jobsList}</ul>
         ${toggleButton}
@@ -2317,17 +2318,17 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     `))
   }
 
-  // 6-2. 관련 전공 (sidebarMajors 우선, 없으면 relatedMajorsByCategory)
+  // 6-2. 愿???꾧났 (sidebarMajors ?곗꽑, ?놁쑝硫?relatedMajorsByCategory)
   const sidebarMajorsArr = (profile as any)?.sidebarMajors || relatedMajorsByCategory || []
   if (sidebarMajorsArr && sidebarMajorsArr.length > 0) {
-    // 자기 자신 제외 (DB에서 이미 제외하지만 방어 로직)
+    // ?먭린 ?먯떊 ?쒖쇅 (DB?먯꽌 ?대? ?쒖쇅?섏?留?諛⑹뼱 濡쒖쭅)
     const currentMajorName = profile.name?.trim().toLowerCase()
     const filteredMajors = sidebarMajorsArr.filter((major: any) => 
       major.name?.trim().toLowerCase() !== currentMajorName
     )
     
     if (filteredMajors.length === 0) {
-      // 필터링 후 남은 게 없으면 섹션 표시 안 함
+      // ?꾪꽣留????⑥? 寃??놁쑝硫??뱀뀡 ?쒖떆 ????
     } else {
     const limit = 5
     const hasMore = filteredMajors.length > limit
@@ -2358,13 +2359,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     
     const toggleButton = hasMore ? `
       <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-        <span class="toggle-text">더보기</span>
+        <span class="toggle-text">?붾낫湲?/span>
         <span class="toggle-count text-xs opacity-75">(+${hiddenMajors.length})</span>
         <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
       </button>
     ` : ''
     
-    sidebarSections.push(renderSidebarSection('관련 전공', 'fa-graduation-cap', `
+    sidebarSections.push(renderSidebarSection('愿???꾧났', 'fa-graduation-cap', `
       <div class="expandable-list">
         <ul class="space-y-2" role="list">${majorsList}</ul>
         ${toggleButton}
@@ -2373,7 +2374,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     }
   }
 
-  // 6-3. 관련 가이드 (이 전공을 참조하는 HowTo)
+  // 6-3. 愿??媛?대뱶 (???꾧났??李몄“?섎뒗 HowTo)
   if (relatedHowtos?.length) {
     const guideLimit = 5
     const visibleHowtos = relatedHowtos.slice(0, guideLimit)
@@ -2401,7 +2402,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
 
     const toggleButton = hasMoreGuides ? `
       <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-        <span class="toggle-text">더보기</span>
+        <span class="toggle-text">?붾낫湲?/span>
         <span class="toggle-count text-xs opacity-75">(+${hiddenHowtos.length})</span>
         <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
       </button>
@@ -2409,7 +2410,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
 
     sidebarSections.push(
       renderSidebarSection(
-        '관련 가이드',
+        '愿??媛?대뱶',
         'fa-book-open',
         `
           <div class="expandable-list">
@@ -2421,34 +2422,34 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     )
   }
 
-  // 7. 추천 자격증 (계층 구조로 표시)
+  // 7. 異붿쿇 ?먭꺽利?(怨꾩링 援ъ“濡??쒖떆)
   const careernetLicenses = partials?.CAREERNET?.licenses || profile.licenses || []
   const goyong24Licenses = partials?.GOYONG24?.licenses || []
   
-  // 병합 및 자격증 파싱 (계층 구조 유지, 중복 제거)
+  // 蹂묓빀 諛??먭꺽利??뚯떛 (怨꾩링 援ъ“ ?좎?, 以묐났 ?쒓굅)
   const allLicenses = [...careernetLicenses, ...goyong24Licenses]
   const licenseMap = new Map<string, string[]>() // name -> subLicenses
   
-  // '등'과 같은 불필요한 접미사를 필터링하는 함수
+  // '??怨?媛숈? 遺덊븘?뷀븳 ?묐??щ? ?꾪꽣留곹븯???⑥닔
   const isValidLicense = (text: string): boolean => {
     if (!text || !text.trim()) return false
     const trimmed = text.trim()
-    // '등', '등등' 같은 일반적인 접미사 필터링
-    if (/^등+$/.test(trimmed)) return false
+    // '??, '?깅벑' 媛숈? ?쇰컲?곸씤 ?묐????꾪꽣留?
+    if (/^??$/.test(trimmed)) return false
     return true
   }
   
   allLicenses.forEach(rawLicense => {
-    // 객체 안전 처리: { name: "..." } 형태일 수 있음
+    // 媛앹껜 ?덉쟾 泥섎━: { name: "..." } ?뺥깭?????덉쓬
     const license = typeof rawLicense === 'string' ? rawLicense : extractDisplayText(rawLicense)
     if (!license || !license.trim()) return
 
     let text = license.trim()
     
-    // "국가자격 :", "민간자격 :" 등의 prefix 제거
-    text = text.replace(/^(국가자격|민간자격|공인자격|기타자격)\s*[:：]\s*/i, '')
+    // "援???먭꺽 :", "誘쇨컙?먭꺽 :" ?깆쓽 prefix ?쒓굅
+    text = text.replace(/^(援???먭꺽|誘쇨컙?먭꺽|怨듭씤?먭꺽|湲고??먭꺽)\s*[:竊?\s*/i, '')
     
-    // 괄호가 있는 항목 처리: "전문간호사(A, B, C)" → { name: "전문간호사", subLicenses: ["A", "B", "C"] }
+    // 愿꾪샇媛 ?덈뒗 ??ぉ 泥섎━: "?꾨Ц媛꾪샇??A, B, C)" ??{ name: "?꾨Ц媛꾪샇??, subLicenses: ["A", "B", "C"] }
     const bracketMatches = text.match(/([^(,]+)\(([^)]+)\)/g)
     if (bracketMatches) {
       bracketMatches.forEach(match => {
@@ -2456,10 +2457,10 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         if (prefix?.trim() && isValidLicense(prefix.trim())) {
           const name = prefix.trim()
           const subLicenses = inner
-            ? inner.split(/[,、]/).map(s => s.trim()).filter(s => isValidLicense(s))
+            ? inner.split(/[,??/).map(s => s.trim()).filter(s => isValidLicense(s))
             : []
           
-          // 중복 처리: 이미 있으면 서브 자격증 병합
+          // 以묐났 泥섎━: ?대? ?덉쑝硫??쒕툕 ?먭꺽利?蹂묓빀
           if (licenseMap.has(name)) {
             const existing = licenseMap.get(name) || []
             const merged = Array.from(new Set([...existing, ...subLicenses]))
@@ -2468,13 +2469,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
             licenseMap.set(name, subLicenses)
           }
         }
-        // 원본에서 제거
+        // ?먮낯?먯꽌 ?쒓굅
         text = text.replace(match, '')
       })
     }
     
-    // 나머지를 쉼표로 분리 (서브 자격증 없음)
-    text.split(/[,、]/).forEach(item => {
+    // ?섎㉧吏瑜??쇳몴濡?遺꾨━ (?쒕툕 ?먭꺽利??놁쓬)
+    text.split(/[,??/).forEach(item => {
       const trimmed = item.trim()
       if (isValidLicense(trimmed) && !licenseMap.has(trimmed)) {
         licenseMap.set(trimmed, [])
@@ -2482,7 +2483,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     })
   })
   
-  // Map을 배열로 변환
+  // Map??諛곗뿴濡?蹂??
   const parsedLicenses = Array.from(licenseMap.entries()).map(([name, subLicenses]) => ({
     name,
     subLicenses: subLicenses.length > 0 ? subLicenses : undefined
@@ -2536,7 +2537,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         
         const subToggle = hasMoreSubs ? `
           <button class="sub-expand-toggle ml-7 mt-1 px-2 py-1 rounded text-xs text-wiki-secondary hover:text-wiki-secondary/80 transition-colors flex items-center gap-1" data-expanded="false">
-            <span class="sub-toggle-text">+${hiddenSubs.length}개 더보기</span>
+            <span class="sub-toggle-text">+${hiddenSubs.length}媛??붾낫湲?/span>
             <i class="fas fa-chevron-down text-[8px] sub-toggle-icon transition-transform"></i>
           </button>
         ` : ''
@@ -2565,13 +2566,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
     
     const toggleButton = hasMore ? `
       <button class="expand-toggle mt-3 w-full px-3 py-2 rounded-lg border border-wiki-border/40 bg-wiki-bg/40 hover:border-wiki-primary/60 hover:bg-wiki-primary/5 transition-all duration-200 flex items-center justify-center gap-2 text-sm text-wiki-muted hover:text-wiki-primary" data-expanded="false">
-        <span class="toggle-text">더보기</span>
+        <span class="toggle-text">?붾낫湲?/span>
         <span class="toggle-count text-xs opacity-75">(+${hiddenLicenses.length})</span>
         <i class="fas fa-chevron-down text-xs toggle-icon transition-transform"></i>
       </button>
     ` : ''
     
-    sidebarSections.push(renderSidebarSection('추천 자격증', 'fa-certificate', `
+    sidebarSections.push(renderSidebarSection('異붿쿇 ?먭꺽利?, 'fa-certificate', `
       <div class="expandable-list">
         <ul class="space-y-2" role="list">${licensesList}</ul>
         ${toggleButton}
@@ -2582,17 +2583,17 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
   const hasSidebar = sidebarSections.length > 0
   const sidebarContent = sidebarSections.join('')
 
-  // 사용자 출처 평탄화 (페이지 표시 순서대로 정렬, 1부터 번호 부여)
+  // ?ъ슜??異쒖쿂 ?됲깂??(?섏씠吏 ?쒖떆 ?쒖꽌?濡??뺣젹, 1遺??踰덊샇 遺??
   const normalizeUserSources = (src: any): Array<{ id: number; fieldKey: string; text: string; displayOrder: number }> => {
     if (!src || typeof src !== 'object') return []
     
-    // 필드 표시 순서 (개요 탭 → 상세정보 탭)
+    // ?꾨뱶 ?쒖떆 ?쒖꽌 (媛쒖슂 ?????곸꽭?뺣낫 ??
     const fieldOrder: string[] = [
-      // 개요 탭
+      // 媛쒖슂 ??
       'overview.summary', 'summary', 'property', 'aptitude', 'enterField', 'trivia',
-      // 상세정보 탭
+      // ?곸꽭?뺣낫 ??
       'whatStudy', 'mainSubject', 'relateSubject', 'careerAct',
-      // 사이드바
+      // ?ъ씠?쒕컮
       'sidebarJobs', 'sidebarMajors', 'sidebarHowtos'
     ]
     
@@ -2606,7 +2607,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         const originalId = item?.id || 0
         const displayOrder = fieldOrder.indexOf(fieldKey)
         flat.push({ 
-          id: 0, // 나중에 순서대로 재부여
+          id: 0, // ?섏쨷???쒖꽌?濡??щ???
           fieldKey, 
           text, 
           displayOrder: displayOrder >= 0 ? displayOrder : 999,
@@ -2615,14 +2616,14 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       })
     }
     
-    // 표시 순서대로 정렬 후 1부터 번호 재부여
+    // ?쒖떆 ?쒖꽌?濡??뺣젹 ??1遺??踰덊샇 ?щ???
     flat.sort((a, b) => {
       if (a.displayOrder !== b.displayOrder) return a.displayOrder - b.displayOrder
-      return a.originalId - b.originalId // 같은 필드 내에서는 입력 순서
+      return a.originalId - b.originalId // 媛숈? ?꾨뱶 ?댁뿉?쒕뒗 ?낅젰 ?쒖꽌
     })
     
     return flat.map((item, idx) => ({
-      id: idx + 1, // 1부터 순차 번호
+      id: idx + 1, // 1遺???쒖감 踰덊샇
       fieldKey: item.fieldKey,
       text: item.text,
       displayOrder: item.displayOrder
@@ -2631,7 +2632,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
 
   const userSourcesFlat = normalizeUserSources((profile as any)._sources)
 
-  // 데이터 출처 collapsible (직업 템플릿과 동일)
+  // ?곗씠??異쒖쿂 collapsible (吏곸뾽 ?쒗뵆由욧낵 ?숈씪)
   const sourcesCollapsible = renderMajorSourcesCollapsible(profile, sources, partials, userSourcesFlat)
 
   const sourcesBlock = sourcesCollapsible
@@ -2641,15 +2642,15 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
   const adSlotBlock = renderAdSlot({ entityType: 'major' })
   const communityBlock = `<div data-major-community>${commentsPlaceholder}</div>`
 
-  // 6. 히어로 태그: ETL에서 병합된 heroTags 사용, 없으면 폴백
+  // 6. ?덉뼱濡??쒓렇: ETL?먯꽌 蹂묓빀??heroTags ?ъ슜, ?놁쑝硫??대갚
   const etlHeroTags = (profile as any).heroTags
   let heroTags: string[] = []
   
   if (etlHeroTags && Array.isArray(etlHeroTags) && etlHeroTags.length > 0) {
-    // ETL에서 미리 병합된 태그 사용 (제한 없음) — 객체 안전 처리
+    // ETL?먯꽌 誘몃━ 蹂묓빀???쒓렇 ?ъ슜 (?쒗븳 ?놁쓬) ??媛앹껜 ?덉쟾 泥섎━
     heroTags = etlHeroTags.map((t: any) => extractDisplayText(t)).filter(Boolean)
   } else {
-    // 폴백: 기존 로직으로 계산
+    // ?대갚: 湲곗〈 濡쒖쭅?쇰줈 怨꾩궛
     const careernetRelated = partials?.CAREERNET?.relatedMajors || []
     const goyong24Related = partials?.GOYONG24?.relatedMajors || []
     
@@ -2675,10 +2676,10 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
         .map(m => m.trim())
     ))
     
-    heroTags = uniqueRelatedMajors  // 제한 없음
+    heroTags = uniqueRelatedMajors  // ?쒗븳 ?놁쓬
   }
 
-  // 태그 15개 + 더보기/접기 버튼
+  // ?쒓렇 15媛?+ ?붾낫湲??묎린 踰꾪듉
   const visibleLimit = 15
   const hasMoreTags = heroTags.length > visibleLimit
   const visibleTags = heroTags.slice(0, visibleLimit)
@@ -2699,13 +2700,13 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
   
   const moreTagsButton = hasMoreTags
     ? `<button type="button" id="${majorContainerId}-more" class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-wiki-bg/80 text-xs text-wiki-muted font-medium border-2 border-dashed border-wiki-border hover:border-wiki-primary/50 hover:text-wiki-primary hover:bg-wiki-primary/5 transition-all duration-200" onclick="document.getElementById('${majorContainerId}-hidden').classList.remove('hidden'); this.classList.add('hidden'); document.getElementById('${majorContainerId}-less').classList.remove('hidden');">
-        <i class="fas fa-plus text-[10px]" aria-hidden="true"></i>${hiddenTags.length}개 더보기
+        <i class="fas fa-plus text-[10px]" aria-hidden="true"></i>${hiddenTags.length}媛??붾낫湲?
       </button>`
     : ''
   
   const lessTagsButton = hasMoreTags
     ? `<button type="button" id="${majorContainerId}-less" class="hidden inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-wiki-bg/80 text-xs text-wiki-muted font-medium border-2 border-dashed border-wiki-border hover:border-wiki-primary/50 hover:text-wiki-primary hover:bg-wiki-primary/5 transition-all duration-200" onclick="document.getElementById('${majorContainerId}-hidden').classList.add('hidden'); this.classList.add('hidden'); document.getElementById('${majorContainerId}-more').classList.remove('hidden');">
-        <i class="fas fa-minus text-[10px]" aria-hidden="true"></i>접기
+        <i class="fas fa-minus text-[10px]" aria-hidden="true"></i>?묎린
       </button>`
     : ''
   
@@ -2749,11 +2750,11 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                 data-edit-mode-trigger
                 data-entity-type="major"
                 data-entity-id="${escapeHtml(profile.id)}"
-                aria-label="편집 모드"
-                title="이 페이지 편집하기"
+                aria-label="?몄쭛 紐⑤뱶"
+                title="???섏씠吏 ?몄쭛?섍린"
               >
                 <i class="fas fa-edit" aria-hidden="true"></i>
-                편집
+                ?몄쭛
               </button>
               <button 
                 type="button" 
@@ -2761,19 +2762,19 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                 data-history-trigger
                 data-entity-type="major"
                 data-entity-id="${escapeHtml(profile.id)}"
-                aria-label="역사"
-                title="이 페이지의 편집 이력 보기"
+                aria-label="??궗"
+                title="???섏씠吏???몄쭛 ?대젰 蹂닿린"
               >
                 <i class="fas fa-history" aria-hidden="true"></i>
-                역사
+                ??궗
               </button>
               <div class="relative" data-share-root>
                 <button type="button" class="px-4 py-2 bg-wiki-primary text-white rounded-lg text-sm hover:brightness-110 transition inline-flex items-center gap-2" data-share-trigger data-share-path="/major/${escapeHtml(profile.id)}" data-share-title="${escapeHtml(profile.name)}" data-share-og-image="${heroImageUrl ? escapeHtml(heroImageUrl) : '/images/og-default.png'}">
                   <i class="fas fa-share-nodes" aria-hidden="true"></i>
-                  공유
+                  怨듭쑀
                 </button>
               </div>
-              <!-- 저장 버튼 -->
+              <!-- ???踰꾪듉 -->
               <button 
                 type="button" 
                 class="p-2.5 bg-wiki-card border border-wiki-border/60 text-wiki-text rounded-lg text-sm hover:border-amber-400/50 hover:text-amber-400 transition"
@@ -2781,8 +2782,8 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                 data-bookmark-type="major"
                 data-bookmark-slug="${escapeHtml(profile.id)}"
                 data-bookmark-title="${escapeHtml(profile.name)}"
-                aria-label="저장"
-                title="저장함에 추가"
+                aria-label="???
+                title="??ν븿??異붽?"
               >
                 <i class="fas fa-bookmark" aria-hidden="true"></i>
               </button>
@@ -2800,28 +2801,28 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       ${communityBlock}
 
       <script>
-        // 사용자 추가 출처 데이터 (평탄화된 배열)
+        // ?ъ슜??異붽? 異쒖쿂 ?곗씠??(?됲깂?붾맂 諛곗뿴)
         window.__USER_SOURCES__ = ${JSON.stringify(userSourcesFlat)};
         
-        // 필드별 주석 번호 매핑 (major)
+        // ?꾨뱶蹂?二쇱꽍 踰덊샇 留ㅽ븨 (major)
         window.__SOURCE_FIELD_MAP__ = {
-          'overview.summary': ['전공 개요', 'h3:contains("전공 개요")', '[data-field="overview.summary"]'],
-          'summary': ['전공 설명', 'h3:contains("전공 설명")', '[data-field="summary"]'],
-          'property': ['전공 특성', 'h3:contains("전공 특성")', '[data-field="property"]'],
-          'aptitude': ['이 전공에 어울리는 사람', 'h3:contains("이 전공에 어울리는 사람")', '[data-field="aptitude"]'],
-          'enterField': ['졸업 후 진출 분야', 'h3:contains("졸업 후 진출 분야")', '[data-field="enterField"]'],
-          'trivia': ['여담', 'h3:contains("여담")', '[data-field="trivia"]'],
-          'whatStudy': ['배우는 내용', 'h3:contains("배우는 내용")', '[data-field="whatStudy"]'],
-          'mainSubject': ['주요 교과목', 'h3:contains("주요 교과목")', '[data-field="mainSubject"]'],
-          'relateSubject': ['고교 추천 교과목', 'h3:contains("고교 추천 교과목")', '[data-field="relateSubject"]'],
-          'careerAct': ['진로 탐색 활동', 'h3:contains("진로 탐색 활동")', '[data-field="careerAct"]'],
-          'sidebarJobs': ['관련 직업', 'h3:contains("관련 직업")', '[data-field="sidebarJobs"]'],
-          'sidebarMajors': ['관련 전공', 'h3:contains("관련 전공")', '[data-field="sidebarMajors"]'],
-          'sidebarHowtos': ['관련 HowTo', 'h3:contains("관련 HowTo")', '[data-field="sidebarHowtos"]']
+          'overview.summary': ['?꾧났 媛쒖슂', 'h3:contains("?꾧났 媛쒖슂")', '[data-field="overview.summary"]'],
+          'summary': ['?꾧났 ?ㅻ챸', 'h3:contains("?꾧났 ?ㅻ챸")', '[data-field="summary"]'],
+          'property': ['?꾧났 ?뱀꽦', 'h3:contains("?꾧났 ?뱀꽦")', '[data-field="property"]'],
+          'aptitude': ['???꾧났???댁슱由щ뒗 ?щ엺', 'h3:contains("???꾧났???댁슱由щ뒗 ?щ엺")', '[data-field="aptitude"]'],
+          'enterField': ['議몄뾽 ??吏꾩텧 遺꾩빞', 'h3:contains("議몄뾽 ??吏꾩텧 遺꾩빞")', '[data-field="enterField"]'],
+          'trivia': ['?щ떞', 'h3:contains("?щ떞")', '[data-field="trivia"]'],
+          'whatStudy': ['諛곗슦???댁슜', 'h3:contains("諛곗슦???댁슜")', '[data-field="whatStudy"]'],
+          'mainSubject': ['二쇱슂 援먭낵紐?, 'h3:contains("二쇱슂 援먭낵紐?)', '[data-field="mainSubject"]'],
+          'relateSubject': ['怨좉탳 異붿쿇 援먭낵紐?, 'h3:contains("怨좉탳 異붿쿇 援먭낵紐?)', '[data-field="relateSubject"]'],
+          'careerAct': ['吏꾨줈 ?먯깋 ?쒕룞', 'h3:contains("吏꾨줈 ?먯깋 ?쒕룞")', '[data-field="careerAct"]'],
+          'sidebarJobs': ['愿??吏곸뾽', 'h3:contains("愿??吏곸뾽")', '[data-field="sidebarJobs"]'],
+          'sidebarMajors': ['愿???꾧났', 'h3:contains("愿???꾧났")', '[data-field="sidebarMajors"]'],
+          'sidebarHowtos': ['愿??HowTo', 'h3:contains("愿??HowTo")', '[data-field="sidebarHowtos"]']
         };
       </script>
       <script>
-        // 사용자 추가 출처에 주석 번호 추가 및 클릭 이벤트 등록 (배열 지원)
+        // ?ъ슜??異붽? 異쒖쿂??二쇱꽍 踰덊샇 異붽? 諛??대┃ ?대깽???깅줉 (諛곗뿴 吏??
         (function() {
           const sources = window.__USER_SOURCES__ || [];
           const fieldMap = window.__SOURCE_FIELD_MAP__ || {};
@@ -2833,11 +2834,11 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
             if (!mapping) return;
             const fieldLabel = mapping[0];
             
-            // content-heading 혹은 h3/h4에서 필드명 포함 요소 찾기
+            // content-heading ?뱀? h3/h4?먯꽌 ?꾨뱶紐??ы븿 ?붿냼 李얘린
             const headings = document.querySelectorAll('.content-heading, h3, h4');
             headings.forEach(function(heading) {
               if (heading.textContent && heading.textContent.includes(fieldLabel)) {
-                // 동일 ID 주석이 이미 있으면 패스
+                // ?숈씪 ID 二쇱꽍???대? ?덉쑝硫??⑥뒪
                 if (heading.querySelector('#user-fnref-' + sourceId)) return;
                 
                 const footnoteRef = document.createElement('sup');
@@ -2846,7 +2847,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                 footnoteRef.setAttribute('data-source-id', sourceId);
                 footnoteRef.innerHTML = '<span style="display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px;font-size:11px;font-weight:700;color:var(--wiki-secondary,#a78bfa);background:rgba(167,139,250,0.12);border-radius:9999px;">' + sourceId + '</span>';
                 
-                // flex justify-between로 밀리는 문제를 피하기 위해 텍스트 컨테이너에 부착
+                // flex justify-between濡?諛由щ뒗 臾몄젣瑜??쇳븯湲??꾪빐 ?띿뒪??而⑦뀒?대꼫??遺李?
                 const attachTarget =
                   heading.querySelector('.section-title-text') ||
                   heading.querySelector('.content-heading-text') ||
@@ -2856,7 +2857,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                 
                 attachTarget.appendChild(footnoteRef);
                 
-                // 클릭 시 출처 섹션으로 이동
+                // ?대┃ ??異쒖쿂 ?뱀뀡?쇰줈 ?대룞
                 footnoteRef.addEventListener('click', function(e) {
                   e.preventDefault();
                   e.stopPropagation();
@@ -2881,7 +2882,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
             });
           });
           
-          // 출처 리스트에서 해당 필드로 스크롤 (탭 전환 포함)
+          // 異쒖쿂 由ъ뒪?몄뿉???대떦 ?꾨뱶濡??ㅽ겕濡?(???꾪솚 ?ы븿)
           document.querySelectorAll('.user-source-item').forEach(function(item) {
             item.addEventListener('click', function() {
               const sourceId = this.id.replace('user-fn-', '');
@@ -2889,7 +2890,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
               const mapping = fieldMap[fieldKey || ''];
               if (!mapping) return;
               
-              // 상세정보 탭 필드 목록
+              // ?곸꽭?뺣낫 ???꾨뱶 紐⑸줉
               const detailFields = [
                 'whatStudy', 'mainSubject', 'relateSubject', 'careerAct', 'jobProspect',
                 'detailReady.curriculum', 'detailReady.recruit', 'detailReady.training', 'detailReady.researchList'
@@ -2899,19 +2900,19 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                 targetTabId = 'details';
               }
               
-              // 탭 전환 함수 (job 템플릿과 동일한 cw-tabset 스펙)
+              // ???꾪솚 ?⑥닔 (job ?쒗뵆由욧낵 ?숈씪??cw-tabset ?ㅽ럺)
               function switchToTab(tabId) {
                 const tabBtn = document.getElementById('cw-tab-' + tabId);
                 const tabPanel = document.getElementById('cw-tab-panel-' + tabId);
                 if (!tabBtn || !tabPanel) return;
                 
-                // 이미 활성 탭이면 스킵
+                // ?대? ?쒖꽦 ??씠硫??ㅽ궢
                 if (tabBtn.getAttribute('aria-selected') === 'true') return;
                 
                 const ACTIVE_CLASSES = ['text-white', 'border-wiki-primary', 'md:bg-wiki-border/30'];
                 const INACTIVE_CLASSES = ['text-wiki-muted', 'border-transparent', 'bg-transparent'];
                 
-                // 모든 탭 버튼 비활성화
+                // 紐⑤뱺 ??踰꾪듉 鍮꾪솢?깊솕
                 document.querySelectorAll('[data-cw-tab-trigger]').forEach(function(btn) {
                   btn.setAttribute('aria-selected', 'false');
                   btn.setAttribute('tabindex', '-1');
@@ -2920,7 +2921,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                   INACTIVE_CLASSES.forEach(function(cls) { btn.classList.add(cls); });
                 });
                 
-                // 모든 탭 패널 비활성화
+                // 紐⑤뱺 ???⑤꼸 鍮꾪솢?깊솕
                 document.querySelectorAll('[data-cw-tab-panel]').forEach(function(panel) {
                   panel.setAttribute('aria-hidden', 'true');
                   panel.setAttribute('tabindex', '-1');
@@ -2928,37 +2929,37 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
                   panel.classList.add('is-hidden');
                 });
                 
-                // 대상 탭 버튼 활성화
+                // ?????踰꾪듉 ?쒖꽦??
                 tabBtn.setAttribute('aria-selected', 'true');
                 tabBtn.setAttribute('tabindex', '0');
                 tabBtn.classList.add('is-active');
                 INACTIVE_CLASSES.forEach(function(cls) { tabBtn.classList.remove(cls); });
                 ACTIVE_CLASSES.forEach(function(cls) { tabBtn.classList.add(cls); });
                 
-                // 대상 탭 패널 활성화
+                // ??????⑤꼸 ?쒖꽦??
                 tabPanel.setAttribute('aria-hidden', 'false');
                 tabPanel.setAttribute('tabindex', '0');
                 tabPanel.classList.add('is-active');
                 tabPanel.classList.remove('is-hidden');
                 
-                // tabset의 data-active-tab 업데이트
+                // tabset??data-active-tab ?낅뜲?댄듃
                 const tabset = document.querySelector('[data-cw-tabset]');
                 if (tabset) {
                   tabset.setAttribute('data-active-tab', tabId);
                 }
               }
               
-              // 탭 전환 필요 시 전환
+              // ???꾪솚 ?꾩슂 ???꾪솚
               if (targetTabId) {
                 switchToTab(targetTabId);
               }
               
-              // 약간의 지연 후 스크롤 (탭 전환 후 렌더링 대기)
+              // ?쎄컙??吏?????ㅽ겕濡?(???꾪솚 ???뚮뜑留??湲?
               setTimeout(function() {
                 const refEl = document.getElementById('user-fnref-' + sourceId);
                 if (refEl) {
                   refEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
-                  // 하이라이트 효과
+                  // ?섏씠?쇱씠???④낵
                   refEl.classList.add('ring-2', 'ring-wiki-secondary');
                   setTimeout(function() {
                     refEl.classList.remove('ring-2', 'ring-wiki-secondary');
@@ -2973,7 +2974,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
       ${metaScript}
       
       <script>
-        // _t 파라미터 제거 (캐시 우회 후 URL 정리)
+        // _t ?뚮씪誘명꽣 ?쒓굅 (罹먯떆 ?고쉶 ??URL ?뺣━)
         (function() {
           if (window.location.search.includes('_t=')) {
             const url = new URL(window.location.href);
@@ -2982,9 +2983,9 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
           }
         })();
         
-        // 사이드바 접기/펼치기 기능
+        // ?ъ씠?쒕컮 ?묎린/?쇱튂湲?湲곕뒫
         (function() {
-          // 메인 자격증 목록 토글
+          // 硫붿씤 ?먭꺽利?紐⑸줉 ?좉?
           const expandButtons = document.querySelectorAll('.expand-toggle');
           
           expandButtons.forEach(button => {
@@ -2997,21 +2998,21 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
               const toggleCount = this.querySelector('.toggle-count');
               
               if (isExpanded) {
-                // 접기
+                // ?묎린
                 hiddenItems.forEach(item => {
                   item.style.display = 'none';
                 });
-                toggleText.textContent = '더보기';
+                toggleText.textContent = '?붾낫湲?;
                 toggleIcon.style.transform = 'rotate(0deg)';
                 toggleCount.style.display = '';
                 this.setAttribute('data-expanded', 'false');
               } else {
-                // 펼치기
+                // ?쇱튂湲?
                 hiddenItems.forEach(item => {
                   item.style.display = '';
                   item.style.animation = 'fadeIn 0.3s ease-in-out';
                 });
-                toggleText.textContent = '접기';
+                toggleText.textContent = '?묎린';
                 toggleIcon.style.transform = 'rotate(180deg)';
                 toggleCount.style.display = 'none';
                 this.setAttribute('data-expanded', 'true');
@@ -3019,7 +3020,7 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
             });
           });
           
-          // 서브 자격증 토글 (하위 3개씩 접기/펼치기)
+          // ?쒕툕 ?먭꺽利??좉? (?섏쐞 3媛쒖뵫 ?묎린/?쇱튂湲?
           const subExpandButtons = document.querySelectorAll('.sub-expand-toggle');
           
           subExpandButtons.forEach(button => {
@@ -3031,21 +3032,21 @@ export const renderUnifiedMajorDetail = ({ profile, partials, sources, existingJ
               const toggleIcon = this.querySelector('.sub-toggle-icon');
               
               if (isExpanded) {
-                // 접기
+                // ?묎린
                 hiddenSubItems.forEach(item => {
                   item.style.display = 'none';
                 });
                 const count = hiddenSubItems.length;
-                toggleText.textContent = '+' + count + '개 더보기';
+                toggleText.textContent = '+' + count + '媛??붾낫湲?;
                 toggleIcon.style.transform = 'rotate(0deg)';
                 this.setAttribute('data-expanded', 'false');
               } else {
-                // 펼치기
+                // ?쇱튂湲?
                 hiddenSubItems.forEach(item => {
                   item.style.display = '';
                   item.style.animation = 'fadeIn 0.3s ease-in-out';
                 });
-                toggleText.textContent = '접기';
+                toggleText.textContent = '?묎린';
                 toggleIcon.style.transform = 'rotate(180deg)';
                 this.setAttribute('data-expanded', 'true');
               }
@@ -3085,3 +3086,4 @@ export const createMajorJsonLd = (profile: UnifiedMajorDetail, canonicalUrl: str
   return `<script type="application/ld+json">${script}</script>`
 }
 
+
diff --git a/src/types/app.ts b/src/types/app.ts
index 5c45279..811c764 100644
--- a/src/types/app.ts
+++ b/src/types/app.ts
@@ -11,23 +11,23 @@ import type { UserRole } from '../services/commentService'
 export type Bindings = {
   DB: D1Database;
   KV: KVNamespace;
-  UPLOADS: R2Bucket;  // R2 이미지 업로드
-  VECTORIZE: VectorizeIndex;  // Vectorize 인덱스 (AI 분석기 후보 검색)
-  AI: Ai;  // Workers AI (임베딩 생성)
+  UPLOADS: R2Bucket;  // R2 ?대?吏 ?낅줈??
+  VECTORIZE: VectorizeIndex;  // Vectorize ?몃뜳??(AI 遺꾩꽍湲??꾨낫 寃??
+  AI: Ai;  // Workers AI (?꾨쿋???앹꽦)
   CAREER_NET_API_KEY?: string;
   GOYONG24_MAJOR_API_KEY?: string;
   GOYONG24_JOB_API_KEY?: string;
   PERF_ALERT_WEBHOOK?: string;
   ADMIN_SECRET?: string;
   ENVIRONMENT?: string;
-  // Z-Image Turbo API (이미지 생성)
+  // Z-Image Turbo API (?대?吏 ?앹꽦)
   EVOLINK_API_KEY?: string;
-  // Phase 3: Google OAuth 환경 변수
+  // Phase 3: Google OAuth ?섍꼍 蹂??
   GOOGLE_CLIENT_ID: string;
   GOOGLE_CLIENT_SECRET: string;
   GOOGLE_CALLBACK_URL: string;
   JWT_SECRET: string;
-  // Cloudflare API (releases page 등)
+  // Cloudflare API (releases page ??
   CLOUDFLARE_ACCOUNT_ID?: string;
   CLOUDFLARE_API_TOKEN?: string;
   CF_ACCOUNT_ID?: string;
@@ -35,12 +35,13 @@ export type Bindings = {
   CF_PAGES_API_TOKEN?: string;
   GITHUB_TOKEN?: string;
   GOOGLE_SITE_VERIFICATION?: string;
-  // Google Search Console / Indexing API (서비스 계정)
+  GEMINI_API_KEY?: string;
+  // Google Search Console / Indexing API (?쒕퉬??怨꾩젙)
   GOOGLE_SERVICE_ACCOUNT_EMAIL?: string;
   GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY?: string;
 }
 
-// User 타입 (auth-helpers에서 정의)
+// User ???(auth-helpers?먯꽌 ?뺤쓽)
 export interface User {
   id: number;
   google_id: string;
@@ -49,11 +50,11 @@ export interface User {
   email: string;
   name: string | null;
   picture_url: string | null;
-  custom_picture_url: string | null;  // 사용자가 직접 업로드한 프로필 이미지
+  custom_picture_url: string | null;  // ?ъ슜?먭? 吏곸젒 ?낅줈?쒗븳 ?꾨줈???대?吏
   role: 'user' | 'expert' | 'admin';
   username: string | null;
   ban_reason: string | null;
-  onboarded?: number;  // 온보딩 완료 여부 (0 or 1)
+  onboarded?: number;  // ?⑤낫???꾨즺 ?щ? (0 or 1)
   created_at: string;
   updated_at: string;
 }
@@ -67,8 +68,8 @@ export type Variables = {
 
 export type AppEnv = { Bindings: Bindings; Variables: Variables }
 
-// Phase 3 Day 3: 기존 헤더 기반 인증을 JWT 기반으로 변경
-// 기존 코드 호환을 위한 타입 유지
+// Phase 3 Day 3: 湲곗〈 ?ㅻ뜑 湲곕컲 ?몄쬆??JWT 湲곕컲?쇰줈 蹂寃?
+// 湲곗〈 肄붾뱶 ?명솚???꾪븳 ????좎?
 export type RequestUser = {
   id: string
   role: UserRole
@@ -77,3 +78,4 @@ export type RequestUser = {
   picture_url?: string | null
   custom_picture_url?: string | null
 }
+
